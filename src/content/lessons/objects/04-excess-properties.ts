import type { Lesson } from "../../types";

export const lesson: Lesson = {
  id: "ob4",
  region: 3,
  title: "Проверка лишних свойств и слабые типы",
  q: "Когда TypeScript ругается на лишнее свойство, а когда нет? Как его обойти и почему не стоит?",
  answer: "Лишние свойства проверяются только у свежего объектного литерала, который сразу присваивают в переменную с типом или передают в функцию. Это ловит опечатки вроде `widht`. Если тот же объект сначала положить в переменную, проверки нет — по правилам структурной типизации лишние поля допустимы. Отдельное правило для «слабых» типов, где все поля необязательные: объект без единого общего поля отвергается даже из переменной. Обойти проверку можно через промежуточную переменную или `as`, но обычно это прячет настоящую ошибку.",
  theory: {
    p: [
      "Структурная типизация разрешает лишние поля: объект `{ id, name, age }` подходит туда, где ждут `{ id, name }`. Но для литерала, написанного прямо на месте, TypeScript делает исключение и проверяет лишние свойства. Такой литерал никто больше не использует, значит, лишнее поле — скорее всего опечатка.",
      "Особенно это полезно с необязательными полями. `resize({ widht: 100 })` при типе `{ width?: number }` без проверки прошёл бы: `width` необязательный, а `widht` лишний. Проверка ловит опечатку и даже подсказывает правильное имя.",
      "Слабый тип — тип, у которого все поля необязательные. К нему подходит почти всё, поэтому для него есть своё правило: если у значения нет ни одного общего поля со слабым типом, это ошибка, даже когда объект пришёл из переменной.",
      "Как обойти: положить объект в переменную, написать `as Options` или добавить в тип index signature. Но делать это стоит, только если лишние поля действительно ожидаются. Чаще проверка права: поле названо с ошибкой или тип устарел.",
    ],
    example: `type Options = { width?: number; height?: number };
function resize(o: Options) {}

resize({ widht: 100 });            // ошибка: опечатка поймана
const raw = { widht: 100 };
resize(raw);                       // ошибка: у слабого типа нет общих полей
const raw2 = { width: 100, color: "red" };
resize(raw2);                      // можно: есть общее поле, лишнее из переменной`,
    keys: ["Лишние поля проверяются только у свежего литерала, написанного на месте.", "Слабый тип (все поля необязательные) отвергает значения без общих полей даже из переменной.", "Обход через переменную или `as` обычно прячет настоящую ошибку."],
  },
  tasks: [
    {
      type: "quiz",
      q: "Почему `resize({ widht: 100 })` — ошибка, а `const r = { width: 1, color: \"red\" }; resize(r)` — нет?",
      opts: ["Лишние поля проверяются только у литерала на месте, а у объекта из переменной — нет", "`color` — встроенное свойство", "Вторая запись вообще не проверяется", "Потому что `width` число"],
      a: 0,
      why: "Литерал на месте больше никто не использует, поэтому лишнее поле в нём — почти наверняка ошибка.",
    },
    {
      type: "predict",
      q: "Какой тип TypeScript выведет для переменной `w`?",
      probe: "w",
      code: `type Options = { width?: number };
const extra = { width: 1, color: "red" };
const o: Options = extra;
const w = o.width;`,
      opts: ["number | undefined", "number", "1", "unknown"],
      a: 0,
      why: "Тип `o` — `Options`, поле необязательное. То, что в переменной на самом деле лежит число, TypeScript уже не помнит.",
    },
    {
      type: "code",
      kind: "fix",
      goal: "Вызов не компилируется из-за опечатки в имени поля. Исправь вызов, не обманывая компилятор.",
      code: `type NewUser = { name: string; email?: string };
function createUser(u: NewUser) {
  return u;
}

createUser({ name: "Аня", emial: "anna@mail.ru" });`,
      forbid: ["any", "as", "ignore"],
      must: ["email: \"anna@mail.ru\""],
      hint: "Посмотри на подсказку компилятора: он предлагает правильное имя поля.",
      solution: `type NewUser = { name: string; email?: string };
function createUser(u: NewUser) {
  return u;
}

createUser({ name: "Аня", email: "anna@mail.ru" });`,
    },
  ],
};
