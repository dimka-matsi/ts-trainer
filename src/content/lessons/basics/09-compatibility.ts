import type { Lesson } from "../../types";

export const lesson: Lesson = {
  id: "b10",
  region: 0,
  title: "Совместимость типов",
  q: "Что такое структурная типизация и почему лишнее поле в объекте то ошибка, то нет?",
  answer: "TypeScript сравнивает типы по набору свойств, а не по названию. Объект подходит, если в нём есть все нужные свойства, лишние не мешают. Исключение — объект, написанный прямо в месте, где его передают или присваивают: там лишнее поле считается опечаткой. Если тот же объект сначала положить в переменную, ошибки не будет.",
  theory: {
    p: [
      "TypeScript сравнивает типы по форме, то есть по набору свойств и их типов. Название типа роли не играет. Такой подход называется структурной типизацией. Объект подходит к типу, если в нём есть все нужные свойства, а лишние не мешают.",
      "Есть исключение. Если объект написан прямо в месте присваивания или вызова, TypeScript считает лишнее поле опечаткой и выдаёт ошибку. Если тот же объект сначала сохранить в переменную, а потом передать, проверки лишних полей не будет.",
      "`unknown` принимает любое значение, но пользоваться им нельзя, пока тип не уточнён проверкой, например `typeof`. Поэтому `unknown` подходит для данных, про которые ничего не известно: ответа сервера, результата `JSON.parse`.",
      "`any` тоже принимает любое значение, но разрешает с ним любые операции без проверок. `never`, наоборот, не содержит ни одного значения. `unknown` — безопасная замена `any`, когда тип данных заранее неизвестен.",
    ],
    example: `interface Point { x: number; y: number }

const p3 = { x: 1, y: 2, z: 3 };
const a: Point = p3;                   // можно: лишнее поле не мешает
const b: Point = { x: 1, y: 2, z: 3 }; // ошибка: лишнее поле z

let u: unknown = 42;
u.toFixed();                           // ошибка: сначала проверь тип
if (typeof u === "number") u.toFixed();`,
    keys: ["Типы сравниваются по набору свойств, а не по названию.", "Лишнее поле — ошибка, только если объект написан прямо на месте.", "`unknown` нельзя использовать без проверки типа."],
  },
  tasks: [
    {
      type: "quiz",
      q: "Есть `interface Cat { name: string }` и `interface Dog { name: string }`. Можно ли передать объект типа `Dog` в функцию, которая ждёт `Cat`?",
      opts: ["Да, у них одинаковый набор свойств", "Нет, это разные интерфейсы", "Только через `as`", "Только если `Dog extends Cat`"],
      a: 0,
      why: "TypeScript сравнивает типы по свойствам. У обоих интерфейсов одно поле `name: string`, поэтому они взаимозаменяемы.",
      example: `interface Cat { name: string }
interface Dog { name: string }

function pet(c: Cat) { return c.name; }
const rex: Dog = { name: "Rex" };
pet(rex); // можно`,
    },
    {
      type: "predict",
      q: "Какой тип TypeScript выведет для переменной `r`?",
      probe: "r",
      code: `declare const v: unknown;
const r = typeof v === "string" ? v : "none";`,
      opts: ["unknown", "string", "\"none\"", "any"],
      a: 1,
      why: "В первой ветке проверка сделала `v` строкой. Во второй ветке стоит строка `\"none\"`. Обе ветки дают строку, поэтому результат — `string`.",
    },
    {
      type: "code",
      kind: "fix",
      goal: "`describe` получает значение неизвестного типа. Если это строка, верни `\"Длина: N\"`, где N — её длина. Иначе верни `\"не строка\"`. Без `any` и `as`.",
      code: `function describe(value: unknown): string {
  return "Длина: " + value.length;
}`,
      runtime: [["describe(\"abc\")", "\"Длина: 3\""], ["describe(5)", "\"не строка\""]],
      forbid: ["any", "as", "ignore"],
      must: ["value: unknown"],
      hint: "Сначала проверь `typeof value === \"string\"`: внутри этой проверки у `value` есть `length`.",
      solution: `function describe(value: unknown): string {
  if (typeof value === "string") return "Длина: " + value.length;
  return "не строка";
}`,
    },
  ],
};
