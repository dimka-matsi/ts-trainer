import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "case4",
  region: 7,
  level: "senior",
  title: "Тяжёлая таблица в админке",
  q: "Таблица на 20 000 строк с фильтрами и сортировкой тормозит: ввод в фильтр залипает, прокрутка дёргается. Что будете делать?",
  answer:
    "Сначала запись в Performance и React Profiler, чтобы понять, что тяжелее: рендер строк или вычисления. Дальше по порядку: не держать 20 000 строк на клиенте, а фильтровать и сортировать на сервере с пагинацией; если данные всё же на клиенте — виртуализировать таблицу, чтобы в DOM были только видимые строки. Фильтр — с debounce, а пересчёт списка — в `useTransition`, чтобы ввод не ждал. Сортировку и агрегации держать в `useMemo` или вынести в Web Worker. Строки обернуть в `React.memo` и не передавать им новые объекты и функции на каждом рендере.",
  theory: {
    p: [
      "Три разных вида работы: получить и хранить 20 000 строк, пересчитать фильтр и сортировку, отрисовать строки в DOM. Каждое тормозит по-своему, поэтому сначала запись: Performance покажет длинные задачи, React Profiler — какие компоненты рендерятся и сколько.",
      "Самое сильное решение — не делать работу на клиенте. Фильтрация, сортировка и пагинация на сервере: клиент получает 50 строк, а база с индексами делает свою работу быстро. Если без клиентских данных нельзя (офлайн, мгновенные фильтры), — виртуализация: в DOM около 30 видимых строк, прокрутка плавная при любом объёме.",
      "Отзывчивость фильтра. Ввод обновляет только поле, пересчёт запускается с debounce 200–300 мс или помечается переходом через `useTransition`, чтобы символы появлялись сразу. Тяжёлый расчёт — сортировка 20 000 строк, группировки, суммы — в `useMemo`, чтобы он повторялся только при изменении данных или условий, а если он всё равно долгий — в Web Worker.",
      "Ререндеры строк. Строка — в `React.memo`, данные строки и обработчики передаются стабильными (`useCallback`, `useMemo`), состояние выделения строки — рядом со строкой, а не во всей таблице. Тогда клик по одной строке перерисует одну строку, а не 30. После каждого шага — снова запись, чтобы видеть, что стало лучше.",
    ],
    flow: {
      actors: ["Ввод в фильтр", "Состояние", "Расчёт\nфильтр и сортировка", "Таблица"],
      steps: [
        { from: 0, to: 1, label: "символ в поле — сразу", note: "срочное обновление" },
        { from: 1, to: 2, label: "debounce 250 мс → пересчёт в useTransition", note: "или запрос к серверу с фильтром" },
        { from: 2, to: 2, label: "сортировка в useMemo или Web Worker", note: "не повторяется без причины" },
        { from: 2, to: 3, label: "виртуализация: рендер ~30 видимых строк", note: "DOM не растёт" },
        { from: 3, to: 3, label: "строки в React.memo со стабильными пропсами", note: "клик перерисовывает одну строку" },
      ],
    },
    keys: [
      "Сначала замер: Performance и React Profiler показывают, что тяжелее — расчёт или рендер.",
      "Лучше всего — фильтровать и сортировать на сервере. Иначе — виртуализация таблицы.",
      "Debounce и `useTransition` для фильтра, `useMemo` или Web Worker для расчёта, `React.memo` для строк.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь проблему таблицы и решение.",
      pairs: [
        ["в DOM 20 000 строк, прокрутка дёргается", "виртуализация"],
        ["ввод в фильтр залипает", "debounce и `useTransition`"],
        ["сортировка повторяется при каждом рендере", "`useMemo`"],
        ["клик по строке перерисовывает всю таблицу", "`React.memo` и стабильные пропсы"],
        ["расчёт итогов занимает 800 мс", "Web Worker"],
      ],
      why: "У каждой проблемы своя причина: объём DOM, приоритет обновлений, повторные вычисления, лишние ререндеры, тяжёлый расчёт в главном потоке.",
    },
    {
      type: "quiz",
      q: "Какое решение для таблицы на 20 000 строк обычно самое сильное?",
      opts: [
        "Фильтровать, сортировать и отдавать страницами на сервере",
        "Обернуть всё в `React.memo`",
        "Увеличить мощность компьютера пользователя",
        "Сжать ответ Brotli",
      ],
      a: 0,
      why: "Лучшая оптимизация — не делать работу. Клиенту редко нужны все 20 000 строк сразу.",
    },
    {
      type: "quiz",
      q: "Чем `useTransition` помогает полю фильтра над большой таблицей?",
      opts: [
        "Символы появляются сразу, а пересчёт таблицы идёт как прерываемое несрочное обновление",
        "Уменьшает количество строк",
        "Ускоряет сортировку",
        "Кэширует ответ сервера",
      ],
      a: 0,
      why: "Работа не уменьшается, но ввод перестаёт её ждать. Объём уменьшают виртуализация и серверная фильтрация.",
    },
  ],
};
