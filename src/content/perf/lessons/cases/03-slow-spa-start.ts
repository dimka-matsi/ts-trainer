import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "case3",
  region: 7,
  title: "Медленная первая загрузка SPA",
  q: "Одностраничное приложение на React первый раз открывается 6 секунд. Что будете делать?",
  answer:
    "Смотрю водопад и бандл. Обычная картина — один бандл на 2 МБ, который надо скачать и выполнить, прежде чем появится хоть что-то, а потом ещё запрос за данными. Делю код по маршрутам и тяжёлым компонентам (`React.lazy`, динамический `import()`), выношу библиотеки в отдельный чанк, убираю тяжёлые зависимости по анализатору бандла. Отдаю первую страницу готовым HTML через SSR или SSG, чтобы контент был виден до выполнения скрипта. Плюс сжатие Brotli, долгий кэш файлов с хэшем, CDN, `preload` критичных ресурсов и данные первого экрана — сразу в HTML, а не отдельным запросом.",
  theory: {
    p: [
      "Цепочка при CSR: HTML почти пустой → скачать бандл → разобрать и выполнить → запросить данные → отрисовать. Каждое звено ждёт предыдущее. Бандл на 2 МБ на среднем телефоне — это секунда-две загрузки и ещё столько же выполнения. Пока всё это не закончилось, пользователь видит белый экран или спиннер.",
      "Уменьшить JavaScript на старте. Анализатор бандла покажет, что занимает место. Код делят по маршрутам: пользователь главной не качает админку. Тяжёлые блоки — через `React.lazy`. Библиотеки — в отдельный `vendor`, который живёт в кэше. Большие зависимости заменяют лёгкими, лишние полифилы убирают. Цель — начальный JavaScript в пределах бюджета, например до 170 КБ в сжатом виде.",
      "Показать контент раньше скрипта. SSR или SSG отдают готовый HTML первой страницы: главный контент виден сразу, а скрипт гидратирует его потом. Данные первого экрана встраивают в HTML или начинают грузить параллельно со скриптом, а не после его выполнения. Критичный CSS — маленький и ранний.",
      "Доставка. Brotli для JavaScript и CSS, файлы с хэшем в имени и `immutable` — повторный визит почти без загрузок. CDN рядом с пользователем, HTTP/2 или HTTP/3. `preload` для главного шрифта и картинки, `preconnect` к API на другом домене. После изменений — замер в Lighthouse и по данным реальных пользователей, и бюджет в CI, чтобы бандл не разросся снова.",
    ],
    requests: [
      {
        name: "index.html",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 60], ["Загрузка", 2]],
        request: msg("GET / HTTP/2", [[":authority", "app.shop.ru"]]),
        response: msg("HTTP/2 200", [["cache-control", "no-cache"]], { type: "text/html; charset=utf-8", text: "<div id=\"root\"></div><script src=\"/bundle.js\"></script>" }),
      },
      {
        name: "bundle.js (2 МБ)",
        type: "script",
        start: 70,
        timing: [["Очередь", 1], ["Ожидание ответа", 50], ["Загрузка", 2300]],
        request: msg("GET /bundle.js HTTP/2", [[":authority", "app.shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "text/javascript"], ["content-length", "2097152"], ["cache-control", "max-age=600"]]),
      },
      {
        name: "api/home",
        type: "fetch",
        start: 4200,
        timing: [["Очередь", 1], ["Ожидание ответа", 380], ["Загрузка", 6]],
        request: msg("GET /api/home HTTP/2", [[":authority", "app.shop.ru"]]),
        response: msg("HTTP/2 200", [], { type: "application/json", text: "{\"banners\":[],\"items\":[]}" }),
      },
    ],
    keys: [
      "При CSR цепочка HTML → бандл → выполнение → данные → отрисовка, и каждое звено ждёт предыдущее.",
      "Меньше JavaScript на старте: разделение по маршрутам, `React.lazy`, `vendor`, замена тяжёлых зависимостей.",
      "Контент раньше скрипта: SSR или SSG, данные в HTML. Плюс Brotli, долгий кэш, CDN и бюджет в CI.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь звенья цепочки первой загрузки CSR-приложения.",
      items: ["почти пустой HTML", "загрузка бандла", "разбор и выполнение бандла", "запрос данных к API", "отрисовка контента"],
      why: "Каждое звено ждёт предыдущее. SSR убирает ожидание контента, разделение кода укорачивает второе и третье звено, данные в HTML — четвёртое.",
    },
    {
      type: "quiz",
      q: "Во вкладке «Сеть» запрос к `api/home` начинается только через 4 секунды после HTML. Как это ускорить?",
      opts: [
        "Встроить данные первого экрана в HTML или начать запрос параллельно с загрузкой бандла",
        "Увеличить таймаут запроса",
        "Сжать ответ API",
        "Перейти на HTTP/1.1",
      ],
      a: 0,
      why: "Запрос ждёт, пока бандл скачается и выполнится. Если данные приходят вместе с HTML, это ожидание исчезает.",
    },
    {
      type: "sort",
      q: "Что укорачивает первую загрузку SPA?",
      groups: ["Помогает", "Не помогает"],
      items: [
        ["разделение кода по маршрутам", 0],
        ["SSR первой страницы", 0],
        ["вынести библиотеки в отдельный `vendor`", 0],
        ["обернуть все компоненты в `React.memo`", 1],
        ["добавить ещё одну библиотеку для анимаций", 1],
      ],
      why: "`React.memo` помогает с ререндерами, а не с первой загрузкой. На старте решают байты и цепочка ожиданий.",
    },
  ],
};
