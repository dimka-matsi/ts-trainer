import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "bnd1",
  region: 2,
  title: "Как работает сборщик: webpack",
  q: "Что делает webpack? Что такое точка входа, граф модулей, лоадеры, плагины и чанки?",
  answer:
    "webpack начинает с точки входа (entry) и по всем `import` строит граф модулей — всё, от чего зависит приложение. Файлы, которые не являются JavaScript (TypeScript, CSS, картинки), он пропускает через лоадеры: они превращают их в модули. Плагины работают со сборкой целиком: создают HTML, выносят CSS в файлы, минифицируют, подставляют переменные окружения. На выходе граф режется на чанки — файлы, которые браузер скачает, — с хэшем содержимого в имени. В режиме production включаются минификация и tree shaking.",
  theory: {
    p: [
      "Браузеру нужны готовые файлы, а в проекте сотни модулей, TypeScript, JSX, CSS-модули, картинки и пакеты из `node_modules`. Сборщик превращает всё это в несколько оптимизированных файлов. Самые известные — webpack, Vite (внутри Rollup, в новых версиях Rolldown), Rspack и Turbopack; идеи у них общие, разберём на webpack.",
      "Точка входа (entry) — файл, с которого начинается приложение, например `src/index.tsx`. webpack читает его, находит все `import`, читает импортированные файлы и так далее. Получается граф модулей — всё, что реально нужно приложению. Файл, до которого по импортам не дойти, в сборку не попадёт.",
      "Лоадеры (loaders) превращают не-JavaScript в модули: `babel-loader` или `ts-loader` компилируют TypeScript и JSX, `css-loader` — стили, `asset`-модули — картинки и шрифты. Плагины (plugins) работают со сборкой целиком: HtmlWebpackPlugin создаёт `index.html` со ссылками на файлы, MiniCssExtractPlugin выносит CSS в отдельные файлы, TerserPlugin минифицирует, DefinePlugin подставляет переменные окружения.",
      "Выход (output) — чанки (chunks): куски графа, записанные в файлы. Начальный чанк нужен сразу, асинхронные грузятся по требованию. В имени файла — хэш содержимого (`[contenthash]`), чтобы файлы можно было кэшировать на год. В режиме `production` webpack сам включает минификацию, tree shaking (выбрасывание неиспользуемого кода, подробно — следующий урок) и стабильные имена модулей, поэтому для боевой сборки режим важен.",
    ],
    flow: {
      actors: ["entry\nsrc/index.tsx", "Граф модулей", "Лоадеры", "Плагины", "Чанки\ndist/"],
      steps: [
        { from: 0, to: 1, label: "обход всех import от точки входа", note: "то, до чего не дойти, в сборку не попадёт" },
        { from: 1, to: 2, label: ".tsx → babel-loader, .css → css-loader, .png → asset" },
        { from: 2, to: 1, label: "всё стало модулями JavaScript" },
        { from: 1, to: 3, label: "минификация, CSS в файлы, index.html" },
        { from: 3, to: 4, label: "main.3f9a1c.js, vendor.8b2d7e.js, checkout.5d0b.js", note: "имена с хэшем содержимого" },
      ],
    },
    keys: [
      "От точки входа по импортам строится граф модулей — всё, что нужно приложению.",
      "Лоадеры превращают TypeScript, CSS и картинки в модули, плагины обрабатывают сборку целиком.",
      "На выходе чанки с хэшем в имени. `production` включает минификацию и tree shaking.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь понятие webpack и его роль.",
      pairs: [
        ["entry", "файл, с которого начинается обход импортов"],
        ["loader", "превращает TypeScript, CSS, картинки в модули"],
        ["plugin", "обрабатывает сборку целиком: HTML, минификация, CSS в файлы"],
        ["chunk", "кусок графа модулей, записанный в файл"],
      ],
      why: "Лоадер работает с одним файлом, плагин — со всей сборкой. Чанки — то, что в итоге скачает браузер.",
    },
    {
      type: "quiz",
      q: "Зачем в имени выходного файла `[contenthash]`?",
      opts: [
        "Имя меняется только при изменении содержимого, поэтому файл можно кэшировать на год",
        "Чтобы файл нельзя было скачать без пароля",
        "Для сжатия файла",
        "Чтобы webpack собирал быстрее",
      ],
      a: 0,
      why: "Не изменился код — не изменилось имя, и браузер берёт файл из кэша. Изменился — новое имя, и старый кэш не мешает.",
    },
    {
      type: "quiz",
      q: "Почему боевую сборку обязательно делают в режиме `production`?",
      opts: [
        "Только в нём webpack включает минификацию, tree shaking и другие оптимизации",
        "Иначе не создаётся index.html",
        "Иначе не работают лоадеры",
        "Режим влияет только на цвет логов",
      ],
      a: 0,
      why: "В `development` сборка рассчитана на скорость пересборки и отладку: код не минифицирован, есть лишние проверки. Такой бандл в разы больше.",
    },
  ],
};
