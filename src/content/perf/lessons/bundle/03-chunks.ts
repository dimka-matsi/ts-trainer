import { msg } from "../../../course/http";
import type { NetRequest, WebLesson } from "../../../course/types";

const js = (name: string, start: number, size: string, wait = 30): NetRequest => ({
  name,
  type: "script",
  start,
  timing: [["Очередь", 1], ["Ожидание ответа", wait], ["Загрузка", 12]],
  request: msg(`GET /assets/${name} HTTP/2`, [[":authority", "shop.ru"]]),
  response: msg("HTTP/2 200", [["content-type", "text/javascript"], ["content-length", size], ["cache-control", "max-age=31536000, immutable"]]),
});

export const lesson: WebLesson = {
  id: "bnd2",
  region: 2,
  title: "Чанки: splitChunks и динамический импорт",
  q: "Как webpack делит код на чанки? Что делают динамический `import()` и `splitChunks`?",
  answer:
    "Динамический `import()` — точка разреза: всё, что импортировано так, webpack выносит в отдельный асинхронный чанк, который скачается при первом обращении. Так делят код по страницам и тяжёлым компонентам. `splitChunks` решает, что вынести в общие чанки: библиотеки из `node_modules` в `vendor`, код, общий для нескольких страниц, в свой файл, чтобы не качать его дважды. `runtimeChunk` отделяет служебный код webpack, чтобы хэши остальных файлов не менялись без причины. Чанков не должно быть ни слишком мало, ни слишком много.",
  theory: {
    p: [
      "Один большой бандл плох: пользователь главной страницы качает код оплаты, админки и графиков. Любое изменение меняет хэш всего файла, и кэш пропадает целиком. Цель — начальный чанк только с тем, что нужно сразу, остальное — по требованию и в стабильных файлах.",
      "Динамический `import()` возвращает промис, а webpack видит в нём точку разреза: модуль и всё, что нужно только ему, уходят в отдельный асинхронный чанк. Так делят по маршрутам (страница оплаты грузится при переходе на неё) и по тяжёлым компонентам (редактор, карта, график грузятся, когда их открыли). Чанку можно дать имя и подсказку: комментарий `webpackChunkName` задаёт имя файла, а `webpackPrefetch` добавляет `<link rel=\"prefetch\">`, чтобы браузер скачал чанк заранее в простое.",
      "`optimization.splitChunks` выносит общий код. По умолчанию webpack делит только асинхронные чанки. С настройкой `chunks: \"all\"` он выносит и библиотеки из начального: `react`, `react-dom` и прочее из `node_modules` попадают в `vendor`. Библиотеки меняются редко, и файл с ними живёт в кэше, пока меняется код приложения. Модули, общие для нескольких асинхронных чанков, выносятся в отдельный общий чанк, чтобы не качать их дважды.",
      "`runtimeChunk: \"single\"` выносит служебный код webpack (он знает, какой чанк где лежит) в маленький отдельный файл. Иначе этот список сидит в главном чанке, и добавление новой страницы меняет хэш главного файла. Мера важна: сотни крошечных чанков — это сотни запросов и хуже сжатие, а один огромный — лишние байты. С HTTP/2 несколько десятков чанков — нормально.",
    ],
    requests: [
      js("runtime.1a2b.js", 0, "2100"),
      js("vendor.8b2d7e.js", 2, "140000"),
      js("main.3f9a1c.js", 3, "48000"),
      js("checkout.5d0b.js", 3800, "22800", 34),
    ],
    keys: [
      "Динамический `import()` — точка разреза: модуль уходит в асинхронный чанк и грузится по требованию.",
      "`splitChunks` с `chunks: \"all\"` выносит библиотеки в `vendor` и общий код в общие чанки.",
      "`runtimeChunk` защищает хэши от лишних изменений. Чанков не слишком мало и не слишком много.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь настройку и что она делает.",
      pairs: [
        ["динамический `import()`", "выносит модуль в асинхронный чанк"],
        ["`splitChunks: { chunks: \"all\" }`", "выносит библиотеки и общий код из всех чанков"],
        ["`runtimeChunk: \"single\"`", "служебный код webpack в отдельном файле"],
        ["комментарий `webpackPrefetch`", "браузер скачает чанк заранее в простое"],
      ],
      why: "Разрез задаёт динамический импорт, группировку общего — `splitChunks`, стабильность хэшей — `runtimeChunk`.",
    },
    {
      type: "quiz",
      q: "После каждой выкладки пользователи заново качают `vendor.js` на 400 КБ, хотя библиотеки не менялись. Что проверить?",
      opts: [
        "Что служебный код webpack вынесен в отдельный runtime-чанк, а имена модулей стабильны",
        "Что включено сжатие Brotli",
        "Что у сервера HTTP/3",
        "Что картинки в формате AVIF",
      ],
      a: 0,
      why: "Если в файле с библиотеками лежит список всех чанков или меняются id модулей, его хэш меняется при любой правке приложения, и кэш пропадает.",
    },
    {
      type: "quiz",
      q: "Почему не стоит делить приложение на сотни мелких чанков?",
      opts: [
        "Много запросов и хуже сжатие: мелкие файлы сжимаются хуже, чем один средний",
        "webpack не умеет больше десяти чанков",
        "Браузер не кэширует мелкие файлы",
        "Это запрещено HTTP/2",
      ],
      a: 0,
      why: "У каждого запроса есть накладные расходы, а словарь сжатия на маленьком файле почти не успевает помочь. Нужен баланс.",
    },
  ],
};
