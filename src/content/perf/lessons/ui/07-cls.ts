import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ui7",
  region: 4,
  title: "CLS: откуда сдвиги и как их убрать",
  q: "Откуда берутся сдвиги макета и как снизить CLS?",
  answer:
    "CLS растёт, когда видимые элементы неожиданно сдвигаются. Частые причины: картинки и видео без размеров, реклама и встраиваемые блоки, которые приходят позже, баннеры, вставленные над контентом, и замена шрифта с другими размерами букв. Лечение — заранее резервировать место: `width` и `height` или `aspect-ratio` у медиа, минимальная высота для рекламных слотов, новые блоки — под контентом или по действию пользователя, шрифты с `size-adjust` и похожим запасным шрифтом. Сдвиги в течение 500 мс после ввода пользователя в метрику не входят.",
  theory: {
    p: [
      "CLS считает неожиданные сдвиги: для каждого кадра со сдвигом — доля экрана, которую задели сдвинутые элементы, умноженная на расстояние сдвига. Сдвиги группируются в «окна» до 5 секунд, и итог — самое тяжёлое окно за визит. Хорошо — не больше 0,1. Сдвиг в течение 500 мс после клика или нажатия клавиши не считается: пользователь его ожидает.",
      "Медиа без размеров — главная причина. Браузер не знает высоту картинки, пока не скачает её, и сначала отводит ноль — потом текст уезжает вниз. Атрибуты `width` и `height` у `<img>` и `<video>` позволяют браузеру вычислить пропорцию заранее, даже при адаптивной ширине в CSS. Для блоков — `aspect-ratio`. Для рекламы и встроек — контейнер с минимальной высотой под самый частый размер.",
      "Динамический контент. Баннер cookie, уведомление «установите приложение», подгруженный блок рекомендаций, вставленные над текстом, сдвигают всё. Их показывают поверх контента (фиксированное позиционирование), резервируют место или добавляют ниже видимой области. Анимации делают через `transform`, а не через `top` и `height`: трансформация не сдвигает соседей.",
      "Шрифты. Пока грузится веб-шрифт, текст рисуется запасным, и при замене строки меняют длину — абзацы прыгают. Помогают предзагрузка важного шрифта, `font-display: optional` для второстепенных и подгонка запасного шрифта через `size-adjust`, `ascent-override`, чтобы метрики совпали. Проверяют во вкладке Performance DevTools: отмеченные сдвиги показывают, какой элемент сдвинулся и почему.",
    ],
    flow: {
      actors: ["HTML", "Браузер", "Картинка", "Пользователь"],
      steps: [
        { from: 0, to: 1, label: "<img> без width и height: высота 0" },
        { from: 1, to: 3, label: "текст отрисован, пользователь начал читать" },
        { from: 2, to: 1, label: "картинка загрузилась: 400px высоты" },
        { from: 1, to: 3, label: "текст уехал вниз — сдвиг попал в CLS", note: "с width и height место было бы отведено сразу" },
      ],
    },
    keys: [
      "CLS — доля задетого экрана × расстояние сдвига, худшее окно до 5 секунд. Хорошо — до 0,1. Сдвиги после ввода не считаются.",
      "Резервируй место: `width` и `height` или `aspect-ratio` у медиа, минимальная высота рекламных слотов.",
      "Новые блоки — поверх или ниже, анимации — через `transform`. Шрифты — предзагрузка и `size-adjust` у запасного.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что вызывает сдвиги макета, а что их предотвращает?",
      groups: ["вызывает сдвиг", "предотвращает"],
      items: [
        ["`<img>` без `width` и `height`", 0],
        ["баннер, вставленный над статьёй после загрузки", 0],
        ["анимация `height` у раскрывающегося блока в потоке", 0],
        ["`aspect-ratio` у контейнера видео", 1],
        ["минимальная высота рекламного слота", 1],
        ["`size-adjust` у запасного шрифта", 1],
      ],
      why: "Всё сводится к одному: браузер должен знать размер места заранее, а появление нового не должно двигать уже показанное.",
    },
    {
      type: "quiz",
      q: "Пользователь нажал «Показать ещё», и список вырос вниз, сдвинув подвал. Попадёт ли этот сдвиг в CLS?",
      opts: [
        "Нет: сдвиги в течение 500 мс после ввода пользователя считаются ожидаемыми",
        "Да, любой сдвиг учитывается",
        "Да, если подвал был виден",
        "Нет, CLS считается только до события load",
      ],
      a: 0,
      why: "CLS наказывает только неожиданные сдвиги. Если ответ на клик приходит дольше 500 мс, сдвиг уже будет засчитан — это повод показывать заглушку сразу.",
    },
    {
      type: "quiz",
      q: "Зачем указывать `width` и `height` у картинки, если её ширина в CSS — `100%`?",
      opts: [
        "Браузер вычислит пропорцию и отведёт правильную высоту до загрузки картинки",
        "Чтобы картинка загрузилась быстрее",
        "Чтобы картинку не сжимал сервер",
        "Незачем: CSS всё равно переопределит размеры",
      ],
      a: 0,
      why: "Браузеры строят `aspect-ratio` из атрибутов автоматически. Ширина берётся из CSS, высота — из пропорции, и место резервируется сразу.",
    },
  ],
};
