import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "srv3",
  region: 2,
  title: "База данных: N+1, индексы, пагинация",
  q: "Что такое проблема N+1 и как ускоряют запросы к базе данных?",
  answer:
    "N+1 — когда сначала одним запросом берут список, а потом для каждого элемента делают ещё по запросу: 1 запрос за 50 заказами и 50 запросов за их товарами. Лечится одним запросом с JOIN или вторым запросом `WHERE id IN (…)`. Медленный запрос ищут через `EXPLAIN`, ускоряют индексом по полям из `WHERE` и `ORDER BY`. Для длинных списков вместо `OFFSET 10000` используют пагинацию по курсору, а соединения с базой держат в пуле.",
  theory: {
    p: [
      "N+1. Страница заказов: `SELECT * FROM orders WHERE user_id = 5` — 50 заказов. Дальше для каждого заказа `SELECT * FROM products WHERE id = …` — ещё 50 запросов. Каждый быстрый, но 51 круг до базы складывается в сотни миллисекунд. Часто это прячет ORM: обращение к `order.product` незаметно делает запрос. Решение — загрузить связанные данные заранее: JOIN или один запрос `WHERE id IN (1, 2, …)`.",
      "Индекс — упорядоченная структура (обычно B-дерево) по одному или нескольким полям. Без индекса база читает всю таблицу, чтобы найти строки; с индексом — находит за логарифмическое время. Индексируют поля из `WHERE`, `JOIN` и `ORDER BY`. Цена: индексы занимают место и замедляют запись, поэтому их не ставят на всё подряд. Как база выполняет запрос, показывает `EXPLAIN`: `Seq Scan` по большой таблице — повод для индекса.",
      "Пагинация. `LIMIT 20 OFFSET 10000` заставляет базу пройти и выбросить 10 000 строк — чем дальше страница, тем медленнее. Пагинация по курсору (keyset): `WHERE id < 12345 ORDER BY id DESC LIMIT 20` — база сразу прыгает по индексу к нужному месту. Минус — нельзя перейти сразу на страницу 500, зато бесконечная лента работает одинаково быстро.",
      "Ещё приёмы. Пул соединений: открывать соединение с базой на каждый запрос дорого, их держат открытыми и переиспользуют. Выбирать только нужные столбцы, а не `SELECT *`. Реплики для чтения, чтобы разгрузить основную базу. И кэш из прошлого урока для того, что читается часто, а меняется редко.",
    ],
    flow: {
      actors: ["Приложение", "База данных"],
      steps: [
        { from: 0, to: 1, label: "SELECT * FROM orders WHERE user_id = 5", note: "1 запрос: 50 заказов" },
        { from: 0, to: 1, label: "SELECT * FROM products WHERE id = 11", note: "+1" },
        { from: 0, to: 1, label: "SELECT * FROM products WHERE id = 12", note: "+1, и так ещё 48 раз" },
        { from: 1, to: 0, label: "51 запрос, ~250 мс", lost: true, note: "N+1" },
        { from: 0, to: 1, label: "SELECT … FROM orders JOIN products … WHERE user_id = 5", note: "вместо этого — один запрос" },
        { from: 1, to: 0, label: "всё сразу, ~10 мс" },
      ],
    },
    keys: [
      "N+1 — запрос на каждый элемент списка. Лечится JOIN или `WHERE id IN (…)`.",
      "Индексы по полям `WHERE`, `JOIN`, `ORDER BY`. Проверяют через `EXPLAIN`.",
      "Курсорная пагинация вместо большого `OFFSET`, пул соединений, только нужные столбцы.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Страница со списком из 100 пользователей делает 101 запрос к базе. Что это и как исправить?",
      opts: [
        "Проблема N+1: загрузить связанные данные одним запросом с JOIN или `WHERE id IN (…)`",
        "Нехватка индексов: добавить индекс на каждое поле",
        "Медленная сеть: включить HTTP/3",
        "Так и должно быть",
      ],
      a: 0,
      why: "Один запрос за списком и по запросу на каждый элемент. Круги до базы складываются, даже если каждый запрос быстрый.",
    },
    {
      type: "quiz",
      q: "Лента с `LIMIT 20 OFFSET 50000` открывается секунды. Почему и как исправить?",
      opts: [
        "База проходит и выбрасывает 50 000 строк; перейти на пагинацию по курсору `WHERE id < … LIMIT 20`",
        "Слишком маленький LIMIT: взять 1000",
        "Нужно больше реплик",
        "Нужно сжать ответ",
      ],
      a: 0,
      why: "OFFSET не умеет прыгать. Курсор использует индекс и сразу оказывается в нужном месте.",
    },
    {
      type: "sort",
      q: "Что ускоряет чтение, а что может его замедлить или ничего не даст?",
      groups: ["Ускоряет", "Не помогает"],
      items: [
        ["индекс по полю из `WHERE`", 0],
        ["пул соединений", 0],
        ["выбирать только нужные столбцы", 0],
        ["индекс на каждое поле таблицы", 1],
        ["`SELECT *` с большими текстовыми полями", 1],
      ],
      why: "Лишние индексы замедляют запись и занимают память, а лишние столбцы гоняют ненужные байты.",
    },
  ],
};
