import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "srv1",
  region: 3,
  level: "middle",
  title: "TTFB и Server-Timing",
  q: "Что такое TTFB, из чего он складывается и как понять, где сервер тратит время?",
  answer:
    "TTFB (time to first byte) — время от начала навигации до первого байта ответа. В него входят перенаправления, DNS, соединение и TLS, путь запроса до сервера и работа самого сервера. Хороший TTFB — до 800 мс, но для быстрого LCP лучше меньше. Сетевую часть уменьшают CDN и переиспользованием соединений, серверную — кэшем, быстрыми запросами к базе и потоковой отдачей. Где сервер тратит время, показывает заголовок `Server-Timing`: DevTools рисует его разбивку во вкладке «Сеть».",
  theory: {
    p: [
      "TTFB — время от начала запроса до первого байта ответа. Для документа он включает всё до ответа: перенаправления, DNS, TCP и TLS, путь запроса к серверу и обратно и время, пока сервер думает. Во вкладке «Сеть» последняя часть — фаза «Ожидание ответа». Ориентир Google — до 800 мс, но каждая миллисекунда TTFB сдвигает LCP.",
      "Сетевую часть лечат близостью и переиспользованием: CDN рядом с пользователем, HTTP/2 и HTTP/3, короткие цепочки перенаправлений (каждое `301` — ещё круг туда-обратно). Серверную часть надо сначала измерить: без измерения оптимизируют не то.",
      "`Server-Timing` — заголовок ответа, в котором сервер сам рассказывает, куда ушло время: `Server-Timing: db;dur=120, cache;desc=\"miss\", render;dur=45`. DevTools показывает эти строки на вкладке тайминга запроса, а системы мониторинга собирают их с реальных пользователей. Так видно, что из 300 мс сервера 120 ушло на базу.",
      "Типичные причины медленного сервера: медленные или лишние запросы к базе, отсутствие кэша, синхронные вызовы других сервисов один за другим, холодный старт серверных функций, сборка тяжёлой страницы целиком до отправки первого байта. Про каждую — следующие уроки региона.",
    ],
    requests: [
      {
        name: "product/7",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["DNS", 20], ["TCP", 30], ["TLS", 32], ["Ожидание ответа", 310], ["Загрузка", 8]],
        request: msg("GET /product/7 HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["server-timing", "auth;dur=12, db;dur=180, api-prices;dur=70, render;dur=40, cache;desc=\"miss\""]], {
          type: "text/html; charset=utf-8",
          text: "<!doctype html><title>Чайник</title>",
        }),
      },
    ],
    keys: [
      "TTFB = перенаправления + DNS + соединение + TLS + путь запроса + работа сервера.",
      "Сетевую часть уменьшают CDN, HTTP/2–3 и отказ от лишних перенаправлений.",
      "`Server-Timing` показывает, куда ушло время на сервере, прямо в DevTools.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "В ответе `Server-Timing: auth;dur=12, db;dur=180, api-prices;dur=70, render;dur=40`. С чего начать оптимизацию?",
      opts: ["С запросов к базе данных", "С проверки авторизации", "С отрисовки HTML", "Со сжатия ответа"],
      a: 0,
      why: "База заняла больше половины серверного времени. Оптимизировать то, что стоит 12 мс, почти бесполезно.",
    },
    {
      type: "sort",
      q: "Что уменьшает сетевую часть TTFB, а что серверную?",
      groups: ["Сетевую", "Серверную"],
      items: [
        ["CDN рядом с пользователем", 0],
        ["убрать цепочку перенаправлений", 0],
        ["HTTP/3 вместо HTTP/1.1", 0],
        ["кэш готовых страниц на сервере", 1],
        ["индекс для медленного запроса к базе", 1],
        ["параллельные вызовы других сервисов вместо последовательных", 1],
      ],
      why: "Сетевая часть — путь и рукопожатия, серверная — то, что происходит между приходом запроса и первым байтом ответа.",
    },
    {
      type: "quiz",
      q: "Сайт открывается по `http://shop.ru` → `https://shop.ru` → `https://www.shop.ru` → `https://www.shop.ru/ru/`. Что это делает с TTFB?",
      opts: [
        "Каждое перенаправление добавляет минимум круг туда-обратно, а новый хост — ещё и соединение",
        "Ничего: перенаправления мгновенные",
        "Уменьшает TTFB за счёт кэша",
        "Влияет только на SEO",
      ],
      a: 0,
      why: "Три лишних ответа до настоящего документа. Ссылки и рекламу ведут сразу на конечный адрес, а HSTS убирает переход с HTTP.",
    },
  ],
};
