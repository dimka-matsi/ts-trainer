import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "perf9",
  region: 1,
  title: "Мгновенные переходы: Speculation Rules",
  q: "Как сделать переход на следующую страницу мгновенным без SPA? Что такое Speculation Rules?",
  answer:
    "Speculation Rules — правила в теге `<script type=\"speculationrules\">`, по которым браузер заранее загружает или даже полностью отрисовывает в фоне страницы, на которые пользователь, скорее всего, перейдёт. `prefetch` скачивает только HTML, `prerender` строит страницу целиком со скриптами — тогда переход происходит мгновенно. Параметр `eagerness` задаёт, когда начинать: сразу, при наведении или при нажатии. Цена — лишний трафик и нагрузка на сервер, поэтому пререндерят осторожно и не для страниц с побочными эффектами. Поддержка — браузеры на Chromium, остальные правила просто игнорируют.",
  theory: {
    p: [
      "Подсказки `prefetch` и `preload` из урока про подсказки браузеру работают с отдельными файлами. Speculation Rules работают с целыми переходами: в странице описывают, какие ссылки стоит подготовить заранее. Правила пишут в формате JSON в теге `<script type=\"speculationrules\">` или отдают заголовком `Speculation-Rules` со ссылкой на файл правил.",
      "Два вида. `prefetch` скачивает документ следующей страницы и кладёт его в память — переход сэкономит время ответа сервера. `prerender` идёт дальше: в невидимой вкладке загружает страницу со всеми ресурсами, выполняет скрипты и рисует. При переходе браузер просто показывает готовую страницу — LCP почти нулевой.",
      "`eagerness` управляет моментом. `immediate` и `eager` — сразу, как правило увидено. `moderate` — при наведении на ссылку примерно на 200 мс. `conservative` — при нажатии кнопки мыши или пальца, за доли секунды до клика. Ссылки выбирают по шаблону `href_matches` или CSS-селектору, а исключают выход, корзину и всё, что меняет данные при открытии.",
      "Осторожность. Пререндер тратит трафик, память и процессор пользователя и нагружает сервер — если пользователь не перешёл, работа выброшена. Страница, которую пререндерят, не должна считать просмотр в аналитике до реального показа: для этого есть свойство `prerendering` у документа и событие `prerenderingchange` — аналитика должна их учитывать. Поддержка в 2026 году — Chrome и Edge, в Safari реализация за флагом, Firefox не поддерживает; остальные браузеры правила просто пропускают.",
    ],
    requests: [
      {
        name: "catalog (prerender)",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 90], ["Загрузка", 12]],
        request: msg("GET /catalog HTTP/2", [[":authority", "shop.ru"], ["sec-purpose", "prefetch;prerender"]]),
        response: msg("HTTP/2 200", [["content-type", "text/html; charset=utf-8"], ["cache-control", "private, max-age=300"]]),
      },
    ],
    keys: [
      "Speculation Rules готовят переходы заранее: `prefetch` — только HTML, `prerender` — страница целиком.",
      "`eagerness`: сразу, при наведении (`moderate`) или при нажатии (`conservative`). Изменяющие данные ссылки исключают.",
      "Цена — трафик и нагрузка. Аналитика учитывает пререндер. Работает в Chromium, остальные браузеры правила пропускают.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь значение `eagerness` и когда браузер начинает подготовку.",
      pairs: [
        ["`immediate`", "сразу, как увидел правило"],
        ["`moderate`", "при наведении на ссылку"],
        ["`conservative`", "при нажатии на ссылку, до отпускания"],
      ],
      why: "Чем раньше начать, тем мгновеннее переход и тем больше выброшенной работы, если пользователь не перешёл.",
    },
    {
      type: "quiz",
      q: "Чем `prerender` отличается от `prefetch` в Speculation Rules?",
      opts: [
        "`prerender` загружает и отрисовывает страницу целиком со скриптами, `prefetch` — только скачивает документ",
        "Ничем, это синонимы",
        "`prefetch` работает только для картинок",
        "`prerender` выполняется на сервере",
      ],
      a: 0,
      why: "После `prefetch` браузеру ещё нужно загрузить ресурсы и нарисовать страницу. После `prerender` переход — просто показ готового.",
    },
    {
      type: "quiz",
      q: "Какие ссылки нельзя пререндерить?",
      opts: [
        "Те, открытие которых меняет данные: «выйти», «добавить в корзину» по ссылке",
        "Ссылки на страницы каталога",
        "Ссылки с картинками",
        "Все ссылки на свой сайт",
      ],
      a: 0,
      why: "Пререндер выполняет запрос и скрипты страницы, даже если пользователь не перейдёт. Действие произошло бы само.",
    },
  ],
};
