import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "perf1",
  region: 1,
  level: "junior",
  title: "Сжатие: gzip, Brotli и zstd",
  q: "Как работает сжатие ответов? Чем Brotli лучше gzip и что сжимать не стоит?",
  answer:
    "Браузер сообщает в `Accept-Encoding`, какие алгоритмы понимает, сервер сжимает ответ одним из них и пишет его в `Content-Encoding`. Текст — HTML, CSS, JavaScript, JSON, SVG — сжимается в 3–10 раз. Brotli на тексте обычно на 15–25% лучше gzip, а zstd быстрее сжимает на лету. Статику сжимают заранее при сборке с максимальным уровнем. Картинки JPEG, PNG, WebP, видео и шрифты WOFF2 уже сжаты — повторное сжатие только тратит процессор.",
  theory: {
    p: [
      "Договорённость о сжатии идёт через заголовки. Запрос: `Accept-Encoding: gzip, deflate, br, zstd` — что браузер умеет распаковать. Ответ: `Content-Encoding: br` — чем сжато тело. `Content-Length` тогда — размер сжатого тела. Кэши должны различать сжатые версии, поэтому сервер добавляет `Vary: Accept-Encoding`.",
      "gzip — старый и поддерживается везде. Brotli (`br`) создан Google для веба: у него встроенный словарь частых слов HTML, CSS и JavaScript, и на тексте он даёт на 15–25% меньше. zstd — новый алгоритм от Meta, быстро сжимает на лету с хорошей степенью. Все основные браузеры понимают Brotli, а zstd — Chrome, Firefox и новые версии Safari. Браузеры принимают `br` и `zstd` только по HTTPS.",
      "Как сжимать. Статику (бандлы, CSS, SVG) сжимают один раз при сборке с максимальным уровнем Brotli: это медленно, но делается один раз, а сервер отдаёт готовый `app.js.br`. Динамические ответы (HTML с SSR, JSON API) сжимают на лету средним уровнем: высокий уровень съест больше времени, чем сэкономит на передаче.",
      "Что не сжимать: JPEG, PNG, WebP, AVIF, видео, WOFF2, архивы. Они уже сжаты своими алгоритмами, и gzip выиграет пару процентов, потратив процессор. Очень маленькие ответы, меньше килобайта, тоже не стоит: заголовки сжатия съедят выигрыш. Проверить сжатие можно во вкладке «Сеть»: DevTools показывает два размера — переданный и исходный.",
    ],
    requests: [
      {
        name: "app.3f9a1c.js",
        type: "script",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 30], ["Загрузка", 18]],
        request: msg("GET /assets/app.3f9a1c.js HTTP/2", [[":authority", "shop.ru"], ["accept-encoding", "gzip, deflate, br, zstd"]]),
        response: msg("HTTP/2 200", [["content-type", "text/javascript"], ["content-encoding", "br"], ["content-length", "52410"], ["vary", "Accept-Encoding"]]),
      },
      {
        name: "hero.avif",
        type: "avif",
        start: 5,
        timing: [["Очередь", 1], ["Ожидание ответа", 28], ["Загрузка", 40]],
        request: msg("GET /img/hero.avif HTTP/2", [[":authority", "shop.ru"], ["accept-encoding", "gzip, deflate, br, zstd"]]),
        response: msg("HTTP/2 200", [["content-type", "image/avif"], ["content-length", "84112"]]),
      },
    ],
    keys: [
      "`Accept-Encoding` — что умеет браузер, `Content-Encoding` — чем сжат ответ, плюс `Vary: Accept-Encoding`.",
      "Brotli лучше gzip на тексте. Статику сжимают заранее максимальным уровнем, динамику — на лету средним.",
      "Картинки, видео и WOFF2 уже сжаты — их не сжимают повторно.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что стоит сжимать Brotli или gzip, а что нет?",
      groups: ["Сжимать", "Не сжимать"],
      items: [
        ["`app.js`", 0],
        ["`style.css`", 0],
        ["ответ API в JSON", 0],
        ["`logo.svg`", 0],
        ["`photo.jpg`", 1],
        ["`font.woff2`", 1],
        ["`video.mp4`", 1],
      ],
      why: "SVG — это текст, он отлично сжимается. JPEG, WOFF2 и MP4 уже сжаты внутри, и второй раз выигрыша почти нет.",
    },
    {
      type: "quiz",
      q: "Почему статику сжимают при сборке, а не на каждый запрос?",
      opts: [
        "Максимальный уровень Brotli медленный: выгодно сжать один раз и отдавать готовый файл",
        "Сервер не умеет сжимать на лету",
        "Браузеры не принимают сжатие на лету",
        "Так файл не попадёт в кэш",
      ],
      a: 0,
      why: "Сжатие с максимальным уровнем может занять секунды. Для файла, который отдадут миллион раз, это копейки, а на каждый запрос — огромная задержка.",
    },
    {
      type: "quiz",
      q: "Зачем сжатому ответу заголовок `Vary: Accept-Encoding`?",
      opts: [
        "Чтобы кэш не отдал Brotli-версию клиенту, который понимает только gzip",
        "Чтобы браузер распаковал ответ",
        "Он включает сжатие",
        "Он нужен только для картинок",
      ],
      a: 0,
      why: "По одному адресу лежат разные варианты тела. `Vary` добавляет `Accept-Encoding` в ключ кэша.",
    },
  ],
};
