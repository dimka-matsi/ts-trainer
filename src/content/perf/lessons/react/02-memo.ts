import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rx2",
  region: 5,
  title: "React.memo, useMemo и useCallback",
  q: "Зачем нужны `React.memo`, `useMemo` и `useCallback` и когда они не помогают?",
  answer:
    "`React.memo` оборачивает компонент: он не перерисуется при ререндере родителя, если пропсы поверхностно не изменились. `useMemo` запоминает результат дорогого вычисления до изменения зависимостей, `useCallback` запоминает саму функцию, чтобы её ссылка не менялась на каждом рендере. Они работают в паре: если передать в мемоизированный компонент новый объект или новую функцию, созданные при рендере, `memo` ничего не даст. Мемоизация стоит памяти и сравнений, поэтому её ставят по замерам, а React Compiler умеет расставлять её автоматически.",
  theory: {
    p: [
      "`React.memo(Component)` — компонент, который при ререндере родителя сначала сравнивает старые и новые пропсы. Сравнение поверхностное: числа и строки по значению, объекты, массивы и функции — по ссылке. Совпали все — рендер пропускается. Это полезно для тяжёлых компонентов, которые часто получают одни и те же пропсы: строка таблицы, карточка в ленте, график.",
      "Ловушка: объект, массив или функция, созданные прямо при рендере родителя, каждый раз новые. Родитель передаёт `onSelect` — новую функцию на каждом рендере, и `memo` видит новый пропс, так что сравнение только добавило работы. `useCallback` сохраняет одну и ту же функцию, пока не изменятся её зависимости. `useMemo` так же сохраняет объект или результат вычисления.",
      "`useMemo` нужен и сам по себе, если вычисление дорогое: отфильтровать и отсортировать 10 000 строк при каждом нажатии в соседнем поле — лишняя работа. С `useMemo` сортировка повторится только при изменении данных или условий сортировки. Для дешёвых вычислений он не нужен: сравнить зависимости и хранить результат может стоить дороже самого расчёта.",
      "Порядок действий: сначала структура (опустить состояние ниже, передавать компонент детям через `children`, чтобы он не зависел от состояния родителя), потом замер в Profiler, и только потом точечная мемоизация. React Compiler (стабильная версия вышла в 2025 году) анализирует компоненты при сборке и сам расставляет мемоизацию, поэтому в новых проектах ручные `useMemo` и `useCallback` нужны реже.",
    ],
    flow: {
      actors: ["Родитель", "memo(Row)\nпропсы: item, onSelect", "Сравнение пропсов"],
      steps: [
        { from: 0, to: 2, label: "ререндер: onSelect — новая функция", note: "создана заново при рендере" },
        { from: 2, to: 1, label: "onSelect изменился по ссылке → рендер Row", lost: true, note: "memo не помог" },
        { from: 0, to: 0, label: "onSelect обёрнут в useCallback" },
        { from: 0, to: 2, label: "ререндер: onSelect — та же ссылка" },
        { from: 2, to: 0, label: "пропсы не изменились → рендер Row пропущен", note: "работает в паре" },
      ],
    },
    keys: [
      "`React.memo` пропускает рендер, если пропсы поверхностно равны.",
      "Новые объекты и функции при рендере ломают `memo`. `useCallback` и `useMemo` сохраняют ссылки.",
      "Сначала структура и замер, потом точечная мемоизация. React Compiler делает её автоматически.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь инструмент и что он запоминает.",
      pairs: [
        ["`React.memo`", "результат рендера компонента при тех же пропсах"],
        ["`useMemo`", "результат вычисления при тех же зависимостях"],
        ["`useCallback`", "саму функцию, чтобы ссылка не менялась"],
      ],
      why: "Все три — про одно: не делать заново то, что не изменилось. `useCallback` — частный случай `useMemo` для функций.",
    },
    {
      type: "quiz",
      q: "Строка таблицы обёрнута в `React.memo`, но всё равно перерисовывается при каждом рендере родителя. Самая частая причина?",
      opts: [
        "Родитель передаёт новый объект или функцию, созданные при рендере",
        "`React.memo` не работает со строками таблиц",
        "Не включён production-режим",
        "Таблица слишком большая",
      ],
      a: 0,
      why: "Сравнение поверхностное, по ссылке. Новая функция или новый объект на каждом рендере — это «изменившийся» пропс.",
    },
    {
      type: "quiz",
      q: "Когда `useMemo` скорее вредит, чем помогает?",
      opts: [
        "Когда вычисление дешёвое: сравнение зависимостей и хранение результата стоят больше",
        "Когда вычисление сортирует 10 000 строк",
        "Когда зависимости редко меняются",
        "Никогда",
      ],
      a: 0,
      why: "Мемоизация — не бесплатная. Её ставят там, где Profiler показал дорогой расчёт или лишние рендеры.",
    },
  ],
};
