import type { Flashcard } from "../flashcards";
import { makeCourse, type WebLesson, type WebRegion } from "../course/types";
import { lessons as actions } from "./lessons/actions";
import { lessons as arch } from "./lessons/arch";
import { lessons as components } from "./lessons/components";
import { lessons as context } from "./lessons/context";
import { lessons as hooks } from "./lessons/hooks";
import { lessons as local } from "./lessons/local";
import { lessons as patterns } from "./lessons/patterns";
import { lessons as perf } from "./lessons/perf";
import { lessons as redux } from "./lessons/redux";
import { lessons as rendering } from "./lessons/rendering";
import { lessons as server } from "./lessons/server";
import { lessons as ssr } from "./lessons/ssr";
import { lessons as stores } from "./lessons/stores";
import { lessons as testing } from "./lessons/testing";

/**
 * «React»: всё, что спрашивают о React на собеседованиях. Порядок: основы (компоненты, состояние, хуки, рендеринг) →
 * управление состоянием (Context, Redux, внешние хранилища, серверное состояние, выбор) → производительность →
 * React 19 (формы и Actions, сервер и Server Components) → паттерны → тестирование.
 * Каждая страница справочника react.dev привязана к уроку в `reference.ts`.
 */
const REGION_LESSONS: WebLesson[][] = [components, local, hooks, rendering, context, redux, stores, server, arch, perf, actions, ssr, patterns, testing];

const REGIONS: WebRegion[] = [
  { name: "Компоненты и JSX", kind: "lessons", desc: "JSX и элементы, пропсы и чистые компоненты, условия, списки и ключи, события, Fragment, порталы и StrictMode." },
  { name: "Состояние в компоненте", kind: "lessons", desc: "`useState` и `useReducer`, где держать состояние, подъём состояния и props drilling, формы, виды состояния: интерфейс, клиентское, серверное." },
  { name: "Хуки и эффекты", kind: "lessons", desc: "Правила хуков, `useEffect` и очистка, устаревшие замыкания и `useEffectEvent`, когда эффект не нужен, ссылки, эффекты раскладки, мемоизация, свои хуки." },
  { name: "Рендеринг", kind: "lessons", desc: "Рендер и коммит, виртуальный DOM и reconciliation, состояние и позиция в дереве, Fiber и конкурентный рендеринг, переходы, Suspense, Activity и ViewTransition." },
  { name: "Context", kind: "lessons", desc: "Как работает контекст, почему он перерисовывает всех потребителей и как это лечить, свой хук и провайдеры, `use` из React 19." },
  { name: "Redux и Redux Toolkit", kind: "lessons", desc: "Принципы Redux и Flux, `useSelector` и подписки, middleware, thunk и saga, Redux Toolkit с Immer, `createAsyncThunk` и нормализация, Context + `useReducer` вместо Redux." },
  { name: "Zustand, MobX и другие", kind: "lessons", desc: "Как хранилища подключаются к React через `useSyncExternalStore`, Zustand, MobX, атомы Jotai и Effector." },
  { name: "Серверное состояние", kind: "lessons", desc: "Почему данные с сервера — это кэш, TanStack Query: ключи, `staleTime` и `gcTime`, мутации и оптимистичные обновления, RTK Query." },
  { name: "Выбор и архитектура", kind: "lessons", desc: "Как выбрать инструмент для состояния, что хранить в адресе страницы и формах, как устроить состояние в большом приложении." },
  { name: "Производительность", kind: "lessons", desc: "Как найти лишние ререндеры: Profiler и DevTools, React Compiler, приёмы без мемоизации." },
  { name: "Формы и Actions", kind: "lessons", desc: "Actions и `<form action>`, `useActionState`, `useFormStatus`, `useOptimistic`, что нового в React 18 и 19." },
  { name: "Сервер и Server Components", kind: "lessons", desc: "SSR, гидратация и стриминг, Server Components и `'use client'`, Server Functions и `'use server'`, метаданные, ресурсы и `cache`." },
  { name: "Паттерны", kind: "lessons", desc: "Композиция и составные компоненты, HOC, render props и хуки, границы ошибок, классовые компоненты и жизненный цикл." },
  { name: "Тестирование", kind: "lessons", desc: "React Testing Library: что и как тестировать, запросы и события, асинхронность, `act` и моки запросов." },
];

/** Дополнительные карточки: частые вопросы собеседований, которые не стали отдельным уроком. id начинаются с `react-`. */
const EXTRA_CARDS: Flashcard[] = [
  {
    id: "react-context-vs-redux", region: 5, level: "middle",
    q: "Чем Context принципиально отличается от Redux?",
    a: "Context — механизм передачи данных: он доставляет значение вниз по дереву, а хранит его состояние провайдера. Redux — внешний стор со своей системой подписок, middleware и журналом действий. Context перерисовывает всех потребителей при изменении значения, а Redux — только компоненты, у которых изменился выбранный селектором кусок.",
  },
  {
    id: "react-redux-rerenders", region: 5, level: "middle",
    q: "Почему Redux вызывает меньше лишних ререндеров, чем Context?",
    a: "Потому что `useSelector` подписывает каждый компонент отдельно и после каждого действия сравнивает результат селектора с прошлым через `===`. Компонент перерисуется, только если изменилось выбранное значение. Важно, чтобы селектор не создавал новый объект на каждый вызов — иначе сравнение всегда видит изменение.",
  },
  {
    id: "react-immutability", region: 1, level: "junior",
    q: "Почему состояние в React нельзя менять напрямую?",
    a: "React узнаёт об изменениях через сеттер и сравнивает старое и новое значение по ссылке. Если изменить объект на месте и передать его же, ссылка та же, и React пропустит ререндер. Кроме того, старые значения нужны для сравнения и отладки, поэтому объекты и массивы копируют: `{ ...user, name }`, `[...items, x]`.",
  },
  {
    id: "react-batching", region: 1, level: "middle",
    q: "Что такое batching обновлений в React 18?",
    a: "React собирает несколько вызовов сеттеров в один ререндер. До React 18 это работало только внутри обработчиков событий React, а с 18-й версии — везде: в таймерах, промисах и обработчиках сети. Если нужно обновить экран немедленно, есть `flushSync`, но пользуются им редко.",
  },
  {
    id: "react-flux", region: 5, level: "junior",
    q: "Что такое Flux и как с ним связан Redux?",
    a: "Flux — архитектура от Facebook с однонаправленным потоком данных: действие проходит через диспетчер в хранилища, а представление подписано на хранилища. Redux упростил её: одно хранилище вместо нескольких и чистые редьюсеры вместо диспетчера. Главное, что осталось от Flux, — данные текут в одну сторону, и изменения описываются действиями.",
  },
  {
    id: "react-reselect", region: 5, level: "middle",
    q: "Зачем нужны мемоизированные селекторы, например `createSelector`?",
    a: "Селектор, который фильтрует или собирает новый объект, после каждого действия возвращает новую ссылку, и компонент перерисовывается. `createSelector` запоминает результат и пересчитывает его, только когда изменились входные данные, поэтому ссылка остаётся той же. Заодно не повторяются тяжёлые вычисления.",
  },
  {
    id: "react-no-store", region: 8, level: "junior",
    q: "Когда стейт-менеджер в проекте не нужен?",
    a: "Когда состояние в основном локальное, данные с сервера живут в библиотеке кэша вроде TanStack Query, а глобальных значений немного и они меняются редко — тема, язык, пользователь. Тогда хватает `useState`, `useReducer` и пары контекстов. Отдельное хранилище добавляют, когда появляется сложное общее клиентское состояние, а не заранее.",
  },
  {
    id: "react-server-client-split", region: 7, level: "senior",
    q: "Почему серверное и клиентское состояние держат в разных инструментах?",
    a: "У них разная природа. Клиентское состояние принадлежит приложению, и его меняет только пользователь. Серверное — копия чужих данных: она устаревает, её нужно перезапрашивать, объединять одинаковые запросы, повторять при ошибке и обновлять после изменений. Библиотеки кэша делают это из коробки, а хранилище для этого пришлось бы допиливать руками.",
  },
  {
    id: "react-why", region: 0, level: "junior",
    q: "Что такое React и какие у него главные идеи? Это библиотека или фреймворк?",
    a: "React — библиотека для построения интерфейсов из компонентов. Главные идеи: декларативность — описываешь, каким должен быть интерфейс при текущих данных, а React сам обновляет DOM; компоненты как переиспользуемые функции; однонаправленный поток данных сверху вниз. Сам React отвечает только за интерфейс, поэтому его называют библиотекой; роутинг, загрузку данных и сборку дают фреймворки вроде Next.js и React Router.",
  },
  {
    id: "react-one-way", region: 0, level: "junior",
    q: "Что такое однонаправленный поток данных в React? Как передать данные от ребёнка родителю и между соседями?",
    a: "Данные текут сверху вниз через пропсы: родитель владеет состоянием и передаёт его детям. Ребёнок не меняет данные родителя напрямую, а вызывает функцию, которую родитель передал в пропсах, например `onChange`. Соседи обмениваются данными через общего родителя: состояние поднимают к нему. Для далёких компонентов — контекст или внешнее хранилище.",
  },
  {
    id: "react-setstate-async", region: 1, level: "junior",
    q: "`setState` синхронный или асинхронный?",
    a: "Ни то ни другое в привычном смысле: сеттер не возвращает промис и не меняет значение сразу, а ставит обновление в очередь. React применяет его при следующем рендере, собирая несколько обновлений в пачку. Поэтому сразу после `setCount(5)` переменная `count` в этом рендере всё ещё старая, а новое значение будет в следующем рендере.",
  },
  {
    id: "react-setstate-in-render", region: 1, level: "junior",
    q: "Что будет, если вызвать сеттер состояния прямо в теле компонента?",
    a: "Бесконечный цикл: сеттер вызывает ререндер, в рендере снова вызывается сеттер, и так далее. React остановит это ошибкой «Too many re-renders». Сеттеры вызывают в обработчиках событий и эффектах, а значение, которое можно вычислить, считают в рендере без состояния.",
  },
  {
    id: "react-effect-loop", region: 2, level: "junior",
    q: "Почему `useEffect` может уйти в бесконечный цикл?",
    a: "Эффект меняет состояние, от этого компонент перерисовывается, и эффект запускается снова. Так бывает, если у эффекта нет массива зависимостей или в зависимостях объект или функция, которые создаются заново при каждом рендере. Лечат честными зависимостями из примитивов, выносом объекта за пределы компонента, мемоизацией или тем, что вычисляемое значение вообще не хранят в состоянии.",
  },
  {
    id: "react-abort", region: 2, level: "middle",
    q: "Как отменить запрос в `useEffect`, если компонент размонтировался или параметры изменились?",
    a: "Создать `AbortController` в эффекте, передать `signal` в `fetch` и вызвать `controller.abort()` в функции очистки. Тогда при смене параметров или уходе со страницы старый запрос отменится, а его ответ не перезапишет новые данные. Если отменить нельзя, в очистке ставят флаг `ignore` и не вызывают сеттер для устаревшего ответа.",
  },
  {
    id: "react-virtual-vs-shadow", region: 3, level: "middle",
    q: "Чем виртуальный DOM отличается от Shadow DOM?",
    a: "Это разные вещи. Виртуальный DOM — внутренний механизм React: дерево объектов, которое сравнивают с прошлым, чтобы применить к настоящему DOM минимальные изменения. Shadow DOM — возможность браузера для веб-компонентов: изолированное поддерево со своими стилями, которые не протекают наружу. React может рендерить веб-компоненты, но виртуальный DOM с Shadow DOM не связан.",
  },
  {
    id: "react-csr-ssr-ssg", region: 11, level: "middle",
    q: "Чем CSR отличается от SSR, SSG и ISR?",
    a: "CSR — страница собирается в браузере: сервер отдаёт почти пустой HTML и JavaScript. SSR — HTML рендерится на сервере на каждый запрос, контент виден сразу, потом идёт гидратация. SSG — HTML генерируется заранее при сборке, отдаётся с CDN, подходит для редко меняющихся страниц. ISR — вариант SSG во фреймворках вроде Next.js: статическая страница пересобирается в фоне по таймеру или по событию. Выбор зависит от того, как часто меняются данные и насколько важны первый показ и поисковики.",
  },
  {
    id: "react-styling", region: 12, level: "junior",
    q: "Какие есть способы стилизации компонентов в React?",
    a: "Обычный CSS и CSS-модули — классы изолированы по файлу, без затрат во время работы. Утилитарный CSS вроде Tailwind — классы прямо в разметке. CSS-in-JS вроде styled-components и Emotion — стили в JavaScript с доступом к пропсам, но с затратами во время работы и сложностями с Server Components; поэтому сейчас популярны варианты, которые извлекают CSS при сборке. Инлайн-стили через `style` — для динамических значений.",
  },
  {
    id: "react-routing", region: 12, level: "junior",
    q: "Как устроен роутинг в React-приложении?",
    a: "Сам React роутинга не содержит. В одностраничных приложениях используют React Router: он сопоставляет адрес с компонентами и меняет адрес через History API без перезагрузки страницы. В Next.js маршруты задаются файлами в папке приложения. Страницы обычно грузят лениво, чтобы каждый маршрут был отдельным чанком, а параметры фильтров держат в адресе.",
  },
];

/** Курс «React». Карточки уроков — `react-lesson-<id>`, экзамены — 400 + регион. В курсе есть код. */
export const REACT = makeCourse("react", "React", REGIONS, REGION_LESSONS, EXTRA_CARDS, "react", 400, { withCode: true });
