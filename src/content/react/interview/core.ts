import type { FollowUp } from "../../course/types";

/** Уточняющие вопросы к урокам регионов «Компоненты» … «Context». */
export const CORE: Record<string, FollowUp[]> = {
  jsx1: [
    {
      level: "middle",
      q: "Во что компилируется JSX в React 17+ и что изменил новый JSX transform?",
      a: "Раньше JSX превращался в `React.createElement(...)`, поэтому в каждом файле нужен был `import React`. Новый transform вызывает функцию `jsx` из `react/jsx-runtime`, которую компилятор импортирует сам. Он чуть быстрее, отдельно передаёт `key` и позволил убрать импорт React из файлов.",
    },
    {
      level: "senior",
      q: "Что такое React-элемент как объект и почему его нельзя изменять?",
      a: "Элемент — неизменяемый объект-описание: `type`, `props`, `key`, `ref` и служебное поле `$$typeof` с символом, которое защищает от внедрения поддельных элементов из JSON. React сравнивает старое и новое описание при reconciliation, поэтому изменение элемента после создания сломало бы сравнение. В режиме разработки элементы заморожены.",
    },
  ],
  jsx2: [
    {
      level: "middle",
      q: "Почему нельзя мутировать пропсы и что будет, если это сделать?",
      a: "Пропсы принадлежат родителю: компонент — функция от них, и React ожидает, что при тех же входных данных результат тот же. Мутация меняет данные родителя незаметно для React: интерфейс рассинхронизируется, мемоизация сравнивает одну и ту же ссылку и пропускает обновления. В режиме разработки объект пропсов заморожен, и мутация бросит ошибку.",
    },
    {
      level: "senior",
      q: "Почему в StrictMode компоненты рендерятся дважды в режиме разработки?",
      a: "React специально вызывает рендер, инициализаторы состояния и редьюсеры дважды, чтобы выявить нечистый код: если результат зависит от побочного эффекта или мутации, двойной вызов это покажет. Эффекты тоже монтируются, размонтируются и монтируются снова — проверка очистки. В продакшене двойных вызовов нет.",
    },
  ],
  jsx3: [
    {
      level: "middle",
      q: "Почему индекс массива как `key` приводит к ошибкам и когда он допустим?",
      a: "Ключ связывает элемент с его состоянием и DOM. При вставке в начало или сортировке индексы сдвигаются, и React отдаёт состояние одного элемента другому: введённый текст оказывается в чужой строке. Индекс допустим, только если список статичен — не меняется порядок и не удаляются элементы — и у элементов нет состояния.",
    },
    {
      level: "middle",
      q: "Как намеренно сбросить состояние компонента через `key`?",
      a: "Если изменить `key` компонента, React считает его новым: старый размонтируется со всем состоянием, новый смонтируется с нуля. Так делают форму редактирования, которая должна очищаться при смене пользователя: `<ProfileForm key={userId} />`. Это проще и надёжнее, чем сбрасывать поля в эффекте.",
    },
  ],
  jsx4: [
    {
      level: "middle",
      q: "Чем синтетические события React отличаются от нативных и как получить нативное?",
      a: "React передаёт обработчику `SyntheticEvent` — обёртку с одинаковым API во всех браузерах. Под ней нативное событие, доступное через `event.nativeEvent`. React использует делегирование на корне приложения. `onChange` в React срабатывает на каждый ввод, как нативный `input`, а не на потерю фокуса.",
    },
    {
      level: "middle",
      q: "Как передать параметр в обработчик события и чем плоха запись `onClick={handle(id)}`?",
      a: "`onClick={handle(id)}` вызывает функцию сразу при рендере и передаёт в `onClick` её результат. Нужна функция: `onClick={() => handle(id)}` или обработчик, который читает id из `data-*` атрибута. Новая стрелка на каждый рендер не проблема сама по себе, но ломает `memo` у дочернего компонента.",
    },
  ],
  jsx5: [
    {
      level: "middle",
      q: "Как события всплывают из портала и почему это неожиданно?",
      a: "Портал рендерит DOM в другое место документа, но событие всплывает по дереву React, а не по DOM. Клик внутри модального окна в портале дойдёт до обработчика `onClick` родительского компонента, хотя в DOM модальное окно не внутри него. Это удобно для контекста, но может сработать обработчик «клик мимо».",
    },
    {
      level: "senior",
      q: "Какие проверки включает StrictMode помимо двойного рендера?",
      a: "Повторное монтирование эффектов для проверки очистки, двойной вызов колбэков ref, предупреждения об устаревших API вроде строковых ref и `findDOMNode`. В React 19 проверки сосредоточены на чистоте рендера и корректной очистке. Всё это только в режиме разработки и готовит код к конкурентному рендерингу и Activity.",
    },
  ],
  jsx6: [
    {
      level: "middle",
      q: "Как сделать темы оформления, в том числе тёмную, без перерисовки всего приложения?",
      a: "Через CSS-переменные: цвета задают переменными на корне, а тема меняет только атрибут `data-theme` или класс у `<html>`. React не перерисовывает дерево — меняются только значения переменных, и браузер перекрашивает страницу. Выбор системы учитывают через `prefers-color-scheme`, а сохранённый выбор применяют до гидратации, чтобы не было вспышки.",
    },
    {
      level: "senior",
      q: "Как организовать стили в дизайн-системе, которую используют несколько приложений?",
      a: "Дизайн-токены — цвета, отступы, типографика — в одном источнике и генерация из них CSS-переменных. Компоненты со стилями без выполнения в браузере, чтобы работать с серверными компонентами. Изоляция через CSS Modules или слои `@layer`, чтобы стили приложений не ломали систему. Версионирование и визуальные тесты компонентов.",
    },
  ],
  st1: [
    {
      level: "middle",
      q: "Почему после `setCount(count + 1)` в `console.log(count)` старое значение?",
      a: "Сеттер не меняет переменную `count` текущего рендера — он планирует новый рендер, в котором `count` будет новым. В этом рендере `count` — константа, снимок значения. Поэтому три вызова `setCount(count + 1)` подряд увеличат на один. Для последовательных изменений — функция: `setCount((c) => c + 1)`.",
    },
    {
      level: "middle",
      q: "Как React группирует обновления состояния (batching) и что изменилось в React 18?",
      a: "Несколько вызовов сеттеров в одном событии React объединяет в один ререндер. До React 18 это работало только в обработчиках событий React, а в `setTimeout`, промисах и нативных обработчиках каждый вызов давал отдельный рендер. С `createRoot` в React 18 группировка автоматическая везде. Отменить её можно через `flushSync`.",
    },
  ],
  st2: [
    {
      level: "middle",
      q: "Как `useReducer` помогает тестировать логику и почему редьюсер должен быть чистым?",
      a: "Редьюсер — обычная функция «состояние и действие → новое состояние», её тестируют без React, просто вызывая. Он должен быть чистым: React может вызвать его дважды в StrictMode и в конкурентном режиме. Запросы и другие эффекты делают в обработчиках или эффектах, а редьюсер только вычисляет состояние.",
    },
    {
      level: "senior",
      q: "Как передать `dispatch` глубоко в дерево и почему он стабилен?",
      a: "`dispatch` из `useReducer` не меняется между рендерами, поэтому его можно положить в контекст отдельно от состояния: компоненты, которые только отправляют действия, не будут перерисовываться при изменении состояния. Это основа паттерна «контекст + редьюсер» как лёгкой альтернативы Redux для средних приложений.",
    },
  ],
  st3: [
    {
      level: "middle",
      q: "Почему не стоит хранить в состоянии то, что можно вычислить?",
      a: "Производное значение, например отфильтрованный список, в отдельном состоянии нужно синхронизировать с исходным — появляются эффекты и рассинхронизации. Вычисляй его при рендере из исходного состояния, а если вычисление дорогое — оберни в `useMemo`. Единственный источник правды проще и надёжнее.",
    },
    {
      level: "middle",
      q: "Как избежать противоречивого состояния, например одновременно `isLoading` и `error`?",
      a: "Хранить не несколько флагов, а одно состояние со статусом: `{ status: \"loading\" }`, `{ status: \"error\", error }`, `{ status: \"success\", data }`. Тогда невозможные сочетания не выразить в коде. Это размеченное объединение, которое удобно проверять и в TypeScript.",
    },
  ],
  st5: [
    {
      level: "middle",
      q: "Почему React ругается «A component is changing an uncontrolled input to be controlled»?",
      a: "Поле сначала получило `value={undefined}` — неуправляемое, а потом строку — управляемое. React не может переключать режим на лету. Причина обычно в начальном состоянии `undefined` или `null`. Решение — начальное значение `\"\"` для текстовых полей и `false` для чекбоксов.",
    },
    {
      level: "middle",
      q: "Когда неуправляемое поле с `ref` или `FormData` лучше управляемого?",
      a: "Когда значение нужно только при отправке: простая форма, поиск, загрузка файла — поле `<input type=\"file\">` вообще только неуправляемое. Меньше кода и нет ререндера на каждый символ. Управляемое нужно для мгновенной проверки, форматирования ввода и полей, зависящих друг от друга.",
    },
  ],
  st4: [
    {
      level: "middle",
      q: "Почему данные с сервера нельзя просто положить в глобальное хранилище?",
      a: "Данные сервера — это кэш чужого состояния: они устаревают, их запрашивают несколько компонентов, нужны повторные запросы, отмена, загрузка и ошибки. В глобальном хранилище всё это пишут вручную. Библиотеки серверного состояния делают это из коробки, а хранилище оставляют для клиентского состояния.",
    },
    {
      level: "senior",
      q: "Как классифицировать состояние в большом приложении и где хранить каждый вид?",
      a: "Серверное — в библиотеке кэша данных. Состояние адреса — фильтры, вкладки, страница — в адресе. Данные форм — в форме или библиотеке форм. Локальное состояние интерфейса — в компоненте. Глобальное клиентское — тема, сессия, корзина — в контексте или небольшом хранилище. Такое разделение убирает большую часть «глобального состояния».",
    },
  ],
  st6: [
    {
      level: "middle",
      q: "Как в React Hook Form сделать поле, которое зависит от значения другого поля?",
      a: "`watch(\"country\")` подписывает компонент на значение и перерисовывает его при изменении, а `useWatch` делает то же в дочернем компоненте, не перерисовывая всю форму. Для проверки, зависящей от нескольких полей, используют `superRefine` в схеме zod. Скрытые поля отключают или снимают с регистрации, чтобы они не проверялись.",
    },
    {
      level: "senior",
      q: "Как одна и та же схема проверки работает на клиенте и сервере в React 19 с Server Functions?",
      a: "Схему zod выносят в общий модуль. На клиенте React Hook Form проверяет форму через резолвер для быстрых подсказок. Server Function получает `FormData`, разбирает её той же схемой через `safeParse` и возвращает ошибки по полям, которые форма показывает. Клиентская проверка — удобство, серверная — защита.",
    },
  ],
  hk1: [
    {
      level: "middle",
      q: "Почему хуки нельзя вызывать в условиях и циклах?",
      a: "React хранит состояние хуков компонента списком и сопоставляет их по порядку вызова, а не по имени. Если в одном рендере хук пропущен условием, все следующие получат чужое состояние. Поэтому порядок вызовов должен быть одинаковым в каждом рендере; условие переносят внутрь хука.",
    },
    {
      level: "senior",
      q: "Почему `use` можно вызывать в условиях, в отличие от других хуков?",
      a: "`use` не хранит состояние в списке хуков компонента: он читает промис или контекст. Для промиса он либо возвращает готовое значение, либо приостанавливает рендер через Suspense. Поэтому порядок вызовов для него не важен, и его можно вызывать в условиях и циклах, но не в обычных функциях вне компонента.",
    },
  ],
  hk2: [
    {
      level: "middle",
      q: "Почему в разработке эффект срабатывает дважды и как на это правильно реагировать?",
      a: "StrictMode в режиме разработки монтирует компонент, сразу размонтирует и монтирует снова, чтобы проверить очистку. Если из-за этого что-то ломается — например, два соединения или двойной запрос с побочным действием, — значит, эффект без очистки. Правильно добавить очистку, а не отключать StrictMode.",
    },
    {
      level: "middle",
      q: "Что будет, если указать объект или функцию в зависимостях эффекта?",
      a: "Объект или функция, созданные при рендере, каждый раз новые, и эффект перезапускается на каждом рендере. Решение — перенести создание внутрь эффекта, зависеть от примитивных полей объекта или стабилизировать через `useMemo` и `useCallback`. Для функций, которые нужны эффекту, но не должны его перезапускать, — `useEffectEvent`.",
    },
  ],
  hk3: [
    {
      level: "middle",
      q: "Как получить актуальное значение в колбэке `setInterval` внутри эффекта?",
      a: "Колбэк замыкает значение первого рендера. Варианты: функциональное обновление `setCount((c) => c + 1)`, если нужно только предыдущее состояние; ref с последним значением, который обновляется при каждом рендере; или `useEffectEvent` — функция, которая всегда видит свежие пропсы и состояние, но не является зависимостью эффекта.",
    },
    {
      level: "senior",
      q: "Почему `useEffectEvent` нельзя передавать в другие компоненты и вызывать при рендере?",
      a: "Функция из `useEffectEvent` предназначена только для вызова из эффектов своего компонента: она всегда читает самые свежие значения, и её идентичность не стабильна как контракт. Вызов при рендере сделал бы рендер нечистым, а передача другим компонентам скрыла бы зависимость. Линтер хуков проверяет эти правила.",
    },
  ],
  hk4: [
    {
      level: "middle",
      q: "Как сбросить состояние при смене пропса без эффекта?",
      a: "Лучший способ — `key`: при смене `key` компонент монтируется заново с чистым состоянием. Если сбросить нужно только часть, сравнивают предыдущее значение пропса во время рендера и вызывают сеттер прямо там — React перерисует до отрисовки. Эффект со сбросом даёт лишний кадр со старым состоянием.",
    },
    {
      level: "middle",
      q: "Почему цепочки эффектов, где один эффект меняет состояние, а другой на него реагирует, — плохой признак?",
      a: "Каждый шаг цепочки — отдельный рендер и коммит, интерфейс мигает промежуточными состояниями, а логику трудно проследить. Обычно всё можно вычислить при рендере или выполнить в одном обработчике события, который сразу выставляет всё нужное состояние. Эффекты оставляют для синхронизации с внешним миром.",
    },
  ],
  hk5: [
    {
      level: "middle",
      q: "Почему изменение `ref.current` не вызывает ререндер и когда это полезно?",
      a: "ref — изменяемый контейнер, о котором React не следит: изменение не планирует рендер. Это полезно для значений, которые не влияют на отрисовку: id таймера, предыдущее значение, флаг «уже отправлено», экземпляр сторонней библиотеки. Читать и писать ref во время рендера нельзя — это делают в эффектах и обработчиках.",
    },
    {
      level: "middle",
      q: "Что такое колбэк-ref и что нового в React 19?",
      a: "Вместо объекта в `ref` передают функцию: React вызывает её с узлом при монтировании и с `null` при удалении. Это удобно, чтобы сразу измерить элемент или подписаться на него. В React 19 колбэк-ref может вернуть функцию очистки, как эффект, и её вызовут при удалении узла вместо вызова с `null`.",
    },
  ],
  hk6: [
    {
      level: "senior",
      q: "Почему `useLayoutEffect` выдаёт предупреждение при серверном рендеринге и как это обойти?",
      a: "На сервере эффекты не выполняются, а `useLayoutEffect` нужен до отрисовки — сервер не может его выполнить, и HTML может не совпасть с первым кадром клиента. React предупреждал об этом. Решения: использовать `useEffect`, если можно, отложить рендер зависимой части до клиента или применить изоморфный хук, который на сервере — `useEffect`.",
    },
    {
      level: "senior",
      q: "Для чего нужен `useInsertionEffect` и почему он вызывается раньше `useLayoutEffect`?",
      a: "Он для библиотек CSS-in-JS: вставить теги `<style>` до того, как любые эффекты раскладки прочитают размеры элементов. Иначе чтение в `useLayoutEffect` вызвало бы раскладку без нужных стилей и лишний пересчёт. В нём нет доступа к ref и нельзя обновлять состояние. В прикладном коде его не используют.",
    },
  ],
  hk7: [
    {
      level: "middle",
      q: "Гарантирует ли `useMemo`, что значение не будет пересчитано?",
      a: "Нет: это оптимизация, а не гарантия. React может выбросить кэш, например для скрытых через Activity частей или в будущих версиях для экономии памяти. Код должен работать правильно и без `useMemo`. Если нужно гарантированно одно значение на жизнь компонента — `useState` с функцией-инициализатором или ref.",
    },
    {
      level: "senior",
      q: "Как `memo` сравнивает пропсы и как задать своё сравнение?",
      a: "По умолчанию — поверхностно, через `Object.is` для каждого пропса. Вторым аргументом `memo(Component, arePropsEqual)` можно передать функцию сравнения. Но своя функция должна сравнивать все пропсы, включая функции, иначе компонент будет работать со старыми обработчиками. Глубокое сравнение внутри часто дороже самого рендера.",
    },
  ],
  hk8: [
    {
      level: "middle",
      q: "Почему для id в разметке нельзя использовать `Math.random()` и чем помогает `useId`?",
      a: "Случайное значение на сервере и клиенте будет разным — гидратация не совпадёт, и связи `htmlFor` и `aria-describedby` сломаются. `useId` генерирует одинаковый id на сервере и клиенте на основе положения компонента в дереве. Он для связей в разметке, а не для ключей списков.",
    },
    {
      level: "senior",
      q: "Как спроектировать свой хук, чтобы им было удобно пользоваться?",
      a: "Название на `use` и одна задача. Возвращать стабильные функции, чтобы их можно было ставить в зависимости, и объект с именованными полями, если значений больше двух. Не прятать внутри эффекты с неожиданными побочными действиями, принимать параметры, а не читать глобальные данные. И тестировать его через `renderHook`.",
    },
  ],
  rn1: [
    {
      level: "middle",
      q: "Что вызывает рендер компонента в React?",
      a: "Первый рендер при монтировании и повторные: изменение его состояния, рендер родителя, изменение используемого контекста или подписки внешнего хранилища. Изменение пропсов само по себе рендер не запускает — пропсы меняются потому, что перерисовался родитель. Рендер — вызов функции, а в DOM попадают только изменения, найденные при сравнении.",
    },
    {
      level: "senior",
      q: "Что происходит в фазе коммита и почему её нельзя прервать?",
      a: "В коммите React применяет найденные изменения к DOM, обновляет ref и вызывает эффекты раскладки, а потом браузер отрисовывает и выполняются обычные эффекты. Прерывать коммит нельзя: пользователь увидел бы наполовину обновлённый интерфейс. Поэтому прерываемой сделана только фаза рендера, а коммит выполняется синхронно одним куском.",
    },
  ],
  rn2: [
    {
      level: "middle",
      q: "Почему React сравнивает деревья за O(n), а не находит оптимальную разницу?",
      a: "Точный алгоритм сравнения деревьев имеет сложность O(n³) — для тысячи элементов это миллиард операций. React использует эвристику: элементы разных типов считаются разными деревьями и пересоздаются целиком, а дети списка сопоставляются по ключам. Это линейно и почти всегда даёт хороший результат.",
    },
    {
      level: "senior",
      q: "Правда ли, что виртуальный DOM быстрее прямой работы с DOM?",
      a: "Нет: он добавляет работу поверх прямых изменений. Его выигрыш — в удобной модели «опиши интерфейс целиком», при которой изменения всё равно собираются в минимальный набор операций. Фреймворки с мелкозернистой реактивностью вроде Solid и Svelte обходятся без виртуального DOM и часто быстрее, а React снижает цену за счёт компилятора.",
    },
  ],
  rn3: [
    {
      level: "middle",
      q: "Почему компонент, объявленный внутри другого компонента, теряет состояние на каждом рендере?",
      a: "При каждом рендере внешнего компонента создаётся новая функция внутреннего — новый тип компонента. Для React это другой тип на той же позиции, поэтому старый размонтируется со всем состоянием и DOM, а новый монтируется заново. Компоненты объявляют на верхнем уровне модуля.",
    },
    {
      level: "middle",
      q: "Как сохранить состояние при переключении между двумя разными компонентами на одном месте?",
      a: "Состояние привязано к позиции и типу. Если нужно сохранить состояние скрытой части — рендерить обе и скрывать одну через CSS или обернуть в `<Activity mode=\"hidden\">`. Если нужно сохранить общие данные — поднять состояние в родителя. Одинаковый тип на той же позиции сохранит состояние, если не хочешь сброса.",
    },
  ],
  rn4: [
    {
      level: "senior",
      q: "Что такое дорожки (lanes) в React и как они связаны с приоритетами?",
      a: "Lanes — битовая маска приоритетов обновлений. Ввод пользователя попадает в синхронную дорожку, переходы — в дорожки переходов, фоновые — в самые низкие. Планировщик React берёт работу с высшим приоритетом первой и может отложить или отбросить устаревшую низкоприоритетную. Так обновления от ввода не ждут тяжёлого рендера.",
    },
    {
      level: "senior",
      q: "Что такое двойная буферизация деревьев Fiber?",
      a: "React держит два дерева: текущее, отображённое на экране, и рабочее, которое строится при рендере. Узлы связаны через `alternate`. Рабочее дерево можно строить по частям, прерывать и отбрасывать, не трогая текущее. При коммите указатель переключается, и рабочее дерево становится текущим.",
    },
  ],
  rn5: [
    {
      level: "senior",
      q: "Почему обновление внутри перехода может «потеряться», если в нём есть `await`?",
      a: "Всё, что вызвано синхронно внутри `startTransition`, помечается как переход. После `await` код выполняется позже, уже вне контекста перехода, и сеттеры там станут обычными срочными обновлениями. В React 19 асинхронные переходы поддерживаются, но состояние после `await` снова оборачивают в `startTransition`, пока в языке нет асинхронного контекста.",
    },
    {
      level: "senior",
      q: "Как переходы взаимодействуют с Suspense и почему экран не заменяется спиннером?",
      a: "Если обновление внутри перехода приостанавливает уже показанную границу Suspense, React не заменяет её содержимое запасным интерфейсом, а продолжает показывать старый экран, пока новый не будет готов. `isPending` позволяет показать ненавязчивый индикатор. Без перехода та же приостановка спрятала бы показанный контент за спиннером.",
    },
  ],
  rn6: [
    {
      level: "middle",
      q: "Почему промис для `use` нельзя создавать прямо при рендере клиентского компонента?",
      a: "При каждом рендере создался бы новый промис, компонент бы снова приостанавливался и никогда не показал результат. Промис должен быть стабильным: создан в серверном компоненте и передан пропсом, закэширован библиотекой данных или создан вне рендера. React предупреждает о промисах, созданных без кэша.",
    },
    {
      level: "senior",
      q: "Как граница ошибок и Suspense работают вместе при загрузке данных?",
      a: "Suspense показывает запасной интерфейс, пока данные грузятся, а если промис отклонился, ошибка поднимается до ближайшей границы ошибок. Поэтому их ставят парой вокруг блока: граница ошибок снаружи показывает «не удалось загрузить» с кнопкой повтора, а Suspense внутри — индикатор загрузки.",
    },
  ],
  rn7: [
    {
      level: "senior",
      q: "Что происходит с эффектами и DOM скрытого через `Activity` компонента?",
      a: "В режиме `hidden` React сохраняет состояние и DOM, но скрывает элементы через `display: none` и очищает эффекты, как при размонтировании. Обновления скрытой части идут с низким приоритетом. При показе эффекты запускаются снова. Поэтому эффекты должны корректно очищаться и повторно подписываться.",
    },
    {
      level: "senior",
      q: "Как `ViewTransition` использует браузерный View Transitions API?",
      a: "`<ViewTransition>` помечает элементы, а при обновлении в переходе React вызывает `document.startViewTransition`: браузер делает снимок старого состояния, React применяет изменения, и браузер анимирует разницу по CSS. React сам расставляет имена переходов. Если браузер API не поддерживает, обновление происходит без анимации.",
    },
  ],
  ctx1: [
    {
      level: "middle",
      q: "Какое значение получит компонент, если над ним нет провайдера?",
      a: "Значение по умолчанию, переданное в `createContext(defaultValue)`. Это удобно для тестов и необязательных контекстов, но часто скрывает ошибку «забыли провайдер». Поэтому по умолчанию ставят `null`, а свой хук проверяет его и бросает понятную ошибку.",
    },
    {
      level: "middle",
      q: "Можно ли вложить несколько провайдеров одного контекста и какое значение получит потребитель?",
      a: "Да: потребитель получает значение ближайшего провайдера выше по дереву. Так переопределяют тему для части страницы или контекст формы для вложенной формы. Это отличие контекста от глобальной переменной: значение зависит от места компонента в дереве.",
    },
  ],
  ctx2: [
    {
      level: "middle",
      q: "Почему объект в `value` провайдера вызывает ререндер всех потребителей?",
      a: "Если `value={{ user, setUser }}` создаётся при каждом рендере провайдера, это новая ссылка, и React считает значение изменённым, даже если `user` тот же. Все потребители перерисуются. Значение стабилизируют через `useMemo`, а редко и часто меняющиеся данные разносят по разным контекстам.",
    },
    {
      level: "senior",
      q: "Как сделать подписку на часть контекста, если React этого не умеет?",
      a: "Разделить контекст на несколько маленьких. Передавать через контекст стабильное хранилище, а не значение, и подписываться на его части через `useSyncExternalStore` с селектором — так устроены библиотеки вроде use-context-selector и Zustand с провайдером. Обернуть потребителей в `memo` с передачей нужного куска пропсом.",
    },
  ],
  ctx3: [
    {
      level: "middle",
      q: "Зачем оборачивать `useContext` в свой хук вроде `useAuth`?",
      a: "Свой хук прячет сам контекст, проверяет, что провайдер есть, и выдаёт понятную ошибку. Он даёт место для производных значений и удобных функций и позволяет заменить реализацию — например, перейти на хранилище — без изменения компонентов. Это стандартный паттерн для любого контекста.",
    },
    {
      level: "senior",
      q: "Чем `use(Context)` отличается от `useContext` и где это полезно?",
      a: "`use` можно вызывать в условиях и после ранних возвратов: например, читать контекст только если пропс показывает, что он нужен. В остальном он возвращает то же значение и так же подписывает на изменения. Для серверных компонентов контекст недоступен вовсе — там данные передают пропсами.",
    },
  ],
};
