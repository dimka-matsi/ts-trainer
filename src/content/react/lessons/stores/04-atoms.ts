import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "atm1",
  region: 6,
  level: "senior",
  title: "Атомы: Jotai и Recoil",
  q: "Что такое атомарный подход к состоянию? Чем Jotai отличается от Zustand и Redux?",
  answer:
    "В атомарном подходе состояние — не одно дерево, а много маленьких независимых атомов. Компонент подписывается на конкретные атомы и перерисовывается только при их изменении, а производные атомы вычисляются из других, как формулы в таблице. Jotai — самая популярная библиотека такого типа: атом создаётся одной строкой, а используется почти как `useState`, только общий для всех компонентов. Recoil от Meta ввёл эту идею, но в 2025 году его репозиторий архивировали, поэтому в новых проектах берут Jotai.",
  theory: {
    p: [
      "Redux и Zustand идут «сверху вниз»: одно общее состояние, из которого компоненты выбирают куски селекторами. Атомарный подход — «снизу вверх»: много маленьких кусочков состояния — атомов, — из которых собирается нужное. Каждый атом хранит одно значение: тему, фильтр, выбранный товар.",
      "В Jotai `atom(0)` создаёт атом, а `useAtom(countAtom)` возвращает значение и сеттер — как `useState`, но состояние общее для всех, кто использует этот атом. Есть `useAtomValue` — только прочитать, и `useSetAtom` — только менять: компонент с одним сеттером не перерисуется при изменении значения.",
      "Производный атом: `atom((get) => get(priceAtom) * get(qtyAtom))`. Он пересчитывается, только когда изменились атомы, которые он читает, и компоненты подписаны именно на него. Похоже на ячейки таблицы с формулами. Асинхронные атомы могут возвращать промис и работают вместе с `Suspense`.",
      "Когда выбирать. Атомы удобны, когда состояние естественно разбито на много независимых кусков: редактор с множеством элементов, сложные формы, фильтры. Провайдер в Jotai не обязателен. Recoil — прародитель подхода от Meta, но его репозиторий архивирован в январе 2025 года и больше не развивается, поэтому для новых проектов его не выбирают.",
    ],
    code: `const priceAtom = atom(1000);
const qtyAtom = atom(1);
// Производный атом: как формула в таблице
const totalAtom = atom((get) => get(priceAtom) * get(qtyAtom));

function Qty() {
  const [qty, setQty] = useAtom(qtyAtom);
  return <button onClick={() => setQty(qty + 1)}>{qty}</button>;
}

function Total() {
  const total = useAtomValue(totalAtom); // перерисуется при изменении цены или количества
  return <p>{total}</p>;
}`,
    flow: {
      actors: ["Qty: setQty", "qtyAtom", "totalAtom = price × qty", "Total", "PriceLabel (priceAtom)"],
      steps: [
        { from: 0, to: 1, label: "qty: 1 → 2" },
        { from: 1, to: 2, label: "пересчёт: 1000 × 2", note: "зависит от qtyAtom" },
        { from: 2, to: 3, label: "ререндер Total" },
        { from: 1, to: 4, label: "PriceLabel не перерисован", note: "подписан на priceAtom, он не менялся" },
      ],
    },
    keys: [
      "Атомарный подход: много маленьких независимых атомов вместо одного дерева.",
      "Jotai: `atom` и `useAtom` — как общий `useState`. Производные атомы пересчитываются только от своих зависимостей.",
      "Удобно для множества независимых кусков состояния. Recoil архивирован в 2025 году — в новых проектах берут Jotai.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Чем атомарный подход отличается от Redux?",
      opts: [
        "Вместо одного дерева с селекторами — много маленьких атомов, на которые подписываются напрямую",
        "Атомы хранятся на сервере",
        "Атомы нельзя менять",
        "Разницы нет",
      ],
      a: 0,
      why: "Redux собирает всё в одно дерево и режет его селекторами. Атомы изначально мелкие, и резать нечего.",
    },
    {
      type: "match",
      q: "Сопоставь понятие Jotai и его смысл.",
      pairs: [
        ["`atom(0)`", "кусочек общего состояния"],
        ["`useAtom`", "значение и сеттер, как у `useState`"],
        ["производный атом", "вычисляется из других атомов, как формула"],
        ["`useSetAtom`", "только менять, без ререндера при изменении значения"],
      ],
      why: "API Jotai повторяет `useState`, поэтому к нему легко привыкнуть.",
    },
    {
      type: "quiz",
      q: "Почему в новом проекте не стоит выбирать Recoil?",
      opts: [
        "Его репозиторий архивирован в 2025 году, библиотека больше не развивается",
        "Он работает только с классами",
        "Он платный",
        "Он не поддерживает TypeScript",
      ],
      a: 0,
      why: "Архивированная библиотека не получает исправлений и поддержки новых версий React. Её идеи живут в Jotai.",
    },
  ],
};
