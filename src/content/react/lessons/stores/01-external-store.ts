import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ext1",
  region: 6,
  level: "senior",
  title: "`useSyncExternalStore`: как хранилища подключаются к React",
  q: "Как внешние стейт-менеджеры подключаются к React? Что такое tearing и зачем `useSyncExternalStore`?",
  answer:
    "Внешнее хранилище живёт вне React, и React нужно узнать о его изменениях. Для этого есть хук `useSyncExternalStore(subscribe, getSnapshot)`: React подписывается на хранилище и читает снимок данных, а когда снимок меняется, перерисовывает компонент. Хук защищает от tearing — «разрыва», когда при конкурентном рендеринге разные компоненты показывают разные версии одного хранилища. На нём построены react-redux и другие библиотеки с подписками. Главное требование: `getSnapshot` возвращает ту же ссылку, пока данные не изменились, иначе будет бесконечный цикл.",
  theory: {
    p: [
      "Состояние внутри React — `useState`, `useReducer`, контекст — React контролирует сам. Внешнее хранилище — объект вне React, который меняется когда угодно: по ответу сервера, по таймеру, из другого модуля. React нужно узнать об изменении и получить новые данные.",
      "Раньше библиотеки делали это вручную: подписка в `useEffect` и принудительный ререндер. С конкурентным рендерингом React 18 это стало опасно. React может прервать рендер, хранилище за это время изменится, и одна часть экрана покажет старые данные, а другая — новые. Это называют tearing — «разрыв» интерфейса.",
      "`useSyncExternalStore(subscribe, getSnapshot)` решает проблему. `subscribe` подписывает React на изменения и возвращает функцию отписки. `getSnapshot` возвращает текущий снимок данных. Если во время рендера снимок изменился, React перерисует синхронно, чтобы все компоненты показали одну версию. Третий аргумент — снимок для рендеринга на сервере.",
      "Требование: `getSnapshot` возвращает одну и ту же ссылку, пока хранилище не изменилось, — React сравнивает снимки через `Object.is`. Если каждый раз собирать новый объект, React решит, что данные всё время меняются, и уйдёт в бесконечный цикл. react-redux и другие популярные хранилища построены на этом хуке, и его знание показывает, что ты понимаешь, как библиотеки устроены внутри.",
    ],
    code: `// Своё мини-хранилище без библиотек
let state = { count: 0 };
const listeners = new Set<() => void>();

const store = {
  subscribe(listener: () => void) {
    listeners.add(listener);
    return () => listeners.delete(listener);
  },
  getSnapshot() {
    return state; // та же ссылка, пока не было изменений
  },
  increment() {
    state = { count: state.count + 1 }; // новое состояние — новая ссылка
    listeners.forEach((l) => l());
  },
};

function Counter() {
  const { count } = useSyncExternalStore(store.subscribe, store.getSnapshot);
  return <button onClick={store.increment}>{count}</button>;
}`,
    flow: {
      actors: ["Хранилище вне React", "React", "Header (count)", "Footer (count)"],
      steps: [
        { from: 1, to: 0, label: "subscribe + getSnapshot", note: "подписка при первом рендере" },
        { from: 0, to: 1, label: "increment: новая ссылка на state", note: "слушатели оповещены" },
        { from: 1, to: 0, label: "getSnapshot(): снимок изменился", note: "Object.is сравнил ссылки" },
        { from: 1, to: 2, label: "ререндер с count = 1" },
        { from: 1, to: 3, label: "ререндер с count = 1", note: "одна версия у всех — без разрыва" },
      ],
    },
    keys: [
      "Внешнее хранилище живёт вне React. `useSyncExternalStore(subscribe, getSnapshot)` подписывает React и читает снимок.",
      "Tearing — когда при конкурентном рендеринге разные компоненты показывают разные версии хранилища. Хук это предотвращает.",
      "`getSnapshot` возвращает ту же ссылку, пока данные не изменились, иначе бесконечный цикл. На хуке построены библиотеки с подписками.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Что такое tearing?",
      opts: [
        "Разные компоненты в одном кадре показывают разные версии одного хранилища",
        "Утечка памяти при отписке",
        "Ошибка при гидратации",
        "Повторный запрос к серверу",
      ],
      a: 0,
      why: "Рендер прервался, хранилище изменилось, и половина экрана осталась со старыми данными. `useSyncExternalStore` не даёт этому случиться.",
    },
    {
      type: "quiz",
      code: `function getSnapshot() {
  return { todos: store.todos };
}`,
      q: "Что случится, если передать такой `getSnapshot` в `useSyncExternalStore`?",
      opts: [
        "Бесконечный цикл: каждый вызов возвращает новый объект, и React думает, что данные изменились",
        "Всё будет работать",
        "Компонент не перерисуется никогда",
        "React выбросит ошибку типов",
      ],
      a: 0,
      why: "Снимок сравнивается по `Object.is`. Нужно вернуть `store.todos` — ту же ссылку, пока данные не менялись.",
    },
    {
      type: "order",
      q: "Расставь, что происходит при изменении внешнего хранилища.",
      items: [
        "хранилище создаёт новое состояние",
        "хранилище вызывает слушателей",
        "React вызывает `getSnapshot` и сравнивает снимки",
        "снимок другой — React планирует ререндер",
        "компоненты показывают одну и ту же новую версию",
      ],
      why: "Хранилище только сообщает «что-то изменилось». Решает, перерисовывать ли, React — по сравнению снимков.",
    },
  ],
};
