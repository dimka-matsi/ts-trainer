import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "mbx1",
  region: 6,
  level: "senior",
  title: "MobX: наблюдаемые объекты",
  q: "В чём разница между Redux и MobX? Как MobX понимает, какой компонент перерисовать?",
  answer:
    "MobX построен на реактивности: состояние — обычные изменяемые объекты, помеченные наблюдаемыми (observable), и их меняют напрямую, `store.count++`. Компонент, обёрнутый в `observer`, во время рендера запоминает, какие наблюдаемые поля он прочитал, и перерисуется только при их изменении — без селекторов. Производные значения — `computed`, они кэшируются. Redux — неизменяемое состояние, явные действия и чистые редьюсеры: больше кода, зато каждое изменение предсказуемо и видно в журнале. MobX — меньше кода и точные обновления, но изменения менее явные.",
  theory: {
    p: [
      "MobX реализует паттерн «наблюдатель» (Observer). Состояние — класс или объект, который `makeAutoObservable(this)` превращает в наблюдаемый: поля становятся `observable`, геттеры — `computed`, методы — `action`. Менять состояние можно прямо: `this.items.push(item)` — MobX сам узнает об изменении.",
      "Компонент оборачивают в `observer`. Во время рендера MobX записывает, какие наблюдаемые поля компонент прочитал. Когда одно из них меняется, перерисовывается именно этот компонент. Селекторы не нужны: зависимости отслеживаются автоматически и точно.",
      "`computed` — производное значение, например сумма корзины. Оно пересчитывается, только когда изменились поля, от которых оно зависит, и кэшируется между чтениями. `reaction` и `autorun` запускают побочные эффекты при изменениях: сохранить в `localStorage`, отправить аналитику.",
      "Сравнение с Redux. Redux: одно неизменяемое дерево, явные действия, чистые редьюсеры, журнал и путешествие во времени — предсказуемо, но многословно. MobX: изменяемые объекты, меньше кода, точные ререндеры, удобно для сложных связанных моделей; но изменения неявные, и в большой команде без правил легко потерять, кто и где поменял данные. Поэтому в MobX 6 по умолчанию действует правило: наблюдаемые поля, которые кто-то читает, меняют только внутри `action`, иначе будет предупреждение.",
    ],
    code: `class CartStore {
  items: { price: number }[] = [];

  constructor() {
    makeAutoObservable(this);
  }

  add(item: { price: number }) {
    this.items.push(item); // меняем прямо, MobX отследит
  }

  get total() {
    return this.items.reduce((sum, i) => sum + i.price, 0); // computed: кэшируется
  }
}

const cart = new CartStore();

const CartTotal = observer(() => {
  // Прочитали cart.total — перерисуемся, только когда он изменится
  return <p>Итого: {cart.total}</p>;
});`,
    flow: {
      actors: ["cart.add(item)", "observable items", "computed total", "observer CartTotal"],
      steps: [
        { from: 0, to: 1, label: "items.push(item)", note: "меняем напрямую" },
        { from: 1, to: 2, label: "total устарел — пересчёт", note: "зависимость отслежена автоматически" },
        { from: 2, to: 3, label: "total изменился" },
        { from: 3, to: 3, label: "ререндер только CartTotal", note: "компонент читал total в прошлом рендере" },
      ],
    },
    keys: [
      "MobX: изменяемые наблюдаемые объекты, меняешь напрямую, паттерн «наблюдатель».",
      "`observer` запоминает прочитанные поля и перерисовывает компонент только при их изменении. `computed` кэширует производные.",
      "Redux — явность, неизменяемость и журнал. MobX — меньше кода и точные обновления, но изменения неявные.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что относится к Redux, а что к MobX?",
      groups: ["Redux", "MobX"],
      items: [
        ["неизменяемое состояние и чистые редьюсеры", 0],
        ["журнал действий и путешествие во времени", 0],
        ["изменяемые наблюдаемые объекты", 1],
        ["автоматическое отслеживание прочитанных полей", 1],
        ["`computed` для производных значений", 1],
      ],
      why: "Redux делает изменения явными, MobX — автоматическими. Отсюда и плюсы, и минусы каждого.",
    },
    {
      type: "quiz",
      q: "Как MobX понимает, что `CartTotal` нужно перерисовать?",
      opts: [
        "Во время рендера `observer` запомнил, что компонент прочитал `cart.total`, и следит за ним",
        "Сравнивает всё состояние через `===`",
        "Перерисовывает все компоненты при любом изменении",
        "По селектору, который передают в хук",
      ],
      a: 0,
      why: "Подписка строится из того, что компонент реально прочитал. Перестал читать поле — перестал от него зависеть.",
    },
    {
      type: "quiz",
      q: "Какой паттерн лежит в основе MobX?",
      opts: [
        "Наблюдатель (Observer): наблюдаемые данные и подписчики на них",
        "Flux с редьюсерами",
        "Singleton",
        "Команда (Command)",
      ],
      a: 0,
      why: "Наблюдаемые поля знают своих подписчиков — компоненты и вычисления — и оповещают их об изменениях.",
    },
  ],
};
