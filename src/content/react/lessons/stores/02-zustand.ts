import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "zs1",
  region: 3,
  title: "Zustand: хранилище-хук без провайдера",
  q: "Чем Zustand отличается от Redux и Context? Когда его выбирать?",
  answer:
    "Zustand — маленькая библиотека, где хранилище — это хук: `create` описывает состояние и функции изменения в одном месте, провайдер не нужен, шаблонного кода почти нет. Главное — селективные подписки: компонент, вызвавший `useStore(s => s.count)`, перерисуется только когда изменился `count`, в отличие от контекста. Внутри он работает на `useSyncExternalStore`, а хранилище можно читать и менять и вне React. Его выбирают для клиентского состояния небольших и средних проектов, когда Redux избыточен, а контекста мало.",
  theory: {
    p: [
      "`create` создаёт хранилище и возвращает хук. В одной функции описывают и данные, и действия: `set` меняет состояние, `get` читает текущее. Провайдер не нужен — хранилище живёт в модуле, и любой компонент просто вызывает хук.",
      "Подписка через селектор: `useCartStore(s => s.items.length)`. После каждого `set` Zustand вызывает селектор и сравнивает результат со старым по `Object.is`. Не изменилось — ререндера нет. Это то же, что `useSelector` в Redux, и главное отличие от контекста, где перерисовываются все потребители.",
      "`set` сливает объект с состоянием на один уровень: `set({ count: 1 })` не сотрёт остальные поля, а вложенные объекты нужно копировать самому. Если селектор собирает несколько полей в новый объект, в Zustand 5 это приведёт к бесконечному циклу обновлений: объект каждый раз новый. Для этого есть `useShallow` — он сравнивает поля объекта поверхностно.",
      "Хранилище — обычный объект вне React: `useCartStore.getState()` и `setState` работают в любом модуле, например в обработчике ответа сервера. Есть middleware: `persist` сохраняет состояние в `localStorage`, `devtools` подключает Redux DevTools, `immer` разрешает «мутирующий» синтаксис. Минус — меньше строгой структуры, чем в Redux: в большой команде правила организации кода придётся договорить самим.",
    ],
    code: `const useCartStore = create<CartState>()((set) => ({
  items: [],
  add: (item) => set((state) => ({ items: [...state.items, item] })),
  clear: () => set({ items: [] }),
}));

function CartBadge() {
  // Перерисуется, только если изменилось количество
  const count = useCartStore((s) => s.items.length);
  return <span>{count}</span>;
}

function CartActions() {
  // Несколько полей сразу — через useShallow, иначе новый объект на каждый вызов
  const { clear } = useCartStore(useShallow((s) => ({ add: s.add, clear: s.clear })));
  return <button onClick={clear}>Очистить</button>;
}

// Вне React
useCartStore.getState().clear();`,
    flow: {
      actors: ["add(item)", "Хранилище Zustand", "CartBadge: s.items.length", "CartActions: s.clear"],
      steps: [
        { from: 0, to: 1, label: "set: новый массив items" },
        { from: 1, to: 2, label: "селектор: 2 → 3", note: "изменилось — ререндер" },
        { from: 1, to: 3, label: "селектор: clear → та же функция", note: "совпало — ререндера нет" },
        { from: 2, to: 2, label: "CartBadge перерисован", note: "провайдера нет, подписка напрямую" },
      ],
    },
    keys: [
      "Хранилище — хук из `create`: данные и действия вместе, провайдер не нужен, шаблонного кода почти нет.",
      "Селектор + `Object.is`: компонент перерисуется, только если изменился его кусок. Несколько полей — через `useShallow`.",
      "Хранилище доступно вне React через `getState`. Middleware: `persist`, `devtools`, `immer`.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      code: `const { name, avatar } = useUserStore((s) => ({ name: s.name, avatar: s.avatar }));`,
      q: "Что случится с этим кодом в Zustand 5?",
      opts: [
        "Бесконечный цикл обновлений: селектор каждый раз возвращает новый объект. Нужен `useShallow`",
        "Всё работает без лишних ререндеров",
        "Zustand не разрешает выбирать два поля",
        "Компонент не перерисуется никогда",
      ],
      a: 0,
      why: "Zustand сравнивает результат по `Object.is`. Новый объект — «изменение» на каждом вызове. `useShallow` сравнивает поля объекта поверхностно.",
    },
    {
      type: "match",
      q: "Сопоставь инструмент и его главную черту.",
      pairs: [
        ["Context", "перерисовывает всех потребителей"],
        ["Redux Toolkit", "строгая структура, middleware и журнал действий"],
        ["Zustand", "хранилище-хук без провайдера с селекторами"],
        ["`useShallow`", "выбрать несколько полей без лишних ререндеров"],
      ],
      why: "Zustand берёт у Redux селекторы, а у хуков — простоту, и обходится без провайдера.",
    },
    {
      type: "quiz",
      q: "Нужно очистить корзину в модуле, который обрабатывает ответ сервера, вне компонентов. Как это сделать в Zustand?",
      opts: ["`useCartStore.getState().clear()`", "Никак: хранилище доступно только в компонентах", "Через провайдер", "Через `useContext`"],
      a: 0,
      why: "Хранилище Zustand — обычный объект модуля. Хук — лишь способ подписаться на него из компонента.",
    },
  ],
};
