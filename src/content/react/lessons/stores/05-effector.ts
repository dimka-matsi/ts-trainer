import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "eff1",
  region: 3,
  title: "Effector: события, хранилища и эффекты",
  q: "Что такое Effector и чем он отличается от Redux? Почему о нём часто спрашивают в российских компаниях?",
  answer:
    "Effector — реактивный стейт-менеджер, популярный в российских компаниях. Логика строится из трёх единиц: событие (`createEvent`) — что-то произошло, хранилище (`createStore`) — данные, эффект (`createEffect`) — побочное действие вроде запроса, со встроенными статусами. Связи между ними описывают декларативно через `sample`: когда сработало одно, взять данные и запустить другое. Логика живёт вне компонентов и не зависит от React, а в компоненты её подключают хуком `useUnit`. В отличие от Redux, нет одного дерева и редьюсеров со `switch`: много маленьких хранилищ и явные связи.",
  theory: {
    p: [
      "Effector вырос в русскоязычном сообществе и часто встречается в крупных российских продуктах, поэтому на собеседованиях в России о нём спрашивают заметно чаще, чем в мире. Идея: бизнес-логика отдельно от интерфейса, описанная как граф связей между событиями, данными и эффектами.",
      "Три единицы. Событие `createEvent()` — сигнал «что-то произошло», его вызывают как функцию. Хранилище `createStore(initial)` — данные; оно меняется через `.on(event, (state, payload) => newState)` и, как в Redux, требует нового значения, а не изменения старого. Эффект `createEffect(async () => …)` — асинхронная работа; у него есть `pending` и события `done` и `fail`.",
      "`sample` связывает единицы: «когда сработал `clock`, возьми данные из `source` и отправь в `target`». Так описывают сценарии вроде «при нажатии кнопки взять форму и запустить запрос». Логика объявляется статически, на уровне модуля, а не внутри компонентов.",
      "В React подключают `useUnit`: он возвращает значения хранилищ и привязанные события. Компонент перерисуется только при изменении хранилищ, которые он взял. Плюсы — логика отдельно от интерфейса, её легко тестировать, точные обновления. Минусы — свой способ мышления и меньшая популярность за пределами русскоязычного сообщества.",
    ],
    code: `const searchChanged = createEvent<string>();
const searchFx = createEffect(async (query: string) => {
  const res = await fetch('/api/search?q=' + encodeURIComponent(query));
  return (await res.json()) as string[];
});

const $query = createStore('').on(searchChanged, (_, value) => value);
const $results = createStore<string[]>([]).on(searchFx.doneData, (_, list) => list);

// Когда изменился запрос — запустить поиск с его значением
sample({ clock: searchChanged, target: searchFx });

function Search() {
  const [query, onChange] = useUnit([$query, searchChanged]);
  return <input value={query} onChange={(e) => onChange(e.target.value)} />;
}`,
    flow: {
      actors: ["Search: onChange", "searchChanged (событие)", "$query (хранилище)", "searchFx (эффект)", "$results (хранилище)"],
      steps: [
        { from: 0, to: 1, label: "searchChanged('чай')" },
        { from: 1, to: 2, label: ".on: $query = 'чай'" },
        { from: 1, to: 3, label: "sample: запустить searchFx('чай')", note: "связь описана заранее, вне компонента" },
        { from: 3, to: 4, label: "doneData → $results", note: "pending, done и fail встроены" },
        { from: 2, to: 0, label: "ререндер Search", note: "useUnit подписан на $query" },
      ],
    },
    keys: [
      "Effector: событие — что произошло, хранилище — данные, эффект — асинхронная работа со статусами.",
      "`sample` декларативно связывает единицы: когда, что взять и куда отправить. Логика вне компонентов.",
      "В React — `useUnit`. Популярен в российских компаниях, поэтому о нём спрашивают на собеседованиях в России.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь единицу Effector и её роль.",
      pairs: [
        ["`createEvent`", "сигнал «что-то произошло»"],
        ["`createStore`", "данные, которые меняются по событиям"],
        ["`createEffect`", "асинхронная работа со статусами"],
        ["`sample`", "связь: когда, что взять и куда отправить"],
      ],
      why: "Из этих четырёх кирпичиков в Effector собирается вся бизнес-логика.",
    },
    {
      type: "quiz",
      q: "Чем Effector отличается от Redux по устройству?",
      opts: [
        "Много маленьких хранилищ и явные связи через `sample` вместо одного дерева и редьюсеров",
        "Effector хранит состояние на сервере",
        "В Effector состояние меняют напрямую, без нового значения",
        "Effector работает только с классовыми компонентами",
      ],
      a: 0,
      why: "Оба за неизменяемость и явные события, но Redux собирает всё в одно дерево, а Effector — в граф маленьких единиц.",
    },
    {
      type: "quiz",
      q: "Где в Effector живёт бизнес-логика?",
      opts: [
        "Вне компонентов, на уровне модулей: компоненты только подключают её через `useUnit`",
        "Внутри каждого компонента в `useEffect`",
        "В редьюсерах со `switch`",
        "На сервере",
      ],
      a: 0,
      why: "Поэтому логику Effector можно тестировать и переиспользовать без React.",
    },
  ],
};
