import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "jsx5",
  region: 0,
  level: "middle",
  title: "Fragment, порталы и StrictMode",
  q: "Зачем нужны Fragment, порталы и StrictMode?",
  answer:
    "Fragment — `<>…</>` — группирует несколько элементов без лишней обёртки в DOM, а с ключом `<Fragment key>` подходит для списков. Портал `createPortal(children, domNode)` рисует детей в другом месте DOM, например модальное окно в `body`, но в дереве React они остаются на своём месте: получают контекст и отдают события родителям. StrictMode включает проверки только в разработке: React дважды вызывает компоненты и повторно монтирует их, чтобы найти нечистый код и забытую очистку эффектов.",
  theory: {
    p: [
      "Fragment. Компонент должен вернуть один корень, а лишний `div` ломает вёрстку таблиц и флекс-контейнеров. `<>…</>` ничего не добавляет в DOM. Ключ можно дать только полной записи `<Fragment key={id}>`. В React 19.3 фрагменту можно передать `ref` и получить объект для работы с его DOM-узлами — слушатели событий, фокус, наблюдатели за видимостью — без обёртки.",
      "Портал: `createPortal(<Modal />, document.body)` из `react-dom`. Модальному окну, подсказке, выпадающему списку нужно вырваться из родителя с `overflow: hidden` и своим `z-index`. В DOM узлы лежат в `body`, но для React портал — ребёнок компонента: контекст доступен, события всплывают к родителям по дереву React, а не по DOM.",
      "StrictMode: `<StrictMode>` вокруг приложения включает проверки в разработке. Компоненты вызываются дважды — нечистый рендер проявится. Эффекты при монтировании запускаются, очищаются и запускаются снова — забытая очистка, например незакрытая подписка или двойной запрос, станет видна. В продакшене StrictMode ничего не делает.",
      "Частый вопрос: «Почему эффект срабатывает дважды?» — это StrictMode в разработке, а не ошибка. Правильный ответ — не отключать StrictMode, а написать очистку: отписаться, отменить запрос, закрыть соединение. Тогда двойной запуск ничего не ломает.",
    ],
    code: `function Columns() {
  // Две ячейки без лишнего div внутри <tr>
  return (
    <>
      <td>Имя</td>
      <td>Email</td>
    </>
  );
}

function Modal({ children }: { children: React.ReactNode }) {
  // В DOM — в конце body, в дереве React — внутри родителя
  return createPortal(<div className="modal">{children}</div>, document.body);
}

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);`,
    flow: {
      actors: ["Page (дерево React)", "Modal через портал", "DOM: #root", "DOM: body"],
      steps: [
        { from: 0, to: 1, label: "Modal — ребёнок Page", note: "контекст Page доступен" },
        { from: 1, to: 3, label: "узлы рисуются в body", note: "вне overflow: hidden родителя" },
        { from: 1, to: 0, label: "клик в модалке всплывает к Page", note: "по дереву React, а не по DOM" },
        { from: 0, to: 2, label: "остальная страница — в #root" },
      ],
    },
    keys: [
      "`<>…</>` группирует без обёртки в DOM, `<Fragment key>` — для списков, в React 19.3 у фрагмента есть `ref`.",
      "`createPortal` рисует в другом месте DOM, но контекст и всплытие событий — по дереву React.",
      "StrictMode в разработке дважды вызывает компоненты и повторно монтирует эффекты, чтобы найти ошибки.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь инструмент и задачу.",
      pairs: [
        ["`<>…</>`", "несколько элементов без обёртки в DOM"],
        ["`<Fragment key={id}>`", "группа узлов как элемент списка"],
        ["`createPortal`", "отрисовать детей в другом месте DOM"],
        ["`<StrictMode>`", "проверки только в разработке"],
      ],
      why: "Все четыре не меняют логику приложения: они про структуру DOM и поиск ошибок.",
    },
    {
      type: "quiz",
      q: "Почему в разработке эффект при монтировании срабатывает дважды?",
      opts: [
        "StrictMode монтирует, очищает и монтирует снова, чтобы найти забытую очистку",
        "Это ошибка React 18",
        "Компонент смонтирован дважды и в продакшене",
        "Так работает любой эффект всегда",
      ],
      a: 0,
      why: "Если очистка написана правильно, двойной запуск незаметен. В продакшене эффект запускается один раз.",
    },
    {
      type: "quiz",
      q: "Модалка отрисована порталом в `body`. Дойдёт ли клик по кнопке внутри неё до `onClick` родительского компонента?",
      opts: [
        "Да: события всплывают по дереву React, а не по DOM",
        "Нет: в DOM модалка вне родителя",
        "Только с `onClickCapture`",
        "Только если портал в `#root`",
      ],
      a: 0,
      why: "Для React портал — обычный ребёнок. Иногда это сюрприз: клик в модалке может сработать как клик по карточке под ней.",
    },
  ],
};
