import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "jsx1",
  region: 0,
  level: "junior",
  title: "JSX и элементы",
  q: "Что такое JSX и во что он превращается? Чем компонент отличается от элемента?",
  answer:
    "JSX — синтаксис, похожий на HTML, который сборщик превращает в вызовы функций: `<Button color='red'>OK</Button>` становится `jsx(Button, { color: 'red', children: 'OK' })`. Результат вызова — элемент: обычный неизменяемый объект, который описывает, что показать. Компонент — функция, которая получает пропсы и возвращает элементы; React сам вызывает её, когда встречает элемент с этим типом. Компоненты пишут с заглавной буквы: по ней JSX отличает компонент от HTML-тега.",
  theory: {
    p: [
      "JSX не часть JavaScript: браузер его не понимает. Сборщик — Babel, SWC, esbuild или компилятор TypeScript — превращает каждый тег в вызов функции. С React 17 это функция `jsx()` из `react/jsx-runtime`, которую сборщик подключает сам. Раньше это был `React.createElement`, поэтому в каждом файле с JSX нужно было писать `import React`.",
      "Элемент — объект вида `{ type, props, key }`. Он ничего не рисует и не меняется после создания: это только описание. Если `type` — строка, например `'div'`, это HTML-тег. Если функция — компонент. Поэтому компоненты называют с заглавной буквы: `<button>` — тег, `<Button>` — компонент.",
      "Внутри фигурных скобок — любое выражение JavaScript: переменная, вызов функции, тернарный оператор. Инструкции вроде `if` и `for` внутри JSX писать нельзя — только выражения. Атрибуты называют как свойства DOM: `className` вместо `class`, `htmlFor` вместо `for`, стили — объектом `style={{ color: 'red' }}`, обработчики — в camelCase: `onClick`.",
      "React экранирует строки: если в `{comment}` лежит `<script>`, на экран попадёт текст, а не тег. Поэтому JSX сам защищает от XSS. Вставить сырой HTML можно только явно — через `dangerouslySetInnerHTML={{ __html: html }}`, и тогда HTML нужно очищать самому, например библиотекой DOMPurify.",
    ],
    code: `// Что пишем
const el = <Button color="red" onClick={save}>Сохранить</Button>;

// Во что превращает сборщик (упрощённо)
const el2 = jsx(Button, { color: 'red', onClick: save, children: 'Сохранить' });

// Элемент — просто объект-описание:
// { type: Button, props: { color: 'red', onClick: save, children: 'Сохранить' }, key: null }`,
    flow: {
      actors: ["Код с JSX", "Сборщик", "Вызов jsx()", "Элемент (объект)", "React"],
      steps: [
        { from: 0, to: 1, label: "<Button color='red'>OK</Button>", note: "браузер JSX не понимает" },
        { from: 1, to: 2, label: "jsx(Button, { color: 'red', children: 'OK' })" },
        { from: 2, to: 3, label: "{ type: Button, props, key }", note: "описание, а не DOM" },
        { from: 3, to: 4, label: "type — функция: вызвать компонент Button" },
        { from: 4, to: 4, label: "Button вернул новые элементы — и так до HTML-тегов" },
      ],
    },
    keys: [
      "JSX — синтаксис, который сборщик превращает в вызовы `jsx()`. Результат — элемент: неизменяемый объект-описание.",
      "Компонент — функция, которая возвращает элементы. Заглавная буква отличает компонент от HTML-тега.",
      "В скобках — только выражения. Строки экранируются, сырой HTML — только через `dangerouslySetInnerHTML`.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Во что сборщик превращает `<Title size={2}>Привет</Title>`?",
      opts: [
        "В вызов `jsx(Title, { size: 2, children: 'Привет' })`",
        "В HTML-строку",
        "В DOM-узел",
        "В экземпляр класса `Title`",
      ],
      a: 0,
      why: "JSX — только удобная запись вызова функции. Дети попадают в проп `children`.",
    },
    {
      type: "sort",
      q: "Что это: HTML-тег или компонент?",
      groups: ["HTML-тег", "Компонент"],
      items: [
        ["`<div>`", 0],
        ["`<input>`", 0],
        ["`<section>`", 0],
        ["`<Header>`", 1],
        ["`<UserCard>`", 1],
      ],
      why: "С маленькой буквы JSX создаёт элемент с типом-строкой — это тег. С заглавной — ссылку на функцию-компонент.",
    },
    {
      type: "quiz",
      code: `const comment = '<img src=x onerror=alert(1)>';
return <p>{comment}</p>;`,
      q: "Что увидит пользователь?",
      opts: [
        "Текст `<img src=x onerror=alert(1)>`: React экранирует строки",
        "Картинку и всплывающее окно",
        "Пустой абзац",
        "Ошибку сборки",
      ],
      a: 0,
      why: "Строки в фигурных скобках вставляются как текст. Чтобы выполнился HTML, нужен `dangerouslySetInnerHTML` — поэтому он так и назван.",
    },
  ],
};
