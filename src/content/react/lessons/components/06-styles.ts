import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "jsx6",
  region: 0,
  title: "Стили: CSS Modules, CSS-in-JS и Tailwind",
  q: "Какие есть способы стилизовать React-компоненты? Чем они отличаются и что выбрать сегодня?",
  answer:
    "Основные подходы: обычный CSS с соглашением об именах, CSS Modules — классы, уникальные для файла, CSS-in-JS со стилями в коде компонента, и утилитарный CSS вроде Tailwind — готовые классы прямо в разметке. Классический CSS-in-JS (styled-components, Emotion) считает стили во время работы программы: это стоит времени на каждом рендере и плохо сочетается с серверными компонентами, поэтому новые проекты чаще берут CSS Modules, Tailwind или CSS-in-JS без выполнения в браузере — vanilla-extract, Panda. Инлайн-стиль `style={{…}}` годится для вычисляемых значений, но не для оформления целиком.",
  theory: {
    p: [
      "В JSX класс задают атрибутом `className`, а инлайн-стиль — объектом `style={{ color: \"red\", marginTop: 8 }}` с именами в camelCase и числами как пикселями. Инлайн-стили не умеют `:hover`, медиазапросы и псевдоэлементы, поэтому их оставляют для значений, вычисляемых на лету: ширина прогресса, координаты.",
      "CSS Modules: файл `Button.module.css` импортируется как объект, `styles.primary` — уникальное имя класса вида `Button_primary__x7k2`. Стили не пересекаются между компонентами, работают на этапе сборки и ничего не стоят во время работы. Классы по условию склеивают маленькой функцией вроде `clsx`.",
      "CSS-in-JS пишет стили в JavaScript рядом с компонентом и умеет стили по пропсам. Классические библиотеки со «временем выполнения» (styled-components, Emotion) вычисляют и вставляют стили в браузере во время рендера — это дополнительная работа и конфликт с серверными компонентами, где нет контекста и рендера на клиенте. Поэтому появились библиотеки без выполнения: vanilla-extract, Panda, Linaria — они превращают стили в обычный CSS на этапе сборки. Сама styled-components в 2025 году перешла в режим поддержки.",
      "Tailwind — утилитарные классы: `className=\"px-4 py-2 rounded bg-blue-600 hover:bg-blue-700\"`. Не нужно придумывать имена, неиспользуемые классы выбрасываются при сборке, дизайн-система задана в теме. Минус — длинные строки классов, их выносят в компоненты. Выбор команды обычно: CSS Modules или Tailwind, а для дизайн-систем — CSS-переменные для тем.",
    ],
    code: `import styles from './Button.module.css';
import clsx from 'clsx';

type Props = { primary?: boolean; progress: number; children: React.ReactNode };

export function Button({ primary, progress, children }: Props) {
  return (
    <button className={clsx(styles.button, primary && styles.primary)}>
      {children}
      {/* инлайн-стиль — только для вычисляемого значения */}
      <span className={styles.bar} style={{ width: \`\${progress}%\` }} />
    </button>
  );
}

export function TailwindButton() {
  return <button className="px-4 py-2 rounded bg-blue-600 text-white hover:bg-blue-700">Купить</button>;
}`,
    flow: {
      actors: ["Button.module.css", "Сборщик", "Бандл CSS", "Браузер"],
      steps: [
        { from: 0, to: 1, label: ".primary { … }" },
        { from: 1, to: 2, label: "Button_primary__x7k2 — уникальное имя" },
        { from: 2, to: 3, label: "обычный CSS-файл, стили готовы до рендера", note: "CSS-in-JS со временем выполнения считал бы их в браузере" },
      ],
    },
    keys: [
      "`className` и `style={{…}}`: инлайн — только для вычисляемых значений, без `:hover` и медиазапросов.",
      "CSS Modules и Tailwind работают на этапе сборки и ничего не стоят во время работы.",
      "CSS-in-JS со временем выполнения (styled-components, Emotion) тормозит рендер и плохо дружит с серверными компонентами; есть варианты без выполнения.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь подход и его особенность.",
      pairs: [
        ["CSS Modules", "уникальные имена классов на уровне файла, CSS на этапе сборки"],
        ["styled-components", "стили вычисляются в браузере во время рендера"],
        ["Tailwind", "готовые утилитарные классы прямо в разметке"],
        ["`style={{…}}`", "значения на лету, но без `:hover` и медиазапросов"],
      ],
      why: "Главная ось выбора — когда считаются стили: при сборке или во время работы в браузере.",
    },
    {
      type: "quiz",
      q: "Почему классический CSS-in-JS вроде styled-components плохо сочетается с серверными компонентами React?",
      opts: [
        "Он вычисляет стили во время рендера через контекст на клиенте, а серверные компоненты не рендерятся в браузере и не имеют контекста",
        "Серверные компоненты не поддерживают CSS вообще",
        "styled-components нельзя установить через npm",
        "Серверные компоненты запрещают `className`",
      ],
      a: 0,
      why: "Поэтому такие компоненты приходится делать клиентскими. Стили, собранные на этапе сборки, работают везде.",
    },
    {
      type: "quiz",
      q: "Кнопке нужна ширина полосы прогресса по пропсу `percent`. Как её задать?",
      code: `<div className={styles.bar} style={{ width: \`\${percent}%\` }} />`,
      opts: [
        "Так и задать — инлайн-стиль подходит для вычисляемого значения",
        "Создать по классу на каждый процент",
        "Только через styled-components",
        "Нельзя: `style` в React не работает",
      ],
      a: 0,
      why: "Оформление — в классах, а одно динамическое значение — инлайн-стилем или CSS-переменной: `style={{ \"--w\": percent + \"%\" }}`.",
    },
  ],
};
