import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "jsx2",
  region: 0,
  title: "Пропсы и чистые компоненты",
  q: "Что такое пропсы и почему компонент должен быть чистой функцией?",
  answer:
    "Пропсы — входные данные компонента: объект, который родитель передаёт через атрибуты JSX. Они только для чтения: компонент не меняет свои пропсы, а если данные должны меняться, для этого есть состояние. Компонент обязан быть чистым: при тех же пропсах, состоянии и контексте он возвращает тот же JSX и во время рендера ничего не меняет снаружи. React может вызвать компонент сколько угодно раз и в любой момент, поэтому побочные действия живут в обработчиках событий и эффектах.",
  theory: {
    p: [
      "Компонент — функция, которая получает один аргумент — объект пропсов — и возвращает JSX. Пропсы обычно разбирают прямо в параметрах и задают значения по умолчанию: `function Avatar({ user, size = 40 })`. Особый проп `children` — всё, что написано между открывающим и закрывающим тегом.",
      "Пропсы только для чтения. Данные текут сверху вниз: родитель передаёт, ребёнок читает. Если ребёнку нужно что-то изменить у родителя, родитель передаёт функцию, например `onChange`. Разница с состоянием: пропсы приходят снаружи и меняются, только когда родитель передал новые, а состояние живёт внутри компонента и меняется им самим.",
      "Правило чистоты — одно из главных в React. При одних и тех же входных данных компонент возвращает один и тот же результат и во время рендера не трогает ничего снаружи: не меняет глобальные переменные, не отправляет запросы, не пишет в DOM и не меняет объекты из пропсов. React может рендерить компонент дважды — в `StrictMode` так и делает в разработке, — откладывать и отбрасывать рендер. Нечистый компонент тогда ведёт себя непредсказуемо.",
      "Где же побочные действия? В обработчиках событий — они не вызываются при рендере, — а когда нужно синхронизироваться с внешним миром, в эффектах: о них регион «Хуки и эффекты». И ещё одно правило: компонент не вызывают как обычную функцию — `Avatar(props)`. Его используют только в JSX, тогда React сам решает, когда рендерить, и хуки внутри работают правильно.",
    ],
    code: `function Avatar({ user, size = 40 }: { user: { name: string; photo: string }; size?: number }) {
  return <img src={user.photo} alt={user.name} width={size} height={size} />;
}

function Card({ children }: { children: React.ReactNode }) {
  return <div className="card">{children}</div>;
}

// Нечисто: меняет внешнюю переменную во время рендера
let guests = 0;
function Guest() {
  guests = guests + 1; // при двойном рендере в StrictMode номера «перепрыгнут»
  return <p>Гость №{guests}</p>;
}`,
    flow: {
      actors: ["Родитель", "Пропсы (только чтение)", "Ребёнок"],
      steps: [
        { from: 0, to: 1, label: "user, size, onSelect" },
        { from: 1, to: 2, label: "ребёнок читает и рисует", note: "сам пропсы не меняет" },
        { from: 2, to: 0, label: "onSelect(id)", note: "наверх — через функцию-проп" },
        { from: 0, to: 0, label: "родитель меняет своё состояние" },
        { from: 0, to: 2, label: "новые пропсы — ререндер ребёнка" },
      ],
    },
    keys: [
      "Пропсы — входные данные от родителя, только для чтения. Наверх данные идут через функции-пропсы.",
      "Компонент чистый: те же входные данные — тот же JSX, и никаких изменений снаружи во время рендера.",
      "Побочные действия — в обработчиках и эффектах. Компонент используют в JSX, а не вызывают как функцию.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      code: `let guests = 0;
function Guest() {
  guests = guests + 1;
  return <p>Гость №{guests}</p>;
}`,
      q: "Что не так с компонентом `Guest`?",
      opts: [
        "Он нечистый: меняет внешнюю переменную во время рендера",
        "Нельзя возвращать `<p>`",
        "Нельзя объявлять переменные вне компонента",
        "Всё правильно",
      ],
      a: 0,
      why: "Результат зависит от того, сколько раз React вызвал функцию. Номер гостя должен приходить пропсом.",
    },
    {
      type: "sort",
      q: "Можно ли делать это во время рендера компонента?",
      groups: ["Можно", "Нельзя"],
      items: [
        ["вычислить полное имя из пропсов", 0],
        ["отфильтровать массив из пропсов в новую переменную", 0],
        ["отправить запрос на сервер", 1],
        ["изменить объект, пришедший в пропсах", 1],
        ["записать значение в `localStorage`", 1],
      ],
      why: "В рендере — только вычисления. Всё, что меняет мир снаружи, — в обработчиках или эффектах.",
    },
    {
      type: "quiz",
      q: "Чем пропсы отличаются от состояния?",
      opts: [
        "Пропсы приходят от родителя и только читаются, состояние живёт в компоненте и меняется им самим",
        "Пропсы быстрее состояния",
        "Состояние бывает только в классах",
        "Ничем",
      ],
      a: 0,
      why: "Одно и то же значение может быть состоянием у родителя и пропсом у ребёнка.",
    },
  ],
};
