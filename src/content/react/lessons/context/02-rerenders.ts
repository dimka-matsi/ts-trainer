import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ctx2",
  region: 4,
  level: "middle",
  title: "Context и лишние ререндеры",
  q: "Почему Context может тормозить? Что происходит с потребителями, когда меняется `value`?",
  answer:
    "Когда значение провайдера меняется, React перерисовывает все компоненты, которые читают этот контекст, даже если им нужна одна его часть. Сравнение идёт по `Object.is`, поэтому объект, созданный заново при рендере провайдера, «меняется» каждый раз. `React.memo` не спасает: потребитель всё равно получает новое значение. Лечат так: мемоизируют `value` через `useMemo`, делят контекст по частоте изменений, выносят функции изменения в отдельный контекст, а часто меняющиеся данные держат не в контексте.",
  theory: {
    p: [
      "Правило простое: изменилось значение провайдера — перерисовываются все компоненты, которые вызывают `useContext` этого контекста. Выбрать часть значения нельзя. Если в одном контексте лежат и тема, и позиция мыши, движение мыши перерисует всё, что читает тему.",
      "React сравнивает старое и новое значение по `Object.is`. Если провайдер пишет `value={{ user, login }}`, при каждом его рендере создаётся новый объект, и все потребители перерисуются, даже если `user` тот же. Лекарство — `useMemo` для объекта и `useCallback` для функций внутри него.",
      "`React.memo` на потребителе не помогает: он пропускает рендер, когда не изменились пропсы, а контекст — не пропс. Потребитель перерисуется при новом значении контекста в любом случае. Зато `memo` полезен для промежуточных компонентов, которые контекст не читают: их можно не перерисовывать вместе с провайдером.",
      "Приёмы. Делить контексты по смыслу и частоте изменений: тема отдельно, пользователь отдельно. Отделять данные от функций: компоненты, которым нужно только отправлять действия, читают `DispatchContext` и не перерисовываются при изменении данных — `dispatch` стабилен. А данные, которые меняются часто и нужны многим, держат в хранилище с подпиской на часть состояния — о таких следующие регионы.",
    ],
    code: `function AuthProvider({ children }: { children: React.ReactNode }) {
  const [user, setUser] = useState<User | null>(null);
  const login = useCallback((u: User) => setUser(u), []);

  // Плохо: value={{ user, login }} — новый объект при каждом рендере провайдера
  // Хорошо: ссылка меняется, только когда изменился user
  const value = useMemo(() => ({ user, login }), [user, login]);

  return <AuthContext value={value}>{children}</AuthContext>;
}

function CartProvider({ children }: { children: React.ReactNode }) {
  const [items, dispatch] = useReducer(cartReducer, []);
  // Данные и функции — в разных контекстах
  return (
    <CartStateContext value={items}>
      <CartDispatchContext value={dispatch}>{children}</CartDispatchContext>
    </CartStateContext>
  );
}`,
    flow: {
      actors: ["Провайдер AppContext", "Header (читает theme)", "Sidebar (читает user)", "Cursor (читает mouse)"],
      steps: [
        { from: 0, to: 3, label: "mouse изменился: новое value", note: "десятки раз в секунду" },
        { from: 0, to: 1, label: "ререндер Header", note: "лишний: тема не менялась", lost: true },
        { from: 0, to: 2, label: "ререндер Sidebar", note: "лишний: пользователь тот же", lost: true },
        { from: 0, to: 3, label: "ререндер Cursor", note: "нужен" },
        { from: 0, to: 0, label: "разделили: ThemeContext, UserContext, MouseContext" },
        { from: 0, to: 3, label: "mouse изменился → ререндер только Cursor", note: "Header и Sidebar не тронуты" },
      ],
    },
    keys: [
      "Новое `value` — ререндер всех потребителей контекста. Часть значения выбрать нельзя.",
      "Сравнение по `Object.is`: объект в `value` мемоизируют через `useMemo`, иначе он новый при каждом рендере. `React.memo` потребителя не спасает.",
      "Делят контексты по частоте изменений, данные отдельно от функций. Частые данные — в хранилище с подпиской на часть состояния.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      code: `const SettingsPanel = memo(function SettingsPanel() {
  const settings = useContext(SettingsContext);
  return <p>{settings.theme}</p>;
});

function App() {
  const [count, setCount] = useState(0);
  return (
    <SettingsContext value={{ theme: 'dark', lang: 'ru' }}>
      <button onClick={() => setCount(count + 1)}>{count}</button>
      <SettingsPanel />
    </SettingsContext>
  );
}`,
      q: "Перерисуется ли `SettingsPanel` при клике по кнопке?",
      opts: [
        "Да: при каждом рендере App создаётся новый объект `value`, и `memo` не спасает от контекста",
        "Нет: `memo` пропускает рендер, пропсов у панели нет",
        "Нет: тема и язык не изменились",
        "Только в режиме разработки",
      ],
      a: 0,
      why: "App перерисовывается из-за `count`, объект в `value` создаётся заново, и по `Object.is` это новое значение. Объект выносят за пределы компонента или мемоизируют.",
    },
    {
      type: "sort",
      q: "Что помогает сократить ререндеры от контекста?",
      groups: ["Помогает", "Не помогает"],
      items: [
        ["мемоизировать `value` через `useMemo`", 0],
        ["разделить контекст на тему и пользователя", 0],
        ["вынести `dispatch` в отдельный контекст", 0],
        ["обернуть потребителя в `React.memo`", 1],
        ["положить в общий контекст позицию мыши", 1],
      ],
      why: "Помогает всё, что реже меняет значение контекста или сужает круг его читателей. `memo` сравнивает пропсы, а не контекст.",
    },
    {
      type: "quiz",
      q: "Почему `React.memo` не спасает потребителя контекста от ререндера?",
      opts: [
        "`memo` сравнивает пропсы, а контекст приходит не через пропсы",
        "`memo` не работает с функциональными компонентами",
        "`memo` отключается внутри провайдера",
        "Спасает, если передать второй аргумент",
      ],
      a: 0,
      why: "Новое значение контекста доставляется потребителю напрямую, мимо сравнения пропсов.",
    },
  ],
};
