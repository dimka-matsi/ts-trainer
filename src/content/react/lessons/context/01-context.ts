import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ctx1",
  region: 1,
  title: "Context: данные без пропсов",
  q: "Что такое React Context и как он работает? Это стейт-менеджер?",
  answer:
    "Context передаёт значение вниз по дереву без пропсов на каждом уровне. `createContext` создаёт контекст со значением по умолчанию, провайдер задаёт значение для своей ветки, а `useContext` читает значение ближайшего провайдера сверху. Сам по себе Context ничего не хранит и не управляет изменениями: он только доставляет значение, а хранит его обычный `useState` или `useReducer` в провайдере. Поэтому Context — механизм передачи данных, а не стейт-менеджер.",
  theory: {
    p: [
      "Context решает props drilling: значение, которое нужно многим компонентам на разной глубине, не нужно передавать через каждый уровень. Типичные примеры — тема, язык, вошедший пользователь, настройки.",
      "Три части. `createContext(defaultValue)` создаёт контекст. Провайдер задаёт значение для всех потомков: `<ThemeContext value={theme}>` в React 19, а раньше писали `<ThemeContext.Provider value={theme}>`. `useContext(ThemeContext)` в любом потомке читает значение ближайшего провайдера сверху. Если провайдера нет, вернётся значение по умолчанию.",
      "Провайдеры можно вкладывать: внутренний переопределяет значение для своей ветки. Так делают, например, тёмный блок внутри светлой страницы. Компонент всегда видит ближайший провайдер над собой.",
      "Важное различие. Context не хранит данные и не решает, как их менять. Хранит их обычное состояние компонента-провайдера, а контекст лишь доставляет значение вниз. Нет выборочной подписки на часть значения, нет инструментов отладки изменений, нет промежуточных обработчиков. Поэтому на собеседовании говорят: Context — механизм передачи данных, а не стейт-менеджер.",
    ],
    code: `const ThemeContext = createContext<'light' | 'dark'>('light');

function App() {
  const [theme, setTheme] = useState<'light' | 'dark'>('dark');
  return (
    <ThemeContext value={theme}>
      <Page />
      <button onClick={() => setTheme('light')}>Светлая тема</button>
    </ThemeContext>
  );
}

function Button() {
  const theme = useContext(ThemeContext); // 'dark' — от ближайшего провайдера
  return <button className={theme}>OK</button>;
}`,
    flow: {
      actors: ["App: useState(theme)", "Провайдер ThemeContext", "Page", "Button: useContext"],
      steps: [
        { from: 0, to: 1, label: "value = 'dark'", note: "значение хранит состояние App" },
        { from: 1, to: 2, label: "Page ничего не получает через пропсы", note: "промежуточные уровни не знают о теме" },
        { from: 1, to: 3, label: "useContext → 'dark'", note: "ближайший провайдер сверху" },
        { from: 0, to: 1, label: "setTheme('light'): новое value", note: "App перерисовался" },
        { from: 1, to: 3, label: "Button перерисован с 'light'" },
      ],
    },
    keys: [
      "`createContext` — контекст со значением по умолчанию, провайдер — значение для ветки, `useContext` — чтение ближайшего сверху.",
      "Решает props drilling для редко меняющихся общих значений: тема, язык, пользователь.",
      "Context доставляет значение, а хранит его состояние провайдера. Это механизм передачи, а не стейт-менеджер.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь, как значение доходит до компонента через контекст.",
      items: [
        "создать контекст через `createContext`",
        "в родителе хранить значение в `useState`",
        "обернуть ветку провайдером с этим значением",
        "в потомке прочитать значение через `useContext`",
        "при изменении состояния потомок получает новое значение",
      ],
      why: "Контекст — канал доставки. Значение живёт в состоянии родителя, провайдер отдаёт его вниз, `useContext` читает.",
    },
    {
      type: "quiz",
      code: `const LangContext = createContext('ru');

function Title() {
  const lang = useContext(LangContext);
  return <h1>{lang}</h1>;
}

// Title отрисован без провайдера LangContext`,
      q: "Что покажет `Title`?",
      opts: ["`ru` — значение по умолчанию из `createContext`", "Пустую строку", "Ошибку: провайдер обязателен", "`undefined`"],
      a: 0,
      why: "Без провайдера `useContext` возвращает значение по умолчанию. Но провайдер с `value={undefined}` передаст именно `undefined`.",
    },
    {
      type: "quiz",
      q: "Почему Context называют механизмом передачи данных, а не стейт-менеджером?",
      opts: [
        "Он не хранит данные и не управляет изменениями — только доставляет значение, которое хранит состояние провайдера",
        "Он работает только с примитивами",
        "Он медленнее стейт-менеджеров",
        "Он не умеет обновлять значение",
      ],
      a: 0,
      why: "У стейт-менеджера есть своё хранилище, правила изменений и подписки. У контекста — только доставка значения по дереву.",
    },
  ],
};
