import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ctx3",
  region: 4,
  title: "Паттерны: свой хук, провайдеры и `use`",
  q: "Как правильно организовать контекст в приложении? Зачем оборачивать `useContext` в свой хук?",
  answer:
    "Контекст обычно прячут за собственным хуком вроде `useAuth`: он читает контекст и бросает понятную ошибку, если провайдера нет, а компоненты не знают о деталях. Провайдер держит состояние и логику, наружу отдаёт мемоизированное значение. Несколько провайдеров собирают в одном компоненте, а ставят их как можно ниже — только над той веткой, которой они нужны. В React 19 есть `use(Context)`: его, в отличие от `useContext`, можно вызывать внутри условий.",
  theory: {
    p: [
      "Свой хук. Вместо `useContext(AuthContext)` в каждом компоненте пишут `useAuth()`. Внутри — `useContext` и проверка: если значение `null`, компонент стоит вне провайдера, и хук бросает ошибку с понятным текстом. Так ошибка видна сразу, а не превращается в «не могу прочитать поле у null» где-то глубоко.",
      "Провайдер — компонент с логикой: он хранит состояние, объявляет функции изменения, мемоизирует значение и отдаёт его вниз. Компоненты используют готовые `login` и `logout` и не знают, как они устроены. Логику можно менять, не трогая потребителей.",
      "Где ставить провайдер. Не обязательно в корне: если контекст нужен только странице настроек, провайдер ставят над ней. Тогда изменения значения не трогают остальное приложение. Много провайдеров в корне — «лестницу провайдеров» — собирают в один компонент `AppProviders`, чтобы корень оставался читаемым.",
      "В React 19 появился `use`: `use(ThemeContext)` читает контекст так же, как `useContext`, но его можно вызывать внутри `if` и циклов. Если же нужно выборочно подписываться на часть значения, это сигнал, что контекста мало и пора брать хранилище с селекторами.",
    ],
    code: `const AuthContext = createContext<AuthValue | null>(null);

export function useAuth() {
  const value = useContext(AuthContext);
  if (!value) throw new Error('useAuth нужно вызывать внутри AuthProvider');
  return value;
}

function Profile() {
  const { user, logout } = useAuth();
  return <button onClick={logout}>Выйти, {user.name}</button>;
}

function AppProviders({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider>
      <AuthProvider>{children}</AuthProvider>
    </ThemeProvider>
  );
}`,
    flow: {
      actors: ["Profile", "useAuth()", "AuthContext", "AuthProvider"],
      steps: [
        { from: 0, to: 1, label: "useAuth()" },
        { from: 1, to: 2, label: "useContext(AuthContext)" },
        { from: 2, to: 1, label: "null: провайдера выше нет", note: "компонент стоит не там" },
        { from: 1, to: 0, label: "Error: useAuth нужно вызывать внутри AuthProvider", note: "ошибка сразу и с понятным текстом", lost: true },
        { from: 3, to: 2, label: "провайдер поставили над страницей" },
        { from: 2, to: 1, label: "{ user, logout }" },
        { from: 1, to: 0, label: "готовые данные и функции", note: "Profile не знает, как устроен вход" },
      ],
    },
    keys: [
      "Контекст прячут за своим хуком: он читает контекст и бросает понятную ошибку без провайдера.",
      "Провайдер хранит состояние и логику, отдаёт мемоизированное значение. Потребители не знают деталей.",
      "Провайдер ставят как можно ниже. В React 19 `use(Context)` можно вызывать в условиях.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Зачем свой хук `useAuth` бросает ошибку, если контекст равен `null`?",
      opts: [
        "Чтобы сразу и понятно показать, что компонент стоит вне провайдера",
        "Чтобы ускорить рендер",
        "Так требует React",
        "Чтобы сбросить состояние",
      ],
      a: 0,
      why: "Без проверки ошибка всплывёт позже и в другом месте. С проверкой текст ошибки прямо говорит, что не так.",
    },
    {
      type: "match",
      q: "Сопоставь приём и что он даёт.",
      pairs: [
        ["свой хук `useAuth`", "понятная ошибка и один способ чтения"],
        ["провайдер над одной страницей", "изменения не задевают остальное приложение"],
        ["компонент `AppProviders`", "читаемый корень вместо лестницы провайдеров"],
        ["`use(ThemeContext)`", "чтение контекста внутри условия"],
      ],
      why: "Все приёмы про одно: контекст должен быть удобным, узким и безопасным в использовании.",
    },
    {
      type: "quiz",
      q: "Чем `use(Context)` из React 19 отличается от `useContext`?",
      opts: [
        "Его можно вызывать внутри условий и циклов",
        "Он не вызывает ререндеров",
        "Он работает без провайдера и всегда возвращает `null`",
        "Он читает только значение по умолчанию",
      ],
      a: 0,
      why: "На чтение контекста и ререндеры `use` влияет так же, как `useContext`. Отличие — его не ограничивают правила порядка хуков.",
    },
  ],
};
