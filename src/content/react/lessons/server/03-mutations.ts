import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "sq3",
  region: 7,
  title: "Мутации, инвалидация и оптимистичные обновления",
  q: "Как обновить данные на экране после изменения на сервере? Как сделать оптимистичное обновление?",
  answer:
    "Изменения отправляют через `useMutation`, а после успеха помечают связанные запросы устаревшими через `queryClient.invalidateQueries({ queryKey: ['todos'] })` — они перезапросятся, и экран покажет правду с сервера. Если сервер вернул обновлённый объект, его можно сразу положить в кэш через `setQueryData`. Оптимистичное обновление — показать результат до ответа сервера: в `onMutate` отменить текущие запросы, сохранить старые данные, записать новые в кэш; при ошибке в `onError` вернуть старые, а в `onSettled` всё равно перезапросить.",
  theory: {
    p: [
      "`useMutation({ mutationFn })` отправляет изменение: создать, изменить, удалить. У мутации свои статусы — `isPending`, `isError` — и колбэки `onSuccess`, `onError`, `onSettled`. Сама по себе мутация не знает, какие данные в кэше устарели: это решаешь ты.",
      "Инвалидация — самый простой и надёжный способ. После успешной мутации `invalidateQueries({ queryKey: ['todos'] })` помечает устаревшими все запросы, ключ которых начинается с `['todos']`. Те, что сейчас на экране, перезапросятся сразу. Второй способ — `setQueryData`: если сервер вернул обновлённый объект, положить его в кэш без лишнего запроса.",
      "Оптимистичное обновление — показать результат сразу, не дожидаясь сервера: лайк загорается мгновенно. Схема: в `onMutate` отменить текущие запросы этого ключа через `cancelQueries`, чтобы они не перезаписали наше изменение, сохранить старые данные, записать новые через `setQueryData` и вернуть старые как контекст. В `onError` — откатить из контекста. В `onSettled` — инвалидировать, чтобы сверить с сервером.",
      "Когда оптимистично, а когда нет. Для частых и почти всегда успешных действий — лайк, отметка задачи, перетаскивание карточки — оптимистичное обновление делает интерфейс мгновенным. Для оплаты, оформления заказа и необратимых действий нужен честный индикатор загрузки: показать «успех», а потом откатить, хуже, чем подождать секунду.",
    ],
    code: `const queryClient = useQueryClient();

const toggle = useMutation({
  mutationFn: (todo: Todo) => api.updateTodo({ ...todo, done: !todo.done }),
  onMutate: async (todo) => {
    await queryClient.cancelQueries({ queryKey: ['todos'] }); // старые ответы не перезапишут наше изменение
    const previous = queryClient.getQueryData<Todo[]>(['todos']);
    queryClient.setQueryData<Todo[]>(['todos'], (old = []) =>
      old.map((t) => (t.id === todo.id ? { ...t, done: !t.done } : t)),
    );
    return { previous }; // контекст для отката
  },
  onError: (_error, _todo, context) => {
    queryClient.setQueryData(['todos'], context?.previous); // откат
  },
  onSettled: () => queryClient.invalidateQueries({ queryKey: ['todos'] }), // сверить с сервером
});`,
    flow: {
      actors: ["Пользователь", "Кэш ['todos']", "useMutation", "Сервер"],
      steps: [
        { from: 0, to: 2, label: "отметил задачу" },
        { from: 2, to: 1, label: "onMutate: отменить запросы, сохранить старое, записать новое", note: "галочка видна сразу" },
        { from: 2, to: 3, label: "PATCH /todos/7" },
        { from: 3, to: 2, label: "500: ошибка", note: "сервер не принял изменение", lost: true },
        { from: 2, to: 1, label: "onError: вернуть старые данные", note: "откат" },
        { from: 2, to: 1, label: "onSettled: invalidateQueries", note: "сверили с сервером" },
      ],
    },
    keys: [
      "`useMutation` отправляет изменения. После успеха — `invalidateQueries` по ключу или `setQueryData` с ответом сервера.",
      "Оптимистично: `onMutate` — отменить запросы, сохранить старое, записать новое; `onError` — откатить; `onSettled` — инвалидировать.",
      "Оптимистично — для частых обратимых действий. Для оплаты и необратимого — честная загрузка.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь шаги оптимистичного обновления.",
      items: [
        "отменить текущие запросы этого ключа",
        "сохранить старые данные из кэша",
        "записать новые данные в кэш — пользователь видит результат",
        "при ошибке вернуть старые данные",
        "в конце инвалидировать запрос, чтобы сверить с сервером",
      ],
      why: "Отмена защищает от старых ответов, сохранение — для отката, инвалидация — чтобы в итоге на экране была правда с сервера.",
    },
    {
      type: "quiz",
      q: "После добавления задачи список на экране не обновился. Что забыли?",
      opts: [
        "Инвалидировать запрос `['todos']` после успешной мутации",
        "Увеличить `gcTime`",
        "Обернуть список в `React.memo`",
        "Добавить `staleTime`",
      ],
      a: 0,
      why: "Мутация не знает, что список зависит от неё. Инвалидация говорит кэшу, какие данные устарели.",
    },
    {
      type: "quiz",
      q: "Для какого действия оптимистичное обновление подходит хуже всего?",
      opts: ["Оплата заказа", "Лайк поста", "Отметка задачи выполненной", "Перетаскивание карточки на доске"],
      a: 0,
      why: "Если оплата не прошла, показанный заранее «успех» введёт человека в заблуждение. Здесь честнее подождать ответа.",
    },
  ],
};
