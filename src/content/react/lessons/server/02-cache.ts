import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "sq2",
  region: 7,
  title: "TanStack Query: ключи, `staleTime` и `gcTime`",
  q: "Как работает кэш TanStack Query? Чем `staleTime` отличается от `gcTime`?",
  answer:
    "Каждый запрос хранится в кэше по ключу — массиву вроде `['product', id]`: одинаковый ключ — одни данные на всех. `staleTime` — сколько данные считаются свежими: по умолчанию 0, то есть они сразу устаревшие и перезапрашиваются в фоне при новом монтировании компонента, возврате на вкладку и восстановлении сети. `gcTime` — сколько данные живут в кэше, когда их не показывает ни один компонент: по умолчанию 5 минут. Устаревшие данные показываются сразу, а обновление идёт в фоне; при ошибке запрос повторяется 3 раза.",
  theory: {
    p: [
      "`useQuery({ queryKey, queryFn })`. Ключ — массив, который описывает данные: `['todos']`, `['product', 7]`, `['products', { page: 2, sort: 'price' }]`. Всё, от чего зависит запрос, входит в ключ: изменился `id` — другой ключ, другой элемент кэша. Компоненты с одинаковым ключом делят один запрос и одни данные.",
      "`staleTime` — сколько данные считаются свежими. По умолчанию 0: сразу после загрузки они уже устаревшие. Это не значит, что их не показывают, — показывают, но при удобном случае перезапрашивают в фоне: когда монтируется новый компонент с этим ключом, когда окно снова получает фокус и когда восстанавливается сеть. Для справочников, которые меняются редко, `staleTime` ставят в минуты.",
      "`gcTime` (до версии 5 назывался `cacheTime`) — сколько данные лежат в кэше, когда их не использует ни один компонент. По умолчанию 5 минут, потом кэш очищается. Вернулся на страницу за это время — данные показываются мгновенно из кэша и тихо обновляются. Итого: `staleTime` — когда перезапрашивать, `gcTime` — когда выбросить.",
      "Ещё поведение по умолчанию. Ошибочный запрос повторяется 3 раза с растущей паузой. Новый результат сравнивается со старым по структуре (structural sharing): если данные не изменились, ссылка остаётся той же, и лишнего ререндера нет. В версии 5 первый статус загрузки называется `isPending`, а `isFetching` показывает любой запрос, включая фоновый.",
    ],
    code: `function ProductPage({ id }: { id: number }) {
  const { data, isPending, isFetching, error } = useQuery({
    queryKey: ['product', id], // id в ключе: другой товар — другой элемент кэша
    queryFn: () => api.getProduct(id),
    staleTime: 60_000, // минуту данные свежие: без фоновых перезапросов
    // gcTime по умолчанию 5 минут: столько неиспользуемые данные живут в кэше
  });

  if (isPending) return <Skeleton />;
  if (error) return <ErrorBox error={error} />;
  return <Card product={data} updating={isFetching} />;
}`,
    flow: {
      actors: ["Страница товара", "Кэш ['product', 7]", "Сервер"],
      steps: [
        { from: 0, to: 1, label: "useQuery(['product', 7])" },
        { from: 1, to: 2, label: "данных нет: запрос" },
        { from: 2, to: 1, label: "товар, свежий 60 секунд" },
        { from: 0, to: 0, label: "ушёл со страницы", note: "данные лежат в кэше: gcTime 5 минут" },
        { from: 0, to: 1, label: "вернулся через 2 минуты" },
        { from: 1, to: 0, label: "товар из кэша сразу", note: "показали устаревшее мгновенно" },
        { from: 1, to: 2, label: "фоновый перезапрос", note: "staleTime прошёл" },
        { from: 2, to: 1, label: "свежие данные" },
        { from: 1, to: 0, label: "тихое обновление" },
      ],
    },
    keys: [
      "Ключ — массив, в нём всё, от чего зависит запрос. Одинаковый ключ — одни данные и один запрос.",
      "`staleTime` (по умолчанию 0) — когда перезапрашивать, `gcTime` (по умолчанию 5 минут) — когда выбросить неиспользуемое.",
      "Устаревшее показывается сразу, свежее приходит в фоне: при монтировании, фокусе окна, восстановлении сети. При ошибке — 3 повтора.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь настройку TanStack Query и её смысл.",
      pairs: [
        ["`staleTime`", "сколько данные считаются свежими"],
        ["`gcTime`", "сколько неиспользуемые данные живут в кэше"],
        ["`queryKey`", "адрес данных в кэше"],
        ["`isFetching`", "идёт любой запрос, в том числе фоновый"],
      ],
      why: "Путаница `staleTime` и `gcTime` — самая частая. Первое — про перезапрос, второе — про удаление.",
    },
    {
      type: "quiz",
      q: "Настройки по умолчанию. Пользователь переключился на другую вкладку и вернулся. Что сделает TanStack Query?",
      opts: [
        "Покажет данные из кэша и перезапросит их в фоне: они уже устаревшие",
        "Ничего: данные свежие 5 минут",
        "Очистит кэш и покажет загрузку",
        "Перезагрузит страницу",
      ],
      a: 0,
      why: "`staleTime` по умолчанию 0, а возврат фокуса на окно — один из поводов обновить устаревшие данные.",
    },
    {
      type: "quiz",
      code: `const { data } = useQuery({
  queryKey: ['products'],
  queryFn: () => api.getProducts({ page, sort }),
});`,
      q: "Страница и сортировка меняются, а список не обновляется. В чём ошибка?",
      opts: [
        "`page` и `sort` не входят в ключ: для кэша это один и тот же запрос",
        "Нужен `staleTime: 0`",
        "`queryFn` должен быть синхронным",
        "Запрос нужно обернуть в `useEffect`",
      ],
      a: 0,
      why: "Ключ должен включать всё, от чего зависит запрос: `['products', { page, sort }]`.",
    },
  ],
};
