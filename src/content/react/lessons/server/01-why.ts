import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "sq1",
  region: 7,
  title: "Серверное состояние: почему не `useEffect` + `useState`",
  q: "Почему данные с сервера лучше не загружать вручную в `useEffect` и не хранить в Redux? Что дают библиотеки вроде TanStack Query?",
  answer:
    "Загрузка в `useEffect` кажется простой, но быстро обрастает проблемами: гонки ответов, одинаковые запросы из разных компонентов, нет кэша, нет повторов при ошибке, данные устаревают, а загрузку и ошибку нужно хранить руками. В Redux это превращается в кучу кода на каждый запрос. Библиотеки серверного состояния — TanStack Query, RTK Query, SWR — решают это одним хуком: кэш по ключу, объединение одинаковых запросов, обновление в фоне, повторы, сброс после изменений. Они управляют кэшем, а не хранилищем, и после них глобального состояния часто почти не остаётся.",
  theory: {
    p: [
      "Типичный код: `useEffect` с `fetch` и три `useState` — данные, загрузка, ошибка. Проблемы начинаются сразу. Пользователь быстро переключает товары, и ответ на старый запрос приходит позже нового — на экране не тот товар. Три компонента показывают профиль — три одинаковых запроса. Ушёл со страницы и вернулся — всё грузится заново.",
      "Серверное состояние — копия чужих данных, и она устаревает. Нужно решать: когда считать данные свежими, когда перезапросить, как обновить после изменения, что показывать во время загрузки, повторять ли при ошибке. Если хранить это в Redux, на каждый запрос появляются действия загрузки, успеха и ошибки, редьюсеры и ручная логика устаревания.",
      "Библиотеки серверного состояния делают это за тебя. Данные лежат в кэше по ключу запроса: одинаковые запросы объединяются в один, результат делят все компоненты. Устаревшие данные показываются сразу, а свежие подтягиваются в фоне. Есть повторы при ошибке, обновление при возвращении на вкладку и после изменений.",
      "Самые известные: TanStack Query (раньше React Query), RTK Query из Redux Toolkit и SWR — название от принципа stale-while-revalidate: «показать устаревшее, пока проверяем свежее». После перехода на них в глобальном хранилище остаётся мало: тема, пользователь, открытые панели. Поэтому на вопрос «какой стейт-менеджер» сильный ответ начинается с разделения серверного и клиентского состояния.",
    ],
    code: `// Вручную: гонки, нет кэша, дубли запросов, и так в каждом компоненте
function Product({ id }: { id: number }) {
  const [data, setData] = useState<ProductData | null>(null);
  const [error, setError] = useState<Error | null>(null);

  useEffect(() => {
    let ignore = false; // защита от гонки: ответ на старый id не должен победить
    fetch('/api/products/' + id)
      .then((r) => r.json())
      .then((d) => { if (!ignore) setData(d); })
      .catch((e) => { if (!ignore) setError(e); });
    return () => { ignore = true; };
  }, [id]);

  return <Card product={data} error={error} />;
}

// С библиотекой: кэш, повторы, обновление в фоне, один запрос на всех
function ProductQ({ id }: { id: number }) {
  const { data, isPending, error } = useQuery({
    queryKey: ['product', id],
    queryFn: () => fetch('/api/products/' + id).then((r) => r.json()),
  });
  return <Card product={data} loading={isPending} error={error} />;
}`,
    flow: {
      actors: ["ProductCard", "ProductTitle", "Кэш запросов", "Сервер"],
      steps: [
        { from: 0, to: 2, label: "['product', 7]", note: "нет в кэше — запрос" },
        { from: 1, to: 2, label: "['product', 7]", note: "тот же ключ — ждёт тот же запрос, дубля нет" },
        { from: 2, to: 3, label: "GET /api/products/7", note: "один запрос на двоих" },
        { from: 3, to: 2, label: "товар 7" },
        { from: 2, to: 0, label: "данные" },
        { from: 2, to: 1, label: "данные", note: "оба компонента получили один результат" },
      ],
    },
    keys: [
      "`useEffect` + `fetch` вручную: гонки, дубли запросов, нет кэша, повторов и обновления в фоне.",
      "Серверное состояние — кэш с правилами устаревания, а не хранилище. Держать его в Redux — много ручного кода.",
      "TanStack Query, RTK Query, SWR: кэш по ключу, дедупликация, фон, повторы, сброс. После них глобального состояния остаётся мало.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что решает библиотека серверного состояния, а что остаётся твоей задачей?",
      groups: ["Решает библиотека", "Остаётся тебе"],
      items: [
        ["два компонента запрашивают одно и то же — запрос один", 0],
        ["повтор запроса при ошибке сети", 0],
        ["обновление данных при возвращении на вкладку", 0],
        ["какие поля показать в карточке товара", 1],
        ["выбранная тема оформления", 1],
      ],
      why: "Библиотека берёт на себя жизнь серверных данных: загрузку, кэш и обновление. Интерфейс и клиентское состояние — по-прежнему твои.",
    },
    {
      type: "quiz",
      q: "Пользователь быстро переключает товары, и иногда на экране не тот товар. Что произошло?",
      opts: [
        "Гонка: ответ на старый запрос пришёл позже нового и перезаписал данные",
        "Сервер вернул ошибку",
        "React не успел перерисовать",
        "Кэш браузера отдал старую страницу",
      ],
      a: 0,
      why: "Запросы не обязаны отвечать по порядку. Библиотеки кэша привязывают ответ к ключу, и старый ответ не попадёт на экран нового товара.",
    },
    {
      type: "quiz",
      q: "Откуда название библиотеки SWR?",
      opts: [
        "От принципа stale-while-revalidate: показать устаревшее, пока проверяем свежее",
        "От Server-Worker-Render",
        "От имени автора",
        "От формата ответа сервера",
      ],
      a: 0,
      why: "Тот же принцип, что у одноимённой директивы кэширования HTTP: мгновенный ответ из кэша и тихое обновление.",
    },
  ],
};
