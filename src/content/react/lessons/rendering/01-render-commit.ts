import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rn1",
  region: 3,
  level: "middle",
  title: "Рендер и коммит",
  q: "Что происходит, когда React обновляет экран? Что такое фазы render и commit и что вызывает ререндер?",
  answer:
    "Обновление проходит три шага. Триггер: первый показ через `createRoot(…).render()` или изменение состояния. Рендер: React вызывает компоненты и вычисляет, как должен выглядеть интерфейс; эта фаза чистая, её можно прервать и повторить. Коммит: React применяет к DOM минимальные изменения, привязывает ref и запускает эффекты раскладки, после чего браузер рисует кадр, а потом выполняются обычные эффекты. Компонент перерисовывается, когда меняется его состояние, контекст, который он читает, или перерисовался родитель. Ререндер не значит изменение DOM: если результат тот же, DOM не трогается.",
  theory: {
    p: [
      "Триггер. Первый показ — `createRoot(domNode).render(<App />)`. Дальше React перерисовывает, когда вызывают сеттер состояния или `dispatch`, меняется значение контекста, который читает компонент, или перерисовывается родитель — тогда по умолчанию перерисовываются и все его дети. Изменение пропсов само по себе не причина: пропсы меняются, потому что перерисовался родитель.",
      "Рендер (render phase). React вызывает функции компонентов, начиная с того, где произошло изменение, и спускается к детям. Результат — новое дерево элементов. Эта фаза должна быть чистой: React может вызвать компонент несколько раз, прервать работу ради более срочной и отбросить результат. Поэтому в рендере нет побочных действий.",
      "Коммит (commit phase). React сравнивает новое дерево с прошлым и применяет к DOM только различия: вставить, удалить, поменять атрибут или текст. Эта фаза синхронная, её не прерывают. В ней привязываются ref и выполняются `useLayoutEffect`. Потом браузер рисует экран, а после отрисовки выполняются `useEffect`.",
      "Batching: несколько обновлений в одном обработчике, таймере или промисе React собирает в один рендер. Если нужно применить изменение к DOM немедленно — например, прокрутить к только что добавленному элементу, — есть `flushSync(() => setItems(…))` из `react-dom`. Им пользуются редко: он отключает пакетную обработку и замедляет работу.",
    ],
    code: `const root = createRoot(document.getElementById('root')!);
root.render(<App />); // триггер: первый показ

function List() {
  const [items, setItems] = useState<string[]>([]);
  const listRef = useRef<HTMLUListElement>(null);

  function add(text: string) {
    // Применить к DOM сразу, чтобы прокрутить к новому элементу
    flushSync(() => setItems([...items, text]));
    listRef.current!.lastElementChild!.scrollIntoView();
  }

  return <ul ref={listRef}>{items.map((t) => <li key={t}>{t}</li>)}</ul>;
}`,
    flow: {
      actors: ["Триггер", "Render: вызов компонентов", "Commit: DOM", "Браузер", "Эффекты"],
      steps: [
        { from: 0, to: 1, label: "setState в обработчике", note: "или первый root.render" },
        { from: 1, to: 1, label: "вызвать компонент и детей, получить новое дерево", note: "чисто, можно прервать" },
        { from: 1, to: 2, label: "применить только различия", note: "синхронно, ref и useLayoutEffect" },
        { from: 2, to: 3, label: "отрисовка кадра" },
        { from: 3, to: 4, label: "useEffect после отрисовки" },
      ],
    },
    keys: [
      "Три шага: триггер → рендер (вызов компонентов, чисто и прерываемо) → коммит (изменения DOM, синхронно).",
      "Ререндер вызывают своё состояние, прочитанный контекст и ререндер родителя. DOM меняется только там, где изменился результат.",
      "Обновления собираются в пачку. `flushSync` применяет изменение немедленно — редко и осознанно.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь шаги обновления экрана.",
      items: [
        "вызван сеттер состояния",
        "React вызывает компонент и его детей",
        "React применяет различия к DOM",
        "браузер рисует кадр",
        "выполняются `useEffect`",
      ],
      why: "Рендер вычисляет, коммит применяет, браузер рисует, эффекты догоняют.",
    },
    {
      type: "quiz",
      q: "Компонент перерисовался, но вернул тот же JSX. Что произойдёт с DOM?",
      opts: [
        "Ничего: React сравнит результат и не тронет DOM",
        "DOM будет полностью пересоздан",
        "Обновятся все атрибуты",
        "Браузер перезагрузит страницу",
      ],
      a: 0,
      why: "Ререндер — это вызов функции и сравнение. Изменения DOM случаются только там, где результат другой.",
    },
    {
      type: "quiz",
      q: "Какая фаза может быть прервана и повторена?",
      opts: ["Рендер", "Коммит", "Отрисовка браузером", "Никакая"],
      a: 0,
      why: "Рендер чистый и ничего не меняет снаружи, поэтому его можно отбросить. Коммит меняет DOM и выполняется целиком.",
    },
  ],
};
