import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rn7",
  region: 3,
  level: "senior",
  title: "Activity и ViewTransition",
  q: "Что такое `<Activity>` и `<ViewTransition>` из React 19.2 и 19.3?",
  answer:
    "`<Activity mode='hidden'>` из React 19.2 прячет часть интерфейса, сохраняя её состояние и DOM: узлы скрываются через `display: none`, эффекты на это время очищаются, а при показе создаются снова. В отличие от условного рендера, состояние не теряется, а в отличие от простого CSS, скрытые компоненты не держат подписки. Скрытое содержимое можно заранее отрендерить с низким приоритетом. `<ViewTransition>`, стабильный с React 19.3, анимирует появление, исчезновение и перемещение элементов через View Transition API браузера — для обновлений в переходах, при раскрытии Suspense и в `useDeferredValue`.",
  theory: {
    p: [
      "Три способа спрятать вкладку. Условный рендер `{tab === 'a' && <A />}` — компонент уничтожается вместе с состоянием, введённым текстом и прокруткой. CSS `display: none` — состояние живёт, но эффекты продолжают работать: подписки, таймеры, видео. `<Activity mode={visible ? 'visible' : 'hidden'}>` — лучшее из двух: состояние и DOM сохранены, а эффекты очищены, пока скрыто.",
      "Как работает Activity. В режиме `hidden` React скрывает DOM через `display: none`, вызывает очистку всех эффектов внутри и обновляет скрытое содержимое с низким приоритетом, когда меняются пропсы. При возврате в `visible` эффекты создаются снова, а текст в полях и позиция видео на месте. Концептуально скрытый Activity — как размонтированный компонент, который помнит своё состояние.",
      "Применения Activity: вкладки и боковые панели, которые должны помнить своё состояние; предзагрузка следующего экрана — отрендерить его скрыто, чтобы он загрузил код и данные; выборочная гидратация. Подвох: у `<video>` и `<iframe>` есть свои побочные эффекты, которые не прекращаются от `display: none`, — их останавливают в очистке эффекта.",
      "`<ViewTransition>` оборачивает часть интерфейса и анимирует её изменения через View Transition API браузера: появление, исчезновение, обновление и «общий элемент», который перелетает с одного места на другое. Анимация срабатывает только для обновлений в `startTransition`, при раскрытии Suspense и в `useDeferredValue`, а не на каждый рендер. Через `addTransitionType('next')` можно выбрать анимацию в зависимости от причины перехода, например листать вперёд или назад.",
    ],
    code: `function Tabs({ tab }: { tab: 'feed' | 'profile' }) {
  return (
    <>
      {/* Состояние и DOM сохраняются, эффекты скрытой вкладки очищены */}
      <Activity mode={tab === 'feed' ? 'visible' : 'hidden'}>
        <Feed />
      </Activity>
      <Activity mode={tab === 'profile' ? 'visible' : 'hidden'}>
        <Profile />
      </Activity>
    </>
  );
}

function Gallery({ slide }: { slide: number }) {
  return (
    <ViewTransition>
      <Slide index={slide} />
    </ViewTransition>
  );
}

function nextSlide(setSlide: (fn: (n: number) => number) => void) {
  startTransition(() => {
    addTransitionType('next'); // своя анимация для «вперёд»
    setSlide((n) => n + 1);
  });
}`,
    flow: {
      actors: ["Вкладка «Лента»", "Activity", "DOM", "Эффекты ленты"],
      steps: [
        { from: 0, to: 1, label: "переключились на «Профиль»: mode = 'hidden'" },
        { from: 1, to: 2, label: "display: none", note: "узлы и прокрутка сохранены" },
        { from: 1, to: 3, label: "очистка эффектов", note: "подписки остановлены" },
        { from: 0, to: 1, label: "вернулись: mode = 'visible'" },
        { from: 1, to: 3, label: "эффекты созданы заново", note: "состояние ленты на месте" },
      ],
    },
    keys: [
      "`<Activity>` (React 19.2) прячет через `display: none`, сохраняет состояние и DOM, очищает эффекты, пока скрыто.",
      "Лучше условного рендера (не теряет состояние) и простого CSS (не держит подписки). Можно заранее рендерить скрытое.",
      "`<ViewTransition>` (стабилен с React 19.3) анимирует изменения через View Transition API, только для переходов, Suspense и `useDeferredValue`.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь способ спрятать вкладку и что происходит с ней.",
      pairs: [
        ["условный рендер `show && <Tab />`", "состояние уничтожается"],
        ["CSS `display: none`", "состояние живёт, эффекты продолжают работать"],
        ["`<Activity mode='hidden'>`", "состояние живёт, эффекты очищены"],
      ],
      why: "Activity объединяет плюсы двух способов: помнит состояние и не тратит ресурсы на подписки.",
    },
    {
      type: "quiz",
      q: "Что происходит с эффектами внутри `<Activity mode='hidden'>`?",
      opts: [
        "Они очищаются, а при показе создаются снова",
        "Продолжают работать как обычно",
        "Выполняются дважды",
        "Удаляются навсегда",
      ],
      a: 0,
      why: "Скрытый Activity ведёт себя как размонтированный компонент, который сохранил состояние.",
    },
    {
      type: "quiz",
      q: "Какие обновления анимирует `<ViewTransition>`?",
      opts: [
        "Обновления в `startTransition`, раскрытие Suspense и `useDeferredValue`",
        "Любой рендер компонента",
        "Только изменения CSS-классов",
        "Только первое монтирование страницы",
      ],
      a: 0,
      why: "Анимация на каждый рендер мешала бы. Она нужна для заметных смен интерфейса, а они и так идут через переходы.",
    },
  ],
};
