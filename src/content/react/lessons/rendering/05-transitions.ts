import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rn5",
  region: 3,
  title: "useTransition, useDeferredValue и startTransition",
  q: "Зачем нужны `useTransition` и `useDeferredValue`? Чем они отличаются?",
  answer:
    "Оба делят обновления на срочные и несрочные. Срочное — то, что пользователь ждёт сразу: символ в поле ввода. Несрочное — тяжёлый результат: отфильтрованный список, новая вкладка. `useTransition` помечает обновление состояния как переход: React начнёт его, но прервёт ради срочного и до готовности покажет старый интерфейс, а `isPending` скажет, что идёт работа. `useDeferredValue` делает то же для значения, когда управлять обновлением нельзя, например оно пришло пропсом: компонент получает отстающую копию. Работы меньше не становится — просто ввод перестаёт её ждать.",
  theory: {
    p: [
      "Поле фильтра над списком из 5000 карточек. Каждое нажатие обновляет запрос, и React синхронно перерисовывает список — 200 мс. Пока идёт рендер, следующий символ появится с задержкой. Конкурентный рендеринг умеет прервать тяжёлую работу, но ему нужно сказать, какая работа несрочная.",
      "`const [isPending, startTransition] = useTransition()`. Обновление поля — как обычно, оно срочное. Обновление состояния, от которого зависит тяжёлый список, оборачивают в `startTransition(() => setQuery(value))`. Символ появляется сразу, список считается в фоне, а если пользователь печатает дальше, недоделанный рендер выбрасывается. Есть и функция `startTransition` без хука — для кода вне компонентов.",
      "`useDeferredValue(query)` — то же, но со стороны значения. Список получает отложенную копию запроса, которая отстаёт от настоящей, пока идёт тяжёлый рендер. Удобно, когда значение пришло пропсом и обновление не в твоих руках. Сравнение `query !== deferredQuery` показывает, что результат устарел, — можно слегка затемнить список.",
      "В React 19 в `startTransition` можно передать асинхронную функцию — это Action: `isPending` будет `true`, пока идёт запрос. Переходы не для полей ввода: значение `<input>` в переходе будет отставать от набранного. И они не уменьшают работу: для списка на 50 000 строк нужна ещё и виртуализация.",
    ],
    code: `function Search({ items }: { items: Item[] }) {
  const [text, setText] = useState(''); // срочное: символ виден сразу
  const [query, setQuery] = useState('');
  const [isPending, startTransition] = useTransition();

  function handleChange(value: string) {
    setText(value);
    startTransition(() => setQuery(value)); // несрочное: список можно прервать
  }

  return (
    <>
      <input value={text} onChange={(e) => handleChange(e.target.value)} />
      <div style={{ opacity: isPending ? 0.6 : 1 }}>
        <HeavyList items={items} query={query} />
      </div>
    </>
  );
}

function Results({ query }: { query: string }) {
  const deferredQuery = useDeferredValue(query); // отстающая копия пропса
  return <HeavyList items={allItems} query={deferredQuery} />;
}`,
    flow: {
      actors: ["Пользователь", "Срочное: поле ввода", "Переход: список 5000 карточек"],
      steps: [
        { from: 0, to: 1, label: "нажал «ч»", note: "символ виден сразу" },
        { from: 1, to: 2, label: "startTransition: список по «ч»", note: "в фоне, по частям" },
        { from: 0, to: 1, label: "нажал «а»", note: "срочное прерывает переход" },
        { from: 2, to: 2, label: "рендер по «ч» выброшен", note: "устарел", lost: true },
        { from: 1, to: 2, label: "список по «ча»" },
        { from: 2, to: 0, label: "готовый список", note: "ввод ни разу не подтормозил" },
      ],
    },
    keys: [
      "Срочные обновления — ввод, несрочные — тяжёлый результат. Переход можно прервать ради срочного.",
      "`useTransition` помечает обновление состояния и даёт `isPending`. `useDeferredValue` откладывает значение, например пропс.",
      "В React 19 переход может быть асинхронным (Action). Работы меньше не становится, поле ввода в переход не кладут.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Какое обновление срочное, а какое можно пометить переходом?",
      groups: ["Срочное", "Переход"],
      items: [
        ["символ в поле ввода", 0],
        ["отметка чекбокса", 0],
        ["отфильтрованный список на 5000 карточек", 1],
        ["перерисовка графика по новому диапазону дат", 1],
        ["содержимое новой вкладки", 1],
      ],
      why: "Срочное — прямой отклик на действие. Переход — тяжёлый результат, который может появиться на мгновение позже.",
    },
    {
      type: "quiz",
      q: "Что делает React с незавершённым рендером перехода, если пользователь напечатал следующий символ?",
      opts: [
        "Выбрасывает его и начинает новый с актуальным значением",
        "Доделывает старый, потом делает новый",
        "Показывает ошибку",
        "Блокирует ввод до окончания рендера",
      ],
      a: 0,
      why: "Переход прерываемый. Устаревший результат никому не нужен.",
    },
    {
      type: "quiz",
      q: "Значение фильтра приходит пропсом, и обернуть его обновление в переход нельзя. Что взять?",
      opts: ["`useDeferredValue`", "`useTransition`", "`useLayoutEffect`", "`flushSync`"],
      a: 0,
      why: "`useDeferredValue` работает со значением, а не с вызовом сеттера, поэтому подходит, когда обновление происходит не у тебя.",
    },
  ],
};
