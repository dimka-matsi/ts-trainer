import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rn3",
  region: 3,
  title: "Позиция в дереве: сохранение и сброс состояния",
  q: "Когда React сохраняет состояние компонента, а когда сбрасывает? Почему нельзя объявлять компонент внутри другого компонента?",
  answer:
    "Состояние привязано не к переменной и не к JSX, а к позиции компонента в дереве. Пока на том же месте рендерится компонент того же типа, React сохраняет его состояние, даже если пропсы изменились. Состояние сбрасывается, если на этом месте появился другой тип, если компонент исчез из дерева или если у него сменился `key`. Компонент, объявленный внутри другого, — новая функция при каждом рендере родителя, то есть новый тип: React каждый раз пересоздаёт его, теряет состояние и DOM, и это ещё и медленно.",
  theory: {
    p: [
      "React хранит состояние у себя, в дереве, и сопоставляет его с компонентом по месту: «первый ребёнок `div`, тип `Counter`». Поэтому `{isFancy ? <Counter fancy /> : <Counter />}` сохраняет счёт при переключении: на этом месте всё тот же `Counter`, поменялся только проп.",
      "Сброс. Если на месте `Counter` отрисовался `<p>`, а потом снова `Counter`, это новый экземпляр с начальным состоянием. Если компонент убрали из дерева условием `{show && <Counter />}`, его состояние уничтожается. Скрыть и сохранить состояние можно через CSS или специальный компонент — о нём последний урок этого региона.",
      "Ключ меняет правило. `<Chat key={contactId} />` — при смене контакта ключ другой, и React создаёт новый чат с пустым черновиком. Без ключа черновик письма одному человеку остался бы в чате с другим. Ключ — способ сказать «это другой объект», даже если тип тот же.",
      "Компонент внутри компонента. `function Form() { function Input() {…} return <Input /> }` — при каждом рендере `Form` создаётся новая функция `Input`. Для React это новый тип на том же месте: он размонтирует старый `Input` и смонтирует новый. Поле теряет фокус и введённое значение после каждого символа. Компоненты объявляют на верхнем уровне модуля.",
    ],
    code: `function Form() {
  const [text, setText] = useState('');

  // Ошибка: новый тип при каждом рендере — поле теряет фокус на каждом символе
  function Field() {
    return <input value={text} onChange={(e) => setText(e.target.value)} />;
  }

  return <Field />;
}

function Messenger({ contactId }: { contactId: number }) {
  // Новый ключ — новый чат с пустым черновиком
  return <Chat key={contactId} contactId={contactId} />;
}`,
    flow: {
      actors: ["Рендер 1", "Позиция: первый ребёнок", "Рендер 2"],
      steps: [
        { from: 0, to: 1, label: "<Counter /> — счёт 3" },
        { from: 2, to: 1, label: "<Counter fancy />", note: "тот же тип — счёт 3 сохранён" },
        { from: 2, to: 1, label: "<p>Пауза</p>", note: "другой тип — Counter уничтожен", lost: true },
        { from: 2, to: 1, label: "снова <Counter />", note: "новый экземпляр — счёт 0" },
        { from: 2, to: 1, label: "<Counter key='b' /> вместо key='a'", note: "другой ключ — тоже сброс" },
      ],
    },
    keys: [
      "Состояние привязано к позиции в дереве и типу компонента, а не к переменной.",
      "Сброс — другой тип на месте, удаление из дерева или новый `key`. Ключом сбрасывают состояние нарочно.",
      "Компоненты не объявляют внутри компонентов: каждый рендер — новый тип и пересоздание с потерей состояния.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      code: `{isFancy ? <Counter color="gold" /> : <Counter color="gray" />}`,
      q: "Счётчик показывает 5. Что будет после переключения `isFancy`?",
      opts: [
        "Счёт останется 5: на том же месте тот же тип",
        "Счёт сбросится в 0",
        "Появятся два счётчика",
        "Ошибка из-за одинаковых компонентов",
      ],
      a: 0,
      why: "Это разные строки JSX, но для React — один и тот же `Counter` на одной позиции.",
    },
    {
      type: "quiz",
      q: "После каждого введённого символа поле теряет фокус. Самая вероятная причина?",
      opts: [
        "Компонент поля объявлен внутри другого компонента и пересоздаётся при каждом рендере",
        "Не указан `autoFocus`",
        "Слишком много ререндеров",
        "Поле неуправляемое",
      ],
      a: 0,
      why: "Новая функция — новый тип. React размонтирует старое поле и монтирует новое, фокус и DOM теряются.",
    },
    {
      type: "sort",
      q: "Сохранится ли состояние компонента?",
      groups: ["Сохранится", "Сбросится"],
      items: [
        ["на том же месте тот же компонент с другими пропсами", 0],
        ["родитель перерисовался по своему состоянию", 0],
        ["на его месте на один рендер появился другой тег", 1],
        ["у компонента сменился `key`", 1],
        ["компонент убрали условием `show && …` и вернули", 1],
      ],
      why: "Состояние живёт, пока на той же позиции остаётся тот же тип с тем же ключом.",
    },
  ],
};
