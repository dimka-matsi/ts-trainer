import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rn6",
  region: 3,
  level: "middle",
  title: "Suspense, lazy и use",
  q: "Как работают `Suspense` и `lazy`? Как в React 19 прочитать промис через `use`?",
  answer:
    "`lazy(() => import('./Editor'))` выносит компонент в отдельный чанк и загружает его при первом рендере. Пока код или данные грузятся, компонент «приостанавливается», и ближайший сверху `<Suspense fallback={…}>` показывает заглушку. В React 19 данные можно читать хуком `use(promise)`: если промис ещё не выполнен, компонент приостанавливается так же. Промис при этом нельзя создавать прямо в рендере — его получают из кэша, библиотеки или от серверного компонента. Ошибку загрузки ловит граница ошибок, а переход вместо заглушки оставляет на экране старый интерфейс.",
  theory: {
    p: [
      "`const Editor = lazy(() => import('./Editor'))`. Сборщик видит динамический импорт и выносит компонент со всеми зависимостями в отдельный файл. При первом рендере `<Editor />` React запускает загрузку и приостанавливает рендер. `lazy` объявляют на верхнем уровне модуля, а не внутри компонента — иначе при каждом рендере будет новый тип и новая загрузка.",
      "`<Suspense fallback={<Skeleton />}>` — граница ожидания. Если кто-то внутри приостановился, вместо всего содержимого границы показывается `fallback`. Границы можно вкладывать: шапка страницы появится сразу, а медленный блок отзывов — позже, со своей заглушкой. Ошибки загрузки Suspense не ловит — для этого граница ошибок (error boundary).",
      "Данные. `use(promise)` в React 19 читает результат промиса: если он готов — возвращает значение, если нет — приостанавливает компонент до ближайшего Suspense. Промис должен быть стабильным: созданный прямо в рендере, он будет новым при каждом вызове, и компонент застрянет в загрузке. Поэтому промисы берут из кэша, библиотек с поддержкой Suspense или передают из серверного компонента. `use` умеет читать и контекст, и его можно вызывать в условиях.",
      "Переходы и Suspense. Если обновление, которое приостанавливает уже показанное содержимое, сделано в переходе, React не заменит интерфейс заглушкой, а оставит старый экран до готовности нового. Так при переходе между вкладками не мигает скелетон. Suspense также лежит в основе потокового рендеринга на сервере: HTML каждой границы досылается, когда готов.",
    ],
    code: `const Editor = lazy(() => import('./Editor')); // отдельный чанк, на верхнем уровне модуля

function Page({ commentsPromise }: { commentsPromise: Promise<Comment[]> }) {
  return (
    <>
      <Header />
      <Suspense fallback={<Skeleton />}>
        <Editor />
      </Suspense>
      <Suspense fallback={<p>Загружаем комментарии…</p>}>
        <Comments commentsPromise={commentsPromise} />
      </Suspense>
    </>
  );
}

function Comments({ commentsPromise }: { commentsPromise: Promise<Comment[]> }) {
  const comments = use(commentsPromise); // приостановится, пока промис не выполнен
  return <ul>{comments.map((c) => <li key={c.id}>{c.text}</li>)}</ul>;
}`,
    flow: {
      actors: ["Page", "Suspense: редактор", "Сеть", "Suspense: комментарии"],
      steps: [
        { from: 0, to: 1, label: "рендер <Editor /> — код ещё не загружен", note: "показать Skeleton" },
        { from: 1, to: 2, label: "import('./Editor')" },
        { from: 0, to: 3, label: "use(commentsPromise) — промис не готов", note: "своя заглушка, шапка уже видна" },
        { from: 2, to: 1, label: "чанк загружен — показать редактор" },
        { from: 2, to: 3, label: "комментарии пришли — показать список", note: "границы открываются независимо" },
      ],
    },
    keys: [
      "`lazy` — компонент в отдельном чанке, объявляют на верхнем уровне модуля. `Suspense` показывает `fallback`, пока что-то внутри приостановлено.",
      "`use(promise)` читает данные и приостанавливает компонент. Промис должен быть стабильным: из кэша, библиотеки или от сервера.",
      "Ошибки ловит граница ошибок, переход оставляет старый экран вместо заглушки. Вложенные границы открываются по отдельности.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь, что происходит при первом показе ленивого компонента.",
      items: [
        "React рендерит `<Editor />`, созданный через `lazy`",
        "начинается загрузка его чанка",
        "ближайший `Suspense` показывает заглушку",
        "чанк загружен",
        "React рендерит настоящий компонент",
      ],
      why: "Рендер приостанавливается на время загрузки, а Suspense заполняет паузу заглушкой.",
    },
    {
      type: "quiz",
      code: `function Profile({ id }: { id: number }) {
  const user = use(fetch('/api/users/' + id).then((r) => r.json()));
  return <h1>{user.name}</h1>;
}`,
      q: "Что не так с этим компонентом?",
      opts: [
        "Промис создаётся в рендере заново при каждом вызове, и компонент застрянет в загрузке",
        "`use` нельзя вызывать внутри компонента",
        "Нужен `useEffect` вместо `use`",
        "Всё правильно",
      ],
      a: 0,
      why: "После приостановки React повторит рендер, а там — новый промис. Промис берут из кэша или получают пропсом от сервера.",
    },
    {
      type: "quiz",
      q: "Чанк ленивого компонента не загрузился из-за ошибки сети. Кто это обработает?",
      opts: [
        "Ближайшая граница ошибок (error boundary)",
        "Ближайший `Suspense` покажет заглушку навсегда",
        "`lazy` сам повторит загрузку бесконечно",
        "Никто: страница станет белой без вариантов",
      ],
      a: 0,
      why: "Suspense отвечает за ожидание, а не за ошибки. Границу ошибок ставят рядом, чтобы показать «Не удалось загрузить, повторить».",
    },
  ],
};
