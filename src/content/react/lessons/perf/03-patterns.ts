import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rp3",
  region: 9,
  level: "middle",
  title: "Приёмы против лишних ререндеров",
  q: "Какие есть способы избежать лишних ререндеров, не расставляя `memo` везде?",
  answer:
    "Главные приёмы структурные. Опустить состояние в компонент, которому оно нужно, чтобы его изменение не трогало соседей. Поднять содержимое: передать тяжёлую часть через `children`, тогда при изменении состояния обёртки React её не перерисует — это те же элементы, созданные родителем. Разделить большой компонент и большой контекст. Не объявлять компоненты внутри компонентов и держать ключи стабильными. Длинные списки виртуализировать, редко нужные части грузить через `lazy`. И только после замера — точечная мемоизация или React Compiler.",
  theory: {
    p: [
      "Опустить состояние. Поле поиска хранит текст в компоненте страницы — каждый символ перерисовывает страницу с таблицей на 500 строк. Вынесли поле с его состоянием в отдельный компонент `SearchInput` — перерисовывается только он. Самый дешёвый рендер — тот, которого нет.",
      "Поднять содержимое. `function ScrollTracker({ children })` хранит позицию прокрутки и часто меняет своё состояние. Если тяжёлый `<Article />` передан как `children`, React не перерисует его при изменении состояния обёртки: элемент создан родителем, и между рендерами обёртки это тот же объект. Приём работает и для провайдеров контекста.",
      "Разделять и не пересоздавать. Большой компонент с несколькими независимыми состояниями делят на части. Контекст делят по частоте изменений. Компоненты объявляют на верхнем уровне модуля, иначе каждый рендер — новый тип и пересоздание. Ключи списка берут из данных: случайный ключ пересоздаёт элементы.",
      "Объём работы. Список на 10 000 строк виртуализируют: в DOM только видимые строки. Тяжёлые редкие части — редактор, график, модалку — грузят лениво через `lazy`. Несрочные тяжёлые обновления помечают переходом, чтобы не мешали вводу. Только после этого — `memo` там, где Profiler показал проблему, или React Compiler для всего проекта.",
    ],
    code: `// Состояние опущено: ввод перерисовывает только SearchInput
function Page() {
  return (
    <>
      <SearchInput />
      <HugeTable />
    </>
  );
}

// Содержимое поднято: Article создан родителем и не перерисуется
function ScrollTracker({ children }: { children: React.ReactNode }) {
  const [y, setY] = useState(0);
  return (
    <div onScroll={(e) => setY(e.currentTarget.scrollTop)}>
      <Progress value={y} />
      {children}
    </div>
  );
}

function Post() {
  return (
    <ScrollTracker>
      <Article />
    </ScrollTracker>
  );
}`,
    flow: {
      actors: ["Прокрутка", "ScrollTracker: setY", "Progress", "Article (children)"],
      steps: [
        { from: 0, to: 1, label: "scrollTop изменился" },
        { from: 1, to: 1, label: "ререндер обёртки" },
        { from: 1, to: 2, label: "ререндер Progress", note: "зависит от y — нужен" },
        { from: 1, to: 3, label: "Article не перерисован", note: "тот же элемент от родителя Post" },
      ],
    },
    keys: [
      "Опусти состояние к тому, кто его использует, и подними тяжёлое содержимое в `children`.",
      "Дели большие компоненты и контексты, не объявляй компоненты внутри компонентов, держи ключи стабильными.",
      "Уменьшай объём: виртуализация, `lazy`, переходы. Мемоизация — после замера или через React Compiler.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Почему `<Article />`, переданный как `children`, не перерисовывается при изменении состояния `ScrollTracker`?",
      opts: [
        "Элемент создан родителем, и между рендерами обёртки это тот же объект",
        "`children` всегда мемоизируются React автоматически",
        "Состояние не влияет на детей",
        "Потому что `Article` — функция",
      ],
      a: 0,
      why: "React пропускает элемент, если получил тот же объект, что и в прошлый раз. Новый объект создаётся только при рендере родителя `Post`.",
    },
    {
      type: "sort",
      q: "Поможет ли это сократить лишние ререндеры?",
      groups: ["Поможет", "Не поможет"],
      items: [
        ["вынести поле поиска со своим состоянием в отдельный компонент", 0],
        ["передать тяжёлый блок через `children`", 0],
        ["разделить контекст на тему и пользователя", 0],
        ["объявить компонент строки внутри компонента таблицы", 1],
        ["поставить ключом `Math.random()`", 1],
      ],
      why: "Помогает всё, что сужает круг перерисовки. Новые типы и новые ключи, наоборот, пересоздают компоненты.",
    },
    {
      type: "quiz",
      q: "Ввод в поле поиска тормозит, потому что каждый символ перерисовывает таблицу на 500 строк. Какое решение самое простое?",
      opts: [
        "Опустить состояние поиска в компонент поля, чтобы нажатия не перерисовывали таблицу",
        "Перейти на другой фреймворк",
        "Сжать бандл",
        "Добавить больше строк в таблицу",
      ],
      a: 0,
      why: "Состояние держат как можно ближе к тем, кто его использует. Тогда ререндер затрагивает только их.",
    },
  ],
};
