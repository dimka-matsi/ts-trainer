import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rp2",
  region: 9,
  title: "React Compiler",
  q: "Что такое React Compiler и что он меняет в работе с `useMemo`, `useCallback` и `memo`?",
  answer:
    "React Compiler — инструмент сборки, стабильная версия 1.0 вышла в октябре 2025 года. Он анализирует компоненты и хуки и сам добавляет мемоизацию: значений, функций, JSX и целых компонентов, причём точнее, чем вручную — даже внутри условий. Ручные `useMemo`, `useCallback` и `memo` становятся нужны редко. Компилятор опирается на правила React: компонент, который их нарушает, он пропускает без оптимизации, а проблемы показывает линтер. Лучше всего он работает с React 19, но поддерживает и 17–18 через отдельный пакет.",
  theory: {
    p: [
      "Ручная мемоизация трудна: забыл `useCallback` — `memo` не работает, неверные зависимости — устаревшие значения, лишняя мемоизация — пустые затраты. React Compiler делает это автоматически при сборке: плагин для Babel (есть интеграции с Vite, Next.js, Expo) переписывает код компонентов и кэширует всё, что зависит от неизменившихся входных данных.",
      "Что он даёт. Компонент не перерисовывает детей, если их пропсы не изменились, — как будто всё обёрнуто в `memo`, а функции в `useCallback`. Дорогие вычисления не повторяются. Мемоизация гранулярная: кэшируются отдельные куски JSX и значения, в том числе те, что после раннего `return` или в условии, где вручную `useMemo` не поставить.",
      "Условие — правила React. Компоненты и хуки должны быть чистыми, пропсы и состояние не изменяются на месте, хуки вызываются по правилам. Если компилятор находит нарушение, он просто не оптимизирует этот компонент. Проблемы показывают правила eslint-plugin-react-hooks, которые работают на основе компилятора. Директива `'use no memo'` отключает компиляцию функции, `'use memo'` — включает её в режиме, где компилируется только отмеченное.",
      "Нужны ли теперь `useMemo` и `useCallback`? В новом коде — редко. Их оставляют как явный инструмент, когда значение используется в зависимостях эффекта и нужна гарантированная стабильность ссылки. Существующую ручную мемоизацию компилятор учитывает, удалять её без проверки не советуют. На собеседовании хороший ответ: «компилятор снимает рутину, но понимать, как работает мемоизация, всё равно нужно».",
    ],
    code: `// Пишем без ручной мемоизации
function ProductList({ products, query }: { products: Product[]; query: string }) {
  const visible = products.filter((p) => p.title.includes(query));
  const handleSelect = (id: number) => analytics.track('select', id);
  return <List items={visible} onSelect={handleSelect} />;
}
// Компилятор при сборке сам кэширует visible, handleSelect и JSX:
// пока products и query те же, List не перерисуется.

function LegacyChart() {
  'use no memo'; // отключить компиляцию: временно, с пояснением
  return <canvas />;
}`,
    flow: {
      actors: ["Исходный код компонента", "React Compiler при сборке", "Правила React", "Код с мемоизацией"],
      steps: [
        { from: 0, to: 1, label: "компонент без useMemo и useCallback" },
        { from: 1, to: 2, label: "проверить: чистый рендер, нет изменений на месте" },
        { from: 2, to: 1, label: "правила соблюдены" },
        { from: 1, to: 3, label: "кэш значений, функций и JSX", note: "даже внутри условий" },
        { from: 2, to: 1, label: "если правило нарушено — компонент пропущен", note: "работает как раньше, линтер подскажет" },
      ],
    },
    keys: [
      "React Compiler 1.0 (октябрь 2025) при сборке сам расставляет мемоизацию компонентов, значений и функций.",
      "Работает только с кодом по правилам React; нарушающие их компоненты пропускает. Проблемы показывает eslint-plugin-react-hooks.",
      "Ручные `useMemo` и `useCallback` нужны реже — для гарантированно стабильных зависимостей. `'use no memo'` отключает компиляцию.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Что делает React Compiler?",
      opts: [
        "При сборке автоматически добавляет мемоизацию компонентов, значений и функций",
        "Компилирует React в машинный код",
        "Переносит рендер на сервер",
        "Удаляет неиспользуемые компоненты из бандла",
      ],
      a: 0,
      why: "Это оптимизация на этапе сборки: код остаётся JavaScript, но с кэшем там, где входные данные не изменились.",
    },
    {
      type: "quiz",
      q: "Что компилятор сделает с компонентом, который меняет пропсы на месте?",
      opts: [
        "Пропустит его без оптимизации: правила React нарушены",
        "Исправит код сам",
        "Остановит сборку с ошибкой",
        "Оптимизирует как обычно",
      ],
      a: 0,
      why: "Мемоизация безопасна только для чистого кода. Нарушения видны через линтер.",
    },
    {
      type: "sort",
      q: "Что берёт на себя компилятор, а что остаётся тебе?",
      groups: ["Делает компилятор", "Остаётся тебе"],
      items: [
        ["стабильные ссылки на функции для детей", 0],
        ["кэш дорогих вычислений в рендере", 0],
        ["пропуск рендера детей с теми же пропсами", 0],
        ["писать чистые компоненты по правилам React", 1],
        ["виртуализировать список на 50 000 строк", 1],
      ],
      why: "Компилятор убирает лишнюю работу, но не уменьшает её объём и не чинит нарушения правил.",
    },
  ],
};
