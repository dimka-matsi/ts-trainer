import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rp1",
  region: 9,
  title: "Как найти лишние ререндеры: Profiler",
  q: "Как найти, какие компоненты перерисовываются лишний раз и сколько это стоит? Что такое `<Profiler>`?",
  answer:
    "Сначала замер, потом оптимизация. В React DevTools включают подсветку обновлений — перерисованные компоненты мигают рамкой, — а вкладка Profiler записывает взаимодействие и показывает каждый коммит: какие компоненты рендерились, сколько миллисекунд и почему. Компонент `<Profiler id onRender>` измеряет то же программно, например чтобы отправлять время рендера в мониторинг. С React 19.2 в панели Performance браузера появились дорожки React: видно, чем занят планировщик и какие компоненты рендерились.",
  theory: {
    p: [
      "Ререндер сам по себе не беда: это вызов функции и сравнение. Беда — когда он частый и дорогой: большие деревья, тяжёлые вычисления, списки на тысячи элементов. Поэтому сначала находят, что именно медленно, а не расставляют `memo` везде.",
      "React DevTools. Настройка «Highlight updates when components render» подсвечивает рамкой всё, что перерисовалось, — сразу видно, что клик по кнопке перерисовал всю страницу. Вкладка Profiler записывает сессию и показывает коммиты: диаграмму «пламя» с временем каждого компонента, рейтинг самых дорогих и, если включить запись причин, почему компонент рендерился — изменилось состояние, пропсы, контекст или родитель.",
      "`<Profiler id='Sidebar' onRender={cb}>` — программный замер. После каждого коммита внутри него React вызывает `onRender(id, phase, actualDuration, baseDuration, startTime, commitTime)`: `phase` — `'mount'` или `'update'`, `actualDuration` — сколько занял этот рендер, `baseDuration` — сколько занял бы рендер всего поддерева без мемоизации. Профилирование добавляет накладные расходы и в обычной продакшен-сборке выключено.",
      "Performance-панель браузера. С React 19.2 в записи Chrome DevTools есть дорожки React: Scheduler показывает, какие обновления выполнялись и с каким приоритетом, Components — какие компоненты рендерились и сколько. Так рендер React виден рядом с сетью, раскладкой и отрисовкой. Порядок работы: воспроизвести медленное действие, записать, найти самый дорогой компонент, исправить одно, записать снова.",
    ],
    code: `function onRender(
  id: string,
  phase: 'mount' | 'update' | 'nested-update',
  actualDuration: number,
  baseDuration: number,
) {
  if (actualDuration > 16) analytics.send({ id, phase, actualDuration, baseDuration });
}

function App() {
  return (
    <Profiler id="Sidebar" onRender={onRender}>
      <Sidebar />
    </Profiler>
  );
}`,
    flow: {
      actors: ["Разработчик", "React DevTools", "Profiler: коммит", "Причина рендера"],
      steps: [
        { from: 0, to: 1, label: "включил подсветку обновлений", note: "клик мигает всей страницей" },
        { from: 0, to: 2, label: "записал клик в Profiler" },
        { from: 2, to: 0, label: "Table: 120 мс из 140", note: "самый дорогой компонент" },
        { from: 2, to: 3, label: "почему рендерилась Table?" },
        { from: 3, to: 0, label: "родитель перерисовался из-за поля поиска", note: "лечится переносом состояния, а не memo везде" },
      ],
    },
    keys: [
      "Сначала замер: подсветка обновлений и вкладка Profiler в React DevTools, с причинами рендера.",
      "`<Profiler onRender>` — программный замер: `phase`, `actualDuration`, `baseDuration`. В обычном продакшене выключен.",
      "С React 19.2 рендер React виден в панели Performance браузера на дорожках Scheduler и Components.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Как узнать, какие компоненты перерисовались при клике и почему?",
      opts: [
        "Записать клик во вкладке Profiler React DevTools с записью причин рендера",
        "Посмотреть вкладку «Сеть»",
        "Запустить Lighthouse",
        "Посмотреть размер бандла",
      ],
      a: 0,
      why: "Profiler показывает каждый компонент в коммите, его время и причину: состояние, пропсы, контекст или родитель.",
    },
    {
      type: "match",
      q: "Сопоставь значение из `onRender` и его смысл.",
      pairs: [
        ["`phase`", "первое монтирование или обновление"],
        ["`actualDuration`", "сколько занял этот рендер"],
        ["`baseDuration`", "сколько занял бы рендер поддерева без мемоизации"],
        ["`commitTime`", "когда React закоммитил это обновление"],
      ],
      why: "Если `actualDuration` намного меньше `baseDuration`, мемоизация работает.",
    },
    {
      type: "quiz",
      q: "Почему не стоит сразу оборачивать все компоненты в `memo`?",
      opts: [
        "Мемоизация стоит сравнений и памяти, а без замера неизвестно, что правда медленно",
        "`memo` запрещён в продакшене",
        "`memo` работает только с классами",
        "`memo` ломает хуки",
      ],
      a: 0,
      why: "Часто проблему решает перенос состояния или разделение компонента, а `memo` на всём подряд только добавляет работы.",
    },
  ],
};
