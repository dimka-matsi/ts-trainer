import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "tst3",
  region: 13,
  title: "E2E-тесты: Playwright и пирамида тестов",
  q: "Чем E2E-тесты отличаются от модульных и компонентных? Как писать их так, чтобы они не были нестабильными?",
  answer:
    "Модульные тесты проверяют функции, компонентные — компонент в изолированном DOM через Testing Library, а E2E-тесты запускают настоящий браузер и проходят сценарий пользователя через всё приложение. E2E дают больше уверенности, но медленные и хрупкие, поэтому их пишут только для главных путей: вход, оформление заказа. Сейчас обычный выбор — Playwright: он сам ждёт, пока элемент станет готов, ищет элементы по ролям и тексту, как Testing Library, умеет подменять сеть и сохраняет трассировку упавшего теста. Нестабильность лечат ожиданием состояния, а не таймаутами, и независимыми тестовыми данными.",
  theory: {
    p: [
      "Уровни тестов. Модульные — чистые функции и хуки, быстрые и точные. Компонентные — компонент в jsdom с Testing Library и подменённой сетью: основная масса тестов фронтенда. E2E (end-to-end) — настоящий браузер открывает приложение и выполняет сценарий целиком, вместе с сервером или его подменой. Кент Си Доддс предложил «кубок тестирования»: больше всего интеграционных тестов, а E2E — несколько для критичных путей.",
      "Playwright от Microsoft — стандарт для E2E: Chromium, Firefox и WebKit, параллельный запуск, автоожидание. Локаторы похожи на Testing Library: `page.getByRole(\"button\", { name: \"Оформить\" })`, `getByLabel`, `getByText`. Проверки `await expect(locator).toBeVisible()` сами повторяются, пока условие не станет истинным или не выйдет время. Cypress — альтернатива с удобным интерфейсом отладки, но работает внутри браузера и слабее в нескольких вкладках и доменах.",
      "Нестабильность (flaky) — главный враг E2E. Причины: жёсткие паузы `waitForTimeout`, зависимость от порядка тестов и общих данных, внешние сервисы, анимации. Лечение: ждать состояния (элемент виден, запрос завершился), каждому тесту — свои данные или новый пользователь, подмена внешних API через `page.route`, отключение анимаций. Повторный запуск упавших тестов скрывает проблему, а не решает её.",
      "Отладка и CI. Playwright записывает трассировку: скриншоты каждого шага, сеть, консоль и DOM — её открывают после падения в CI. Визуальные тесты сравнивают скриншоты с эталоном. Storybook с тестами взаимодействия проверяет компоненты в изоляции. В CI E2E гоняют на собранном приложении и в нескольких браузерах, но только для важных сценариев — иначе прогон растягивается на час.",
    ],
    code: `import { test, expect } from '@playwright/test';

test('покупатель оформляет заказ', async ({ page }) => {
  await page.route('**/api/payment', (route) =>          // подмена внешнего сервиса
    route.fulfill({ json: { status: 'ok' } }),
  );
  await page.goto('/catalog');
  await page.getByRole('button', { name: 'В корзину' }).first().click();
  await page.getByRole('link', { name: 'Корзина' }).click();
  await page.getByLabel('Email').fill('anna@shop.ru');
  await page.getByRole('button', { name: 'Оформить' }).click();
  await expect(page.getByRole('heading', { name: 'Заказ оформлен' })).toBeVisible(); // ждёт сам
});`,
    flow: {
      actors: ["Тест", "Playwright", "Браузер", "Приложение"],
      steps: [
        { from: 0, to: 1, label: "getByRole('button', { name: 'Оформить' }).click()" },
        { from: 1, to: 2, label: "ждёт, пока кнопка видна, включена и не перекрыта" },
        { from: 2, to: 3, label: "настоящий клик в настоящем браузере" },
        { from: 1, to: 0, label: "expect(...).toBeVisible() повторяет проверку до таймаута", note: "никаких waitForTimeout" },
      ],
    },
    keys: [
      "Модульные — функции, компонентные — компонент с Testing Library, E2E — настоящий браузер и сценарий целиком.",
      "Playwright: автоожидание, локаторы по ролям, подмена сети `page.route`, трассировка упавших тестов.",
      "E2E — только для главных путей. Нестабильность лечат ожиданием состояния и независимыми данными, а не паузами.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Какой уровень тестов подходит для проверки?",
      groups: ["модульный", "компонентный", "E2E"],
      items: [
        ["функция расчёта скидки", 0],
        ["форма показывает ошибку при пустом email", 1],
        ["выпадающий список фильтрует варианты по вводу", 1],
        ["пользователь входит, добавляет товар и оформляет заказ", 2],
      ],
      why: "Чем выше уровень, тем больше уверенность и тем дороже тест. Основная масса — компонентные, E2E — несколько ключевых сценариев.",
    },
    {
      type: "quiz",
      q: "Тест иногда падает: кнопка «Оформить» ещё неактивна в момент клика. Как исправить правильно?",
      opts: [
        "Использовать локатор и проверки Playwright, которые ждут готовности элемента, или ждать нужного состояния",
        "Добавить `waitForTimeout(3000)` перед кликом",
        "Включить повтор упавших тестов",
        "Удалить тест",
      ],
      a: 0,
      why: "Фиксированная пауза либо слишком коротка, либо тратит время впустую. Ожидание состояния быстрое и надёжное.",
    },
    {
      type: "quiz",
      q: "Зачем в E2E-тесте подменять ответ платёжного сервиса через `page.route`?",
      code: `await page.route('**/api/payment', (route) => route.fulfill({ json: { status: 'ok' } }));`,
      opts: [
        "Внешний сервис медленный и нестабильный, а тест проверяет наш сценарий, а не чужой API",
        "Playwright не умеет делать настоящие запросы",
        "Чтобы тест работал без браузера",
        "Чтобы ускорить сборку приложения",
      ],
      a: 0,
      why: "Зависимость от внешнего сервиса — частая причина нестабильных тестов. Сам сервис проверяют отдельными контрактными тестами.",
    },
  ],
};
