import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "tst2",
  region: 13,
  level: "middle",
  title: "Асинхронность, `act`, моки и хуки в тестах",
  q: "Как тестировать асинхронные компоненты, запросы к API и свои хуки? Зачем нужен `act`?",
  answer:
    "Асинхронный результат ждут через `findBy…` или `waitFor`, а не через таймеры. `act` гарантирует, что все обновления состояния и эффекты применены до проверки; Testing Library уже оборачивает в него `render` и действия, поэтому вручную его пишут редко, а предупреждение про `act` обычно значит, что тест не дождался асинхронного обновления. Запросы к API мокают на уровне сети — библиотекой MSW, — а не подменой модулей: так компонент работает с настоящим `fetch`. Свои хуки проверяют через `renderHook`, компоненты с контекстом — через обёртку с провайдерами. Ключевые пользовательские сценарии дополнительно проверяют end-to-end тестами в Playwright.",
  theory: {
    p: [
      "Асинхронность. Компонент загрузил данные — результат появится не сразу. `await screen.findByText('Чайник')` ждёт до таймаута. `await waitFor(() => expect(…))` повторяет проверку, пока она не пройдёт. Паузы вроде `setTimeout` в тестах — плохо: тест становится медленным и нестабильным. Для таймеров есть поддельные часы раннера.",
      "`act(() => …)` — функция из `react`, которая выполняет код и применяет все обновления и эффекты перед продолжением. `render`, `userEvent` и `findBy` из Testing Library уже используют её. Если в консоли предупреждение «not wrapped in act», значит, после проверки случилось обновление, которого тест не дождался: нужен `findBy` или `waitFor`.",
      "Моки сети. MSW (Mock Service Worker) перехватывает запросы на уровне сети и отвечает заданными данными. Компонент вызывает настоящий `fetch`, работает его код обработки ответа и ошибок, а тест управляет только ответом сервера: успех, 500, пустой список. Это надёжнее, чем подменять модуль API, и те же обработчики можно использовать в разработке.",
      "Хуки и провайдеры. `renderHook(() => useCounter())` отрисовывает хук в тестовом компоненте и даёт `result.current`, изменения — внутри `act`. Компоненты, которым нужны контекст, стор или роутер, рендерят через свою функцию `renderWithProviders`. И пирамида тестов: много быстрых тестов компонентов и логики, меньше интеграционных, несколько end-to-end в настоящем браузере через Playwright для оплаты, входа и других критичных путей.",
    ],
    code: `const server = setupServer(
  http.get('/api/products', () => HttpResponse.json([{ id: 7, title: 'Чайник' }])),
);
beforeAll(() => server.listen());
afterAll(() => server.close());

test('показывает товары с сервера', async () => {
  render(<ProductList />);
  expect(await screen.findByText('Чайник')).toBeInTheDocument(); // ждём запрос
});

test('показывает ошибку при 500', async () => {
  server.use(http.get('/api/products', () => new HttpResponse(null, { status: 500 })));
  render(<ProductList />);
  expect(await screen.findByRole('alert')).toBeInTheDocument();
});

test('useCounter увеличивает значение', () => {
  const { result } = renderHook(() => useCounter());
  act(() => result.current.increment());
  expect(result.current.count).toBe(1);
});`,
    flow: {
      actors: ["Тест", "ProductList", "fetch", "MSW", "Экран"],
      steps: [
        { from: 0, to: 1, label: "render(<ProductList />)" },
        { from: 1, to: 2, label: "GET /api/products", note: "настоящий fetch" },
        { from: 2, to: 3, label: "перехвачен на уровне сети" },
        { from: 3, to: 1, label: "[{ id: 7, title: 'Чайник' }]" },
        { from: 1, to: 4, label: "список отрисован" },
        { from: 0, to: 4, label: "findByText('Чайник')", note: "дождались без таймеров" },
      ],
    },
    keys: [
      "Асинхронное ждут через `findBy` и `waitFor`, не через паузы. Предупреждение про `act` — тест не дождался обновления.",
      "Запросы мокают на уровне сети через MSW: компонент работает с настоящим `fetch`, тест управляет ответом.",
      "Хуки — `renderHook`, контекст и стор — своя функция рендера с провайдерами. Критичные сценарии — end-to-end в Playwright.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "В консоли теста предупреждение, что обновление не обёрнуто в `act`. Что это обычно значит?",
      opts: [
        "Тест закончил проверку раньше, чем случилось асинхронное обновление: нужен `findBy` или `waitFor`",
        "Нужно обернуть каждую строку теста в `act`",
        "React сломан",
        "Нужно отключить StrictMode",
      ],
      a: 0,
      why: "Testing Library уже использует `act` в `render` и событиях. Предупреждение — сигнал о недождавшемся обновлении.",
    },
    {
      type: "quiz",
      q: "Почему запросы к API лучше мокать через MSW, а не подменой модуля API?",
      opts: [
        "Компонент работает с настоящим `fetch` и своей обработкой ответа, а тест задаёт только ответ сервера",
        "MSW быстрее всего остального",
        "Подменять модули запрещено",
        "MSW не требует тестов",
      ],
      a: 0,
      why: "Чем меньше подменено, тем больше кода реально проверяет тест, включая обработку ошибок и разбор ответа.",
    },
    {
      type: "match",
      q: "Сопоставь инструмент и задачу.",
      pairs: [
        ["`findByText`", "дождаться появления элемента"],
        ["`renderHook`", "проверить свой хук отдельно"],
        ["MSW", "ответить на запросы на уровне сети"],
        ["Playwright", "сценарий в настоящем браузере от начала до конца"],
      ],
      why: "Каждый слой пирамиды тестов проверяет своё: логику, компонент, интеграцию и весь путь пользователя.",
    },
    {
      type: "quiz",
      q: "Почему в тесте плохо ждать загрузку через `setTimeout(…, 2000)`?",
      opts: [
        "Тест станет медленным и нестабильным: на медленной машине 2 секунд не хватит",
        "`setTimeout` не работает в jsdom",
        "Это запрещено Vitest",
        "Так тест всегда проходит",
      ],
      a: 0,
      why: "`findBy` ждёт ровно столько, сколько нужно, и падает понятной ошибкой по таймауту.",
    },
  ],
};
