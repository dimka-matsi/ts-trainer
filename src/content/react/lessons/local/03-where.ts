import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "st3",
  region: 1,
  title: "Где держать состояние",
  q: "Где должно жить состояние? Что такое подъём состояния и props drilling?",
  answer:
    "Состояние держат как можно ближе к тем, кто его использует: чем ниже оно в дереве, тем меньше компонентов перерисовывается при его изменении. Если данные нужны двум соседям, состояние поднимают к их общему родителю и передают через пропсы — это подъём состояния. Когда пропс приходится пробрасывать через много уровней, которым он не нужен, это props drilling; его лечат композицией через `children` или контекстом. И не хранят в состоянии то, что можно вычислить из других данных.",
  theory: {
    p: [
      "Правило близости (colocation): состояние живёт в самом нижнем компоненте, которому оно нужно. Открыто ли меню — забота меню. Текст поиска — забота поля поиска. Когда состояние меняется, перерисовывается компонент, где оно лежит, и все его дети. Чем выше состояние, тем больше лишней работы.",
      "Подъём состояния (lifting state up): если двум соседним компонентам нужны одни данные, состояние переносят в их общего родителя. Родитель передаёт значение и функцию изменения через пропсы. Так у данных один источник правды, и соседи не расходятся.",
      "Props drilling — когда пропс идёт через пять уровней компонентов, которым он не нужен, только чтобы дойти до шестого. Такой код неудобно читать и менять. Первое лекарство — композиция: родитель передаёт готовый компонент через `children`, и промежуточные уровни ничего не знают о данных. Второе — контекст, о нём регион «Context».",
      "Производное состояние не хранят. Если `fullName` получается из `firstName` и `lastName`, его вычисляют прямо в рендере, а не держат в отдельном `useState` с синхронизацией через эффект. Два состояния для одних данных рано или поздно разойдутся. Тяжёлое вычисление можно запомнить — как, расскажет регион «Хуки и эффекты».",
    ],
    code: `function Page() {
  const [user] = useState({ name: 'Аня' });
  // Композиция: Layout и Sidebar не знают про user
  return (
    <Layout sidebar={<Sidebar />}>
      <Profile user={user} />
    </Layout>
  );
}

function Cart({ items }: { items: { price: number }[] }) {
  // Производное значение считаем, а не храним в useState
  const total = items.reduce((sum, item) => sum + item.price, 0);
  return <p>Итого: {total}</p>;
}`,
    flow: {
      actors: ["Родитель: useState(query)", "Filters", "List"],
      steps: [
        { from: 0, to: 1, label: "query и setQuery через пропсы", note: "состояние поднято к общему родителю" },
        { from: 1, to: 0, label: "setQuery('чай')", note: "ввод меняет состояние у родителя" },
        { from: 0, to: 1, label: "query = 'чай'" },
        { from: 0, to: 2, label: "query = 'чай'", note: "список видит то же значение: один источник правды" },
      ],
    },
    keys: [
      "Состояние — в самом нижнем компоненте, которому оно нужно. Чем выше, тем больше лишних ререндеров.",
      "Нужно соседям — поднимают к общему родителю. Пробрасывание через много уровней лечат композицией через `children` или контекстом.",
      "Производное не хранят, а вычисляют в рендере. Два состояния для одних данных расходятся.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Фильтр и список — соседние компоненты, и оба должны видеть один и тот же текст поиска. Как сделать?",
      opts: [
        "Поднять состояние к общему родителю и передать через пропсы",
        "Завести `useState` в каждом и синхронизировать эффектом",
        "Хранить текст в глобальной переменной",
        "Передавать текст через `localStorage`",
      ],
      a: 0,
      why: "У данных должен быть один источник правды. Две копии состояния рано или поздно покажут разное.",
    },
    {
      type: "quiz",
      code: `const [items, setItems] = useState<Item[]>([]);
const [count, setCount] = useState(0);

function add(item: Item) {
  setItems([...items, item]);
  setCount(count + 1);
}`,
      q: "Что лучше сделать с `count`?",
      opts: [
        "Убрать это состояние и считать `items.length` прямо в рендере",
        "Перенести `count` в контекст",
        "Вызывать `setCount` ещё и при удалении",
        "Ничего: так и нужно",
      ],
      a: 0,
      why: "Это производное значение. Отдельное состояние придётся обновлять везде, где меняется `items`, и рано или поздно они разойдутся.",
    },
    {
      type: "match",
      q: "Сопоставь проблему и решение.",
      pairs: [
        ["меню перерисовывает всю страницу при открытии", "опустить состояние в компонент меню"],
        ["двум соседям нужны одни данные", "поднять состояние к общему родителю"],
        ["пропс идёт через пять уровней, которым не нужен", "композиция через `children` или контекст"],
        ["`fullName` хранится отдельно от имени и фамилии", "вычислять в рендере"],
      ],
      why: "Состояние держат там, где оно нужно, в одном экземпляре, и не копируют то, что можно вычислить.",
    },
  ],
};
