import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "st5",
  region: 1,
  title: "Формы: управляемые и неуправляемые поля",
  q: "Чем управляемое поле отличается от неуправляемого? Что выбрать?",
  answer:
    "Управляемое поле берёт значение из состояния React: `value={text}` и `onChange`, который это состояние меняет, — React всегда знает текущее значение, можно проверять и форматировать ввод на лету. Неуправляемое хранит значение в самом DOM: начальное задают через `defaultValue`, а читают при отправке, например через `FormData`. Управляемые удобны для проверки при вводе, зависимых полей и масок, неуправляемые проще и не перерисовывают компонент на каждый символ. Большие формы часто делают библиотекой вроде React Hook Form, которая опирается на неуправляемые поля.",
  theory: {
    p: [
      "Управляемое поле: `<input value={name} onChange={e => setName(e.target.value)} />`. Источник правды — состояние. Каждый символ вызывает сеттер, компонент перерисовывается, и поле показывает новое значение. Если написать `value` без `onChange`, поле застынет: React будет возвращать ему старое значение, а в консоли появится предупреждение.",
      "Неуправляемое поле: `<input name='email' defaultValue='' />`. Значение живёт в DOM, React его не отслеживает. При отправке значения берут из события: `new FormData(e.currentTarget)`. Кода меньше, ререндеров при вводе нет. Прочитать значение в любой момент можно через ссылку на DOM-элемент — об этом регион «Хуки и эффекты».",
      "Когда что. Управляемые — проверка на лету, кнопка «Отправить» неактивна, пока форма неверна, маска телефона, одно поле зависит от другого. Неуправляемые — простые формы, которые читают целиком при отправке. А `<input type='file'>` всегда неуправляемый: его значение нельзя задать из кода, только прочитать.",
      "Особенности. У `<textarea>` и `<select>` в React тоже `value` и `defaultValue` вместо содержимого и атрибута `selected`. Поле нельзя переключать из неуправляемого в управляемое: если `value` сначала `undefined`, а потом строка, React предупредит — начальное значение делают пустой строкой. В React 19 форме можно передать функцию в проп `action` — об этом регион «Формы и Actions».",
    ],
    code: `function Controlled() {
  const [email, setEmail] = useState('');
  const invalid = email !== '' && !email.includes('@');
  return (
    <>
      <input value={email} onChange={(e) => setEmail(e.target.value)} />
      {invalid && <p>Нужен символ @</p>}
    </>
  );
}

function Uncontrolled() {
  function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    const data = new FormData(e.currentTarget);
    console.log(data.get('email')); // значение читаем только при отправке
  }
  return (
    <form onSubmit={handleSubmit}>
      <input name="email" defaultValue="" />
      <button>Отправить</button>
    </form>
  );
}`,
    flow: {
      actors: ["Пользователь", "input value={email}", "onChange → setEmail", "React"],
      steps: [
        { from: 0, to: 1, label: "ввёл «a»" },
        { from: 1, to: 2, label: "e.target.value = 'a'" },
        { from: 2, to: 3, label: "новое состояние" },
        { from: 3, to: 1, label: "ререндер: value = 'a'", note: "источник правды — состояние" },
        { from: 3, to: 3, label: "проверка на лету: нет @ — подсказка" },
      ],
    },
    keys: [
      "Управляемое: `value` из состояния и `onChange`. React знает значение на каждом символе.",
      "Неуправляемое: `defaultValue`, значение в DOM, читают при отправке через `FormData`.",
      "Проверка на лету и маски — управляемые, простые формы и файлы — неуправляемые. Большие формы — библиотекой.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      code: `const [name, setName] = useState('Аня');
return <input value={name} />;`,
      q: "Что будет с полем?",
      opts: [
        "Его нельзя изменить: значение из состояния, а обработчика нет",
        "Поле работает как обычно",
        "Ошибка сборки",
        "Поле станет неуправляемым",
      ],
      a: 0,
      why: "React каждый раз возвращает полю `name`. Нужен `onChange`, либо `defaultValue` вместо `value`, либо `readOnly`.",
    },
    {
      type: "sort",
      q: "Какое поле здесь удобнее?",
      groups: ["Управляемое", "Неуправляемое"],
      items: [
        ["маска телефона при вводе", 0],
        ["кнопка неактивна, пока пароль короче 8 символов", 0],
        ["простая форма обратной связи, читаем при отправке", 1],
        ["выбор файла `<input type='file'>`", 1],
      ],
      why: "Если нужно реагировать на каждый символ — управляемое. Если достаточно итога — неуправляемое.",
    },
    {
      type: "quiz",
      q: "Почему `<input type='file'>` всегда неуправляемый?",
      opts: [
        "Его значение нельзя задать из кода, только прочитать",
        "React не поддерживает файлы",
        "У него нет события `onChange`",
        "Так быстрее",
      ],
      a: 0,
      why: "Браузер из соображений безопасности не даёт скрипту подставить файл в поле. Выбирает его только пользователь.",
    },
  ],
};
