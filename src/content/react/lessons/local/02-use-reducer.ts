import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "st2",
  region: 0,
  title: "useReducer: когда состояния много",
  q: "Когда стоит использовать `useReducer` вместо `useState`?",
  answer:
    "`useReducer` выносит логику изменения состояния в отдельную функцию-редьюсер: она получает текущее состояние и действие и возвращает новое состояние. Его берут, когда полей много и они меняются вместе, когда следующее состояние зависит от предыдущего по сложным правилам или когда одно событие меняет несколько полей. Редьюсер — чистая функция, его легко тестировать отдельно от компонента, а `dispatch` не меняется между рендерами.",
  theory: {
    p: [
      "`useState` хорош для независимых значений: открыто ли окно, текст в поле. Когда полей много и они связаны — статус загрузки, данные, ошибка, номер страницы, — логика изменений расползается по обработчикам. `useReducer` собирает её в одном месте.",
      "Редьюсер — функция `(state, action) => newState`. Действие (action) — объект, который описывает, что произошло: `{ type: 'loaded', items }`. Компонент не меняет состояние сам, а отправляет действие через `dispatch`. Редьюсер по полю `type` решает, каким станет новое состояние.",
      "Редьюсер обязан быть чистым: без запросов, таймеров и случайных чисел, и он не меняет старое состояние, а возвращает новое. В режиме разработки со `StrictMode` React вызывает его дважды, чтобы поймать нечистый код. Зато его можно проверить обычным тестом: на входе состояние и действие, на выходе новое состояние.",
      "Функция `dispatch` одна и та же во всех рендерах, её можно передавать детям без `useCallback`. Выбор простой: одно-два независимых значения — `useState`, связанные поля, сложные переходы и много событий — `useReducer`. По скорости разницы нет.",
    ],
    code: `type State = { status: 'idle' | 'loading' | 'error'; items: string[] };
type Action =
  | { type: 'started' }
  | { type: 'loaded'; items: string[] }
  | { type: 'failed' };

function reducer(state: State, action: Action): State {
  switch (action.type) {
    case 'started':
      return { ...state, status: 'loading' };
    case 'loaded':
      return { status: 'idle', items: action.items };
    case 'failed':
      return { ...state, status: 'error' };
  }
}

function List() {
  const [state, dispatch] = useReducer(reducer, { status: 'idle', items: [] });
  // Компонент только сообщает, что произошло: dispatch({ type: 'started' })
  return <p>{state.status}</p>;
}`,
    flow: {
      actors: ["Компонент", "dispatch", "Редьюсер", "React"],
      steps: [
        { from: 0, to: 1, label: "dispatch({ type: 'loaded', items })", note: "компонент сообщает, что произошло" },
        { from: 1, to: 2, label: "reducer(state, action)", note: "чистая функция: только считает" },
        { from: 2, to: 3, label: "новое состояние { status: 'idle', items }", note: "старое не изменено, это новый объект" },
        { from: 3, to: 0, label: "ререндер с новым состоянием" },
      ],
    },
    keys: [
      "Редьюсер — `(state, action) => newState`. Компонент отправляет действие через `dispatch`, логика изменений в одном месте.",
      "Редьюсер чистый: без побочных эффектов и без изменения старого состояния. Его легко тестировать.",
      "`useState` — для независимых простых значений, `useReducer` — для связанных полей и сложных переходов. `dispatch` стабилен.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что удобнее хранить в `useState`, а что в `useReducer`?",
      groups: ["useState", "useReducer"],
      items: [
        ["открыто ли модальное окно", 0],
        ["текст в поле поиска", 0],
        ["форма заказа из восьми связанных полей с проверками", 1],
        ["загрузка: статус, данные, ошибка и повтор", 1],
        ["корзина: добавить, удалить, изменить количество, очистить", 1],
      ],
      why: "Одно независимое значение проще держать в `useState`. Когда поля связаны и событий много, редьюсер собирает логику в одном месте.",
    },
    {
      type: "quiz",
      code: `function reducer(state: { items: string[] }, action: { type: 'added'; text: string }) {
  state.items.push(action.text);
  return state;
}`,
      q: "Что не так с этим редьюсером?",
      opts: [
        "Он меняет старое состояние и возвращает ту же ссылку — React не увидит изменений",
        "Редьюсер обязан быть асинхронным",
        "`push` нельзя вызывать внутри функций",
        "Всё правильно",
      ],
      a: 0,
      why: "Редьюсер должен вернуть новый объект: `return { ...state, items: [...state.items, action.text] }`. Иначе ссылка та же, и ререндера не будет.",
    },
    {
      type: "quiz",
      q: "Почему React в режиме разработки вызывает редьюсер дважды?",
      opts: [
        "Чтобы найти нечистый код: чистая функция даст одинаковый результат",
        "Чтобы ускорить рендер",
        "Это ошибка React",
        "Чтобы сохранить историю действий",
      ],
      a: 0,
      why: "Так работает `StrictMode`, и только в разработке. Если два вызова дают разный результат или есть побочный эффект, это сразу видно.",
    },
  ],
};
