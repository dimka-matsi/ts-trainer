import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "st1",
  region: 1,
  level: "junior",
  title: "useState: состояние и ререндер",
  q: "Как работает `useState`? Почему после трёх вызовов `setCount(count + 1)` подряд счётчик вырастет только на 1?",
  answer:
    "`useState` хранит значение между рендерами и возвращает его вместе с функцией обновления. Вызов сеттера не меняет переменную сразу: он ставит обновление в очередь и просит React перерисовать компонент, а новое значение придёт в следующем рендере. Внутри одного обработчика `count` — снимок, поэтому три `setCount(count + 1)` трижды просят одно и то же `count + 1`. Чтобы опираться на свежее значение, передают функцию: `setCount(c => c + 1)` — React применит все три по очереди.",
  theory: {
    p: [
      "Компонент — это функция. React вызывает её каждый раз, когда нужно показать интерфейс, — это рендер, а повторный вызов называют ререндером. Обычные переменные внутри функции создаются заново при каждом вызове. `useState` даёт значение, которое React хранит у себя между рендерами, и функцию, которая его меняет.",
      "Сеттер не меняет значение сразу. Он ставит обновление в очередь и просит React перерисовать компонент. В текущем рендере `count` — снимок: он не изменится до конца обработчика. Поэтому три вызова `setCount(count + 1)` при `count = 0` трижды просят «поставь 1». Функция-обновление `setCount(c => c + 1)` получает значение из очереди, и три таких вызова дают 3.",
      "Обновления собираются в пачку (batching): несколько вызовов сеттеров в одном обработчике дают один ререндер. С React 18 это работает везде — в обработчиках, таймерах и промисах. Если новое значение совпадает со старым по `Object.is`, React пропускает ререндер компонента и его детей.",
      "Отсюда правило неизменяемости. Объект в состоянии не меняют на месте: `user.name = 'Аня'` и `setUser(user)` передают ту же ссылку, React решит, что ничего не изменилось, и не перерисует. Нужна новая копия: `setUser({ ...user, name: 'Аня' })`. Дорогое начальное значение передают функцией — `useState(() => load())`, — тогда оно вычислится только в первом рендере.",
    ],
    code: `function Counter() {
  const [count, setCount] = useState(0);

  function addTwo() {
    setCount(count + 1); // 0 + 1
    setCount(count + 1); // снова 0 + 1: count — снимок этого рендера
    setCount((c) => c + 1); // берёт значение из очереди: 1 + 1
  }

  // После клика count станет 2, а ререндер будет один
  return <button onClick={addTwo}>{count}</button>;
}`,
    flow: {
      actors: ["Обработчик клика", "Очередь обновлений", "React", "Компонент Counter"],
      steps: [
        { from: 0, to: 1, label: "setCount(count + 1)", note: "count в этом рендере = 0: «поставь 1»" },
        { from: 0, to: 1, label: "setCount(count + 1)", note: "снова «поставь 1»: снимок не изменился" },
        { from: 0, to: 1, label: "setCount(c => c + 1)", note: "функция возьмёт то, что уже в очереди" },
        { from: 1, to: 2, label: "обработчик закончился: 1 → 1 → 1 + 1 = 2", note: "обновления собраны в пачку" },
        { from: 2, to: 3, label: "один ререндер с count = 2" },
      ],
    },
    keys: [
      "`useState` хранит значение между рендерами. Сеттер не меняет переменную, а ставит обновление в очередь и вызывает ререндер.",
      "Внутри рендера состояние — снимок. Если новое значение зависит от старого, передавай функцию: `setCount(c => c + 1)`.",
      "Обновления собираются в пачку. Объекты и массивы не меняют на месте — создают копию, иначе React не увидит изменений.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      code: `const [count, setCount] = useState(0);

function handleClick() {
  setCount(count + 1);
  setCount(count + 1);
  setCount(count + 1);
}`,
      q: "Чему будет равен `count` после одного клика?",
      opts: ["1", "3", "0", "2"],
      a: 0,
      why: "Все три вызова видят один снимок `count = 0` и просят поставить 1. Чтобы получить 3, пишут `setCount(c => c + 1)`.",
    },
    {
      type: "quiz",
      code: `const [user, setUser] = useState({ name: 'Иван', age: 30 });

function rename() {
  user.name = 'Аня';
  setUser(user);
}`,
      q: "Что произойдёт на экране после вызова `rename`?",
      opts: [
        "Имя не обновится: ссылка на объект та же, и React пропустит ререндер",
        "Имя сразу станет «Аня»",
        "React выбросит ошибку",
        "Компонент перерисуется дважды",
      ],
      a: 0,
      why: "React сравнивает старое и новое значение по `Object.is`. Объект тот же — значит, «ничего не изменилось». Нужно `setUser({ ...user, name: 'Аня' })`.",
    },
    {
      type: "sort",
      q: "Что вызовет ререндер компонента, а что нет?",
      groups: ["Вызовет ререндер", "Не вызовет"],
      items: [
        ["`setCount(5)`, когда `count` равен 3", 0],
        ["`setItems([...items, x])`", 0],
        ["`setCount(3)`, когда `count` уже 3", 1],
        ["`items.push(x)` без вызова сеттера", 1],
        ["изменение обычной переменной `let clicks` внутри компонента", 1],
      ],
      why: "React перерисовывает, когда сеттер получает новое значение. Тот же примитив или та же ссылка — пропуск, а изменения мимо сеттера React вообще не видит.",
    },
  ],
};
