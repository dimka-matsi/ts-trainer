import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ssr4",
  region: 11,
  level: "senior",
  title: "Метаданные, ресурсы и `cache`",
  q: "Как в React 19 управлять `<title>`, стилями и предзагрузкой ресурсов? Зачем нужны `cache` и `cacheSignal`?",
  answer:
    "В React 19 `<title>`, `<meta>` и `<link>` можно писать в любом компоненте — React сам поднимет их в `<head>`, без библиотек вроде react-helmet. Таблицы стилей с `precedence` React вставляет в нужном порядке, откладывает показ до их загрузки и не дублирует, асинхронные скрипты тоже не дублируются. Функции `preload`, `preinit`, `preconnect` и `prefetchDNS` из `react-dom` подсказывают браузеру ресурсы заранее. `cache(fn)` в серверных компонентах запоминает результат вызова в пределах одного запроса к серверу, а `cacheSignal` из React 19.2 сообщает, когда этот кэш больше не нужен, чтобы отменить незавершённую работу.",
  theory: {
    p: [
      "Метаданные. Раньше заголовок страницы меняли через `document.title` в эффекте или библиотеку react-helmet. В React 19 компонент страницы просто рендерит `<title>{post.title}</title>` и `<meta name='description' …>` — React поднимет их в `<head>`, и при SSR тоже. Это работает для `<title>`, `<meta>` и `<link>`.",
      "Стили и скрипты. `<link rel='stylesheet' href='…' precedence='default'>` можно поставить рядом с компонентом, которому нужен стиль. React вставит его в `<head>` в порядке `precedence`, не покажет содержимое границы Suspense, пока стиль не загрузился, и не вставит один и тот же файл дважды. `<script async src>` тоже дедуплицируется.",
      "Подсказки ресурсов из `react-dom`: `prefetchDNS` — узнать IP домена заранее, `preconnect` — открыть соединение, `preload` — скачать шрифт, картинку или стиль, `preinit` — скачать и сразу выполнить скрипт или подключить стиль. Их вызывают прямо в рендере или обработчике, а React при SSR превращает их в теги в `<head>` или в ранние подсказки.",
      "`cache(fn)` работает в серверных компонентах: несколько компонентов в одном запросе вызывают `getUser(id)`, а запрос в базу выполняется один раз. Кэш живёт в пределах одного запроса к серверу, между запросами он не общий. `cacheSignal()` из React 19.2 возвращает `AbortSignal`, который срабатывает, когда рендер закончился или прерван, — его передают в `fetch`, чтобы отменить уже ненужную работу.",
    ],
    code: `function BlogPost({ post }: { post: Post }) {
  preload('/fonts/inter.woff2', { as: 'font' }); // подсказка браузеру заранее
  return (
    <article>
      <title>{post.title}</title>
      <meta name="description" content={post.summary} />
      <link rel="stylesheet" href="/post.css" precedence="default" />
      <h1>{post.title}</h1>
    </article>
  );
}

// Серверные компоненты: один запрос в базу на весь рендер
const getUser = cache(async (id: string) => {
  return db.users.find(id, { signal: cacheSignal() });
});`,
    flow: {
      actors: ["Компонент BlogPost", "React", "<head> документа", "Браузер"],
      steps: [
        { from: 0, to: 1, label: "<title>, <meta>, <link precedence>" },
        { from: 1, to: 2, label: "поднять в <head>", note: "без react-helmet" },
        { from: 0, to: 1, label: "preload('/fonts/inter.woff2')" },
        { from: 1, to: 3, label: "<link rel=preload> — начать загрузку заранее" },
        { from: 1, to: 1, label: "показать содержимое после загрузки стиля", note: "без мигания нестилизованного текста" },
      ],
    },
    keys: [
      "`<title>`, `<meta>`, `<link>` пишут в любом компоненте — React 19 поднимет их в `<head>`, и при SSR тоже.",
      "Стили с `precedence` и асинхронные скрипты не дублируются. `preload`, `preinit`, `preconnect`, `prefetchDNS` подсказывают ресурсы.",
      "`cache` — общий результат в пределах одного серверного запроса, `cacheSignal` (19.2) отменяет ненужную работу.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Как в React 19 задать заголовок вкладки на странице статьи?",
      opts: [
        "Отрендерить `<title>{post.title}</title>` в компоненте — React поднимет его в `<head>`",
        "Только через библиотеку react-helmet",
        "Только через `document.title` в эффекте",
        "Никак: заголовок задаётся только в index.html",
      ],
      a: 0,
      why: "Встроенная поддержка метаданных работает и в браузере, и при рендеринге на сервере.",
    },
    {
      type: "match",
      q: "Сопоставь функцию из `react-dom` и её смысл.",
      pairs: [
        ["`prefetchDNS`", "заранее узнать IP домена"],
        ["`preconnect`", "заранее открыть соединение"],
        ["`preload`", "заранее скачать ресурс"],
        ["`preinit`", "скачать и сразу подключить скрипт или стиль"],
      ],
      why: "Это те же подсказки, что `<link rel>` в HTML, но их можно вызвать из компонента.",
    },
    {
      type: "quiz",
      q: "Сколько живёт результат `cache(fn)` в серверных компонентах?",
      opts: [
        "В пределах одного запроса к серверу",
        "Вечно, пока работает сервер",
        "5 минут, как у библиотек кэша",
        "Только в браузере",
      ],
      a: 0,
      why: "`cache` убирает повторные вызовы внутри одного рендера. Общий кэш между пользователями он не создаёт.",
    },
  ],
};
