import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ssr1",
  region: 11,
  title: "SSR, гидратация и стриминг",
  q: "Что такое SSR и гидратация? Откуда берутся ошибки гидратации и что даёт потоковый рендеринг?",
  answer:
    "При SSR сервер рендерит компоненты в HTML, и пользователь видит страницу до загрузки JavaScript. Потом в браузере `hydrateRoot` выполняет гидратацию: React рендерит то же дерево, сопоставляет его с готовой разметкой и привязывает обработчики, не пересоздавая DOM. Если разметка сервера и первый рендер в браузере не совпали — из-за `Date.now()`, случайных чисел, проверок `window` или неверной вложенности тегов, — это ошибка гидратации. Потоковый рендеринг (`renderToPipeableStream`) с Suspense отправляет каркас страницы сразу, а медленные части досылает, и каждую часть можно гидратировать отдельно.",
  theory: {
    p: [
      "Зачем SSR. При рендеринге только в браузере пользователь видит пустую страницу, пока не загрузится и не выполнится весь JavaScript. С SSR сервер присылает готовый HTML: контент виден сразу, поисковики видят текст. Но кнопки заработают только после гидратации. React рекомендует делать SSR через фреймворк: Next.js, React Router и другие.",
      "Гидратация. `hydrateRoot(container, <App />)` не создаёт DOM заново, а проходит по готовой разметке и связывает её с деревом компонентов: привязывает обработчики и состояние. Для этого первый рендер в браузере обязан дать ту же разметку, что и сервер. Причины расхождений: время и случайные числа в рендере, `typeof window` в рендере, данные из `localStorage`, неверная вложенность вроде `<div>` внутри `<p>`, расширения браузера. React 19 показывает одну ошибку с разницей разметки.",
      "Как чинить. То, что есть только в браузере, показывают после монтирования — в эффекте, — или через `browser()` из React 19.3, который на сервере приостанавливает компонент и отдаёт заглушку Suspense. Для неизбежных расхождений в тексте, например времени, есть `suppressHydrationWarning` на одном элементе.",
      "Стриминг. `renderToString` — старый API: ждёт всю страницу и не умеет Suspense. `renderToPipeableStream` для Node.js и `renderToReadableStream` для веб-стримов отправляют каркас сразу, а содержимое каждой границы Suspense досылают в том же ответе, когда оно готово. Выборочная гидратация: части страницы гидратируются независимо, и React в первую очередь гидратирует ту часть, по которой пользователь кликнул.",
    ],
    code: `// Сервер (Node.js): каркас сразу, медленные части потом
app.get('/', (req, res) => {
  const { pipe } = renderToPipeableStream(<App />, {
    bootstrapScripts: ['/main.js'],
    onShellReady() {
      res.setHeader('content-type', 'text/html');
      pipe(res);
    },
  });
});

// Браузер: гидратация готовой разметки
hydrateRoot(document.getElementById('root')!, <App />);

// Ошибка гидратации: на сервере и в браузере разное время
function Clock() {
  return <p>{new Date().toLocaleTimeString()}</p>;
}`,
    flow: {
      actors: ["Сервер", "Браузер: HTML", "Браузер: JavaScript", "React"],
      steps: [
        { from: 0, to: 1, label: "каркас страницы", note: "контент виден до загрузки JS" },
        { from: 0, to: 1, label: "досылает блок отзывов из Suspense", note: "тот же ответ, потоком" },
        { from: 1, to: 2, label: "загружен main.js" },
        { from: 2, to: 3, label: "hydrateRoot: сопоставить с разметкой", note: "DOM не пересоздаётся" },
        { from: 3, to: 1, label: "обработчики привязаны — кнопки работают" },
      ],
    },
    keys: [
      "SSR: HTML с сервера виден до загрузки JavaScript. Гидратация (`hydrateRoot`) привязывает обработчики к готовой разметке.",
      "Первый рендер в браузере обязан совпасть с сервером. Время, случайность, `window`, неверная вложенность — причины расхождений.",
      "`renderToPipeableStream` и `renderToReadableStream` стримят границы Suspense, части гидратируются выборочно. `renderToString` — устаревший путь.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Что делает гидратация?",
      opts: [
        "Связывает готовую серверную разметку с деревом компонентов и привязывает обработчики, не пересоздавая DOM",
        "Заново рендерит всю страницу в браузере с нуля",
        "Загружает данные с сервера",
        "Сжимает HTML",
      ],
      a: 0,
      why: "Разметка уже есть. React только оживляет её, поэтому она должна совпасть с тем, что он отрендерит.",
    },
    {
      type: "sort",
      q: "Приведёт ли это к ошибке гидратации?",
      groups: ["Приведёт", "Не приведёт"],
      items: [
        ["`new Date().toLocaleTimeString()` прямо в рендере", 0],
        ["`Math.random()` в тексте компонента", 0],
        ["`<div>` внутри `<p>`", 0],
        ["чтение `localStorage` в эффекте после монтирования", 1],
        ["текст из пропсов, одинаковых на сервере и в браузере", 1],
      ],
      why: "Первый рендер в браузере должен повторить сервер. Всё браузерное — после монтирования.",
    },
    {
      type: "quiz",
      q: "Чем `renderToPipeableStream` лучше `renderToString`?",
      opts: [
        "Отправляет каркас сразу и досылает границы Suspense по мере готовности",
        "Работает без сервера",
        "Не требует гидратации",
        "Возвращает меньше HTML",
      ],
      a: 0,
      why: "`renderToString` ждёт всю страницу и не поддерживает потоковый Suspense, поэтому медленный блок задерживает всё.",
    },
  ],
};
