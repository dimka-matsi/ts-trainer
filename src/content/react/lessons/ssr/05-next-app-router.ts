import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ssr5",
  region: 11,
  title: "Next.js App Router",
  q: "Как устроен App Router в Next.js? Что делают `layout`, `page`, `loading` и `error`, и как в нём кэшируются данные?",
  answer:
    "App Router строит маршруты из папок: `app/orders/[id]/page.tsx` — страница `/orders/42`. `layout.tsx` — общий макет, который сохраняется при переходах, `loading.tsx` — запасной интерфейс, пока грузится страница (под капотом это Suspense), `error.tsx` — граница ошибок. Все компоненты по умолчанию серверные: данные берут прямо в них через `await`, а интерактивные части помечают `'use client'`. В Next.js 15 и 16 данные по умолчанию не кэшируются — кэширование включают явно, в 16-й версии директивой `'use cache'`. Защищать данные нужно в самих серверных компонентах и функциях, а не только в `proxy.ts` (бывший `middleware.ts`).",
  theory: {
    p: [
      "Маршруты — это папки в `app/`. `page.tsx` делает папку страницей, `[id]` — параметр, `(group)` — группа без влияния на адрес. Рядом лежат специальные файлы: `layout.tsx` оборачивает все вложенные страницы и не перерисовывается при переходах, `loading.tsx` показывается, пока грузятся данные, — Next.js оборачивает страницу в Suspense, `error.tsx` — граница ошибок сегмента, `not-found.tsx` — страница 404.",
      "Все компоненты серверные по умолчанию: они выполняются на сервере, могут быть `async` и читать базу или API прямо в коде, а в браузер уходит только результат. Интерактивность — в клиентских компонентах с `'use client'`. Мутации — через Server Functions с `'use server'`, их вызывают из форм. Стриминг: страница отдаётся по частям, медленные блоки приходят позже внутри своих Suspense.",
      "Кэширование менялось. В Next.js 13–14 `fetch` на сервере кэшировался по умолчанию, и это путало. С 15-й версии по умолчанию данные свежие на каждый запрос. В Next.js 16 появился режим Cache Components: кэширование включают явно директивой `'use cache'` у функции или компонента, со сроком жизни и тегами для сброса. `revalidatePath` и `revalidateTag` сбрасывают кэш после изменения данных.",
      "`proxy.ts` (до 16-й версии — `middleware.ts`) выполняется перед запросом: перенаправления, переписывание путей, заголовки. Полагаться на него как на единственную проверку прав нельзя: в 2025 году уязвимость CVE-2025-29927 позволяла обойти middleware одним заголовком. Права проверяют в серверных компонентах, Server Functions и API. Next.js часто выпускает исправления безопасности — фреймворк обновляют регулярно.",
    ],
    code: `// app/orders/[id]/page.tsx — серверный компонент по умолчанию
import { notFound } from 'next/navigation';
import { AddToCart } from './AddToCart';

async function getOrder(id: string) {
  'use cache';                                    // Next.js 16: явное кэширование
  const res = await fetch(\`https://api.shop.ru/orders/\${id}\`);
  return res.ok ? res.json() : null;
}

export default async function OrderPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const order = await getOrder(id);
  if (!order) notFound();
  return (
    <main>
      <h1>Заказ {order.id}</h1>
      <AddToCart id={order.id} /> {/* клиентский компонент с 'use client' */}
    </main>
  );
}

// app/orders/[id]/loading.tsx
export function Loading() {
  return <p>Загружаем заказ…</p>;
}`,
    flow: {
      actors: ["Браузер", "Next.js на сервере", "layout.tsx", "page.tsx"],
      steps: [
        { from: 0, to: 1, label: "GET /orders/42" },
        { from: 1, to: 2, label: "макет и loading.tsx уходят сразу потоком" },
        { from: 1, to: 3, label: "серверный компонент: await данных заказа" },
        { from: 3, to: 0, label: "готовый HTML страницы догружается в тот же поток", note: "клиентский JS только для AddToCart" },
      ],
    },
    keys: [
      "Маршруты — папки в `app/`: `page`, `layout` (сохраняется при переходах), `loading` (Suspense), `error` (граница ошибок).",
      "Компоненты серверные по умолчанию и читают данные через `await`. Интерактив — `'use client'`, мутации — `'use server'`.",
      "С 15-й версии данные не кэшируются по умолчанию, в 16-й — явный `'use cache'`. Права проверяют не только в `proxy.ts`.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь файл App Router и его роль.",
      pairs: [
        ["`page.tsx`", "делает папку страницей"],
        ["`layout.tsx`", "общий макет, сохраняется при переходах"],
        ["`loading.tsx`", "запасной интерфейс, пока грузятся данные"],
        ["`error.tsx`", "граница ошибок сегмента"],
      ],
      why: "Эти файлы Next.js сам оборачивает в нужные механизмы React: Suspense для `loading`, границу ошибок для `error`.",
    },
    {
      type: "quiz",
      q: "Можно ли защищать доступ к данным только проверкой в `middleware.ts` или `proxy.ts`?",
      opts: [
        "Нет: проверку дублируют в серверных компонентах и функциях — middleware уже обходили, как в CVE-2025-29927",
        "Да, middleware выполняется для каждого запроса без исключений",
        "Да, если включён HTTPS",
        "Нет, потому что middleware работает только в браузере",
      ],
      a: 0,
      why: "Защита в одной точке на входе ломается одной ошибкой. Данные проверяют там, где их читают.",
    },
    {
      type: "quiz",
      q: "Что изменилось в кэшировании данных между Next.js 14 и 16?",
      opts: [
        "Раньше `fetch` на сервере кэшировался по умолчанию, теперь данные свежие, а кэш включают явно — в 16-й версии через `'use cache'`",
        "Кэширование удалили полностью",
        "Кэш перенесли в localStorage",
        "Ничего не изменилось",
      ],
      a: 0,
      why: "Неявный кэш по умолчанию давал устаревшие данные там, где их не ждали. Явное кэширование понятнее.",
    },
  ],
};
