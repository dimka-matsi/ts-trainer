import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ssr3",
  region: 11,
  title: "Server Functions и `'use server'`",
  q: "Что такое Server Functions и директива `'use server'`? Какие у них риски безопасности?",
  answer:
    "Server Function — асинхронная функция, помеченная `'use server'`, которую клиент вызывает как обычную функцию, а выполняется она на сервере: фреймворк сам превращает вызов в сетевой запрос. Её передают в `<form action>`, `useActionState` или вызывают в переходе — такие функции раньше называли Server Actions. Главное про безопасность: каждая серверная функция — публичная точка входа на сервер, поэтому в ней проверяют авторизацию и права и валидируют аргументы, как в обычном API. В декабре 2025 года в React Server Components нашли критическую уязвимость с удалённым выполнением кода — зависимости нужно держать обновлёнными.",
  theory: {
    p: [
      "`'use server'` в начале функции или файла говорит сборщику: эта функция выполняется только на сервере. Клиент получает не код, а ссылку. Когда клиент её вызывает, фреймворк отправляет запрос с аргументами, выполняет функцию на сервере и возвращает результат. Аргументы и результат должны быть сериализуемыми.",
      "Использование. Серверный компонент может передать серверную функцию в `<form action>` — форма работает даже до загрузки JavaScript. В клиентском компоненте её импортируют из файла с `'use server'` и вызывают в `startTransition` или через `useActionState`. До сентября 2024 года все такие функции называли Server Actions; теперь это Server Functions, а «action» — их применение в пропсе `action`.",
      "Безопасность. Серверная функция — это открытый HTTP-эндпоинт, даже если в интерфейсе кнопка видна только администратору. Злоумышленник может вызвать её напрямую с любыми аргументами. Поэтому внутри: проверка сессии и прав, валидация аргументов схемой, никакого доверия к скрытым полям формы. Секреты и доступ к базе остаются на сервере, но возвращать лишнее клиенту нельзя.",
      "Уязвимость CVE-2025-55182 (декабрь 2025, критическая, 10 из 10): ошибка разбора данных, которые приходят на серверные функции, позволяла выполнить произвольный код на сервере без авторизации. Уязвимы были приложения, где просто включены Server Components, — Next.js, React Router и другие — на версиях 19.0–19.2.0; исправлено в 19.0.1, 19.1.2 и 19.2.1. Вывод для собеседования: серверная часть React — такой же сервер, как любой другой, и требует обновлений и проверок.",
    ],
    code: `// actions.ts
'use server';

export async function deletePost(postId: string) {
  const user = await getSessionUser();
  if (!user) throw new Error('Нужен вход'); // публичный эндпоинт: проверяем всегда
  const post = await db.posts.find(postId);
  if (post.authorId !== user.id) throw new Error('Нет прав');
  await db.posts.delete(postId);
}

// DeleteButton.tsx
'use client';
export function DeleteButton({ postId }: { postId: string }) {
  const [isPending, startTransition] = useTransition();
  return (
    <button disabled={isPending} onClick={() => startTransition(() => deletePost(postId))}>
      Удалить
    </button>
  );
}`,
    flow: {
      actors: ["Кнопка в браузере", "Фреймворк", "Сервер: deletePost", "База данных"],
      steps: [
        { from: 0, to: 1, label: "deletePost('42')", note: "выглядит как обычный вызов" },
        { from: 1, to: 2, label: "POST с аргументами", note: "сетевой запрос, который может отправить кто угодно" },
        { from: 2, to: 2, label: "проверить сессию и права автора" },
        { from: 2, to: 3, label: "DELETE post 42" },
        { from: 2, to: 0, label: "результат — перерисовка" },
      ],
    },
    keys: [
      "`'use server'` — функция выполняется на сервере, клиент вызывает её по ссылке через сеть. Раньше их называли Server Actions.",
      "Используют в `<form action>`, `useActionState` и переходах. Аргументы и результат сериализуемые.",
      "Каждая серверная функция — публичный эндпоинт: авторизация, права, валидация. Держать React обновлённым (CVE-2025-55182).",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Кнопка «Удалить пост» видна только автору. Нужно ли проверять права внутри серверной функции?",
      opts: [
        "Да: серверную функцию можно вызвать напрямую запросом, минуя интерфейс",
        "Нет: кнопку видит только автор",
        "Нет: React сам проверяет права",
        "Только в режиме разработки",
      ],
      a: 0,
      why: "Скрытая кнопка — не защита. Серверная функция — такой же эндпоинт, как любой API.",
    },
    {
      type: "match",
      q: "Сопоставь директиву или понятие и смысл.",
      pairs: [
        ["`'use client'`", "граница: модуль и его импорты уходят в браузер"],
        ["`'use server'`", "функция выполняется на сервере, клиент вызывает её по ссылке"],
        ["Server Action", "старое название серверной функции в пропсе `action`"],
        ["серверный компонент", "директивы нет, выполняется только на сервере"],
      ],
      why: "Частая путаница: `'use server'` не делает компонент серверным, он помечает функции.",
    },
    {
      type: "quiz",
      q: "Что показала уязвимость CVE-2025-55182 в React Server Components?",
      opts: [
        "Серверная часть React — это сервер, которому нужны своевременные обновления: ошибка позволяла выполнить код без авторизации",
        "Server Components нельзя использовать в продакшене",
        "Уязвимость была только в классовых компонентах",
        "Проблема касалась только CSS",
      ],
      a: 0,
      why: "Уязвимость с оценкой 10 из 10 исправили в 19.0.1, 19.1.2 и 19.2.1. Затронуты были приложения с включёнными Server Components.",
    },
  ],
};
