import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ssr2",
  region: 11,
  level: "senior",
  title: "Server Components и `'use client'`",
  q: "Что такое React Server Components? Чем они отличаются от SSR и клиентских компонентов?",
  answer:
    "Server Components выполняются только на сервере — при сборке или на каждый запрос — и не попадают в бандл. Они могут быть асинхронными и сразу читать базу или файлы, но у них нет состояния, эффектов и обработчиков. Интерактивные части — клиентские компоненты: файл помечают директивой `'use client'`, и он вместе со своими импортами уходит в браузер. Серверный компонент может рендерить клиентский и передавать ему только сериализуемые пропсы. От SSR отличие в том, что SSR превращает любые компоненты в HTML для первого показа, а серверные компоненты вообще не отправляют свой код в браузер — это модель разделения кода, а не способ отдать HTML.",
  theory: {
    p: [
      "Серверный компонент — обычная функция, которая выполняется на сервере и может быть `async`: `const notes = await db.notes.getAll()`. Никаких запросов из браузера и цепочек эффектов. Тяжёлые библиотеки — разбор Markdown, подсветка кода — остаются на сервере и не увеличивают бандл. Результат передаётся клиенту в особом формате, а React вставляет его в дерево.",
      "Ограничения. У серверных компонентов нет `useState`, `useEffect` и обработчиков событий: в браузере их кода нет. Директивы для серверного компонента нет — во фреймворках с Server Components компоненты серверные по умолчанию. Директива со словом server существует, но она не про компоненты, а про серверные функции — о них следующий урок.",
      "`'use client'` в начале файла отмечает границу: этот модуль и всё, что он импортирует, становятся клиентскими. Серверный компонент может рендерить клиентский и передавать ему пропсы, но только сериализуемые: строки, числа, объекты, промисы, JSX — не функции и не классы. Клиентский не может импортировать серверный, зато может получить его как `children`.",
      "SSR и Server Components — разные вещи, и их часто путают. SSR отрисовывает дерево в HTML для быстрого первого показа, а потом весь код гидратируется в браузере. Server Components решают, какой код вообще попадёт в браузер. Обычно их используют вместе: серверные компоненты плюс SSR для первого HTML. Работают они через фреймворк, например Next.js App Router.",
    ],
    code: `// Серверный компонент: async, читает базу, в бандл не попадает
async function NotesPage() {
  const notes = await db.notes.getAll();
  return (
    <ul>
      {notes.map((note) => (
        <Expandable key={note.id}>
          <p>{note.text}</p>
        </Expandable>
      ))}
    </ul>
  );
}

// Файл Expandable.tsx — клиентский компонент
'use client';
export default function Expandable({ children }: { children: React.ReactNode }) {
  const [open, setOpen] = useState(false);
  return (
    <li>
      <button onClick={() => setOpen(!open)}>{open ? 'Свернуть' : 'Развернуть'}</button>
      {open && children}
    </li>
  );
}`,
    flow: {
      actors: ["Сервер: NotesPage", "База данных", "Граница 'use client'", "Браузер"],
      steps: [
        { from: 0, to: 1, label: "await db.notes.getAll()", note: "прямо в компоненте" },
        { from: 1, to: 0, label: "заметки" },
        { from: 0, to: 2, label: "<Expandable> с текстом заметки", note: "только сериализуемые пропсы" },
        { from: 2, to: 3, label: "в бандл уходит только Expandable", note: "код NotesPage в браузер не попадает" },
        { from: 3, to: 3, label: "useState и клики — в клиентском компоненте" },
      ],
    },
    keys: [
      "Server Components выполняются на сервере, могут быть `async` и читать данные напрямую, их код не попадает в бандл.",
      "Нет состояния, эффектов и обработчиков. Интерактивное — в клиентских компонентах с `'use client'`, пропсы — сериализуемые.",
      "SSR отдаёт HTML для первого показа, Server Components решают, какой код попадёт в браузер. Обычно их сочетают во фреймворке.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что можно в серверном компоненте, а что только в клиентском?",
      groups: ["Серверный", "Только клиентский"],
      items: [
        ["`await` запрос к базе прямо в компоненте", 0],
        ["использовать тяжёлую библиотеку разбора Markdown без роста бандла", 0],
        ["`useState` для раскрытия блока", 1],
        ["обработчик `onClick`", 1],
        ["`useEffect` с подпиской на `resize`", 1],
      ],
      why: "Всё, что требует браузера и повторных рендеров, — клиентское. Всё, что про данные и тяжёлые вычисления, — серверное.",
    },
    {
      type: "quiz",
      q: "Чем Server Components отличаются от SSR?",
      opts: [
        "SSR превращает компоненты в HTML для первого показа, а серверные компоненты вообще не отправляют свой код в браузер",
        "Это одно и то же",
        "Server Components работают без сервера",
        "SSR работает только с классовыми компонентами",
      ],
      a: 0,
      why: "При SSR весь код всё равно гидратируется в браузере. Серверные компоненты уменьшают сам бандл.",
    },
    {
      type: "quiz",
      q: "Можно ли передать функцию из серверного компонента в пропсы клиентского?",
      opts: [
        "Нет: пропсы через границу должны быть сериализуемыми, обычную функцию передать нельзя",
        "Да, любую функцию",
        "Да, но только стрелочную",
        "Да, если компонент асинхронный",
      ],
      a: 0,
      why: "Исключение — серверные функции: клиент получает ссылку на них и вызывает через сеть, об этом следующий урок.",
    },
  ],
};
