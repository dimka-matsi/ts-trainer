import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "pt2",
  region: 12,
  level: "middle",
  title: "HOC, render props и хуки",
  q: "Что такое HOC и render props? Чем их заменили хуки и где они ещё нужны?",
  answer:
    "HOC — компонент высшего порядка: функция, которая принимает компонент и возвращает новый с добавленным поведением, например `withAuth(Page)` или `connect` из react-redux. Render props — компонент получает функцию и вызывает её, чтобы отрисовать содержимое: `<Mouse render={pos => …} />`. Оба паттерна переиспользовали логику в эпоху классов, но давали «ад обёрток», конфликты имён пропсов и неявный поток данных. Хуки решили это проще: `useMouse()` возвращает данные прямо в компонент. HOC ещё уместен для сквозных обёрток — проверки доступа, логирования, границы ошибок.",
  theory: {
    p: [
      "HOC — функция `Component => NewComponent`. Классические примеры: `connect(mapState)(Component)` в старом react-redux, `withRouter`, `withAuth`. HOC оборачивает компонент, добавляет ему пропсы или поведение — например, показывает экран входа, если пользователь не авторизован.",
      "Минусы HOC. Несколько обёрток — `withAuth(withTheme(withRouter(Page)))` — дают глубокое дерево в DevTools. Непонятно, откуда пришёл проп, а два HOC могут передать проп с одинаковым именем, и один перезапишет другой. Статические методы и ref приходится пробрасывать вручную, типизация сложная.",
      "Render props: компонент хранит логику и вызывает функцию из пропсов, чтобы отрисовать результат: `<DataFetcher url='/api' render={data => <List data={data} />} />` или `children` как функция. Поток данных явный, но вложенные render props превращаются в «пирамиду» колбэков.",
      "Хуки заменили оба паттерна для переиспользования логики: `const pos = useMouse()` — данные в компоненте, без обёрток и конфликтов имён. HOC остаётся полезен, когда нужно обернуть компонент целиком: защитить маршрут, добавить границу ошибок, логирование. Render props живы в библиотеках, где разметку определяет пользователь, например в виртуализированных списках.",
    ],
    code: `// HOC: функция от компонента
function withAuth<P extends object>(Component: React.ComponentType<P>) {
  return function Protected(props: P) {
    const user = useUser();
    return user ? <Component {...props} /> : <LoginScreen />;
  };
}
const ProtectedSettings = withAuth(Settings);

// Render props: логика внутри, разметка снаружи
function MouseTracker({ render }: { render: (pos: { x: number; y: number }) => React.ReactNode }) {
  const pos = useMouse();
  return <>{render(pos)}</>;
}
<MouseTracker render={(pos) => <p>{pos.x}, {pos.y}</p>} />;

// Хук: то же без обёрток
function Cursor() {
  const { x, y } = useMouse();
  return <p>{x}, {y}</p>;
}`,
    flow: {
      actors: ["Page", "withAuth", "withTheme", "withRouter"],
      steps: [
        { from: 1, to: 2, label: "обёртка 1 добавляет user" },
        { from: 2, to: 3, label: "обёртка 2 добавляет theme" },
        { from: 3, to: 0, label: "обёртка 3 добавляет router", note: "откуда какой проп — неочевидно" },
        { from: 0, to: 0, label: "с хуками: useUser(), useTheme(), useRouter()", note: "всё видно прямо в компоненте" },
      ],
    },
    keys: [
      "HOC — функция `Component => Component` с добавленным поведением: `withAuth`, старый `connect`.",
      "Render props — компонент вызывает функцию из пропсов, чтобы отрисовать содержимое.",
      "Хуки заменили оба для переиспользования логики. HOC уместен для обёрток целиком: доступ, границы ошибок, логирование.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь паттерн и его форму.",
      pairs: [
        ["HOC", "функция, которая принимает компонент и возвращает новый"],
        ["render props", "компонент вызывает функцию из пропсов, чтобы отрисовать содержимое"],
        ["свой хук", "функция на `use`, возвращающая данные прямо в компонент"],
      ],
      why: "Все три решают одну задачу — переиспользовать логику, но хуки делают это без лишних уровней дерева.",
    },
    {
      type: "quiz",
      q: "Какая главная проблема нескольких HOC подряд?",
      opts: [
        "Глубокое дерево обёрток, неясно, откуда пропсы, и имена пропсов могут конфликтовать",
        "HOC нельзя использовать с функциональными компонентами",
        "HOC работают только на сервере",
        "HOC удалены в React 19",
      ],
      a: 0,
      why: "Хуки решают это: каждый источник данных — отдельный явный вызов в компоненте.",
    },
    {
      type: "quiz",
      q: "Когда HOC всё ещё уместен?",
      opts: [
        "Когда нужно обернуть компонент целиком: защита маршрута, граница ошибок, логирование",
        "Для любого переиспользования логики",
        "Чтобы хранить состояние формы",
        "Никогда",
      ],
      a: 0,
      why: "Для поведения «вокруг» компонента обёртка естественна. Для данных «внутри» компонента удобнее хук.",
    },
  ],
};
