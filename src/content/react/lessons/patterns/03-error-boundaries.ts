import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "pt3",
  region: 12,
  level: "middle",
  title: "Границы ошибок",
  q: "Что такое error boundary? Какие ошибки она ловит, а какие нет?",
  answer:
    "Граница ошибок — компонент, который ловит ошибки при рендере своих потомков и показывает запасной интерфейс вместо белого экрана. Это классовый компонент со `static getDerivedStateFromError` — вернуть состояние для запасного интерфейса — и `componentDidCatch` — отправить ошибку в мониторинг; хука для этого нет, поэтому часто берут библиотеку react-error-boundary. Граница ловит ошибки в рендере, в функциях жизненного цикла и эффектах потомков, а также ошибки Actions и отклонённые промисы из `use`. Не ловит ошибки в обработчиках событий, в обычном асинхронном коде вроде `setTimeout`, при рендеринге на сервере и в самой себе.",
  theory: {
    p: [
      "Без границы ошибка при рендере любого компонента размонтирует всё дерево: пользователь видит пустую страницу. Граница оборачивает часть интерфейса и заменяет упавшее поддерево запасным — «Не удалось загрузить виджет, повторить». Остальная страница продолжает работать.",
      "Устройство: классовый компонент. `static getDerivedStateFromError(error)` вызывается во время рендера и возвращает состояние, по которому `render` покажет запасной интерфейс. `componentDidCatch(error, info)` вызывается после коммита — место для логирования, в `info.componentStack` стек компонентов. Хука-аналога нет, поэтому в функциональном коде используют библиотеку react-error-boundary с готовым `<ErrorBoundary fallback onReset>`.",
      "Что не ловится. Ошибки в обработчиках событий — они происходят не при рендере, их ловят `try/catch` прямо в обработчике. Асинхронный код вне рендера — `setTimeout`, промисы в эффектах без записи в состояние. Рендеринг на сервере. И ошибка в самой границе — её поймает граница выше. В React 19 ошибки в Actions и отклонённые промисы из `use` доходят до ближайшей границы.",
      "Где ставить. Одна граница на всё приложение — последний рубеж. Плюс границы вокруг независимых частей: виджетов, маршрутов, панелей — чтобы сломанный блок не ронял страницу. Сбросить границу после ошибки можно, сменив ей `key`, или через `resetKeys` в react-error-boundary. В React 19 у корня есть `onCaughtError` и `onUncaughtError` — удобно для глобального мониторинга.",
    ],
    code: `class ErrorBoundary extends React.Component<
  { fallback: React.ReactNode; children: React.ReactNode },
  { hasError: boolean }
> {
  state = { hasError: false };

  static getDerivedStateFromError() {
    return { hasError: true }; // показать запасной интерфейс
  }

  componentDidCatch(error: Error, info: React.ErrorInfo) {
    monitoring.report(error, info.componentStack); // логирование
  }

  render() {
    return this.state.hasError ? this.props.fallback : this.props.children;
  }
}

<ErrorBoundary fallback={<p>Виджет не загрузился</p>}>
  <WeatherWidget />
</ErrorBoundary>;`,
    flow: {
      actors: ["WeatherWidget", "ErrorBoundary", "Мониторинг", "Остальная страница"],
      steps: [
        { from: 0, to: 1, label: "ошибка при рендере", lost: true },
        { from: 1, to: 1, label: "getDerivedStateFromError → hasError: true" },
        { from: 1, to: 1, label: "показать запасной интерфейс вместо виджета" },
        { from: 1, to: 2, label: "componentDidCatch: отчёт об ошибке" },
        { from: 3, to: 3, label: "шапка, меню и контент работают", note: "упал только один блок" },
      ],
    },
    keys: [
      "Граница ошибок ловит ошибки рендера потомков и показывает запасной интерфейс вместо белого экрана.",
      "Это класс с `getDerivedStateFromError` и `componentDidCatch`; хука нет, часто берут react-error-boundary.",
      "Не ловит ошибки в обработчиках, в асинхронном коде вне рендера, на сервере и в самой себе. Ставят на приложение и вокруг независимых блоков.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Поймает ли это граница ошибок?",
      groups: ["Поймает", "Не поймает"],
      items: [
        ["ошибка в рендере дочернего компонента", 0],
        ["ошибка в эффекте потомка", 0],
        ["отклонённый промис, прочитанный через `use`", 0],
        ["ошибка в обработчике `onClick`", 1],
        ["ошибка внутри `setTimeout` в обработчике", 1],
      ],
      why: "Граница работает с тем, что происходит при рендере и коммите. Обработчики и таймеры — обычный JavaScript, там `try/catch`.",
    },
    {
      type: "quiz",
      q: "Почему границу ошибок нельзя написать функциональным компонентом с хуком?",
      opts: [
        "Хука-аналога `getDerivedStateFromError` и `componentDidCatch` нет — нужен класс или готовая библиотека",
        "Функциональные компоненты не могут показывать запасной интерфейс",
        "Хуки удалены в React 19",
        "Можно: есть `useErrorBoundary` в React",
      ],
      a: 0,
      why: "Это одно из немногих мест, где классы всё ещё нужны. react-error-boundary прячет класс внутри.",
    },
    {
      type: "match",
      q: "Сопоставь метод и его роль.",
      pairs: [
        ["`getDerivedStateFromError`", "вернуть состояние для запасного интерфейса"],
        ["`componentDidCatch`", "залогировать ошибку после коммита"],
        ["смена `key` у границы", "сбросить её и попробовать снова"],
        ["`onUncaughtError` у корня", "глобальный отчёт о непойманных ошибках"],
      ],
      why: "Показ, логирование, сброс и глобальный мониторинг — четыре части обработки ошибок в React.",
    },
  ],
};
