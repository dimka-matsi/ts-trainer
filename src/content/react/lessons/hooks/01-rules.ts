import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "hk1",
  region: 2,
  level: "junior",
  title: "Правила хуков",
  q: "Какие есть правила хуков и почему хуки нельзя вызывать в условиях?",
  answer:
    "Хуки вызывают только на верхнем уровне компонента или своего хука — не в условиях, циклах, вложенных функциях и не после раннего `return`, — и только из компонентов и других хуков. Причина в устройстве: React не знает имён хуков, он хранит их данные списком по порядку вызова. В каждом рендере хуки должны вызываться в одном и том же порядке, иначе `useState` в следующем рендере получит чужое значение. Правила проверяет плагин eslint-plugin-react-hooks.",
  theory: {
    p: [
      "Хук — функция, имя которой начинается с `use`: `useState`, `useReducer`, свои вроде `useAuth`. Хуки дают компонентам-функциям то, что раньше было только у классов: состояние, эффекты, контекст. Они появились в React 16.8 и почти полностью заменили классовые компоненты.",
      "Как React хранит данные хуков: у каждого экземпляра компонента есть список ячеек. Первый вызов `useState` — первая ячейка, второй — вторая. Имён нет, только порядок. Если в одном рендере хук внутри `if` вызвался, а в следующем нет, все следующие хуки сдвинутся и прочитают чужие ячейки.",
      "Два правила. Только на верхнем уровне: не в условиях, циклах, вложенных функциях, `try/catch` и не после раннего `return`. Только из функций React: компонентов и своих хуков, а не из обычных функций и не из обработчиков событий. Условие переносят внутрь: не «вызвать хук, если…», а «вызвать хук всегда, а внутри проверить условие».",
      "Нарушения ловит eslint-plugin-react-hooks: правило `rules-of-hooks` и `exhaustive-deps` для зависимостей. Исключение — `use` из React 19: его можно вызывать в условиях и циклах, потому что он не занимает ячейку по порядку. Свои хуки обязаны начинаться с `use`: по имени линтер и React понимают, что внутри могут быть хуки.",
    ],
    code: `function Profile({ userId }: { userId: number | null }) {
  if (userId === null) return <p>Войдите</p>;
  const [tab, setTab] = useState('info'); // ошибка: хук после раннего return
  return <Tabs value={tab} onChange={setTab} />;
}

function ProfileFixed({ userId }: { userId: number | null }) {
  const [tab, setTab] = useState('info'); // все хуки — до любых return
  if (userId === null) return <p>Войдите</p>;
  return <Tabs value={tab} onChange={setTab} />;
}`,
    flow: {
      actors: ["Рендер 1", "Ячейки хуков компонента", "Рендер 2"],
      steps: [
        { from: 0, to: 1, label: "useState('info') → ячейка 1" },
        { from: 0, to: 1, label: "useState(false) → ячейка 2" },
        { from: 2, to: 1, label: "первый useState был в if и не вызвался", note: "порядок сдвинулся" },
        { from: 1, to: 2, label: "useState(false) читает ячейку 1 = 'info'", note: "чужое значение", lost: true },
        { from: 1, to: 1, label: "поэтому порядок вызовов должен быть одинаковым в каждом рендере" },
      ],
    },
    keys: [
      "Хуки — только на верхнем уровне: не в условиях, циклах, вложенных функциях и не после раннего `return`.",
      "Хуки — только в компонентах и своих хуках. Свои хуки называют с `use`.",
      "React хранит данные хуков по порядку вызова. Правила проверяет eslint-plugin-react-hooks, исключение — `use`.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Почему хуки нельзя вызывать внутри `if`?",
      opts: [
        "React хранит данные хуков по порядку вызова, и условие сдвинет порядок",
        "Условия замедляют рендер",
        "Хуки не видят переменных из условия",
        "Так решили для красоты кода",
      ],
      a: 0,
      why: "У хуков нет имён внутри React. Одинаковый порядок — единственный способ сопоставить вызов с его данными.",
    },
    {
      type: "sort",
      q: "Можно ли так вызвать хук?",
      groups: ["Можно", "Нельзя"],
      items: [
        ["в начале компонента", 0],
        ["внутри своего хука `useCart`", 0],
        ["внутри обработчика `handleClick`", 1],
        ["внутри цикла `for`", 1],
        ["после раннего `return`", 1],
      ],
      why: "Хук должен вызываться при каждом рендере и в том же месте. Обработчик, цикл и ранний `return` это нарушают.",
    },
    {
      type: "quiz",
      q: "Какой хук из React 19 можно вызывать внутри условия?",
      opts: ["`use`", "`useState`", "`useReducer`", "Никакой: все хуки только на верхнем уровне"],
      a: 0,
      why: "`use` читает промис или контекст и не хранит данных в ячейке по порядку, поэтому правило порядка на него не распространяется.",
    },
  ],
};
