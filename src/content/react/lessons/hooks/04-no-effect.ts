import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "hk4",
  region: 2,
  level: "middle",
  title: "Когда эффект не нужен",
  q: "Когда `useEffect` не нужен? Какие ошибки с эффектами делают чаще всего?",
  answer:
    "Эффект нужен только для синхронизации с внешней системой. Не нужен он, чтобы вычислить данные из пропсов и состояния — это делают прямо в рендере; чтобы отреагировать на действие пользователя — это делают в обработчике события; чтобы сбросить состояние при смене пропса — для этого есть `key`; и чтобы передать данные родителю — родитель сам держит состояние или его колбэк вызывают в обработчике. Цепочки эффектов, которые меняют состояние друг за другом, дают лишние рендеры и путаницу.",
  theory: {
    p: [
      "Правило из документации React: если внешней системы нет, эффект, скорее всего, не нужен. Лишний эффект — это лишний рендер: сначала экран со старыми данными, потом с новыми. Отсюда мигание интерфейса и код, который трудно отлаживать.",
      "Вычисления — в рендере. Полное имя из имени и фамилии, отфильтрованный список, сумма корзины вычисляются прямо в теле компонента. Хранить их в состоянии и обновлять эффектом — анти-паттерн. Тяжёлое вычисление запоминают, об этом урок про мемоизацию.",
      "Действия пользователя — в обработчиках. Отправка формы, покупка, уведомление «Товар добавлен» — реакция на конкретное событие, а не на то, что компонент отрисовался. Если отправлять запрос в эффекте, зависящем от флага «отправлено», легко получить двойную отправку. Эффект — для кода, который должен работать потому, что компонент показан: аналитика просмотра страницы, подписка.",
      "Сброс и цепочки. Сбросить всё состояние при смене пользователя — `<Profile key={userId} />`, а не эффект с сеттерами. Цепочка «эффект меняет A → эффект на A меняет B → эффект на B…» заменяется расчётом в одном обработчике. Уведомить родителя об изменении — вызвать его колбэк в том же обработчике, где меняется своё состояние.",
    ],
    code: `// Плохо: лишнее состояние и лишний рендер
function Greeting({ first, last }: { first: string; last: string }) {
  const [fullName, setFullName] = useState('');
  useEffect(() => setFullName(first + ' ' + last), [first, last]);
  return <p>{fullName}</p>;
}

// Хорошо: вычисляем в рендере
function GreetingFixed({ first, last }: { first: string; last: string }) {
  const fullName = first + ' ' + last;
  return <p>{fullName}</p>;
}

// Покупка — в обработчике, а не в эффекте, который следит за флагом
function BuyButton({ id }: { id: number }) {
  return <button onClick={() => api.buy(id)}>Купить</button>;
}`,
    flow: {
      actors: ["Рендер 1", "Эффект", "Рендер 2", "Экран"],
      steps: [
        { from: 0, to: 3, label: "fullName = '' — пустое имя", note: "первый кадр со старым значением" },
        { from: 3, to: 1, label: "после отрисовки: setFullName(…)" },
        { from: 1, to: 2, label: "лишний ререндер" },
        { from: 2, to: 3, label: "только теперь правильное имя", note: "мигание и двойная работа", lost: true },
        { from: 0, to: 3, label: "с вычислением в рендере — сразу правильно", note: "один рендер" },
      ],
    },
    keys: [
      "Эффект — только для внешних систем. Нет внешней системы — скорее всего, эффект не нужен.",
      "Производные данные считают в рендере, реакции на действия — в обработчиках.",
      "Сброс состояния — через `key`, цепочки эффектов — в один обработчик, уведомление родителя — в том же обработчике.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Нужен ли здесь эффект?",
      groups: ["Нужен", "Не нужен"],
      items: [
        ["подписаться на `resize` окна", 0],
        ["подключиться к WebSocket чата", 0],
        ["посчитать сумму корзины из списка товаров", 1],
        ["отправить заказ после клика «Купить»", 1],
        ["сбросить состояние формы при смене пользователя", 1],
      ],
      why: "Эффект оправдан, когда нужно держать связь с внешней системой, пока компонент на экране.",
    },
    {
      type: "quiz",
      q: "Как сбросить всё состояние `Profile` при смене `userId`?",
      opts: [
        "Передать `key={userId}`: React создаст компонент заново",
        "Написать эффект, который вызывает все сеттеры",
        "Перезагрузить страницу",
        "Обернуть компонент в `memo`",
      ],
      a: 0,
      why: "Новый ключ — новый экземпляр компонента с начальным состоянием. Без лишнего рендера со старыми данными.",
    },
    {
      type: "quiz",
      code: `const [fullName, setFullName] = useState('');
useEffect(() => setFullName(first + ' ' + last), [first, last]);`,
      q: "Что не так с этим кодом?",
      opts: [
        "`fullName` — производное значение: эффект даёт лишний рендер, считать надо прямо в рендере",
        "Нужно добавить `fullName` в зависимости",
        "Эффект обязан вернуть очистку",
        "Всё правильно",
      ],
      a: 0,
      why: "`const fullName = first + ' ' + last` — и никакого состояния, эффекта и мигания.",
    },
  ],
};
