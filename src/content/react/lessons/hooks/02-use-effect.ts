import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "hk2",
  region: 2,
  title: "useEffect: синхронизация с внешним миром",
  q: "Как работает `useEffect`? Когда он запускается, что делает массив зависимостей и зачем функция очистки?",
  answer:
    "`useEffect` синхронизирует компонент с внешней системой: подпиской, таймером, соединением, API браузера. Эффект запускается после того, как React обновил DOM и браузер отрисовал экран. Массив зависимостей говорит, когда перезапускать: без массива — после каждого рендера, `[]` — только после монтирования, `[id]` — когда изменился `id`. Функция, которую возвращает эффект, — очистка: React вызывает её перед следующим запуском и при размонтировании, чтобы отписаться, остановить таймер или отменить запрос.",
  theory: {
    p: [
      "Рендер должен быть чистым, но компоненту бывает нужно связаться с внешним миром: подписаться на событие окна, открыть WebSocket, запустить таймер, подключить стороннюю библиотеку. Для этого есть эффект — код, который выполняется после рендера, когда изменения уже на экране.",
      "Зависимости — все значения из компонента, которые использует эффект: пропсы, состояние, функции. React сравнивает их с прошлым рендером через `Object.is` и перезапускает эффект, если что-то изменилось. `[]` — запуск один раз после монтирования. Без массива — после каждого рендера. Зависимости не выбирают по желанию: правило линтера `exhaustive-deps` требует перечислить всё, что используется.",
      "Очистка. Эффект может вернуть функцию — React вызовет её перед следующим запуском эффекта и при размонтировании. Порядок при изменении зависимостей: рендер → очистка старого эффекта → запуск нового. Подписался — отпишись, запустил таймер — останови, открыл соединение — закрой. В разработке StrictMode специально запускает, очищает и снова запускает эффект, чтобы проверить очистку.",
      "Загрузка данных в эффекте — частый вопрос. Ответы могут прийти не по порядку, поэтому в очистке ставят флаг `ignore` или отменяют запрос через `AbortController`. Но в настоящих приложениях лучше библиотеки кэша или загрузка данных во фреймворке — регион «Серверное состояние». Эффект — запасной выход для синхронизации, а не место для любой логики.",
    ],
    code: `function ChatRoom({ roomId }: { roomId: string }) {
  useEffect(() => {
    const connection = createConnection(roomId);
    connection.connect();
    return () => connection.disconnect(); // очистка: перед сменой комнаты и при уходе
  }, [roomId]); // перезапуск, только когда сменилась комната

  return <h1>Комната {roomId}</h1>;
}

function WindowWidth() {
  const [width, setWidth] = useState(window.innerWidth);
  useEffect(() => {
    const onResize = () => setWidth(window.innerWidth);
    window.addEventListener('resize', onResize);
    return () => window.removeEventListener('resize', onResize);
  }, []); // подписка один раз
  return <p>{width}px</p>;
}`,
    flow: {
      actors: ["React", "DOM и экран", "Эффект", "Сервер чата"],
      steps: [
        { from: 0, to: 1, label: "рендер и коммит: DOM обновлён" },
        { from: 1, to: 2, label: "после отрисовки: эффект для 'general'" },
        { from: 2, to: 3, label: "connect('general')" },
        { from: 0, to: 2, label: "roomId сменился на 'music'", note: "сначала очистка старого эффекта" },
        { from: 2, to: 3, label: "disconnect('general')" },
        { from: 2, to: 3, label: "connect('music')" },
      ],
    },
    keys: [
      "Эффект — синхронизация с внешней системой. Запускается после отрисовки экрана.",
      "Зависимости: нет массива — каждый рендер, `[]` — после монтирования, `[x]` — при изменении `x`. Перечисляют всё, что используется.",
      "Очистка вызывается перед перезапуском и при размонтировании. В разработке StrictMode проверяет её двойным запуском.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь, что происходит, когда у `ChatRoom` меняется `roomId`.",
      items: [
        "React рендерит компонент с новым `roomId`",
        "DOM обновляется, браузер рисует экран",
        "вызывается очистка старого эффекта: отключение от старой комнаты",
        "запускается новый эффект: подключение к новой комнате",
      ],
      why: "Эффект всегда работает после отрисовки, а старая очистка — перед новым запуском.",
    },
    {
      type: "match",
      q: "Сопоставь запись и когда запускается эффект.",
      pairs: [
        ["без массива зависимостей", "после каждого рендера"],
        ["`[]`", "один раз после монтирования"],
        ["`[roomId]`", "когда изменился `roomId`"],
        ["функция, которую вернул эффект", "перед перезапуском и при размонтировании"],
      ],
      why: "Массив зависимостей описывает, от чего зависит синхронизация, а не «когда хочется запустить».",
    },
    {
      type: "quiz",
      q: "Когда запускается `useEffect` относительно отрисовки?",
      opts: [
        "После того как React обновил DOM и браузер отрисовал экран",
        "До рендера компонента",
        "Во время рендера",
        "Только при размонтировании",
      ],
      a: 0,
      why: "Поэтому эффект не задерживает показ. Если нужно успеть до отрисовки, для этого есть отдельный хук — о нём урок про эффекты раскладки.",
    },
    {
      type: "quiz",
      q: "Зачем в эффекте загрузки данных флаг `ignore` в очистке?",
      opts: [
        "Чтобы ответ на старый запрос не перезаписал данные после смены параметров",
        "Чтобы запрос выполнился дважды",
        "Чтобы линтер не ругался на зависимости",
        "Чтобы закэшировать ответ",
      ],
      a: 0,
      why: "Ответы приходят в любом порядке. Очистка помечает старый запрос устаревшим, и его результат игнорируется.",
    },
  ],
};
