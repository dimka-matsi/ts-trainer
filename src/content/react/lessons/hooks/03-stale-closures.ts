import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "hk3",
  region: 2,
  level: "middle",
  title: "Устаревшие замыкания и `useEffectEvent`",
  q: "Что такое устаревшее замыкание (stale closure) в хуках? Почему `setInterval` в эффекте видит старое значение и как это исправить?",
  answer:
    "Каждый рендер создаёт свои функции, и они запоминают значения пропсов и состояния этого рендера. Если эффект с `[]` запустил `setInterval`, колбэк навсегда видит `count` первого рендера — это устаревшее замыкание. Исправления: функциональное обновление `setCount(c => c + 1)`, которому не нужно текущее значение; честные зависимости, чтобы эффект пересоздавался; хранение последнего значения в ref. А в React 19.2 нереактивную логику выносят в `useEffectEvent`: функция из него внутри эффекта всегда видит свежие пропсы и состояние, но не требует перезапуска эффекта.",
  theory: {
    p: [
      "Замыкание — функция вместе с переменными, которые она видела при создании. Компонент — функция, и каждый рендер — отдельный вызов со своими `count`, пропсами и обработчиками. Эффект, созданный в первом рендере, видит значения первого рендера.",
      "Классика: интервал в эффекте с пустым массивом вызывает `setCount(count + 1)`. Счётчик дойдёт до 1 и остановится: колбэк всегда считает `0 + 1`. Правило линтера `exhaustive-deps` предупредит, что `count` не указан в зависимостях. Не отключайте его — оно ловит именно такие ошибки.",
      "Исправления. Функциональное обновление `setCount(c => c + 1)` — эффекту больше не нужен `count`. Если значение правда нужно, его добавляют в зависимости, и интервал пересоздаётся при изменении. Если нужно последнее значение без перезапуска — его держат в ref и читают `ref.current`; об этом урок про ссылки.",
      "`useEffectEvent`, стабильный с React 19.2, решает это правильно. `const onTick = useEffectEvent(() => …)` — функция внутри эффекта всегда видит свежие значения, но её не пишут в зависимости, и она не вызывает перезапуск. Правила: вызывать только из эффектов, не передавать в другие компоненты и не добавлять в зависимости. Пример из документации: чат переподключается при смене комнаты, но не при смене темы, которую показывает уведомление о подключении.",
    ],
    code: `function Timer() {
  const [count, setCount] = useState(0);
  useEffect(() => {
    // Ошибка: count всегда 0 — замыкание первого рендера
    const id = setInterval(() => setCount(count + 1), 1000);
    return () => clearInterval(id);
  }, []);
  return <p>{count}</p>;
}

function TimerFixed() {
  const [count, setCount] = useState(0);
  useEffect(() => {
    const id = setInterval(() => setCount((c) => c + 1), 1000); // текущий count не нужен
    return () => clearInterval(id);
  }, []);
  return <p>{count}</p>;
}

function ChatRoom({ roomId, theme }: { roomId: string; theme: string }) {
  // Видит свежую тему, но смена темы не переподключает чат
  const onConnected = useEffectEvent(() => showNotification('Подключено', theme));

  useEffect(() => {
    const connection = createConnection(roomId);
    connection.on('connected', () => onConnected());
    connection.connect();
    return () => connection.disconnect();
  }, [roomId]);

  return <h1>{roomId}</h1>;
}`,
    flow: {
      actors: ["Рендер 1: count = 0", "Колбэк интервала", "Рендер 2: count = 1", "Экран"],
      steps: [
        { from: 0, to: 1, label: "замыкание запомнило count = 0" },
        { from: 1, to: 3, label: "setCount(0 + 1) → 1" },
        { from: 2, to: 2, label: "новый рендер: count = 1" },
        { from: 1, to: 3, label: "setCount(0 + 1) → снова 1", note: "колбэк из рендера 1 не видит новых значений", lost: true },
        { from: 1, to: 3, label: "с setCount(c => c + 1): 2, 3, 4…", note: "функция берёт значение из очереди" },
      ],
    },
    keys: [
      "Каждый рендер создаёт свои функции со своими значениями. Старая функция видит старые значения.",
      "Лечат функциональным обновлением, честными зависимостями или ref для последнего значения. Правило `exhaustive-deps` не отключают.",
      "`useEffectEvent` (React 19.2): свежие значения внутри эффекта без перезапуска. Только из эффектов и не в зависимости.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      code: `const [count, setCount] = useState(0);
useEffect(() => {
  const id = setInterval(() => setCount(count + 1), 1000);
  return () => clearInterval(id);
}, []);`,
      q: "Что покажет счётчик через 5 секунд?",
      opts: ["1", "5", "0", "Ошибку"],
      a: 0,
      why: "Колбэк интервала создан в первом рендере и всегда вычисляет `0 + 1`. Состояние застревает на 1.",
    },
    {
      type: "quiz",
      q: "Что делает `useEffectEvent`?",
      opts: [
        "Даёт функцию, которая внутри эффекта видит свежие пропсы и состояние, но не требует перезапуска эффекта",
        "Запускает эффект по клику",
        "Заменяет `useEffect` в новых версиях React",
        "Кэширует результат эффекта",
      ],
      a: 0,
      why: "Так разделяют реактивную часть эффекта, от которой зависит перезапуск, и нереактивную, которой нужны только свежие значения.",
    },
    {
      type: "sort",
      q: "Исправит ли это интервал, который видит старый `count`?",
      groups: ["Исправит", "Не исправит"],
      items: [
        ["`setCount(c => c + 1)` вместо `setCount(count + 1)`", 0],
        ["добавить `count` в зависимости эффекта", 0],
        ["отключить правило линтера `exhaustive-deps`", 1],
        ["обернуть компонент в `memo`", 1],
        ["увеличить интервал до 2 секунд", 1],
      ],
      why: "Помогает только то, что даёт колбэку свежее значение или убирает зависимость от него.",
    },
  ],
};
