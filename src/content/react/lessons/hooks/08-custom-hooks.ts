import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "hk8",
  region: 2,
  title: "Свои хуки, useId и useDebugValue",
  q: "Как писать свои хуки? Что они переиспользуют — логику или состояние? Зачем нужен `useId`?",
  answer:
    "Свой хук — функция с именем на `use`, внутри которой вызываются другие хуки. Он выносит повторяющуюся логику: подписку на размер окна, задержку ввода, работу с `localStorage`. Хук переиспользует логику, а не состояние: у каждого компонента, который его вызвал, своё независимое состояние. `useId` создаёт уникальный стабильный id, например чтобы связать `label` и поле, и он одинаковый при рендеринге на сервере и в браузере. `useDebugValue` добавляет подпись своему хуку в React DevTools.",
  theory: {
    p: [
      "Когда в двух компонентах повторяется связка хуков — состояние плюс эффект подписки, — её выносят в функцию `useWindowWidth()`. Имя обязательно начинается с `use`: так линтер проверяет правила хуков внутри, а читатель понимает, что функция держит состояние или эффекты. Внутри свой хук может вызывать любые хуки и возвращать что угодно.",
      "Главное заблуждение: будто свой хук делится состоянием. Нет — каждый вызов создаёт свои ячейки. Два компонента с `useWindowWidth()` получат два независимых состояния и две подписки. Чтобы разные компоненты видели одни данные, нужен общий источник: состояние родителя, контекст или внешнее хранилище.",
      "Хорошие хуки описывают цель: `useOnlineStatus`, `useDebouncedValue`, `useMediaQuery`, `useLocalStorage`. Плохие прячут жизненный цикл: `useMount(fn)` или `useEffectOnce` — они скрывают зависимости и обходят линтер. Хук с одним `useState` внутри обычно не нужен.",
      "`useId()` возвращает строку, уникальную для этого места в дереве: `<label htmlFor={id}>` и `<input id={id}>`. `Math.random()` для этого не подходит: на сервере и в браузере получатся разные значения, и гидратация разойдётся. Для ключей списка `useId` не используют — ключи берут из данных. `useDebugValue(value)` показывает значение хука в DevTools рядом с его именем.",
    ],
    code: `function useDebouncedValue<T>(value: T, delay: number): T {
  const [debounced, setDebounced] = useState(value);
  useEffect(() => {
    const id = setTimeout(() => setDebounced(value), delay);
    return () => clearTimeout(id);
  }, [value, delay]);
  useDebugValue(debounced); // подпись в React DevTools
  return debounced;
}

function Search() {
  const [query, setQuery] = useState('');
  const debouncedQuery = useDebouncedValue(query, 300); // своё состояние у каждого вызова
  const id = useId();
  return (
    <>
      <label htmlFor={id}>Поиск</label>
      <input id={id} value={query} onChange={(e) => setQuery(e.target.value)} />
      <Results query={debouncedQuery} />
    </>
  );
}`,
    flow: {
      actors: ["Header", "Sidebar", "useWindowWidth()", "Окно браузера"],
      steps: [
        { from: 0, to: 2, label: "вызов 1: своё состояние и подписка" },
        { from: 1, to: 2, label: "вызов 2: ещё одно состояние и подписка", note: "логика общая, данные — нет" },
        { from: 3, to: 2, label: "resize" },
        { from: 2, to: 0, label: "setWidth в копии Header" },
        { from: 2, to: 1, label: "setWidth в копии Sidebar", note: "каждый компонент обновляет своё" },
      ],
    },
    keys: [
      "Свой хук — функция на `use`, которая вызывает другие хуки. Выносит повторяющуюся логику.",
      "Хук переиспользует логику, а не состояние: у каждого вызова свои данные. Общие данные — через родителя, контекст или хранилище.",
      "`useId` — стабильный id, одинаковый на сервере и клиенте, не для ключей списка. `useDebugValue` — подпись в DevTools.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Два компонента вызывают один и тот же свой хук `useCounter()`. Увидят ли они одно и то же значение счётчика?",
      opts: [
        "Нет: у каждого вызова своё независимое состояние",
        "Да: хук хранит одно общее состояние",
        "Да, если компоненты рядом в дереве",
        "Только в режиме разработки",
      ],
      a: 0,
      why: "Свой хук — это просто функция с хуками внутри. Каждый вызов создаёт свои ячейки состояния.",
    },
    {
      type: "sort",
      q: "Хорошая ли это идея для своего хука?",
      groups: ["Хорошая", "Плохая"],
      items: [
        ["`useOnlineStatus` — подписка на сеть", 0],
        ["`useDebouncedValue` — задержка значения", 0],
        ["`useMediaQuery` — совпадает ли медиазапрос", 0],
        ["`useMount(fn)` — запустить функцию при монтировании", 1],
        ["хук, внутри которого только один `useState` без логики", 1],
      ],
      why: "Хороший хук описывает, зачем он нужен. Обёртки над жизненным циклом прячут зависимости и мешают линтеру.",
    },
    {
      type: "quiz",
      q: "Почему для связки `label` и `input` берут `useId`, а не `Math.random()`?",
      opts: [
        "`useId` стабилен между рендерами и одинаков на сервере и в браузере, а случайное число сломает гидратацию",
        "`Math.random()` запрещён в React",
        "`useId` быстрее",
        "Разницы нет",
      ],
      a: 0,
      why: "Разметка с сервера должна совпасть с первым рендером в браузере. Случайные id этого не дают.",
    },
  ],
};
