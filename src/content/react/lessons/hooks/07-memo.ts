import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "hk7",
  region: 2,
  level: "middle",
  title: "useMemo, useCallback и memo",
  q: "Зачем нужны `useMemo`, `useCallback` и `memo`? Когда они бесполезны?",
  answer:
    "`memo(Component)` пропускает ререндер, если пропсы поверхностно не изменились. `useMemo` запоминает результат вычисления до изменения зависимостей, `useCallback` запоминает саму функцию, чтобы её ссылка не менялась. Работают они в паре: без `useCallback` новая функция в пропсах ломает `memo`. Бесполезны они, когда вычисление дешёвое, компонент не обёрнут в `memo` или пропсы всё равно каждый раз новые. С React Compiler мемоизация расставляется автоматически, и вручную её пишут всё реже.",
  theory: {
    p: [
      "`memo(Row)` — компонент, который при ререндере родителя сначала сравнивает старые и новые пропсы поверхностно: примитивы по значению, объекты, массивы и функции по ссылке через `Object.is`. Совпали все — рендер пропускается. Полезно для тяжёлых компонентов, которые часто получают одни и те же пропсы: строка таблицы, карточка в ленте, график.",
      "Ловушка: объект, массив или функция, созданные прямо в рендере родителя, каждый раз новые. Родитель передаёт `onSelect={() => …}` — `memo` видит новый проп, и сравнение только добавило работы. `useCallback(fn, deps)` сохраняет одну и ту же функцию, пока не изменятся зависимости. `useMemo(() => value, deps)` так же сохраняет объект или результат вычисления.",
      "`useMemo` нужен и сам по себе, если вычисление дорогое: отфильтровать и отсортировать 10 000 строк при каждом нажатии в соседнем поле — лишняя работа. Ещё он и `useCallback` стабилизируют значения, которые стоят в зависимостях эффекта: иначе эффект перезапускался бы после каждого рендера. Для дешёвых вычислений мемоизация не нужна: сравнить зависимости и хранить результат может стоить дороже.",
      "Порядок действий: сначала структура — опустить состояние ниже, передавать готовые элементы через `children`, — потом замер в Profiler, и только потом точечная мемоизация. React Compiler 1.0 (октябрь 2025) анализирует компоненты при сборке и сам расставляет мемоизацию, поэтому в новых проектах ручные `useMemo` и `useCallback` нужны реже — о нём регион «Производительность».",
    ],
    code: `const Row = memo(function Row({ item, onSelect }: { item: Item; onSelect: (id: number) => void }) {
  return <li onClick={() => onSelect(item.id)}>{item.title}</li>;
});

function List({ items, query }: { items: Item[]; query: string }) {
  const [selected, setSelected] = useState<number | null>(null);

  // Тяжёлое вычисление повторится, только когда изменятся items или query
  const visible = useMemo(() => items.filter((i) => i.title.includes(query)), [items, query]);

  // Одна и та же функция между рендерами — memo(Row) пропустит лишние рендеры
  const handleSelect = useCallback((id: number) => setSelected(id), []);

  return <ul>{visible.map((i) => <Row key={i.id} item={i} onSelect={handleSelect} />)}</ul>;
}`,
    flow: {
      actors: ["Родитель", "Сравнение пропсов memo", "Row"],
      steps: [
        { from: 0, to: 1, label: "onSelect — новая стрелка при каждом рендере" },
        { from: 1, to: 2, label: "проп изменился по ссылке → рендер Row", note: "memo не помог", lost: true },
        { from: 0, to: 0, label: "onSelect обёрнут в useCallback" },
        { from: 0, to: 1, label: "onSelect — та же ссылка" },
        { from: 1, to: 1, label: "пропсы совпали — рендер Row пропущен", note: "работает в паре" },
      ],
    },
    keys: [
      "`memo` пропускает рендер при поверхностно равных пропсах. Новые объекты и функции в пропсах его ломают.",
      "`useCallback` сохраняет функцию, `useMemo` — значение. Нужны для `memo`, дорогих вычислений и стабильных зависимостей эффектов.",
      "Сначала структура и замер, потом точечная мемоизация. React Compiler расставляет её автоматически.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь инструмент и что он запоминает.",
      pairs: [
        ["`memo`", "результат рендера компонента при тех же пропсах"],
        ["`useMemo`", "результат вычисления при тех же зависимостях"],
        ["`useCallback`", "саму функцию, чтобы ссылка не менялась"],
      ],
      why: "`useCallback(fn, deps)` — то же самое, что `useMemo(() => fn, deps)`.",
    },
    {
      type: "quiz",
      q: "Строка таблицы обёрнута в `memo`, но всё равно перерисовывается при каждом рендере родителя. Самая частая причина?",
      opts: [
        "Родитель передаёт новый объект или функцию, созданные при рендере",
        "`memo` не работает со строками таблиц",
        "Не включён режим production",
        "Таблица слишком большая",
      ],
      a: 0,
      why: "Сравнение поверхностное, по ссылке. Новая функция на каждом рендере — это «изменившийся» проп.",
    },
    {
      type: "quiz",
      q: "Когда `useMemo` скорее вредит, чем помогает?",
      opts: [
        "Когда вычисление дешёвое: сравнение зависимостей и хранение результата стоят больше",
        "Когда вычисление сортирует 10 000 строк",
        "Когда зависимости редко меняются",
        "Никогда",
      ],
      a: 0,
      why: "Мемоизация не бесплатна. Её ставят там, где замер показал дорогую работу или лишние рендеры.",
    },
  ],
};
