import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "arch4",
  region: 8,
  title: "Маршрутизация: React Router и загрузчики данных",
  q: "Как устроена маршрутизация в React? Что такое вложенные маршруты и загрузчики данных?",
  answer:
    "Маршрутизатор связывает адрес страницы с деревом компонентов и меняет адрес через History API без перезагрузки. В React Router маршруты описывают деревом: у родителя общий макет и `<Outlet>`, куда подставляется дочерний маршрут, — это вложенные маршруты. Параметры адреса читают через `useParams`, строку запроса — через `useSearchParams`, переходы делают `<Link>` и `useNavigate`. Загрузчики (`loader`) получают данные до показа страницы и параллельно для всех вложенных маршрутов — без «водопада» эффектов. React Router 7 работает как библиотека или как полноценный фреймворк с серверным рендерингом — бывший Remix.",
  theory: {
    p: [
      "В браузере маршрутизатор перехватывает клики по ссылкам, меняет адрес через `history.pushState` и рисует нужные компоненты. Для пользователя это как переход по страницам, но без перезагрузки: состояние и загруженный код сохраняются.",
      "Вложенные маршруты повторяют вложенность интерфейса. Маршрут `/account` рисует макет с меню и `<Outlet />`, а дочерние `/account/orders` и `/account/settings` подставляются в это место. Макет не перерисовывается при переходе между дочерними. Параметры: путь `/orders/:id` и `useParams()` дают `id`, строка запроса — `useSearchParams()`. Фильтры и вкладки удобно хранить в адресе: ссылкой можно поделиться, и кнопка «назад» работает.",
      "Загрузка данных. Если каждый вложенный компонент грузит свои данные в `useEffect`, получается водопад: родитель загрузился, отрисовал ребёнка, ребёнок только начал свой запрос. `loader` у маршрута запускается при переходе, до рендера, и для всех совпавших вложенных маршрутов параллельно. `action` обрабатывает отправку формы маршрута и сам перезапрашивает данные. Код маршрутов разделяют через `lazy`, чтобы страница качала только свой код.",
      "Варианты. React Router 7 работает в режиме библиотеки (просто маршруты), в режиме данных (`createBrowserRouter` с загрузчиками) и в режиме фреймворка с серверным рендерингом — в него влился Remix. TanStack Router делает ставку на строгую типизацию путей и параметров. В Next.js маршрутизация построена на файлах — это следующий регион.",
    ],
    code: `import { createBrowserRouter, RouterProvider, Outlet, Link, useLoaderData, useParams } from 'react-router';

type Order = { id: string; total: number };

function AccountLayout() {
  return (
    <div>
      <nav><Link to="orders">Заказы</Link> <Link to="settings">Настройки</Link></nav>
      <Outlet /> {/* сюда подставится дочерний маршрут */}
    </div>
  );
}

function OrderPage() {
  const order = useLoaderData() as Order;   // данные уже загружены до рендера
  const { id } = useParams();
  return <h1>Заказ {id}: {order.total} ₽</h1>;
}

const router = createBrowserRouter([
  {
    path: '/account',
    element: <AccountLayout />,
    children: [
      { path: 'orders/:id', element: <OrderPage />, loader: ({ params }) => fetch(\`/api/orders/\${params.id}\`) },
      { path: 'settings', lazy: () => import('./Settings') },   // код страницы по требованию
    ],
  },
]);

export const App = () => <RouterProvider router={router} />;`,
    flow: {
      actors: ["Пользователь", "Маршрутизатор", "loader", "Компоненты"],
      steps: [
        { from: 0, to: 1, label: "клик: /account/orders/42" },
        { from: 1, to: 2, label: "запуск загрузчиков всех совпавших маршрутов параллельно" },
        { from: 2, to: 1, label: "данные заказа готовы" },
        { from: 1, to: 3, label: "рендер: макет остаётся, в Outlet — OrderPage", note: "без водопада запросов из эффектов" },
      ],
    },
    keys: [
      "Маршрутизатор меняет адрес через History API и рисует дерево компонентов без перезагрузки.",
      "Вложенные маршруты: макет родителя с `<Outlet>`. `useParams`, `useSearchParams`, `<Link>`, `useNavigate`.",
      "`loader` грузит данные до рендера и параллельно, без водопада эффектов. React Router 7 — библиотека или фреймворк (бывший Remix).",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Зачем в макете родительского маршрута `<Outlet />`?",
      opts: [
        "Это место, куда подставляется совпавший дочерний маршрут",
        "Это ссылка на главную страницу",
        "Это обработчик ошибок маршрута",
        "Это загрузчик данных",
      ],
      a: 0,
      why: "Родитель рисует общий каркас — меню, шапку — и не перерисовывается при переходе между дочерними страницами.",
    },
    {
      type: "quiz",
      q: "Страница заказа грузит заказ в `useEffect`, а вложенный блок доставки — свои данные тоже в `useEffect`. В чём проблема и как её решают загрузчики?",
      opts: [
        "Водопад: запрос ребёнка начнётся только после рендера родителя. Загрузчики стартуют при переходе и параллельно",
        "Эффекты не умеют делать запросы",
        "Данные загрузятся дважды из-за StrictMode, загрузчики это запрещают",
        "Проблемы нет",
      ],
      a: 0,
      why: "Последовательные запросы складывают задержки. Маршрутизатор знает все совпавшие маршруты сразу и может запросить их данные одновременно.",
    },
    {
      type: "match",
      q: "Сопоставь API React Router и назначение.",
      pairs: [
        ["`useParams`", "параметры пути, например `:id`"],
        ["`useSearchParams`", "строка запроса: фильтры, сортировка"],
        ["`loader`", "данные маршрута до рендера"],
        ["`lazy`", "код маршрута по требованию"],
      ],
      why: "Фильтры в строке запроса переживают перезагрузку, ими можно поделиться, а «назад» возвращает предыдущие.",
    },
  ],
};
