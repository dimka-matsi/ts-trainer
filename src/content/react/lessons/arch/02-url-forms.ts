import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "arch2",
  region: 8,
  level: "middle",
  title: "Адрес страницы, формы и сохранение состояния",
  q: "Что хранить в адресе страницы, а что в хранилище? Как быть с формами и сохранением состояния между перезагрузками?",
  answer:
    "Всё, чем пользователь захочет поделиться или к чему вернётся кнопкой «Назад» — фильтры, сортировку, поиск, номер страницы, открытую вкладку, — кладут в адрес страницы: он становится источником правды. Формы держат в локальном состоянии или в библиотеке форм вроде React Hook Form, которая не перерисовывает всю форму на каждый символ, а не в глобальном хранилище. Сохранять между перезагрузками стоит только то, что правда нужно, — тему, черновик, корзину гостя — через `persist` хранилища или `localStorage`, с версией и без личных данных.",
  theory: {
    p: [
      "Состояние в адресе. Фильтры каталога в `?category=tea&sort=price&page=2` дают три вещи: ссылкой можно поделиться, перезагрузка ничего не теряет, «Назад» и «Вперёд» работают. Источник правды — адрес: компонент читает параметры (в React Router — `useSearchParams`) и меняет их вместо своего `useState`. Копировать фильтры в хранилище не нужно: две копии разойдутся.",
      "Формы. Значения полей, ошибки проверки, «поле трогали», «форма отправляется» — это локальное состояние формы. Класть каждый символ в глобальное хранилище — лишние ререндеры и лишний код. Библиотеки форм, например React Hook Form, хранят значения в неуправляемых полях и перерисовывают только то, что нужно, а проверку описывают схемой. Наружу форма отдаёт только итог при отправке.",
      "Сохранение между перезагрузками. Тему, язык, черновик сообщения, корзину гостя можно сохранять в `localStorage`: у Zustand для этого есть middleware `persist`, у Redux — библиотеки вроде redux-persist. Не сохраняют серверные данные — их лучше перезапросить — и токены и личные данные: `localStorage` доступен любому скрипту на странице.",
      "Подводные камни сохранения. При изменении структуры состояния старая сохранённая версия может сломать приложение, поэтому у сохранённых данных есть номер версии и миграция. При рендеринге на сервере `localStorage` нет: сохранённое значение появляется только после загрузки в браузере, и разметка должна пережить это без ошибок гидратации.",
    ],
    code: `function Catalog() {
  // Источник правды — адрес страницы: ?category=tea&page=2
  const [params, setParams] = useSearchParams();
  const page = Number(params.get('page') ?? 1);

  const next = () =>
    setParams((p) => {
      p.set('page', String(page + 1));
      return p;
    });

  return <button onClick={next}>Дальше</button>;
}

// Тема переживёт перезагрузку; версия — на случай смены структуры
const useSettings = create<Settings>()(
  persist((set) => ({ theme: 'light', setTheme: (theme) => set({ theme }) }), {
    name: 'settings',
    version: 1,
  }),
);`,
    flow: {
      actors: ["Пользователь", "Адрес страницы", "Каталог", "Сервер"],
      steps: [
        { from: 0, to: 1, label: "выбрал «Чай», страница 2", note: "?category=tea&page=2" },
        { from: 1, to: 2, label: "useSearchParams: category, page" },
        { from: 2, to: 3, label: "запрос с фильтрами" },
        { from: 0, to: 1, label: "поделился ссылкой или нажал «Назад»", note: "адрес — источник правды, состояние восстановлено" },
      ],
    },
    keys: [
      "Фильтры, сортировку, поиск и страницу — в адрес: можно поделиться ссылкой, «Назад» работает. В хранилище не копировать.",
      "Формы — в локальном состоянии или библиотеке форм, а не в глобальном хранилище на каждый символ.",
      "Сохраняют между перезагрузками только нужное, с версией. Не сохраняют серверные данные, токены и личное.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Где хранить?",
      groups: ["Адрес страницы", "Состояние формы", "Сохранить в localStorage"],
      items: [
        ["сортировка каталога", 0],
        ["номер страницы в списке", 0],
        ["ошибки проверки поля email", 1],
        ["значение поля «Комментарий» до отправки", 1],
        ["выбранная тема оформления", 2],
        ["язык интерфейса, выбранный вручную", 2],
      ],
      why: "Адрес — для того, чем делятся и к чему возвращаются, форма — для ввода до отправки, `localStorage` — для настроек, которые должны пережить перезагрузку.",
    },
    {
      type: "quiz",
      q: "Почему фильтры каталога лучше хранить в адресе страницы, а не в хранилище?",
      opts: [
        "Ссылкой можно поделиться, перезагрузка ничего не теряет, «Назад» работает",
        "Адрес быстрее памяти",
        "Хранилище не умеет хранить строки",
        "Так требует React",
      ],
      a: 0,
      why: "Адрес — уже готовое хранилище состояния с историей, которое браузер ведёт сам.",
    },
    {
      type: "quiz",
      q: "Что не стоит сохранять в `localStorage` через `persist`?",
      opts: ["Токен доступа и личные данные", "Выбранную тему", "Язык интерфейса", "Черновик сообщения"],
      a: 0,
      why: "`localStorage` читает любой скрипт на странице, в том числе внедрённый при XSS. Токены держат в cookie с `HttpOnly` или в памяти.",
    },
  ],
};
