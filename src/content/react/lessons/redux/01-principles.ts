import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rdx1",
  region: 5,
  level: "middle",
  title: "Redux: принципы и поток данных",
  q: "Как устроен Redux? Какие у него три принципа и какой паттерн он реализует?",
  answer:
    "Redux хранит глобальное состояние в одном объекте — сторе (store) — вне React. Изменить его можно только отправив действие (action) — объект с полем `type`, а новое состояние вычисляют чистые функции-редьюсеры. Три принципа: единый источник правды, состояние только для чтения, изменения через чистые функции. Поток данных однонаправленный, как в архитектуре Flux: действие → редьюсер → новое состояние → подписчики. Благодаря этому каждое изменение предсказуемо, записано в журнале и видно в Redux DevTools.",
  theory: {
    p: [
      "Redux — библиотека, которая держит состояние вне компонентов, в одном объекте-сторе. Компоненты подписываются на него и получают нужные данные. Идея пришла из Flux — архитектуры Facebook с однонаправленным потоком данных, а редьюсеры — из функционального программирования.",
      "Три принципа. Единый источник правды: всё глобальное состояние — одно дерево в одном сторе. Состояние только для чтения: напрямую его не меняют, а отправляют действие — объект вроде `{ type: 'cart/added', payload: item }`. Изменения описывают чистые функции: редьюсер получает старое состояние и действие и возвращает новое, не трогая старое.",
      "Поток данных идёт по кругу. Пользователь нажал кнопку → компонент вызывает `dispatch(action)` → стор передаёт действие корневому редьюсеру → редьюсер возвращает новое состояние → стор сообщает подписчикам → компоненты, чьи данные изменились, перерисовываются. Большой редьюсер собирают из маленьких через `combineReducers`: каждый отвечает за свою часть дерева.",
      "Что это даёт. Любое изменение — действие в журнале, поэтому Redux DevTools показывают историю, разницу состояний и умеют «путешествовать во времени» — откатываться к прошлым состояниям. Логику легко тестировать: редьюсер — чистая функция. Цена — много шаблонного кода в классическом Redux. Эту проблему решает Redux Toolkit, о нём дальше в регионе.",
    ],
    code: `// Классический Redux. Сейчас стор создают через Redux Toolkit, но принципы те же
const initialState = { items: [] as string[] };

function cartReducer(state = initialState, action: { type: string; payload?: string }) {
  switch (action.type) {
    case 'cart/added':
      return { ...state, items: [...state.items, action.payload!] };
    case 'cart/cleared':
      return { ...state, items: [] };
    default:
      return state;
  }
}

const store = createStore(combineReducers({ cart: cartReducer }));
store.subscribe(() => console.log(store.getState()));
store.dispatch({ type: 'cart/added', payload: 'чайник' });`,
    flow: {
      actors: ["Компонент", "dispatch", "Редьюсер", "Стор", "Подписчики"],
      steps: [
        { from: 0, to: 1, label: "dispatch({ type: 'cart/added', payload })", note: "компонент сообщает, что произошло" },
        { from: 1, to: 2, label: "reducer(state, action)" },
        { from: 2, to: 3, label: "новое состояние", note: "старое не изменено" },
        { from: 3, to: 4, label: "уведомление подписчикам" },
        { from: 4, to: 0, label: "перерисовываются те, чьи данные изменились" },
      ],
    },
    keys: [
      "Один стор вне React, изменения — только через действия, новое состояние считают чистые редьюсеры.",
      "Однонаправленный поток, как во Flux: `dispatch` → редьюсер → стор → подписчики.",
      "Плюсы — предсказуемость, журнал действий, DevTools с путешествием во времени. Минус классики — шаблонный код.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь путь изменения в Redux.",
      items: [
        "пользователь нажимает кнопку",
        "компонент вызывает `dispatch(action)`",
        "редьюсер возвращает новое состояние",
        "стор сообщает подписчикам",
        "компоненты с изменившимися данными перерисовываются",
      ],
      why: "Данные идут в одну сторону. Компонент не меняет стор, а только сообщает о событии.",
    },
    {
      type: "match",
      q: "Сопоставь принцип Redux и его смысл.",
      pairs: [
        ["единый источник правды", "всё глобальное состояние — одно дерево в одном сторе"],
        ["состояние только для чтения", "менять можно только через действие"],
        ["изменения через чистые функции", "редьюсер возвращает новое состояние, не трогая старое"],
        ["однонаправленный поток", "действие → редьюсер → стор → подписчики"],
      ],
      why: "Эти правила и делают Redux предсказуемым: у каждого изменения есть причина-действие и один способ его обработать.",
    },
    {
      type: "quiz",
      q: "Какой архитектурный паттерн лежит в основе Redux?",
      opts: [
        "Flux с однонаправленным потоком данных",
        "MVC с двусторонней привязкой данных",
        "Изменяемые наблюдаемые объекты",
        "Singleton без подписок",
      ],
      a: 0,
      why: "Redux — упрощённый Flux: одно хранилище, действия и редьюсеры вместо набора хранилищ и диспетчера.",
    },
    {
      type: "quiz",
      q: "Как Redux DevTools умеют «путешествовать во времени»?",
      opts: [
        "Каждое изменение — действие в журнале, а состояние после него — неизменяемый снимок, к которому можно вернуться",
        "Они записывают видео экрана",
        "Они сохраняют страницу в кэш браузера",
        "Они перезапускают приложение с начала",
      ],
      a: 0,
      why: "Раз старое состояние никто не меняет, прошлые снимки остаются целыми, и к любому из них можно вернуться.",
    },
  ],
};
