import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rdx6",
  region: 5,
  title: "Context + `useReducer` вместо Redux?",
  q: "Можно ли заменить Redux связкой Context + `useReducer`? Что при этом теряется?",
  answer:
    "Технически да: `useReducer` даёт редьюсер и `dispatch`, а Context доставляет их вниз, и для небольшого приложения этого хватает. Но теряется многое: подписка на часть состояния через селекторы — контекст перерисует всех потребителей при любом изменении, middleware для асинхронности и логирования, Redux DevTools с журналом и путешествием во времени, готовые инструменты RTK. Поэтому связка подходит для небольшого, редко меняющегося состояния, а для большого и частого берут Redux Toolkit или лёгкое хранилище с подписками.",
  theory: {
    p: [
      "Связка выглядит как мини-Redux: в провайдере `useReducer(reducer, initial)`, а `state` и `dispatch` раздаются через контекст. Компоненты отправляют действия, редьюсер считает новое состояние. Для формы-мастера, корзины небольшого магазина или настроек этого хватает.",
      "Первое, что теряется, — точные подписки. В Redux каждый `useSelector` сравнивает свой кусок, а у контекста при новом `state` перерисовываются все потребители. Частично помогает разделение: `dispatch` в отдельном контексте — он стабилен — и несколько контекстов по частям состояния. Но с ростом приложения это превращается в ручную работу.",
      "Второе — инфраструктура. Нет middleware: логирование, аналитику и асинхронность придётся писать в компонентах или своих хуках. Нет Redux DevTools: не посмотреть журнал действий и не откатиться назад. Нет `createAsyncThunk`, нормализации и проверок в разработке.",
      "Как отвечать на собеседовании: «Можно, но только для небольшого состояния, которое меняется редко. Как только нужны частые обновления у многих компонентов, асинхронные сценарии или отладка по журналу — беру Redux Toolkit или хранилище с селекторами». Сильный ответ показывает, что ты понимаешь цену выбора, а не просто знаешь, что «так тоже можно».",
    ],
    code: `const StateContext = createContext<State | null>(null);
const DispatchContext = createContext<React.Dispatch<Action> | null>(null);

function StoreProvider({ children }: { children: React.ReactNode }) {
  const [state, dispatch] = useReducer(reducer, initialState);
  return (
    <StateContext value={state}>
      {/* dispatch стабилен: кому нужно только отправлять, те не перерисуются */}
      <DispatchContext value={dispatch}>{children}</DispatchContext>
    </StateContext>
  );
}`,
    flow: {
      actors: ["StoreProvider: useReducer", "StateContext", "ProductList (читает state)", "Header (читает state)", "AddButton (читает dispatch)"],
      steps: [
        { from: 4, to: 0, label: "dispatch({ type: 'added' })" },
        { from: 0, to: 1, label: "новый state" },
        { from: 1, to: 2, label: "ререндер ProductList", note: "нужен" },
        { from: 1, to: 3, label: "ререндер Header", note: "лишний: ему нужен только счётчик", lost: true },
        { from: 0, to: 4, label: "AddButton не перерисован", note: "dispatch в отдельном контексте, ссылка та же" },
      ],
    },
    keys: [
      "Context + `useReducer` — мини-Redux для небольшого и редко меняющегося состояния.",
      "Теряются селекторы (все потребители перерисовываются), middleware, Redux DevTools и инструменты RTK.",
      "`dispatch` выносят в отдельный контекст. При частых обновлениях и сложной логике берут RTK или хранилище с подписками.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что есть у обоих подходов, а что только в Redux Toolkit?",
      groups: ["Есть у обоих", "Только в Redux Toolkit"],
      items: [
        ["редьюсер и `dispatch`", 0],
        ["доставка данных без props drilling", 0],
        ["подписка на часть состояния через селектор", 1],
        ["журнал действий в DevTools", 1],
        ["middleware для асинхронности", 1],
      ],
      why: "Связка повторяет форму Redux, но не его инфраструктуру.",
    },
    {
      type: "quiz",
      q: "Какой ответ на вопрос «Можно ли заменить Redux на Context + useReducer?» сильнее всего?",
      opts: [
        "Можно для небольшого редкого состояния, но теряются селекторы, middleware и DevTools; при росте беру RTK",
        "Нельзя ни в каком случае",
        "Можно всегда: это одно и то же",
        "Можно, но только в классовых компонентах",
      ],
      a: 0,
      why: "На собеседовании ценят понимание цены: что работает, до какого предела и чем платишь.",
    },
    {
      type: "quiz",
      q: "Зачем `dispatch` кладут в отдельный контекст?",
      opts: [
        "Он стабилен, и компоненты, которые только отправляют действия, не перерисуются при изменении состояния",
        "Иначе `dispatch` не работает",
        "Так быстрее работает редьюсер",
        "Чтобы отправлять действия на сервер",
      ],
      a: 0,
      why: "Ссылка на `dispatch` не меняется, поэтому значение его контекста не меняется никогда.",
    },
  ],
};
