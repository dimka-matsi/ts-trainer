import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rdx3",
  region: 5,
  level: "middle",
  title: "Middleware и асинхронность: thunk и saga",
  q: "Как в Redux работать с асинхронными запросами? Что такое middleware и чем thunk отличается от saga?",
  answer:
    "Редьюсер обязан быть чистым, поэтому запросы в нём делать нельзя. Асинхронность выносят в middleware — прослойку между `dispatch` и редьюсером, которая видит каждое действие и может его задержать, изменить или отправить новые. redux-thunk позволяет отправить функцию вместо объекта: внутри делают запрос и отправляют обычные действия о начале, успехе и ошибке. redux-saga описывает сложные сценарии генераторами: отмена, гонки, повторы, ожидание нескольких действий. Она мощнее, но сложнее, и в новых проектах её берут редко.",
  theory: {
    p: [
      "Middleware — функция, через которую проходит каждое действие по пути от `dispatch` к редьюсеру. Её форма — `store => next => action => …`: она может посмотреть действие, передать его дальше через `next`, не передать, передать другое или отправить несколько новых. Так устроены логирование, аналитика, обработка ошибок и асинхронность.",
      "redux-thunk — самый простой вариант. Он разрешает отправлять не объект, а функцию `(dispatch, getState) => {…}`. Внутри делают запрос и отправляют обычные действия: сначала «загрузка», потом «загружено» с данными или «ошибка». Редьюсеры остаются чистыми и просто переключают статус.",
      "redux-saga строит побочные эффекты на генераторах — функциях, которые умеют приостанавливаться. Сага ждёт действие (`take`), вызывает запрос (`call`), отправляет действие (`put`). Есть готовые схемы: `takeLatest` отменяет прошлую задачу, если пришло новое действие, — удобно для поиска при вводе. Саги хороши для длинных сценариев: опрос сервера, отмена, гонка между запросом и таймаутом.",
      "Что выбирать. Для большинства задач хватает thunk, и Redux Toolkit включает его по умолчанию. Саги — когда сценарии правда сложные и команда их знает: порог входа высокий. А загрузку данных с сервера сегодня чаще отдают специальным библиотекам кэша, и ни thunk, ни саги для неё не нужны — об этом регион «Серверное состояние».",
    ],
    code: `// Middleware-логгер: видит каждое действие
const logger: Middleware = (store) => (next) => (action) => {
  console.log('до', store.getState());
  const result = next(action);
  console.log('после', store.getState());
  return result;
};

// Thunk: вместо объекта отправляем функцию
const fetchOrders = () => async (dispatch: AppDispatch) => {
  dispatch({ type: 'orders/loading' });
  try {
    const res = await fetch('/api/orders');
    dispatch({ type: 'orders/loaded', payload: await res.json() });
  } catch {
    dispatch({ type: 'orders/failed' });
  }
};

// Saga: при каждом новом вводе прошлый поиск отменяется
function* searchSaga() {
  yield takeLatest('search/changed', function* (action: { type: string; payload: string }) {
    const results: string[] = yield call(api.search, action.payload);
    yield put({ type: 'search/loaded', payload: results });
  });
}`,
    flow: {
      actors: ["Компонент", "Middleware thunk", "API", "Редьюсер"],
      steps: [
        { from: 0, to: 1, label: "dispatch(fetchOrders())", note: "функция, а не объект" },
        { from: 1, to: 3, label: "orders/loading", note: "статус «загрузка»" },
        { from: 1, to: 2, label: "GET /api/orders" },
        { from: 2, to: 1, label: "заказы" },
        { from: 1, to: 3, label: "orders/loaded с данными", note: "редьюсер остаётся чистым" },
        { from: 3, to: 0, label: "ререндер со списком" },
      ],
    },
    keys: [
      "Редьюсер чистый, поэтому асинхронность — в middleware: прослойке между `dispatch` и редьюсером.",
      "thunk: отправляешь функцию, внутри запрос и обычные действия о начале, успехе и ошибке. Включён в Redux Toolkit.",
      "saga: генераторы, `take`, `call`, `put`, `takeLatest`. Для сложных сценариев с отменой и гонками, но порог входа высокий.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Почему запрос к серверу нельзя сделать прямо в редьюсере?",
      opts: [
        "Редьюсер должен быть чистой функцией: без побочных эффектов и с одинаковым результатом на одинаковый вход",
        "Редьюсер не умеет работать с сетью",
        "Запросы запрещены во всём Redux",
        "Редьюсер вызывается только один раз",
      ],
      a: 0,
      why: "Чистота редьюсера даёт предсказуемость и путешествие во времени. Всё «грязное» живёт в middleware.",
    },
    {
      type: "match",
      q: "Сопоставь инструмент и его подход.",
      pairs: [
        ["redux-thunk", "отправить функцию, внутри запрос и обычные действия"],
        ["redux-saga", "генераторы с `take`, `call` и `put`"],
        ["`takeLatest`", "отменить прошлую задачу, если пришло новое действие"],
        ["middleware-логгер", "увидеть каждое действие и состояние до и после"],
      ],
      why: "Все они — middleware или построены на нём: код между `dispatch` и редьюсером.",
    },
    {
      type: "quiz",
      q: "Пользователь быстро печатает в поиске, и ответы на старые запросы приходят позже новых. Что в redux-saga решает это из коробки?",
      opts: ["`takeLatest`: он отменяет прошлую задачу при новом вводе", "`combineReducers`", "`shallowEqual`", "`Provider`"],
      a: 0,
      why: "`takeLatest` держит только последнюю задачу: старый поиск отменяется, и его ответ не перезапишет новый.",
    },
  ],
};
