import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rdx2",
  region: 5,
  level: "middle",
  title: "react-redux: `useSelector` и подписки",
  q: "Как Redux подключается к React? Почему Redux вызывает меньше лишних ререндеров, чем Context?",
  answer:
    "Стор передают через `Provider` из react-redux, а компоненты читают данные хуком `useSelector` и отправляют действия через `useDispatch`. Каждый `useSelector` подписан на стор отдельно: после каждого действия он вызывает свой селектор и сравнивает результат с прошлым через `===`. Компонент перерисуется, только если изменилось выбранное им значение. Context так не умеет — он перерисовывает всех потребителей при любом изменении значения, поэтому Redux точнее.",
  theory: {
    p: [
      "react-redux — официальная связка Redux и React. `<Provider store={store}>` в корне кладёт стор в контекст. Но в контексте лежит сам стор, а его ссылка не меняется, поэтому изменения состояния расходятся не через контекст, а через подписки.",
      "`useSelector(selector)` подписывает компонент на стор. После каждого `dispatch` react-redux вызывает селекторы подписанных компонентов и сравнивает новый результат со старым через `===`. Совпало — ререндера нет. Если изменилась корзина, а компонент выбирает имя пользователя, он не перерисуется. В этом главное отличие от контекста.",
      "Ловушка — селектор, который каждый раз возвращает новый объект или массив. `useSelector(s => ({ name: s.user.name }))` или `s.todos.filter(…)` дают новую ссылку после любого действия, и компонент перерисовывается всегда. Решения: несколько `useSelector` с примитивами, `shallowEqual` вторым аргументом или мемоизированный селектор `createSelector` из библиотеки Reselect: он пересчитывает результат, только когда изменились входные данные.",
      "В режиме разработки react-redux предупреждает о таких селекторах: вызывает селектор дважды и ругается, если результаты разные, а отдельно — если селектор возвращает всё состояние целиком. `useDispatch` возвращает стабильную функцию `dispatch`. Действие можно отправить и вне React: стор — обычный объект.",
    ],
    code: `function CartBadge() {
  // Перерисуется, только если изменилось число товаров
  const count = useSelector((state: RootState) => state.cart.items.length);
  return <span>{count}</span>;
}

// Плохо: filter возвращает новый массив после любого действия
const done = useSelector((state: RootState) => state.todos.filter((t) => t.done));

// Хорошо: мемоизированный селектор пересчитывается, только когда изменились todos
const selectDone = createSelector(
  [(state: RootState) => state.todos],
  (todos) => todos.filter((t) => t.done),
);
const doneMemo = useSelector(selectDone);`,
    flow: {
      actors: ["dispatch(cart/added)", "Стор", "useSelector в CartBadge", "useSelector в UserName"],
      steps: [
        { from: 0, to: 1, label: "новое состояние: корзина изменилась" },
        { from: 1, to: 2, label: "селектор: items.length 2 → 3", note: "3 !== 2 — ререндер" },
        { from: 1, to: 3, label: "селектор: user.name 'Аня' → 'Аня'", note: "=== совпало — ререндера нет" },
        { from: 2, to: 2, label: "CartBadge перерисован", note: "UserName не тронут" },
      ],
    },
    keys: [
      "`Provider` кладёт стор в контекст, но данные доставляют подписки: `useSelector` и `useDispatch`.",
      "После каждого действия селектор вызывается заново, результат сравнивается через `===`. Не изменилось — ререндера нет.",
      "Селектор не должен каждый раз создавать новый объект: несколько селекторов, `shallowEqual` или `createSelector`.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Чем Context принципиально отличается от Redux по ререндерам?",
      opts: [
        "Context перерисовывает всех потребителей, а Redux — только тех, чей выбранный кусок изменился",
        "Redux перерисовывает всё приложение, а Context — ничего",
        "Разницы нет",
        "Context вообще не вызывает ререндеров",
      ],
      a: 0,
      why: "Context — механизм передачи без выборочной подписки. Redux — внешний стор, где каждый компонент подписан через свой селектор.",
    },
    {
      type: "quiz",
      code: `const user = useSelector((state: RootState) => ({
  name: state.user.name,
  avatar: state.user.avatar,
}));`,
      q: "Компонент перерисовывается после любого действия, даже когда пользователь не менялся. Почему?",
      opts: [
        "Селектор каждый раз создаёт новый объект, и `===` видит новое значение",
        "`useSelector` всегда перерисовывает компонент",
        "Нужно обернуть компонент в `Provider` ещё раз",
        "Объект пользователя слишком большой",
      ],
      a: 0,
      why: "Решения: два `useSelector` с примитивами, `shallowEqual` вторым аргументом или мемоизированный селектор через `createSelector`.",
    },
    {
      type: "sort",
      q: "`CartBadge` выбирает `state.cart.items.length`. Перерисуется ли он?",
      groups: ["Перерисуется", "Не перерисуется"],
      items: [
        ["добавили товар в корзину", 0],
        ["удалили товар из корзины", 0],
        ["пользователь сменил аватар", 1],
        ["в сторе переключили тему", 1],
        ["в корзине изменили цену товара, а количество то же", 1],
      ],
      why: "Селектор возвращает число. Пока оно то же, `===` совпадает, и ререндера нет, что бы ни менялось в остальном сторе.",
    },
  ],
};
