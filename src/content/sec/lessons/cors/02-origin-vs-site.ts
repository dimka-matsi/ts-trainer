import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "cors2",
  region: 1,
  level: "middle",
  title: "Same-origin и same-site",
  q: "Чем «тот же источник» (same-origin) отличается от «того же сайта» (same-site)?",
  answer:
    "Источник — это схема, хост и порт целиком, а сайт — только схема и регистрируемый домен. `https://app.shop.ru` и `https://api.shop.ru` — разные источники, но один сайт. Источник проверяют политика одного источника и CORS: читать ответы API фронтенду всё равно нужно через CORS. Сайт проверяют cookies с `SameSite`: если запрос на `api.shop.ru` отправлен с cookies (по умолчанию запросы на другой источник идут без них, об этом урок «Запросы с cookies»), они уйдут даже с `SameSite=Strict`.",
  theory: {
    p: [
      "Источник (origin): схема + хост + порт. Строгое понятие: `app.shop.ru` и `api.shop.ru` уже разные. Сайт (site): схема + регистрируемый домен (eTLD+1): часть имени перед публичным суффиксом вроде `.ru`, `.co.uk`, `github.io` и сам суффикс. Для сайта поддомены и порт не важны.",
      "Кто на что смотрит. Политика одного источника и CORS — на источник: можно ли странице прочитать ответ. `SameSite` у cookies — на сайт: прикладывать ли cookie к запросу. Изоляция процессов в браузере и некоторые заголовки защиты тоже опираются на сайт — о них в регионе «Заголовки защиты» и в «Браузере».",
      "Отсюда частая ситуация. Фронтенд на `https://app.shop.ru`, API на `https://api.shop.ru`. Для cookies это один сайт: если запрос отправлен с cookies (как это включить — урок «Запросы с cookies»), сессия с `SameSite=Lax` или даже `Strict` уйдёт на API без проблем. А для чтения ответов это разные источники: API обязан отвечать заголовками CORS, иначе фронтенд не увидит данные.",
      "И наоборот: `https://shop.ru` и `http://shop.ru` — разные сайты, потому что схема разная. Страница `anna.github.io` для `evil.github.io` — чужой сайт, потому что `github.io` — публичный суффикс, как зона `.ru`. Поэтому хостинги пользовательских страниц регистрируют свои домены в списке публичных суффиксов.",
    ],
    flow: {
      actors: ["Страница\nhttps://app.shop.ru", "Браузер", "API\nhttps://api.shop.ru"],
      steps: [
        { from: 0, to: 1, label: "запрос к api.shop.ru с cookies", note: "другой источник, но тот же сайт" },
        { from: 1, to: 2, label: "GET /me, Cookie: sid (SameSite=Strict)", note: "same-site: cookie приложена" },
        { from: 2, to: 1, label: "200 без Access-Control-Allow-Origin" },
        { from: 1, to: 0, label: "ошибка CORS: ответ скрыт", note: "cross-origin: читать нельзя без разрешения" },
      ],
    },
    keys: [
      "Источник — схема, хост, порт. Сайт — схема и регистрируемый домен.",
      "CORS и политика одного источника смотрят на источник, `SameSite` у cookies — на сайт.",
      "`app.shop.ru` и `api.shop.ru` — один сайт (`SameSite` пропустит cookies), но разные источники (нужен CORS).",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Страница на `https://app.shop.ru`. Разложи адреса.",
      groups: ["Тот же источник", "Другой источник, но тот же сайт", "Другой сайт"],
      items: [
        ["`https://app.shop.ru/orders`", 0],
        ["`https://api.shop.ru`", 1],
        ["`https://shop.ru`", 1],
        ["`http://app.shop.ru`", 2],
        ["`https://shop.com`", 2],
      ],
      why: "Путь на источник не влияет. Поддомены одного домена — один сайт. Смена схемы или домена даёт другой сайт.",
    },
    {
      type: "quiz",
      q: "Фронтенд на `app.shop.ru` получает ответ API с `api.shop.ru`, запрос отправлен с cookies, и сессионная cookie ушла, но в консоли ошибка CORS. Почему так бывает?",
      opts: [
        "Для cookies это один сайт, а для чтения ответа — разные источники, и API не прислал заголовки CORS",
        "Cookie с `SameSite` не уходят на поддомены",
        "Браузер ошибся: одновременно так быть не может",
        "API работает по HTTP/2",
      ],
      a: 0,
      why: "Две разные проверки на разных понятиях. `SameSite` пропустил cookie, потому что сайт тот же. CORS не пропустил ответ, потому что источник другой.",
    },
    {
      type: "quiz",
      q: "Почему `anna.github.io` и `evil.github.io` — разные сайты?",
      opts: [
        "`github.io` в списке публичных суффиксов, как зона `.ru`",
        "Страницы на GitHub всегда разные источники",
        "У них разные IP-адреса",
        "Это одинаковые сайты",
      ],
      a: 0,
      why: "Иначе любой пользователь `github.io` мог бы ставить cookies для всех остальных. Список публичных суффиксов защищает соседей друг от друга.",
    },
  ],
};
