import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "cors6",
  region: 1,
  title: "CORS не защищает сервер",
  q: "Защищает ли CORS от CSRF? Почему запрос из curl проходит без CORS? Как обходят CORS в разработке?",
  answer:
    "CORS — правило браузера, которое защищает пользователя: чужая страница не прочитает его данные. Сервер оно не защищает: curl, Postman и серверный код CORS не проверяют и получают ответы как есть. От CSRF CORS тоже не спасает, потому что простые запросы доходят до сервера и выполняются, скрывается только ответ. В разработке CORS обходят прокси dev-сервера: страница стучится на свой источник, а dev-сервер пересылает запрос в API.",
  theory: {
    p: [
      "CORS работает только в браузере. Это браузер смотрит на `Access-Control-Allow-Origin` и решает, отдать ли ответ странице. `curl https://api.shop.ru/users` получит ответ без всяких заголовков CORS, как и Postman или другой сервер. Поэтому CORS не заменяет проверку доступа: API обязан проверять токен или сессию в каждом запросе.",
      "Кого защищает CORS? Пользователя. У него в браузере cookies от многих сайтов, и страница `evil.com` не должна читать через них его данные. Злоумышленнику без сессии пользователя CORS не мешает никак — он просто сделает запрос не из браузера.",
      "CORS и CSRF — разные вещи. CSRF-атаке не нужно читать ответ, ей нужно, чтобы запрос выполнился. Простой `POST` формы уходит без preflight и выполняется, даже если потом браузер покажет ошибку CORS. От CSRF защищают `SameSite`, CSRF-токены и проверка заголовка `Origin` на сервере.",
      "Разработка: фронтенд на `http://localhost:5173`, API на `https://api.shop.ru`. Настраивать CORS для localhost на боевом API не хочется. Поэтому у dev-сервера включают прокси: страница делает запрос на `http://localhost:5173/api/...` — свой источник, CORS не нужен, — а dev-сервер пересылает его в настоящий API. Браузер CORS не видит, потому что с его точки зрения запрос к своему же источнику. На продакшене то же самое делает обратный прокси, если фронтенд и API раздаются с одного домена.",
    ],
    flow: {
      actors: ["Страница\nlocalhost:5173", "Dev-сервер\nlocalhost:5173", "API\napi.shop.ru"],
      steps: [
        { from: 0, to: 1, label: "GET /api/items", note: "тот же источник — CORS не участвует" },
        { from: 1, to: 2, label: "GET /items", note: "сервер к серверу, CORS не проверяется" },
        { from: 2, to: 1, label: "200 без заголовков CORS" },
        { from: 1, to: 0, label: "200", note: "браузеру нечего блокировать" },
      ],
    },
    keys: [
      "CORS проверяет только браузер. curl и серверы получают ответ без ограничений.",
      "CORS защищает данные пользователя, а не сервер, и не защищает от CSRF.",
      "В разработке CORS обходят прокси dev-сервера: для браузера запрос уходит на свой источник.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Разработчик говорит: «API закрыт, у нас CORS только для нашего фронтенда». Что не так?",
      opts: [
        "CORS проверяет браузер: curl или скрипт на сервере получат ответ без ограничений",
        "Всё верно, CORS блокирует чужие запросы на сервере",
        "CORS работает только для `GET`",
        "CORS надо включать на стороне браузера пользователя",
      ],
      a: 0,
      why: "CORS не мешает запросам вне браузера. Доступ к API закрывает проверка токена или сессии на сервере.",
    },
    {
      type: "sort",
      q: "Что от чего защищает?",
      groups: ["Чтение чужих данных через браузер", "Выполнение запроса от имени пользователя (CSRF)"],
      items: [
        ["политика одного источника и CORS", 0],
        ["`SameSite` у сессионной cookie", 1],
        ["CSRF-токен в форме", 1],
        ["проверка заголовка `Origin` на сервере", 1],
        ["`Access-Control-Allow-Origin` с точным источником", 0],
      ],
      why: "CORS решает, можно ли прочитать ответ. CSRF-атаке ответ не нужен, поэтому против неё нужны другие механизмы.",
    },
    {
      type: "quiz",
      q: "Почему прокси dev-сервера избавляет от ошибок CORS?",
      opts: [
        "Браузер отправляет запрос на свой источник, а до API его пересылает сервер, где CORS не проверяется",
        "Dev-сервер отключает CORS в браузере",
        "Dev-сервер подделывает заголовок Origin",
        "Прокси шифрует запрос",
      ],
      a: 0,
      why: "CORS — правило для страниц в браузере. Запрос сервера к серверу его не касается.",
    },
  ],
};
