import { json, msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

const JWT = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI1IiwiZXhwIjoxNzkwMDAwMDAwfQ.подпись";

export const lesson: WebLesson = {
  id: "ck5",
  region: 4,
  level: "middle",
  title: "Сессия или JWT и где хранить токен",
  q: "Чем сессия в cookie отличается от JWT? Где безопаснее хранить токен?",
  answer:
    "В сессии cookie хранит случайный идентификатор, а данные о пользователе лежат на сервере: выход или блокировка срабатывают мгновенно, но серверу нужно хранилище сессий. JWT — подписанный токен, в котором уже записано, кто пользователь и до какого времени токен действует. Сервер проверяет подпись без базы, но отозвать выданный токен до срока трудно. Хранить токен лучше в cookie с `HttpOnly`, `Secure` и `SameSite`, а не в localStorage, откуда его украдёт любой XSS.",
  theory: {
    p: [
      "Сессия: после входа сервер создаёт запись «сессия a8f3k2 → пользователь 5» в базе или Redis и ставит cookie `sid=a8f3k2`. На каждый запрос ищет запись. Выйти, выкинуть с других устройств, заблокировать — просто удалить запись. Минус — хранилище, общее для всех серверов приложения.",
      "JWT (JSON Web Token) — три части через точку: заголовок, данные и подпись, всё в base64url. В данных `sub` — кто, `exp` — когда токен истекает. Сервер проверяет подпись своим ключом и верит данным без похода в базу, это удобно, когда сервисов много. Но JWT подписан, а не зашифрован: данные прочитает любой. И выданный токен работает до `exp`, а чтобы отозвать его раньше, всё равно нужен чёрный список на сервере.",
      "Где хранить токен в браузере. localStorage доступен любому скрипту страницы: один XSS — и токен уехал к злоумышленнику, где работает до `exp`. Cookie с `HttpOnly` скрипт не прочитает. Взамен cookie сама уходит с запросами, и появляется риск CSRF — его закрывают `SameSite` и CSRF-токен.",
      "Практичная схема для одностраничного приложения (SPA): короткий токен доступа в памяти страницы, а не в localStorage, и долгий токен обновления в cookie `HttpOnly; Secure; SameSite=Strict` — о нём следующий урок. А если фронтенд и API на одном сайте, обычная сессия в cookie — самый простой и надёжный вариант.",
    ],
    requests: [
      {
        name: "me (сессия)",
        type: "fetch",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 45], ["Загрузка", 1]],
        request: msg("GET /api/me HTTP/1.1", [["Host", "shop.ru"], ["Cookie", "__Host-sid=a8f3k2"]]),
        response: msg("HTTP/1.1 200 OK", [], json("{\"id\":5,\"name\":\"Анна\"}")),
      },
      {
        name: "me (JWT)",
        type: "fetch",
        start: 60,
        timing: [["Очередь", 1], ["Ожидание ответа", 20], ["Загрузка", 1]],
        request: msg("GET /api/me HTTP/1.1", [["Host", "api.shop.ru"], ["Authorization", `Bearer ${JWT}`]]),
        response: msg("HTTP/1.1 200 OK", [], json("{\"id\":5,\"name\":\"Анна\"}")),
      },
    ],
    keys: [
      "Сессия: id в cookie, данные на сервере, отзыв мгновенный.",
      "JWT: подписанные, но открытые данные. Проверяется без базы, отзывается трудно.",
      "Токен в localStorage крадёт любой XSS. Надёжнее cookie с `HttpOnly`, `Secure` и `SameSite`.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь термин и смысл.",
      pairs: [
        ["сессия", "случайный id в cookie, данные на сервере"],
        ["JWT", "подписанные данные пользователя внутри токена"],
        ["`exp`", "время, когда токен истекает"],
        ["чёрный список", "способ отозвать JWT до срока"],
      ],
      why: "Сессия хранит состояние на сервере, JWT носит его с собой. За удобство JWT платят сложным отзывом.",
    },
    {
      type: "quiz",
      q: "Почему токен в localStorage опаснее, чем в cookie с `HttpOnly`?",
      opts: [
        "Любой внедрённый скрипт прочитает его и увезёт к себе",
        "localStorage очищается при каждой перезагрузке",
        "localStorage передаётся на сервер открытым текстом",
        "Браузер удаляет токены из localStorage через час",
      ],
      a: 0,
      why: "У скриптов страницы полный доступ к localStorage. `HttpOnly`-cookie браузер отправляет сам, но скриптам её не показывает.",
    },
    {
      type: "quiz",
      q: "Можно ли положить в JWT номер паспорта пользователя?",
      opts: [
        "Нет: JWT подписан, но не зашифрован, его данные прочитает любой",
        "Да: подпись защищает данные от чтения",
        "Да, если токен короткий",
        "Да, если использовать алгоритм HS256",
      ],
      a: 0,
      why: "Среднюю часть JWT можно раскодировать из base64url без всякого ключа. Подпись защищает только от изменения.",
    },
  ],
};
