import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ck6",
  region: 4,
  title: "Access- и refresh-токены",
  q: "Зачем нужны два токена — access и refresh — и как обновить короткий токен?",
  answer:
    "Access-токен короткий, 5–15 минут, и уходит с каждым запросом к API: если его украдут, вред ограничен временем. Refresh-токен живёт дни, хранится в cookie с `HttpOnly` и отправляется только на адрес обновления, где его меняют на новый access. При каждом обновлении выдают и новый refresh, а старый отзывают. Если кто-то предъявит старый, сервер поймёт, что токен украли, и завершит все сессии этой цепочки.",
  theory: {
    p: [
      "Противоречие: токен хочется сделать долгим, чтобы не входить заново, и коротким, чтобы кража стоила мало. Решение — два токена. Access живёт 5–15 минут и уходит с каждым запросом к API в `Authorization: Bearer …`. Refresh живёт дни и нужен только для получения нового access.",
      "Когда access истёк, API отвечает 401. Клиент отправляет `POST /auth/refresh`, браузер сам прикладывает cookie с refresh-токеном. Сервер проверяет его и выдаёт новый access, а клиент повторяет исходный запрос. Пользователь ничего не заметил.",
      "Refresh — самое ценное, поэтому его прячут: `HttpOnly; Secure; SameSite=Strict; Path=/auth/refresh`. `Path` ограничивает отправку одним адресом: с обычными запросами к API refresh не ездит и не попадает в их логи.",
      "Ротация: каждый обмен выдаёт новый refresh, а старый становится недействительным. Если старый refresh предъявили ещё раз, значит, его украли: либо это вор, либо настоящий пользователь уже обменял токен до него. Сервер отзывает всю цепочку, и обоим придётся войти заново. Так даже долгий токен теряет ценность для вора.",
    ],
    flow: {
      actors: ["Браузер", "API shop.ru"],
      steps: [
        { from: 0, to: 1, label: "GET /api/orders, Bearer access (истёк)" },
        { from: 1, to: 0, label: "401 Unauthorized" },
        { from: 0, to: 1, label: "POST /auth/refresh, Cookie: rt=R1", note: "cookie уходит только на этот путь" },
        { from: 1, to: 0, label: "новый access + Set-Cookie: rt=R2", note: "R1 больше не действует" },
        { from: 0, to: 1, label: "GET /api/orders, Bearer новый access", note: "повтор исходного запроса" },
        { from: 1, to: 0, label: "200 OK" },
      ],
    },
    keys: [
      "Access короткий и ходит везде, refresh долгий и ходит только на `/auth/refresh`.",
      "Refresh хранят в cookie `HttpOnly; Secure; SameSite=Strict` с узким `Path`.",
      "Ротация: новый refresh на каждый обмен, повтор старого — сигнал кражи.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Access-токен истёк. Расставь шаги обновления.",
      items: [
        "запрос к API со старым access",
        "ответ 401",
        "`POST /auth/refresh` с cookie refresh",
        "новый access и новый refresh",
        "повтор исходного запроса с новым access",
      ],
      why: "Клиент реагирует на 401 обменом refresh и тихо повторяет запрос. Пользователю не нужно входить заново.",
    },
    {
      type: "quiz",
      q: "Злоумышленник украл refresh R1, но пользователь уже обменял его на R2. Что сделает сервер с ротацией, когда снова увидит R1?",
      opts: [
        "Поймёт, что токен украден, и отзовёт всю цепочку токенов",
        "Выдаст злоумышленнику новый access",
        "Вернёт R2 злоумышленнику",
        "Ничего: старые refresh просто игнорируются",
      ],
      a: 0,
      why: "Повторное использование уже обменянного токена бывает только при краже. Отзыв цепочки выкидывает и вора, и пользователя, а пользователь просто войдёт заново.",
    },
    {
      type: "quiz",
      q: "Зачем cookie с refresh-токеном атрибут `Path=/auth/refresh`?",
      opts: [
        "Чтобы она уходила только на адрес обновления, а не с каждым запросом",
        "Чтобы её мог читать скрипт на странице `/auth/refresh`",
        "Без `Path` cookie не сохраняется",
        "Чтобы токен шифровался",
      ],
      a: 0,
      why: "Чем реже токен путешествует, тем меньше мест, где он может утечь: логи, промежуточные серверы, ошибки в коде.",
    },
  ],
};
