import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "sec2",
  region: 2,
  title: "Защита от XSS",
  q: "Как защищаются от XSS?",
  answer:
    "Главное — экранировать данные при выводе с учётом места: в тексте HTML `<` превращается в `&lt;`, в атрибутах экранируют кавычки, в ссылках проверяют схему, чтобы не пропустить `javascript:`. Фреймворки вроде React экранируют текст сами, опасны только обходы вроде вставки сырого HTML. Если HTML от пользователя нужен, его чистят санитайзером по белому списку тегов. Вторая линия обороны — Content Security Policy, которая не даёт выполнить чужой скрипт, и `HttpOnly`, чтобы украсть сессию было нельзя.",
  theory: {
    p: [
      "Главное правило: данные — это текст, пока ты явно не решил иначе. При выводе в HTML спецсимволы экранируют: `<` → `&lt;`, `>` → `&gt;`, `&` → `&amp;`, кавычки → `&quot;`. Тогда `<script>` из комментария браузер покажет как текст, а не выполнит. Экранировать нужно при выводе, а не при сохранении: одни и те же данные попадают в HTML, JSON, письма, и у каждого места свои правила.",
      "Контекст важен. В тексте хватает экранирования `<` и `&`. В атрибуте нужны кавычки вокруг значения и экранирование кавычек. В `href` проверяют схему: ссылка `javascript:…` выполнит код при клике, даже если в ней нет ни одного `<`. Внутрь `<script>` данные вставлять вообще не стоит.",
      "Шаблонизаторы и фреймворки (React, Vue, Angular) экранируют текст автоматически. Уязвимости появляются там, где это обходят: вставка сырого HTML (`dangerouslySetInnerHTML`, `v-html`, `innerHTML`) и пользовательские ссылки в `href`. Если без HTML не обойтись — например, редактор статей, — его прогоняют через санитайзер вроде DOMPurify: он оставляет только разрешённые теги и атрибуты.",
      "Линии обороны на случай, если XSS всё же пролез. Content Security Policy запрещает выполнять скрипты не из разрешённых источников — о ней отдельный урок. `HttpOnly` не даёт увезти сессионную cookie. Проверка ввода (например, что поле «возраст» — число) полезна, но не заменяет экранирование при выводе.",
    ],
    requests: [
      {
        name: "comments",
        type: "fetch",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 70], ["Загрузка", 1]],
        request: msg("POST /api/comments HTTP/2", [[":authority", "shop.ru"]], { type: "text/plain; charset=utf-8", text: "<script src=\"https://evil.com/x.js\"></script>" }),
        response: msg("HTTP/2 201", [["location", "/api/comments/19"]]),
      },
      {
        name: "product/7",
        type: "document",
        start: 90,
        timing: [["Очередь", 1], ["Ожидание ответа", 60], ["Загрузка", 4]],
        request: msg("GET /product/7 HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [], {
          type: "text/html; charset=utf-8",
          text: "<p class=\"comment\">&lt;script src=&quot;https://evil.com/x.js&quot;&gt;&lt;/script&gt;</p>",
        }),
      },
    ],
    keys: [
      "Экранируй при выводе с учётом контекста: текст, атрибут, ссылка.",
      "Фреймворки экранируют сами. Опасны вставка сырого HTML и `javascript:` в ссылках.",
      "Нужен HTML от пользователя — санитайзер. Страховка — CSP и `HttpOnly`.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что защищает от XSS, а что нет?",
      groups: ["Защищает или ограничивает ущерб", "Не защищает"],
      items: [
        ["экранирование при выводе в HTML", 0],
        ["санитайзер для пользовательского HTML", 0],
        ["Content Security Policy", 0],
        ["HTTPS", 1],
        ["`POST` вместо `GET` для формы комментария", 1],
        ["CORS на API", 1],
      ],
      why: "HTTPS защищает канал, CORS — чтение с других источников. XSS живёт внутри твоего же сайта, поэтому против него работают только экранирование, санитайзер и CSP.",
    },
    {
      type: "quiz",
      q: "Комментарий экранирован, но пользователь может указать ссылку на свой сайт, и она выводится в `href`. Что ещё нужно проверить?",
      opts: [
        "Схему ссылки: разрешить только `https:` и `http:`, чтобы не пропустить `javascript:`",
        "Длину ссылки",
        "Что сайт открывается по HTTPS",
        "Ничего, экранирования достаточно",
      ],
      a: 0,
      why: "`javascript:alert(1)` не содержит ни `<`, ни кавычек, экранирование его не тронет. Выполнится он при клике.",
    },
    {
      type: "quiz",
      q: "Когда экранировать данные: при сохранении в базу или при выводе?",
      opts: [
        "При выводе, по правилам того места, куда они попадают",
        "При сохранении, один раз",
        "Дважды, при сохранении и выводе",
        "Не нужно, если есть HTTPS",
      ],
      a: 0,
      why: "Одни данные идут в HTML, JSON, письма и отчёты, и правила везде разные. Экранирование при сохранении испортит данные для других мест и даст двойное экранирование.",
    },
  ],
};
