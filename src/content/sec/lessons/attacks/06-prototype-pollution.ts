import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "sec10",
  region: 2,
  level: "senior",
  title: "Prototype pollution",
  q: "Что такое prototype pollution и чем она опасна?",
  answer:
    "Prototype pollution — загрязнение прототипа: злоумышленник присылает JSON с ключом `__proto__`, а наивная функция глубокого слияния копирует его в объект и тем самым записывает свойства в `Object.prototype`. После этого у всех объектов программы появляется, например, `isAdmin: true`. Итог — обход проверок, падения, а на сервере Node.js иногда и выполнение кода. Защита: при слиянии пропускать ключи `__proto__`, `constructor` и `prototype`, проверять данные по схеме, для словарей брать `Map` или объекты без прототипа.",
  theory: {
    p: [
      "В JavaScript у объектов есть общий прототип `Object.prototype`: если свойства нет в самом объекте, его ищут там. Если записать что-то в `Object.prototype`, это свойство увидят все обычные объекты программы. Запись `obj.__proto__.x = 1` делает именно это: `__proto__` — ссылка на прототип.",
      "Как это случается. `JSON.parse` превращает `{\"__proto__\": {\"isAdmin\": true}}` в обычный объект с собственным ключом `__proto__` — пока безопасно. Опасность в следующем шаге: функция глубокого слияния настроек или объединения объектов проходит по ключам и присваивает `target[key]` рекурсивно. На ключе `__proto__` присваивание уходит в прототип, и `isAdmin` оказывается у всех объектов.",
      "Последствия. Проверка вида «если у пользователя есть `isAdmin`» пропускает всех. Подменённые значения по умолчанию меняют поведение библиотек. На сервере Node.js загрязнение опций может довести до выполнения кода. Во фронтенде загрязнение из параметров адреса ведёт к XSS через библиотеки, которые читают настройки из объектов. Уязвимыми бывали известные пакеты глубокого слияния, включая старые версии `lodash.merge`.",
      "Защита. В своих функциях слияния пропускать ключи `__proto__`, `constructor` и `prototype`. Проверять входящие данные по схеме и копировать только ожидаемые поля. Для словарей с произвольными ключами — `Map` или объект без прототипа, созданный через `Object.create(null)`. Обновлять зависимости, в которых находили эту уязвимость. В Node.js есть флаг, отключающий `__proto__`.",
    ],
    flow: {
      actors: ["Злоумышленник", "Сервер", "Функция слияния", "Object.prototype"],
      steps: [
        { from: 0, to: 1, label: "настройки: {\"__proto__\": {\"isAdmin\": true}}" },
        { from: 1, to: 2, label: "merge(defaults, userSettings)" },
        { from: 2, to: 3, label: "target[\"__proto__\"].isAdmin = true", note: "запись ушла в общий прототип" },
        { from: 3, to: 1, label: "теперь у любого объекта user.isAdmin — true" },
      ],
    },
    keys: [
      "Запись в `Object.prototype` видна всем объектам. Ключ `__proto__` при наивном слиянии пишет именно туда.",
      "Последствия: обход проверок прав, изменение поведения библиотек, XSS во фронтенде и выполнение кода на сервере.",
      "Защита: пропускать `__proto__`, `constructor`, `prototype`, проверять по схеме, `Map` и `Object.create(null)` для словарей.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь шаги атаки prototype pollution.",
      items: [
        "злоумышленник присылает JSON с ключом `__proto__`",
        "сервер разбирает JSON — ключ пока просто собственное свойство",
        "функция глубокого слияния копирует его и пишет в `Object.prototype`",
        "проверка прав у любого объекта видит подложенное свойство",
      ],
      why: "Сам `JSON.parse` безопасен. Опасна запись по ключу `__proto__` в функции, которая сливает объекты.",
    },
    {
      type: "quiz",
      q: "Какая мера защищает функцию слияния от prototype pollution?",
      opts: [
        "Пропускать ключи `__proto__`, `constructor` и `prototype`",
        "Использовать HTTPS",
        "Проверять JSON на фронтенде",
        "Хранить настройки в cookie",
      ],
      a: 0,
      why: "Именно через эти ключи присваивание добирается до прототипа. Ещё надёжнее — копировать только поля из схемы.",
    },
    {
      type: "quiz",
      q: "Почему для словаря с ключами от пользователя лучше `Map`, а не обычный объект?",
      opts: [
        "У `Map` ключи не пересекаются с прототипом: ключ `__proto__` — просто строка",
        "`Map` быстрее шифрует данные",
        "`Map` нельзя передать по сети",
        "Обычный объект не умеет хранить строки",
      ],
      a: 0,
      why: "У обычного объекта есть унаследованные свойства и особый ключ `__proto__`. `Map` и `Object.create(null)` этого лишены.",
    },
  ],
};
