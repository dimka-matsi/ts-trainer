import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "sec11",
  region: 2,
  level: "senior",
  title: "postMessage и iframe sandbox",
  q: "Как безопасно общаться с iframe и другими окнами через `postMessage`? Зачем атрибут `sandbox`?",
  answer:
    "`postMessage` позволяет окнам разных источников обмениваться сообщениями в обход политики одного источника. Опасность — в обе стороны. Получатель обязан проверять `event.origin` по списку разрешённых источников и формат данных, иначе любое окно пришлёт ему команду. Отправитель должен указывать точный адрес получателя вторым аргументом, а не `\"*\"`, иначе данные прочитает подменённое окно. Атрибут `sandbox` у iframe запрещает встроенной странице скрипты, формы, всплывающие окна и переход верхнего окна; нужное включают по одному через `allow-…`.",
  theory: {
    p: [
      "Политика одного источника не даёт окну читать другое окно чужого источника. Для общения — виджет оплаты в iframe, окно входа на другом сайте, встроенный чат — есть `postMessage`: отправитель вызывает `targetWindow.postMessage(data, targetOrigin)`, получатель обрабатывает событие `message`. Данные копируются структурным клонированием.",
      "Сторона получателя. Событие `message` может прийти от любого окна, которое держит ссылку на твоё: от открывшей вкладки, от iframe рекламы, от сайта злоумышленника, открывшего тебя в `window.open`. Поэтому первым делом сравнивают `event.origin` со списком разрешённых точным совпадением, а не `includes` или `endsWith` (`evil-shop.ru` заканчивается на `shop.ru`). Затем проверяют формат данных и никогда не вставляют их в HTML.",
      "Сторона отправителя. Второй аргумент `targetOrigin` — адрес, которому можно доставить сообщение. `\"*\"` значит «кому угодно»: если в iframe или окне к этому моменту открыт другой сайт (например, после перенаправления), он получит токен или личные данные. Указывай точный источник: `\"https://pay.example.com\"`.",
      "`sandbox` у `<iframe>` включает строгий режим: встроенная страница получает уникальный непрозрачный источник, не может запускать скрипты, отправлять формы, открывать окна и менять адрес вкладки. Разрешения добавляют по одному: `allow-scripts`, `allow-forms`, `allow-popups`. Опасная пара — `allow-scripts` вместе с `allow-same-origin` для контента с твоего же источника: скрипт внутри просто снимет атрибут `sandbox`. Для чужого пользовательского HTML — `sandbox` и отдельный домен.",
    ],
    flow: {
      actors: ["shop.ru", "iframe pay.example.com", "evil.ru"],
      steps: [
        { from: 0, to: 1, label: "postMessage(заказ, \"https://pay.example.com\")" },
        { from: 1, to: 0, label: "message: {status: \"paid\"}", note: "shop.ru проверил event.origin === pay.example.com" },
        { from: 2, to: 0, label: "подделка: message {status: \"paid\"}", lost: true },
        { from: 0, to: 0, label: "origin evil.ru не в списке — сообщение отброшено" },
      ],
    },
    keys: [
      "Получатель проверяет `event.origin` точным совпадением со списком и формат данных.",
      "Отправитель указывает точный `targetOrigin`, а не `\"*\"`.",
      "`sandbox` запрещает встроенной странице скрипты, формы и навигацию; `allow-scripts` с `allow-same-origin` для своего источника сводят защиту на нет.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Какая проверка `event.origin` в обработчике сообщений правильная для доверенного `https://pay.example.com`?",
      opts: [
        "Точное сравнение со строкой `https://pay.example.com` или со списком таких строк",
        "`origin` содержит `example.com`",
        "`origin` заканчивается на `example.com`",
        "Проверка не нужна, если сообщение пришло из iframe",
      ],
      a: 0,
      why: "Частичные проверки обходятся доменами вроде `example.com.evil.ru` или `notexample.com`. Только точное совпадение надёжно.",
    },
    {
      type: "quiz",
      q: "Чем опасен вызов с `targetOrigin` равным `\"*\"` для сообщения с токеном?",
      opts: [
        "Если окно-получатель к этому моменту показывает другой сайт, токен получит он",
        "Сообщение не дойдёт ни до кого",
        "Браузер запрещает `\"*\"`",
        "Токен будет доставлен дважды",
      ],
      a: 0,
      why: "Отправитель не знает наверняка, что сейчас открыто в чужом окне. `targetOrigin` — гарантия, что данные получит только нужный источник.",
    },
    {
      type: "match",
      q: "Сопоставь значение `sandbox` и что оно разрешает.",
      pairs: [
        ["пустой `sandbox`", "ничего: ни скриптов, ни форм, ни навигации"],
        ["`allow-scripts`", "выполнять скрипты"],
        ["`allow-forms`", "отправлять формы"],
        ["`allow-same-origin`", "сохранить настоящий источник страницы"],
      ],
      why: "Каждое разрешение добавляют осознанно. Без `allow-same-origin` у страницы уникальный непрозрачный источник: нет доступа к cookies и хранилищам сайта.",
    },
  ],
};
