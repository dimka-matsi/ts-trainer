import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "ck3",
  region: 0,
  level: "middle",
  title: "SameSite и CSRF",
  q: "Чем отличаются `SameSite=Strict`, `Lax` и `None`? Что такое CSRF?",
  answer:
    "CSRF — атака, когда чужой сайт заставляет браузер отправить запрос на твой сайт, и браузер сам прикладывает твои cookie. `SameSite` решает, отправлять ли cookie с запросами с других сайтов: `Strict` — никогда, `Lax` — только при переходе по ссылке методом GET, `None` — всегда, и только вместе с `Secure`. Chrome считает cookie без атрибута как `Lax`, но указывать его стоит явно.",
  theory: {
    p: [
      "CSRF (cross-site request forgery). Ты вошёл в `bank.ru`, а потом открыл `evil.com`. На нём спрятана форма, которая сама отправляет `POST https://bank.ru/transfer`. По старым правилам браузер прикладывает к запросу cookie `bank.ru`, и банк видит обычный запрос от тебя. Злоумышленник не видит ответ, но ему и не надо: перевод уже ушёл.",
      "Сайт (site) — это схема плюс регистрируемый домен, например `https://shop.ru`. `api.shop.ru` и `www.shop.ru` — один сайт, `evil.com` — другой, `http://shop.ru` и `https://shop.ru` — тоже разные. Для доменов вроде `github.io` действует список публичных суффиксов: `anna.github.io` и `evil.github.io` — разные сайты. Запрос межсайтовый, если сайт страницы и сайт адресата не совпадают.",
      "`Strict`: cookie не уходит ни с одним межсайтовым запросом, даже при переходе по ссылке — откроешь `bank.ru` из письма и увидишь, что ты «не вошёл». `Lax`: cookie уходит при переходе верхнего уровня (ссылка, адресная строка) безопасным методом `GET`, но не с формами `POST`, картинками, iframe и фоновыми запросами с чужих сайтов. `None`: всегда, как раньше, и только вместе с `Secure`.",
      "Chrome и Edge считают cookie без `SameSite` как `Lax`. Firefox и Safari так не делают, поэтому атрибут пишут явно. `Lax` закрывает большинство CSRF-атак, но не все: например, если сайт меняет данные по `GET` или атакующий захватил поддомен — для `SameSite` это тот же сайт. Поэтому для важных действий добавляют CSRF-токен, о нём — в регионе «Атаки».",
    ],
    flow: {
      actors: ["Браузер", "evil.com", "bank.ru"],
      steps: [
        { from: 0, to: 1, label: "открыл evil.com" },
        { from: 1, to: 0, label: "страница со скрытой формой POST на bank.ru/transfer", note: "форма отправится сама" },
        { from: 0, to: 2, label: "POST /transfer, без cookie sid", note: "sid стоит с SameSite=Lax: межсайтовый POST едет без неё" },
        { from: 2, to: 0, label: "401: вы не вошли", note: "перевод не прошёл" },
        { from: 0, to: 2, label: "клик по ссылке на bank.ru из письма: GET /, Cookie: sid", note: "Lax пропускает переход по ссылке" },
      ],
    },
    keys: [
      "CSRF: чужой сайт отправляет запрос от твоего имени, а cookie браузер прикладывает сам.",
      "`Strict` — никогда с чужих сайтов, `Lax` — только переход по ссылке методом GET, `None` — всегда и только с `Secure`.",
      "Сайт — схема и регистрируемый домен: поддомены одного домена — один сайт.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Cookie `sid` стоит с `SameSite=Lax`. С каким запросом она уйдёт на `shop.ru`?",
      opts: [
        "Переход по ссылке на `shop.ru` из письма в веб-почте",
        "Форма `POST` на `shop.ru` со страницы `evil.com`",
        "Картинка с `shop.ru` на странице `evil.com`",
        "iframe с `shop.ru` внутри `evil.com`",
      ],
      a: 0,
      why: "`Lax` пропускает только навигацию верхнего уровня безопасным методом. Формы `POST`, картинки и iframe с чужих сайтов уходят без cookie.",
    },
    {
      type: "match",
      q: "Сопоставь значение `SameSite` и поведение.",
      pairs: [
        ["`Strict`", "ни с одним межсайтовым запросом"],
        ["`Lax`", "только при переходе по ссылке методом GET"],
        ["`None`", "со всеми запросами, нужен `Secure`"],
      ],
      why: "Чем строже значение, тем меньше шансов у CSRF, но `Strict` ломает вход при переходе по ссылке с другого сайта. `Lax` — разумное значение по умолчанию.",
    },
    {
      type: "quiz",
      q: "Какие два адреса относятся к одному сайту?",
      opts: [
        "`https://api.shop.ru` и `https://www.shop.ru`",
        "`https://shop.ru` и `http://shop.ru`",
        "`https://anna.github.io` и `https://evil.github.io`",
        "`https://shop.ru` и `https://shop.com`",
      ],
      a: 0,
      why: "У первой пары одна схема и один регистрируемый домен `shop.ru`. Разные схемы — разные сайты, а `github.io` — публичный суффикс, как `.ru`.",
    },
  ],
};
