import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "up1",
  region: 5,
  level: "middle",
  title: "Загрузка файлов",
  q: "Какие риски у загрузки файлов пользователями и как их закрыть?",
  answer:
    "Файл от пользователя — такие же недоверенные данные. Расширение и `Content-Type` из запроса подделываются, поэтому тип проверяют по содержимому и сверяют со списком разрешённых. Размер ограничивают на сервере, отвечая `413 Content Too Large`. Главный риск для фронтенда — XSS через файл: HTML или SVG со скриптом, открытый с домена сайта, выполнится с его правами. Поэтому файлы отдают с отдельного домена, с `X-Content-Type-Options: nosniff` и `Content-Disposition: attachment` для всего, что не картинка. Картинки лучше пересохранить — это заодно удалит метаданные вроде координат съёмки.",
  theory: {
    p: [
      "Всё в запросе загрузки контролирует пользователь: имя файла, расширение, заголовок `Content-Type`, содержимое. Файл `avatar.png` может оказаться HTML-страницей. Проверяют по содержимому — по первым байтам («магическим числам») или, надёжнее, пытаются открыть файл как картинку и пересохранить. Разрешённые типы — белым списком.",
      "XSS через файл. SVG — это XML, в нём можно написать `<script>`. HTML-файл — тем более. Если такой файл отдаётся с `shop.ru` и открывается в браузере, скрипт выполнится с правами сайта и получит доступ к его cookies и API. Защита: отдавать пользовательские файлы с отдельного домена (`shop-usercontent.ru`), ставить `X-Content-Type-Options: nosniff`, чтобы браузер не угадывал тип, и `Content-Disposition: attachment` для документов, чтобы они скачивались, а не открывались. CSP для этого домена — запрет скриптов.",
      "Сервер и хранилище. Имя файла от пользователя не используют как путь: `../../etc/passwd` уведёт за пределы папки. Генерируют своё имя, хранят вне корня веб-сервера или в объектном хранилище. Ограничивают размер и количество — отвечая `413`, и проверяют архивы на «zip-бомбы», которые распаковываются в гигабайты.",
      "Приватность. Фото с телефона содержит EXIF: модель устройства, время и часто координаты съёмки. Пересохранение изображения на сервере удаляет метаданные и заодно обезвреживает файлы, притворяющиеся картинками. Для приватных файлов — временные подписанные ссылки вместо постоянных публичных адресов.",
    ],
    requests: [
      {
        name: "upload",
        type: "fetch",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 30], ["Загрузка", 1]],
        request: msg("POST /api/avatar HTTP/1.1", [["Host", "shop.ru"], ["Content-Type", "multipart/form-data; boundary=x"], ["Content-Length", "52428800"]], undefined),
        response: msg("HTTP/1.1 413 Content Too Large", [["Connection", "close"]]),
      },
      {
        name: "photo.svg",
        type: "document",
        start: 50,
        timing: [["Очередь", 1], ["DNS", 10], ["TCP", 15], ["TLS", 18], ["Ожидание ответа", 20], ["Загрузка", 3]],
        request: msg("GET /u/7f3a/photo.svg HTTP/2", [[":authority", "shop-usercontent.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "image/svg+xml"], ["x-content-type-options", "nosniff"], ["content-disposition", "attachment"], ["content-security-policy", "default-src 'none'; style-src 'unsafe-inline'; sandbox"]]),
      },
    ],
    keys: [
      "Имя, расширение и `Content-Type` подделываются. Тип проверяют по содержимому, картинки пересохраняют.",
      "SVG и HTML со скриптом дают XSS. Файлы отдают с отдельного домена с `nosniff`, `attachment` и строгой CSP.",
      "Своё имя файла вместо пользовательского, лимит размера (413), удаление EXIF, подписанные ссылки для приватного.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Почему загруженные пользователями файлы отдают с отдельного домена, а не с `shop.ru`?",
      opts: [
        "Если файл окажется HTML или SVG со скриптом, он выполнится на чужом домене без доступа к cookies и API сайта",
        "Так быстрее работает CDN",
        "Браузеры запрещают картинки с основного домена",
        "Чтобы файлы нельзя было скачать",
      ],
      a: 0,
      why: "Политика одного источника изолирует отдельный домен от основного. Скрипт внутри файла ничего не сможет сделать от имени пользователя сайта.",
    },
    {
      type: "sort",
      q: "Чему можно доверять при проверке загруженного файла?",
      groups: ["можно проверять надёжно", "подделывается"],
      items: [
        ["результат открытия файла библиотекой картинок", 0],
        ["размер, который посчитал сервер", 0],
        ["первые байты содержимого", 0],
        ["расширение в имени файла", 1],
        ["заголовок `Content-Type` из запроса", 1],
        ["имя файла", 1],
      ],
      why: "Всё, что прислал клиент как описание файла, — просто текст запроса. Надёжно только то, что сервер посчитал или проверил сам.",
    },
    {
      type: "quiz",
      q: "Пользователь загрузил фото с телефона, и оно стало публичным. Какая скрытая утечка возможна?",
      opts: [
        "Метаданные EXIF с координатами съёмки и моделью телефона",
        "Пароль от аккаунта",
        "Cookies пользователя",
        "История браузера",
      ],
      a: 0,
      why: "Телефоны записывают геопозицию в фото. Пересохранение изображения на сервере удаляет метаданные.",
    },
  ],
};
