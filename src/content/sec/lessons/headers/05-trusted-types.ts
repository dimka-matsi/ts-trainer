import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "sec12",
  region: 3,
  level: "senior",
  title: "Trusted Types",
  q: "Что такое Trusted Types и как они защищают от DOM XSS?",
  answer:
    "DOM XSS возникает, когда код страницы кладёт строку от пользователя в опасное место — `innerHTML`, `eval`, адрес скрипта. Trusted Types включаются директивой CSP `require-trusted-types-for 'script'`: после этого браузер запрещает передавать в такие места обычные строки и принимает только специальные объекты, созданные политикой. Политика — одна функция, которая чистит HTML или проверяет адрес, и её легко проверить на ревью. Директива `trusted-types` ограничивает, какие политики можно создать. С февраля 2026 года Trusted Types работают во всех основных браузерах.",
  theory: {
    p: [
      "DOM XSS не проходит через сервер: код страницы сам берёт данные из адреса, `postMessage` или ответа API и вставляет их в опасное место — sink. Опасные места: `innerHTML` и `outerHTML`, `insertAdjacentHTML`, запись HTML в документ через `write`, `eval` и строковый `setTimeout`, `src` у скрипта. В большом приложении таких мест сотни, и проверить все на ревью трудно.",
      "Trusted Types переворачивают подход. Заголовок `Content-Security-Policy: require-trusted-types-for 'script'` говорит браузеру: в опасные места больше нельзя передавать строки, только объекты `TrustedHTML`, `TrustedScript` и `TrustedScriptURL`. Строка — ошибка `TypeError`, и она видна сразу. Создать такие объекты может только политика, заведённая через `trustedTypes.createPolicy(имя, правила)`: в правилах HTML чистят, например, DOMPurify.",
      "Директива `trusted-types имя1 имя2` разрешает создавать только перечисленные политики — это не даёт стороннему коду завести свою «пропускающую всё». В итоге проверять на безопасность нужно несколько политик, а не сотни мест в коде. Можно начать с режима отчёта `Content-Security-Policy-Report-Only`, чтобы найти нарушения, не ломая сайт.",
      "Поддержка: Chrome и Edge — с 2020 года, Safari — с версии 26, Firefox — с версии 148 в феврале 2026 года, так что Trusted Types стали Baseline. React и Angular совместимы с ними: React не пишет строки в опасные места сам, кроме `dangerouslySetInnerHTML`, для которого и нужна политика.",
    ],
    requests: [
      {
        name: "shop.ru",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 60], ["Загрузка", 8]],
        request: msg("GET / HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "text/html; charset=utf-8"], ["content-security-policy", "require-trusted-types-for 'script'; trusted-types sanitize-html default"], ["content-security-policy-report-only", "require-trusted-types-for 'script'; report-to csp"]]),
      },
    ],
    keys: [
      "DOM XSS — строка от пользователя в опасном месте: `innerHTML`, `eval`, `src` скрипта.",
      "`require-trusted-types-for 'script'` запрещает строки в опасных местах, нужны объекты от политики.",
      "`trusted-types` ограничивает список политик. Проверять нужно несколько политик, а не сотни мест. Поддержка есть во всех основных браузерах.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Что произойдёт на странице с `require-trusted-types-for 'script'`, если код запишет строку в `innerHTML`?",
      opts: [
        "Браузер бросит `TypeError`: нужен объект `TrustedHTML` от политики",
        "Строка вставится, но скрипты в ней не выполнятся",
        "Браузер сам очистит строку",
        "Страница перезагрузится",
      ],
      a: 0,
      why: "Trusted Types не чистят данные сами — они требуют, чтобы через опасное место проходили только объекты, созданные проверенной политикой.",
    },
    {
      type: "quiz",
      q: "Зачем директива `trusted-types` со списком имён политик?",
      opts: [
        "Чтобы сторонний код не завёл свою политику, которая пропускает всё",
        "Чтобы ускорить создание политик",
        "Чтобы включить политику в старых браузерах",
        "Чтобы разрешить `eval`",
      ],
      a: 0,
      why: "Без ограничения любой скрипт мог бы создать политику-заглушку и обойти защиту.",
    },
    {
      type: "match",
      q: "Сопоставь понятие и смысл.",
      pairs: [
        ["опасное место (sink)", "`innerHTML`, `eval`, `src` скрипта"],
        ["политика", "функция, которая чистит строку и создаёт доверенный объект"],
        ["`TrustedHTML`", "объект, который можно положить в `innerHTML`"],
        ["режим Report-Only", "найти нарушения, не ломая сайт"],
      ],
      why: "Защита держится на том, что опасные места принимают только результат политики, а политик мало и они на виду.",
    },
  ],
};
