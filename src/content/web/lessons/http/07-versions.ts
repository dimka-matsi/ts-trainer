import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "http7",
  region: 2,
  title: "HTTP/1.1, HTTP/2 и HTTP/3",
  q: "Что изменилось в HTTP/2 и HTTP/3 и зачем?",
  answer:
    "HTTP/2 передаёт сообщения двоичными кадрами и мультиплексирует много запросов в одном TCP-соединении, а ещё сжимает заголовки. Но под ним TCP, и потеря одного пакета тормозит все потоки. HTTP/3 работает на QUIC поверх UDP: потоки независимы, соединение открывается быстрее и не рвётся при смене сети, например при переходе с Wi-Fi на мобильный интернет.",
  theory: {
    p: [
      "HTTP/1.1: текстовые сообщения, один запрос за раз на соединение, до шести соединений на хост, заголовки повторяются в каждом запросе целиком. Страница с сотней файлов подолгу стоит в очередях.",
      "HTTP/2 (2015) режет сообщения на двоичные кадры, у каждого кадра номер потока. Кадры разных запросов перемешиваются в одном TCP-соединении — это мультиплексирование: сто запросов идут одновременно. Заголовки сжимаются алгоритмом HPACK: повторы передаются короткими ссылками. В браузерах HTTP/2 работает только поверх TLS. Спрайты и раскладка файлов по поддоменам стали не нужны.",
      "Слабое место HTTP/2 — TCP под ним. Потерялся пакет с кадром одного потока — TCP держит все следующие байты, и стоят все сто запросов. На мобильных сетях с потерями HTTP/2 иногда работает хуже шести соединений HTTP/1.1.",
      "HTTP/3 (2022) работает на QUIC поверх UDP. Потоки независимы на уровне транспорта: потеря тормозит только свой поток. Рукопожатие транспорта и TLS совмещены: 1 RTT, а для знакомого сервера — 0 RTT. Соединение определяется идентификатором, а не парой IP и порт, поэтому переживает смену сети. О поддержке HTTP/3 браузер узнаёт из заголовка `Alt-Svc` или DNS-записи HTTPS и в следующий раз идёт по QUIC.",
    ],
    flow: {
      actors: ["Браузер", "Сервер"],
      steps: [
        { from: 0, to: 1, label: "поток 1: HEADERS GET /app.js", note: "одно TCP-соединение на всё" },
        { from: 0, to: 1, label: "поток 3: HEADERS GET /style.css", note: "не ждём ответа на поток 1" },
        { from: 0, to: 1, label: "поток 5: HEADERS GET /logo.png" },
        { from: 1, to: 0, label: "поток 3: DATA style.css целиком", note: "маленький файл пришёл первым" },
        { from: 1, to: 0, label: "поток 1: DATA app.js, часть 1" },
        { from: 1, to: 0, label: "поток 5: DATA logo.png", note: "кадры разных потоков перемешаны" },
        { from: 1, to: 0, label: "поток 1: DATA app.js, конец" },
      ],
    },
    keys: [
      "HTTP/2: двоичные кадры, мультиплексирование в одном соединении, сжатие заголовков.",
      "HTTP/2 страдает от потерь: TCP под ним держит все потоки из-за одного пакета.",
      "HTTP/3 на QUIC: независимые потоки, быстрое рукопожатие, соединение переживает смену сети.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Разложи свойства по версиям протокола.",
      groups: ["HTTP/1.1", "HTTP/2", "HTTP/3"],
      items: [
        ["текстовый формат сообщений", 0],
        ["до шести соединений на хост", 0],
        ["много запросов одновременно в одном TCP-соединении", 1],
        ["сжатие заголовков HPACK", 1],
        ["работает на QUIC поверх UDP", 2],
        ["соединение переживает смену сети", 2],
      ],
      why: "HTTP/2 убрал очереди HTTP/1.1, но остался на TCP. HTTP/3 сменил транспорт на QUIC, чтобы убрать и блокировку из-за потерь.",
    },
    {
      type: "quiz",
      q: "Почему на сети с потерями HTTP/3 обычно работает лучше HTTP/2?",
      opts: [
        "Потеря одного пакета в HTTP/2 задерживает все потоки, а в QUIC — только свой",
        "HTTP/3 не шифрует данные и поэтому быстрее",
        "HTTP/3 передаёт файлы целиком одним пакетом",
        "HTTP/2 не умеет повторять потерянные пакеты",
      ],
      a: 0,
      why: "TCP отдаёт байты строго по порядку для всего соединения. QUIC следит за порядком отдельно в каждом потоке.",
    },
    {
      type: "quiz",
      q: "Как браузер узнаёт, что сервер поддерживает HTTP/3?",
      opts: [
        "Из заголовка `Alt-Svc` в ответе или DNS-записи HTTPS",
        "По расширению файлов на сервере",
        "Сервер отвечает кодом 103",
        "Никак, браузер всегда сначала пробует UDP",
      ],
      a: 0,
      why: "Первый запрос обычно идёт по HTTP/2. Сервер сообщает `Alt-Svc: h3=\":443\"`, и следующие запросы браузер отправит по QUIC.",
    },
  ],
};
