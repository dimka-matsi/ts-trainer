import { msg } from "../../../course/http";
import type { NetRequest, WebLesson } from "../../../course/types";

/** Картинка с того же сервера: новая или переиспользованная связь видна по фазам. */
const img = (n: number, start: number, timing: NetRequest["timing"]): NetRequest => ({
  name: `photo-${n}.jpg`,
  type: "jpeg",
  start,
  timing,
  request: msg(`GET /photo-${n}.jpg HTTP/1.1`, [["Host", "shop.ru"], ["Connection", "keep-alive"]]),
  response: msg("HTTP/1.1 200 OK", [["Content-Type", "image/jpeg"], ["Content-Length", "48213"], ["Keep-Alive", "timeout=75"]]),
});

const fresh: NetRequest["timing"] = [["Очередь", 2], ["TCP", 36], ["TLS", 38], ["Ожидание ответа", 45], ["Загрузка", 30]];
const reused: NetRequest["timing"] = [["Очередь", 2], ["Ожидание ответа", 45], ["Загрузка", 30]];

export const lesson: WebLesson = {
  id: "http5",
  region: 2,
  title: "Соединения и keep-alive",
  q: "Почему браузер не открывает новое соединение на каждый запрос? Что такое keep-alive?",
  answer:
    "Новое соединение стоит времени: TCP-рукопожатие и TLS — это один-два RTT до первого байта. В HTTP/1.1 соединения по умолчанию постоянные (keep-alive): после ответа их не закрывают и шлют по ним следующие запросы. Но по соединению HTTP/1.1 идёт один запрос за раз, поэтому браузер держит до шести соединений на хост, а остальные запросы ждут в очереди.",
  theory: {
    p: [
      "В HTTP/1.0 соединение закрывали после каждого ответа. Каждая картинка — заново TCP и TLS, то есть 2–3 RTT накладных расходов. HTTP/1.1 сделал соединения постоянными: сервер не закрывает соединение до тайм-аута простоя, и клиент шлёт следующий запрос по нему же. Закрыть явно можно заголовком `Connection: close`.",
      "Ограничение HTTP/1.1: на соединении один запрос за раз. Следующий ждёт, пока придёт весь предыдущий ответ. Конвейер (pipelining) — отправить несколько запросов подряд — был в стандарте, но ответы всё равно шли по очереди, и браузеры его выключили. Это та же блокировка начала очереди, что в TCP, только на уровне HTTP.",
      "Поэтому браузер открывает к одному хосту несколько соединений, обычно до шести. Седьмой запрос ждёт свободное — во вкладке «Сеть» это фаза «Очередь». Раньше лимит обходили: раскидывали файлы по поддоменам `img1.shop.ru`, `img2.shop.ru` (domain sharding) и склеивали мелкие картинки в одну (спрайты). С HTTP/2 эти трюки только мешают.",
      "Постоянное соединение не живёт вечно. Сервер закрывает его после тайм-аута простоя (в nginx по умолчанию 75 секунд) или после лимита запросов. Если соединение закрылось посреди запроса, браузер повторит идемпотентный запрос по новому соединению.",
    ],
    requests: [
      img(1, 0, fresh),
      img(2, 0, fresh),
      img(3, 0, fresh),
      img(4, 0, fresh),
      img(5, 0, fresh),
      img(6, 0, fresh),
      img(7, 0, [["Очередь", 152], ["Ожидание ответа", 45], ["Загрузка", 30]]),
      img(8, 230, reused),
    ],
    keys: [
      "keep-alive: в HTTP/1.1 соединение по умолчанию остаётся открытым для следующих запросов.",
      "По соединению HTTP/1.1 идёт один запрос за раз, поэтому браузер держит до шести соединений на хост.",
      "Лишние запросы ждут в фазе «Очередь», повторные идут без TCP и TLS.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Страница по HTTP/1.1 запрашивает с одного сервера 7 картинок сразу. Почему у седьмой длинная фаза «Очередь»?",
      opts: [
        "Все шесть соединений к хосту заняты, запрос ждёт свободное",
        "Седьмая картинка больше остальных",
        "Сервер отвечает на запросы по очереди в алфавитном порядке",
        "Браузер ждёт DNS-ответ заново",
      ],
      a: 0,
      why: "Браузер открывает к хосту не больше шести соединений HTTP/1.1. Седьмой запрос уходит по первому освободившемуся — зато уже без TCP и TLS.",
    },
    {
      type: "quiz",
      q: "Что изменил HTTP/1.1 по сравнению с HTTP/1.0 в работе соединений?",
      opts: [
        "Соединение по умолчанию остаётся открытым для следующих запросов",
        "По одному соединению можно одновременно получать много ответов",
        "Соединения стали работать поверх UDP",
        "Заголовки стали сжиматься",
      ],
      a: 0,
      why: "Постоянные соединения — главное изменение HTTP/1.1. Одновременные ответы по одному соединению и сжатие заголовков появились только в HTTP/2.",
    },
    {
      type: "order",
      q: "Расставь события на одном соединении HTTP/1.1.",
      items: ["TCP- и TLS-рукопожатие", "запрос 1", "весь ответ 1", "запрос 2", "весь ответ 2", "закрытие по тайм-ауту простоя"],
      why: "Второй запрос идёт по тому же соединению, но только после того, как пришёл весь первый ответ. Когда соединение долго простаивает, сервер его закрывает.",
    },
  ],
};
