import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "http2",
  region: 2,
  title: "Методы: безопасность и идемпотентность",
  q: "Чем GET отличается от POST? Какие методы безопасные, а какие идемпотентные?",
  answer:
    "GET получает данные: параметры идут в URL, тела нет, ответ можно кэшировать и безопасно повторять. POST отправляет данные в теле и меняет состояние сервера, поэтому повтор может создать второй заказ. Безопасные методы ничего не меняют: GET, HEAD, OPTIONS. Идемпотентные при повторе дают тот же результат: все безопасные плюс PUT и DELETE, а POST и PATCH в общем случае нет.",
  theory: {
    p: [
      "Основные методы. `GET` — получить. `POST` — создать или выполнить действие. `PUT` — заменить ресурс целиком. `PATCH` — изменить часть. `DELETE` — удалить. `HEAD` — как `GET`, но в ответе только заголовки. `OPTIONS` — узнать, что можно делать с ресурсом.",
      "Безопасный метод не меняет данные на сервере: `GET`, `HEAD`, `OPTIONS`. Поэтому браузер спокойно повторяет их и загружает страницы заранее, а поисковые роботы ходят по всем ссылкам. Если `GET /delete?id=5` удаляет запись, её удалит первый же робот, который найдёт эту ссылку.",
      "Идемпотентный метод: повторить запрос N раз — то же, что сделать его один раз. `PUT /users/5` с тем же телом даёт тот же результат. Второй `DELETE /users/5` ничего не удалит: вернёт 404, но состояние не изменится. А два `POST /orders` создадут два заказа. Поэтому при обрыве связи идемпотентные запросы повторяют смело, а `POST` — нет. Платёжные API принимают ключ в заголовке `Idempotency-Key`, чтобы повтор `POST` не списал деньги дважды.",
      "GET против POST. У `GET` параметры в URL: они видны в истории, в логах серверов, и длина URL ограничена. У `POST` данные в теле. Ответы на `GET` кэшируются, на `POST` почти никогда. Но «POST безопаснее, потому что данные не видны» — полуправда: без HTTPS тело видно так же, как адрес.",
    ],
    requests: [
      {
        name: "users/5",
        type: "fetch",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 40], ["Загрузка", 2]],
        request: { line: "GET /api/users/5 HTTP/1.1", headers: [["Host", "shop.ru"], ["Accept", "application/json"]] },
        response: {
          line: "HTTP/1.1 200 OK",
          headers: [["Content-Type", "application/json"], ["Content-Length", "22"]],
          body: "{\"id\":5,\"name\":\"Anna\"}",
        },
      },
      {
        name: "users/5",
        type: "fetch",
        start: 60,
        timing: [["Очередь", 1], ["Ожидание ответа", 55], ["Загрузка", 2]],
        request: {
          line: "PUT /api/users/5 HTTP/1.1",
          headers: [["Host", "shop.ru"], ["Content-Type", "application/json"], ["Content-Length", "15"]],
          body: "{\"name\":\"Anya\"}",
        },
        response: {
          line: "HTTP/1.1 200 OK",
          headers: [["Content-Type", "application/json"], ["Content-Length", "22"]],
          body: "{\"id\":5,\"name\":\"Anya\"}",
        },
      },
      {
        name: "users/5",
        type: "fetch",
        start: 130,
        timing: [["Очередь", 1], ["Ожидание ответа", 38], ["Загрузка", 1]],
        request: { line: "DELETE /api/users/5 HTTP/1.1", headers: [["Host", "shop.ru"]] },
        response: { line: "HTTP/1.1 204 No Content", headers: [] },
      },
    ],
    keys: [
      "Безопасные методы ничего не меняют: `GET`, `HEAD`, `OPTIONS`.",
      "Идемпотентные можно повторять без последствий: безопасные плюс `PUT` и `DELETE`. `POST` — нет.",
      "У `GET` параметры в URL и ответ кэшируется, у `POST` данные в теле. Защищает данные HTTPS, а не метод.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Разложи методы: идемпотентный или нет в общем случае.",
      groups: ["Идемпотентный", "Не идемпотентный"],
      items: [["`GET`", 0], ["`PUT`", 0], ["`DELETE`", 0], ["`HEAD`", 0], ["`POST`", 1], ["`PATCH`", 1]],
      why: "`PUT` заменяет ресурс целиком, повтор даёт то же. `PATCH` может быть идемпотентным (поставить имя) или нет (прибавить 1 к счётчику), поэтому стандарт его таким не считает. `POST` при повторе создаёт новое.",
    },
    {
      type: "quiz",
      q: "Из-за обрыва связи клиент отправил `DELETE /users/5` дважды. Что стало с данными?",
      opts: [
        "Пользователь 5 удалён один раз, второй запрос ничего не изменил",
        "Удалены пользователи 5 и 6",
        "Сервер вернул пользователя 5 обратно",
        "Второй запрос удалил всю таблицу",
      ],
      a: 0,
      why: "`DELETE` идемпотентен: после первого запроса ресурса нет, второй может вернуть 404, но состояние сервера то же.",
    },
    {
      type: "quiz",
      q: "Почему нельзя удалять запись по ссылке `GET /delete?id=5`?",
      opts: [
        "GET считается безопасным: браузеры и поисковые роботы вызывают его без спроса",
        "GET не умеет передавать параметры",
        "Сервер не может обработать GET с параметром id",
        "GET работает только для картинок",
      ],
      a: 0,
      why: "Браузер может загрузить ссылку заранее, робот пройдёт по всем ссылкам сайта. Всё, что меняет данные, делают через `POST`, `PUT`, `PATCH` или `DELETE`.",
    },
  ],
};
