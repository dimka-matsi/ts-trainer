import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "http1",
  region: 2,
  title: "Устройство запроса и ответа",
  q: "Из чего состоит HTTP-запрос и HTTP-ответ?",
  answer:
    "Запрос — это стартовая строка с методом, путём и версией, затем заголовки, пустая строка и необязательное тело. Ответ устроен так же, только в стартовой строке версия, код состояния и фраза. HTTP не хранит состояние: каждый запрос обрабатывается сам по себе, и сервер не помнит предыдущие.",
  theory: {
    p: [
      "HTTP — протокол прикладного уровня: клиент отправляет запрос, сервер отвечает. Работает поверх TCP, а HTTP/3 — поверх QUIC. В HTTP/1.1 сообщения — обычный текст, его можно набрать руками в терминале.",
      "Запрос начинается со стартовой строки `POST /api/orders HTTP/1.1`: метод, путь с параметрами, версия. Дальше заголовки `Имя: значение`, по одному в строке. `Host` обязателен: на одном IP живут сотни сайтов, и по нему сервер выбирает нужный. Пустая строка отделяет заголовки от тела. У `GET` тела обычно нет, у `POST` есть — например, JSON.",
      "Ответ начинается со строки `HTTP/1.1 201 Created`: версия, код, фраза. Потом заголовки: `Content-Type` говорит, что в теле (`text/html`, `application/json`), `Content-Length` — сколько в нём байт. Потом пустая строка и тело.",
      "HTTP не хранит состояние: сервер не помнит, что ты спрашивал секунду назад. Всё нужное для ответа приходит в самом запросе. Чтобы узнавать пользователя между запросами, придумали отдельные механизмы — о них регион «Cookies, сессии и вход». В HTTP/2 и HTTP/3 смысл сообщений тот же, но они передаются двоичными кадрами, а стартовая строка превратилась в псевдозаголовки `:method` и `:path`.",
    ],
    requests: [
      {
        name: "products?page=2",
        type: "fetch",
        start: 0,
        timing: [["Очередь", 2], ["Ожидание ответа", 48], ["Загрузка", 3]],
        request: {
          line: "GET /api/products?page=2 HTTP/1.1",
          headers: [["Host", "shop.ru"], ["Accept", "application/json"]],
        },
        response: {
          line: "HTTP/1.1 200 OK",
          headers: [["Content-Type", "application/json"], ["Content-Length", "52"]],
          body: "{\"page\":2,\"items\":[{\"id\":7,\"title\":\"Чайник\"}]}",
        },
      },
      {
        name: "orders",
        type: "fetch",
        start: 60,
        timing: [["Очередь", 1], ["Ожидание ответа", 72], ["Загрузка", 2]],
        request: {
          line: "POST /api/orders HTTP/1.1",
          headers: [["Host", "shop.ru"], ["Content-Type", "application/json"], ["Content-Length", "25"]],
          body: "{\"productId\":7,\"count\":2}",
        },
        response: {
          line: "HTTP/1.1 201 Created",
          headers: [["Location", "/api/orders/42"], ["Content-Type", "application/json"], ["Content-Length", "9"]],
          body: "{\"id\":42}",
        },
      },
    ],
    keys: [
      "Сообщение: стартовая строка, заголовки, пустая строка, тело.",
      "В запросе метод, путь и версия, в ответе версия, код и фраза. `Host` в HTTP/1.1 обязателен.",
      "HTTP не хранит состояние: каждый запрос самодостаточен.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь части HTTP-запроса по порядку.",
      items: ["стартовая строка: метод, путь, версия", "заголовки, по одному в строке", "пустая строка", "тело"],
      why: "Пустая строка — единственный способ понять, где кончились заголовки. Всё после неё — тело, его длину подсказывает `Content-Length`.",
    },
    {
      type: "quiz",
      q: "Зачем в HTTP/1.1 обязателен заголовок `Host`?",
      opts: [
        "На одном IP-адресе живёт много сайтов, и сервер выбирает нужный по имени",
        "Без него не заработает шифрование",
        "По нему сервер узнаёт IP-адрес клиента",
        "Он нужен только для перенаправлений",
      ],
      a: 0,
      why: "TCP-соединение приходит на IP и порт. Какой из сайтов на этом сервере нужен клиенту, видно только из `Host`.",
    },
    {
      type: "quiz",
      q: "Что значит «HTTP не хранит состояние»?",
      opts: [
        "Каждый запрос обрабатывается сам по себе, сервер не помнит предыдущие запросы клиента",
        "Сервер не может сохранять данные в базу",
        "Ответы нельзя кэшировать",
        "Соединение закрывается после каждого ответа",
      ],
      a: 0,
      why: "Протокол не связывает запросы между собой. Если серверу нужно помнить пользователя, клиент сам присылает признак в каждом запросе.",
    },
  ],
};
