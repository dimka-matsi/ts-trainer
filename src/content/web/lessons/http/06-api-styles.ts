import { json, msg } from "../../http";
import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "http6",
  region: 2,
  title: "REST, GraphQL и gRPC",
  q: "Что такое REST? Когда выбирают GraphQL или gRPC?",
  answer:
    "REST — стиль API поверх HTTP: ресурсы с адресами вроде `/users/5`, действия через методы, ответы с правильными кодами, сервер не хранит состояние. В GraphQL один адрес, и клиент сам описывает, какие поля ему нужны: меньше лишних данных и запросов, но сложнее кэширование. gRPC — вызов функций по HTTP/2 с двоичным форматом Protobuf, он быстрый и строгий, его чаще используют между сервисами, а в браузере нужен gRPC-Web.",
  theory: {
    p: [
      "REST: всё — ресурсы, у каждого свой адрес: `/users`, `/users/5`, `/users/5/orders`. Действие выбирается методом: `GET /users/5` — прочитать, `PATCH /users/5` — изменить, `DELETE /users/5` — удалить. Имена существительные во множественном числе, без глаголов: не `/getUser?id=5`. Сервер не хранит состояние между запросами, а ответы помечены, можно ли их кэшировать.",
      "Хорошее REST API: 201 с `Location` после создания, 404 для отсутствующего, 422 при ошибке проверки. Пагинация через `?page=2&limit=20` или курсор, фильтры в параметрах, версия в пути `/v1/`, одинаковый формат ошибок. Слабости REST: экрану нужны данные трёх ресурсов — это три запроса. А ресурс отдаёт 40 полей, когда нужны 3, — это лишние данные (overfetching).",
      "GraphQL: один адрес `/graphql`, обычно метод `POST`. В теле — запрос, где клиент перечисляет нужные поля, в том числе вложенные: пользователь, его заказы, суммы — всё одним запросом. Схема типов описывает всё API. Минусы: HTTP-кэш почти не работает, ведь всё идёт `POST` на один адрес. Ошибки часто приходят со статусом 200 внутри тела. А тяжёлый вложенный запрос может положить сервер, поэтому нужны лимиты глубины и сложности.",
      "gRPC: клиент вызывает функции сервиса, как свои. Контракт описан в файле `.proto`, данные передаются в двоичном формате Protobuf, транспорт — HTTP/2 с потоками в обе стороны. Это быстро и строго типизировано, поэтому gRPC любят для связи микросервисов. Браузер не даёт управлять HTTP/2 на нужном уровне, так что нужен gRPC-Web и прокси, который переводит запросы.",
    ],
    requests: [
      {
        name: "users/5/orders",
        type: "fetch",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 64], ["Загрузка", 3]],
        request: msg("GET /api/v1/users/5/orders?limit=2 HTTP/1.1", [["Host", "shop.ru"], ["Accept", "application/json"]]),
        response: msg("HTTP/1.1 200 OK", [], json("{\"items\":[{\"id\":42,\"total\":1990},{\"id\":43,\"total\":540}],\"next\":\"c2\"}")),
      },
      {
        name: "graphql",
        type: "fetch",
        start: 80,
        timing: [["Очередь", 1], ["Ожидание ответа", 90], ["Загрузка", 2]],
        request: msg("POST /graphql HTTP/1.1", [["Host", "shop.ru"]], json("{\"query\":\"{ user(id: 5) { name orders(last: 2) { total } } }\"}")),
        response: msg("HTTP/1.1 200 OK", [], json("{\"data\":{\"user\":{\"name\":\"Анна\",\"orders\":[{\"total\":1990},{\"total\":540}]}}}")),
      },
    ],
    keys: [
      "REST: ресурсы-существительные, действия через методы, правильные коды, без состояния.",
      "GraphQL: один адрес, клиент выбирает поля. Меньше лишних данных, но трудно кэшировать и нужно ограничивать сложность.",
      "gRPC: вызов функций, Protobuf поверх HTTP/2, чаще между сервисами. В браузере — через gRPC-Web.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь термин и описание.",
      pairs: [
        ["REST", "ресурсы с адресами и методы HTTP"],
        ["GraphQL", "один адрес, клиент сам выбирает поля"],
        ["gRPC", "вызов функций, Protobuf поверх HTTP/2"],
        ["overfetching", "ответ содержит лишние поля"],
      ],
      why: "REST описывает ресурсы, GraphQL — нужные клиенту данные, gRPC — функции сервиса. Лишние поля в ответе — типичная жалоба на REST.",
    },
    {
      type: "quiz",
      q: "Какой адрес лучше подходит для REST API?",
      opts: ["`GET /users/5/orders`", "`GET /getUserOrders?userId=5`", "`POST /users/5/orders/get`", "`GET /api?action=orders&user=5`"],
      a: 0,
      why: "Адрес называет ресурс, а действие задаёт метод. Глаголы в пути и действия в параметрах — признак RPC, а не REST.",
    },
    {
      type: "quiz",
      q: "Почему HTTP-кэш плохо работает с GraphQL?",
      opts: [
        "Запросы обычно идут методом POST на один адрес, а ответы на POST не кэшируются",
        "GraphQL запрещает заголовки кэширования",
        "Ответы GraphQL всегда больше 1 МБ",
        "GraphQL работает только по WebSocket",
      ],
      a: 0,
      why: "HTTP-кэш различает ответы по методу и адресу. Когда всё идёт `POST /graphql`, кэшу не за что зацепиться. Поэтому кэш переносят в клиентские библиотеки или используют GET с сохранёнными запросами.",
    },
  ],
};
