import { msg } from "../../http";
import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "cdn2",
  region: 6,
  title: "Кэш CDN и его сброс",
  q: "Выложили новую версию, а пользователи видят старую. Как CDN решает, что хранить, и как его сбросить?",
  answer:
    "Узел CDN кэширует ответ по ключу — обычно хост, путь и параметры — на срок из `s-maxage` или `max-age`, если настройки CDN не говорят иначе. Старая версия видна, пока не истёк срок. Сбросить можно командой purge по адресу или по тегу, но надёжнее не зависеть от сброса: файлы с хэшем в имени кэшировать надолго, HTML — коротко или с проверкой. Попадание в кэш видно по заголовкам вроде `X-Cache: HIT` и `Age`.",
  theory: {
    p: [
      "Узел кэширует по ключу. По умолчанию это хост, путь и параметры запроса: `/app.js?v=1` и `/app.js?v=2` — разные копии. В настройках ключ можно менять: например, выкинуть метки рекламных кампаний `utm_*`, чтобы они не плодили одинаковые копии.",
      "Срок берётся из заголовков origin: `s-maxage`, если он есть, иначе `max-age`. Ответы с `private`, `no-store` и обычно с `Set-Cookie` узел не кэширует. Правила CDN могут это переопределить, и это частый источник сюрпризов: «кэшировать всё на час» закэширует и страницу с корзиной.",
      "Как понять, что произошло: CDN добавляет заголовки. `X-Cache: HIT` или `CF-Cache-Status: HIT` — ответ из кэша узла, `MISS` — узел сходил к origin. `Age` — сколько секунд копия лежит на узле. Если после выкладки видна старая версия, первым делом смотрят на эти заголовки.",
      "Сброс (purge): по адресу, по тегу (origin помечает ответы заголовком вроде `Cache-Tag`) или всё сразу. Он проходит за секунды, но браузеры, которые уже взяли копию с долгим `max-age`, это не заденет. Поэтому правильная схема — та же, что для браузера: файлы с хэшем кэшируются на год, HTML — коротко на CDN и с `no-cache` в браузере, а после выкладки сбрасывают только HTML. Чтобы промахи всех узлов не обрушились на origin, между ними ставят промежуточный слой кэша (origin shield).",
    ],
    requests: [
      {
        name: "app.3f9a1c.js",
        type: "script",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 8], ["Загрузка", 10]],
        request: msg("GET /assets/app.3f9a1c.js HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["cache-control", "max-age=31536000, immutable"], ["x-cache", "HIT"], ["age", "86211"], ["content-type", "text/javascript"], ["content-length", "184233"]]),
      },
      {
        name: "index.html",
        type: "document",
        start: 25,
        timing: [["Очередь", 1], ["Ожидание ответа", 140], ["Загрузка", 3]],
        request: msg("GET / HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["cache-control", "no-cache, s-maxage=60"], ["x-cache", "MISS"], ["cache-tag", "html"]], {
          type: "text/html; charset=utf-8",
          text: "<script type=\"module\" src=\"/assets/app.3f9a1c.js\"></script>",
        }),
      },
    ],
    keys: [
      "Ключ кэша CDN — хост, путь и параметры. Срок — `s-maxage`, иначе `max-age`.",
      "`X-Cache: HIT/MISS` и `Age` показывают, откуда пришёл ответ.",
      "Purge по адресу или тегу работает за секунды. Но надёжнее хэши в именах и короткий срок у HTML.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "После выкладки часть пользователей видит старую страницу. В ответе `X-Cache: HIT` и `Age: 2400`. Что это значит?",
      opts: [
        "Узел CDN отдал копию, которая лежит у него 40 минут",
        "Origin ответил медленно, 2400 мс",
        "Браузер взял страницу из своего кэша",
        "Страница пришла с ошибкой",
      ],
      a: 0,
      why: "`HIT` — ответ из кэша узла, `Age` — возраст копии в секундах. Нужно сбросить HTML на CDN и проверить `s-maxage` для страниц.",
    },
    {
      type: "order",
      q: "Расставь шаги правильной выкладки новой версии сайта за CDN.",
      items: [
        "залить новые файлы с новыми хэшами в именах",
        "залить новый `index.html`",
        "сбросить на CDN только HTML (по тегу `html`)",
        "подождать, пока старые HTML перестанут ссылаться на старые файлы",
        "удалить старые файлы",
      ],
      why: "Файлы с хэшами не конфликтуют со старыми, поэтому их выкладывают первыми. Старые удаляют последними: кто-то ещё держит открытой старую страницу.",
    },
    {
      type: "quiz",
      q: "Какие ответы узел CDN по умолчанию не кэширует?",
      opts: [
        "С `private`, `no-store` и обычно с `Set-Cookie`",
        "Любые ответы больше 1 МБ",
        "Все ответы на `GET`",
        "Картинки и шрифты",
      ],
      a: 0,
      why: "Эти признаки говорят, что ответ личный. Но правила в настройках CDN могут это переопределить, так что их проверяют отдельно.",
    },
  ],
};
