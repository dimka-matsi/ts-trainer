import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "cdn4",
  region: 6,
  title: "Балансировка нагрузки",
  q: "Как балансировщик распределяет запросы и чем L4 отличается от L7?",
  answer:
    "Балансировщик принимает запросы и раздаёт их между копиями приложения по алгоритму: по кругу, на наименее загруженную, по хэшу IP. Он регулярно проверяет здоровье копий и не шлёт запросы упавшим. L4 работает с TCP и UDP: видит только адреса и порты, быстрый, но не понимает HTTP. L7 читает HTTP: может отправлять `/api` в один сервис, а `/images` в другой, завершать TLS и повторять запросы. Чтобы балансировка не ломала вход, сессии хранят в общем хранилище, а не в памяти одной копии.",
  theory: {
    p: [
      "Один сервер не выдержит всех запросов и когда-нибудь упадёт. Поэтому приложение запускают в нескольких копиях, а перед ними ставят балансировщик. Для клиентов у сайта один адрес, а внутри запросы расходятся по копиям.",
      "Алгоритмы. Round robin — по кругу: 1, 2, 3, 1, 2, 3. Взвешенный — мощная машина получает больше. Least connections — туда, где меньше открытых соединений: хорошо, когда запросы разной длины. Хэш по IP или ключу — один клиент всегда на одну копию. Проверки здоровья (health checks) каждые несколько секунд опрашивают, например, `/health` и выводят упавшую копию из ротации.",
      "L4 и L7 — уровни из модели OSI. L4-балансировщик работает на транспортном уровне: пересылает TCP-соединения, видит IP и порты, но не HTTP. Он очень быстрый, и ему всё равно, что внутри. L7 работает на прикладном уровне: читает HTTP, маршрутизирует по пути, заголовкам и cookie, завершает TLS, повторяет неудачный идемпотентный запрос на другой копии. Это медленнее и дороже, зато гибко. Часто их ставят вместе: L4 снаружи, L7 за ним.",
      "Ловушка — состояние в памяти копии. Анна вошла на копию 1, сессия хранится в её памяти, следующий запрос ушёл на копию 2 — и Анна «не вошла». Костыль — липкие сессии (sticky sessions): балансировщик ставит cookie и всегда отправляет Анну на копию 1. Но если копия 1 упадёт, сессия всё равно пропадёт. Правильно — приложение без состояния, а сессии в общем хранилище вроде Redis.",
    ],
    flow: {
      actors: ["Клиенты", "L7-балансировщик", "Копия 1", "Копия 2", "Копия 3"],
      steps: [
        { from: 0, to: 1, label: "GET /api/cart" },
        { from: 1, to: 2, label: "запрос 1 → копия 1", note: "round robin" },
        { from: 0, to: 1, label: "GET /api/orders" },
        { from: 1, to: 3, label: "запрос 2 → копия 2" },
        { from: 1, to: 4, label: "health check: GET /health", note: "копия 3 не ответила", lost: true },
        { from: 0, to: 1, label: "GET /api/items" },
        { from: 1, to: 2, label: "запрос 3 → копия 1", note: "копия 3 выведена из ротации" },
      ],
    },
    keys: [
      "Балансировщик раздаёт запросы копиям: по кругу, по нагрузке, по хэшу. Упавшие убирают проверки здоровья.",
      "L4 видит TCP и адреса, L7 читает HTTP и маршрутизирует по пути и заголовкам.",
      "Не держи сессии в памяти копии: липкие сессии — костыль, нужен общий Redis.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Что умеет балансировщик L4, а что только L7?",
      groups: ["L4 тоже умеет", "Только L7"],
      items: [
        ["распределить TCP-соединения по копиям", 0],
        ["не отправлять запросы упавшей копии", 0],
        ["отправить `/api` в один сервис, а `/images` в другой", 1],
        ["маршрутизировать по заголовку или cookie", 1],
        ["завершить TLS и прочитать запрос", 1],
      ],
      why: "L4 видит только адреса и порты. Всё, что требует понимать HTTP, — работа L7.",
    },
    {
      type: "quiz",
      q: "После запуска трёх копий приложения пользователей случайно выкидывает из аккаунта. В чём вероятная причина?",
      opts: [
        "Сессии хранятся в памяти каждой копии, и запросы попадают на разные копии",
        "Балансировщик удаляет cookie",
        "Три копии — слишком много для HTTPS",
        "Браузер не поддерживает балансировку",
      ],
      a: 0,
      why: "Копия 2 ничего не знает о сессии, созданной в памяти копии 1. Сессии выносят в общее хранилище, и тогда неважно, какая копия ответит.",
    },
    {
      type: "quiz",
      q: "Запросы к API сильно отличаются по длительности: одни 10 мс, другие 5 секунд. Какой алгоритм подойдёт лучше round robin?",
      opts: ["Least connections", "Случайный выбор", "Всегда первая копия", "Хэш по пути запроса"],
      a: 0,
      why: "По кругу долгие запросы могут скопиться на одной копии. Least connections отправит новый запрос туда, где сейчас меньше работы.",
    },
  ],
};
