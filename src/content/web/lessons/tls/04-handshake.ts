import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "tls4",
  region: 3,
  title: "Рукопожатие TLS 1.3",
  q: "Что происходит при рукопожатии TLS и сколько оно длится?",
  answer:
    "В TLS 1.3 клиент в первом же сообщении ClientHello отправляет шифры и свою часть обмена ключами. Сервер отвечает ServerHello со своей частью, а сертификат, подпись и Finished присылает уже зашифрованными. Клиент проверяет сертификат, отправляет Finished и сразу данные. Итого один RTT, а при повторном подключении — 0-RTT. В TLS 1.2 было два RTT.",
  theory: {
    p: [
      "ClientHello: версии и шифры, которые умеет клиент, случайное число и сразу открытая часть ECDHE (key_share). Там же имя сервера в расширении SNI: на одном IP сервер по нему выбирает нужный сертификат. SNI обычно идёт открыто. Encrypted ClientHello (ECH) прячет его, но поддерживают его пока не везде.",
      "Сервер отвечает ServerHello: выбранный шифр и своя часть ECDHE. С этого момента у сторон общий секрет, и всё дальше шифруется: сертификат, подпись всего рукопожатия ключом сертификата (CertificateVerify) и Finished — проверочный код всей переписки.",
      "Клиент проверяет цепочку сертификатов и подпись, отправляет свой Finished и тут же HTTP-запрос. TLS занял один круг туда-обратно. В TLS 1.2 кругов было два: сначала договаривались о шифре, потом обменивались ключами. Если клиент угадал не ту группу ключей, сервер попросит повторить (HelloRetryRequest), и это ещё один RTT.",
      "После рукопожатия сервер выдаёт билет (session ticket). При следующем подключении клиент предъявляет билет и отправляет данные сразу в первом сообщении — это 0-RTT. Минус: такие ранние данные можно перехватить и отправить ещё раз, поэтому 0-RTT используют только для безопасных запросов вроде GET. Вместе с TCP новое HTTPS-соединение по HTTP/2 стоит 2 RTT, а HTTP/3 на QUIC — 1 RTT.",
    ],
    flow: {
      actors: ["Браузер", "Сервер"],
      steps: [
        { from: 0, to: 1, label: "ClientHello: шифры, key_share, SNI=shop.ru", note: "идёт открыто" },
        { from: 1, to: 0, label: "ServerHello: шифр, key_share", note: "общий секрет уже есть" },
        { from: 1, to: 0, label: "{Certificate, CertificateVerify, Finished}", note: "в фигурных скобках — зашифровано" },
        { from: 0, to: 0, label: "проверка цепочки и подписи" },
        { from: 0, to: 1, label: "{Finished} + {GET /}", note: "запрос ушёл через 1 RTT" },
        { from: 1, to: 0, label: "{HTTP-ответ}" },
      ],
    },
    keys: [
      "TLS 1.3: ClientHello с key_share → ServerHello и зашифрованный сертификат → Finished и данные.",
      "Полное рукопожатие — 1 RTT, TLS 1.2 требовал 2.",
      "0-RTT по билету сессии быстрее, но ранние данные можно повторить, поэтому только для безопасных запросов.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь сообщения рукопожатия TLS 1.3 по порядку.",
      items: ["ClientHello", "ServerHello", "сертификат и подпись сервера", "Finished сервера", "Finished клиента", "первый HTTP-запрос"],
      why: "Сервер отправляет ServerHello, сертификат, подпись и Finished одной пачкой. Клиенту остаётся проверить их и ответить своим Finished вместе с запросом.",
    },
    {
      type: "quiz",
      q: "Сколько RTT занимает полное рукопожатие TLS 1.3?",
      opts: ["0", "1", "2", "3"],
      a: 1,
      why: "Клиент сразу отправляет свою часть ключей, поэтому хватает одного круга. 0-RTT бывает только при возобновлении сессии по билету.",
    },
    {
      type: "quiz",
      q: "Почему в 0-RTT отправляют только безопасные запросы?",
      opts: [
        "Ранние данные можно перехватить и отправить серверу повторно",
        "В 0-RTT данные не шифруются",
        "Сервер не знает, кто отправил данные",
        "0-RTT работает только с картинками",
      ],
      a: 0,
      why: "Если злоумышленник повторит ранний `POST /pay`, платёж пройдёт дважды. Повтор `GET` ничего не меняет.",
    },
  ],
};
