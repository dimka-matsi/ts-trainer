import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "tls5",
  region: 3,
  title: "HSTS",
  q: "Зачем заголовок `Strict-Transport-Security` и что такое HSTS preload?",
  answer:
    "Даже если сайт перенаправляет с HTTP на HTTPS, первый запрос по `http://` идёт открыто. Злоумышленник может его перехватить и не пустить пользователя на HTTPS — это SSL stripping. Заголовок `Strict-Transport-Security: max-age=31536000; includeSubDomains` велит браузеру год ходить на домен только по HTTPS, даже если набрали `http://`. HSTS preload — список доменов, вшитый в браузеры: защита работает с самого первого визита.",
  theory: {
    p: [
      "Пользователь набирает `shop.ru` без схемы, браузер идёт на `http://shop.ru`, сервер отвечает 301 на `https://`. Этот первый запрос открыт. Злоумышленник в той же сети перехватывает его и дальше сам общается с сайтом по HTTPS, а пользователю отдаёт страницы по HTTP. Замка в адресной строке нет, но мало кто это заметит. Атака называется SSL stripping.",
      "HSTS: сервер в HTTPS-ответе присылает `Strict-Transport-Security: max-age=31536000; includeSubDomains`. Браузер запоминает на `max-age` секунд: к этому домену — только HTTPS. Набранный `http://` он сам меняет на `https://` до отправки запроса. `includeSubDomains` распространяет правило на все поддомены. А ошибку сертификата на таком домене пользователь уже не сможет пропустить кнопкой «Всё равно перейти».",
      "Слабое место — самый первый визит, пока браузер не видел заголовок. Для этого есть HSTS preload. Домены с `preload` в заголовке, отправленные на hstspreload.org, вшиваются в Chrome, Firefox, Safari и Edge. Целые доменные зоны, например `.dev` и `.app`, внесены в список полностью.",
      "Включают HSTS осторожно. Если какой-то поддомен ещё не умеет HTTPS, `includeSubDomains` сломает его на весь `max-age`, а выход из preload-списка занимает месяцы. Поэтому начинают с маленького `max-age`, например 300, проверяют поддомены и потом поднимают до года. Заголовок принимается только из HTTPS-ответа: по HTTP его мог бы подставить злоумышленник.",
    ],
    requests: [
      {
        name: "shop.ru (http)",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["DNS", 20], ["TCP", 35], ["Ожидание ответа", 40], ["Загрузка", 1]],
        request: msg("GET / HTTP/1.1", [["Host", "shop.ru"]]),
        response: msg("HTTP/1.1 301 Moved Permanently", [["Location", "https://shop.ru/"]]),
      },
      {
        name: "shop.ru (https)",
        type: "document",
        start: 100,
        timing: [["Очередь", 1], ["TCP", 35], ["TLS", 36], ["Ожидание ответа", 60], ["Загрузка", 8]],
        request: msg("GET / HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["strict-transport-security", "max-age=31536000; includeSubDomains; preload"]], {
          type: "text/html; charset=utf-8",
          text: "<!doctype html><title>Магазин</title>",
        }),
      },
    ],
    keys: [
      "SSL stripping: перехват первого открытого запроса до перенаправления на HTTPS.",
      "HSTS заставляет браузер самому ходить только по HTTPS на время `max-age`.",
      "Preload защищает и первый визит. Включают постепенно: маленький `max-age`, проверка поддоменов, потом год.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Сайт делает 301 с HTTP на HTTPS, но HSTS не настроен. Когда пользователь уязвим?",
      opts: [
        "Каждый раз, когда вводит адрес без `https://`: первый запрос идёт открыто",
        "Никогда: перенаправление всё защищает",
        "Только при первом визите за всю жизнь",
        "Только если сертификат просрочен",
      ],
      a: 0,
      why: "Без HSTS браузер каждый раз начинает с `http://`, и каждый раз этот запрос можно перехватить до перенаправления.",
    },
    {
      type: "order",
      q: "Расставь, как работает HSTS после настройки.",
      items: [
        "первый визит по HTTPS приносит заголовок HSTS",
        "браузер запоминает домен на `max-age` секунд",
        "пользователь набирает `http://shop.ru`",
        "браузер сам меняет схему на `https://` до отправки",
        "запрос сразу уходит по HTTPS",
      ],
      why: "После первого HTTPS-ответа открытых запросов к домену больше нет: браузер переписывает адрес у себя, и перехватывать нечего.",
    },
    {
      type: "quiz",
      q: "Почему браузер игнорирует `Strict-Transport-Security` в ответе по обычному HTTP?",
      opts: [
        "Такой ответ мог подставить злоумышленник на пути",
        "По HTTP заголовки не передаются",
        "HSTS работает только в HTTP/2",
        "Это ограничение только Safari",
      ],
      a: 0,
      why: "Открытый ответ ничем не защищён. Если бы браузер ему верил, злоумышленник мог бы включить HSTS чужому сайту без HTTPS и сломать его.",
    },
  ],
};
