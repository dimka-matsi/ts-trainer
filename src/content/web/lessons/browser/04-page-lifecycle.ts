import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "br4",
  region: 6,
  level: "middle",
  title: "Жизнь вкладки: фон, заморозка и выгрузка",
  q: "Что браузер делает с вкладкой в фоне? Почему таймеры в фоне срабатывают реже, а вкладка иногда перезагружается сама?",
  answer:
    "Браузер экономит батарею и память, поэтому фоновые вкладки живут по другим правилам. Скрытая вкладка не рисует кадры, её таймеры срабатывают не чаще раза в секунду, а после нескольких минут — ещё реже. Долго неактивную вкладку браузер может заморозить — остановить все задачи — или выгрузить из памяти, чтобы при возврате загрузить заново. Об этих переходах страница узнаёт из событий `visibilitychange`, `freeze`, `resume` и `pagehide`. Надёжное место сохранить данные — `visibilitychange` при переходе в скрытое состояние: `unload` на телефонах часто не срабатывает вовсе.",
  theory: {
    p: [
      "Состояния страницы по Page Lifecycle: активная (видна и в фокусе), пассивная (видна без фокуса), скрытая (вкладка в фоне или окно свёрнуто), замороженная (задачи остановлены), выгруженная (удалена из памяти) и уничтоженная (закрыта). Браузер переводит вкладку между ними сам, заботясь о батарее и памяти.",
      "Скрытая вкладка не рисует кадры, поэтому анимации, привязанные к кадрам, стоят. Таймеры ограничиваются: не чаще раза в секунду, а после нескольких минут в фоне Chrome может разрешать их лишь раз в минуту. Поэтому часы и опросы сервера в фоне «отстают», а после возврата считают время по часам, а не по числу срабатываний.",
      "Заморозка — остановка всех задач вкладки: таймеров, обработчиков, запросов. Выгрузка — браузер освобождает память и при возврате загружает страницу заново, восстановив адрес и прокрутку. На телефонах это обычное дело. Страница, которая хранит важное только в памяти, при этом теряет введённые данные.",
      "События: `visibilitychange` и состояние `visibilityState` документа — главный сигнал, что пользователь ушёл. `freeze` и `resume` — перед заморозкой и после неё. `pagehide` и `pageshow` — уход со страницы и возврат, в том числе из кэша «назад-вперёд». `unload` ненадёжен и мешает этому кэшу. Правило: сохранять черновики и отправлять аналитику при переходе в скрытое состояние — для отправки при уходе есть надёжный способ, `sendBeacon`.",
    ],
    flow: {
      actors: ["Активная", "Скрытая", "Замороженная", "Выгруженная"],
      steps: [
        { from: 0, to: 1, label: "переключили вкладку: visibilitychange → hidden", note: "сохраняем черновик здесь" },
        { from: 1, to: 2, label: "долго в фоне: freeze — задачи остановлены" },
        { from: 2, to: 3, label: "не хватает памяти: вкладку выгрузили" },
        { from: 3, to: 0, label: "вернулись: страница загружается заново" },
      ],
    },
    keys: [
      "В фоне нет кадров, таймеры не чаще раза в секунду, со временем ещё реже.",
      "Долго неактивную вкладку браузер замораживает или выгружает из памяти — при возврате она загрузится заново.",
      "Сохранять данные — по `visibilitychange` при уходе в скрытое состояние. `unload` ненадёжен.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь состояния вкладки, в которые браузер может перевести её по мере бездействия.",
      items: ["активная", "скрытая", "замороженная", "выгруженная"],
      why: "Каждый шаг экономит больше ресурсов. Из выгруженного состояния вкладка возвращается только новой загрузкой.",
    },
    {
      type: "quiz",
      q: "Где надёжнее всего сохранить черновик формы перед тем, как пользователь уйдёт?",
      opts: [
        "В событии `visibilitychange`, когда страница становится скрытой",
        "В событии `unload`",
        "В событии `load`",
        "Раз в час по таймеру",
      ],
      a: 0,
      why: "С телефона пользователь часто уходит, просто свернув браузер, и `unload` не наступает вовсе. Переход в скрытое состояние бывает всегда.",
    },
    {
      type: "quiz",
      q: "Счётчик на странице увеличивается таймером раз в 100 мс. Пользователь ушёл на другую вкладку на 10 минут. Что увидит при возврате?",
      opts: [
        "Счётчик отстанет: в фоне таймеры срабатывали намного реже",
        "Ровно 6000 срабатываний",
        "Счётчик сбросится в ноль",
        "Страница покажет ошибку",
      ],
      a: 0,
      why: "Браузер ограничивает таймеры в фоне. Время считают по часам (разница отметок времени), а не по числу срабатываний таймера.",
    },
  ],
};
