import { json, msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "cache4",
  region: 4,
  title: "Приватный и общий кэш",
  q: "Зачем `private` и `public` в `Cache-Control`? Что такое `s-maxage`?",
  answer:
    "Кроме кэша браузера (приватного, только для одного пользователя) бывают общие кэши: CDN, кэширующий прокси, nginx перед приложением. Они отдают одну копию многим пользователям. `private` запрещает класть ответ в общий кэш — для личных данных. `public` разрешает кэшировать даже то, что обычно не кэшируют. `s-maxage` задаёт срок только для общих кэшей, не трогая браузер.",
  theory: {
    p: [
      "Кэш браузера приватный: копии видит только этот пользователь. Общий кэш стоит между пользователями и приложением и отдаёт одну копию всем. Это CDN — сеть серверов по всему миру, которые держат копии ответов ближе к пользователям, — а ещё кэширующий прокси компании или nginx перед приложением. CDN подробно разберём в следующем регионе.",
      "Опасность общего кэша — отдать личное чужому. Страница `/account` с именем Анны закэшировалась в CDN, и следующий пользователь видит кабинет Анны. Поэтому личные ответы помечают `Cache-Control: private` — только браузер пользователя может их хранить. Или `no-store`, если хранить нельзя вообще.",
      "`public` разрешает хранить в общих кэшах даже то, что по умолчанию туда не попадает, например ответы на запросы с `Authorization`. Для обычной статики `public` не нужен: достаточно `max-age`.",
      "`s-maxage` задаёт срок свежести только для общих кэшей. `Cache-Control: max-age=60, s-maxage=3600` — браузер держит копию минуту, CDN — час. Когда страница поменялась, CDN можно очистить командой, а браузеры пользователей нельзя. Поэтому общим кэшам дают срок длиннее, чем браузерам. Заголовок `Age` в ответе показывает, сколько секунд копия уже пролежала в общем кэше.",
    ],
    requests: [
      {
        name: "account",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 80], ["Загрузка", 5]],
        request: msg("GET /account HTTP/2", [[":authority", "shop.ru"], ["cookie", "__Host-sid=a8f3k2"]]),
        response: msg("HTTP/2 200", [["cache-control", "private, no-cache"]], { type: "text/html; charset=utf-8", text: "<h1>Кабинет Анны</h1>" }),
      },
      {
        name: "catalog",
        type: "fetch",
        start: 100,
        timing: [["Очередь", 1], ["Ожидание ответа", 12], ["Загрузка", 3]],
        request: msg("GET /api/catalog HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["cache-control", "max-age=60, s-maxage=3600"], ["age", "1740"]], json("{\"items\":[{\"id\":7,\"title\":\"Чайник\"}]}")),
      },
    ],
    keys: [
      "Приватный кэш — браузер, общий — CDN и прокси, одна копия на всех.",
      "`private` — только браузер пользователя. Личные страницы в общий кэш не пускают.",
      "`s-maxage` — срок для общих кэшей, `Age` — сколько копия уже пролежала.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Какой `Cache-Control` подходит ответу?",
      groups: ["`private`", "`public, s-maxage=…`"],
      items: [
        ["страница личного кабинета", 0],
        ["список товаров, одинаковый для всех", 1],
        ["корзина пользователя", 0],
        ["статья в блоге", 1],
        ["история заказов", 0],
      ],
      why: "Общий кэш уместен только для одинакового для всех содержимого. Всё, что зависит от пользователя, — `private` или `no-store`.",
    },
    {
      type: "quiz",
      q: "Ответ пришёл с `Cache-Control: max-age=60, s-maxage=3600`. Сколько его держит браузер и сколько CDN?",
      opts: ["Браузер — минуту, CDN — час", "Оба — час", "Оба — минуту", "Браузер — час, CDN — минуту"],
      a: 0,
      why: "`s-maxage` действует только в общих кэшах и перекрывает для них `max-age`. Браузер смотрит на `max-age`.",
    },
    {
      type: "quiz",
      q: "Пользователи жалуются, что видят чужое имя в шапке сайта. Что вероятнее всего случилось?",
      opts: [
        "Личная страница закэшировалась в общем кэше без `private`",
        "У пользователей одинаковые IP-адреса",
        "Браузер перепутал вкладки",
        "Сервер отдал 304 вместо 200",
      ],
      a: 0,
      why: "Общий кэш отдаёт одну копию всем. Если первой в него попала страница Анны, её увидят все следующие.",
    },
  ],
};
