import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "cache7",
  region: 4,
  title: "Service Worker и Cache API",
  q: "Что такое Service Worker? Какие стратегии кэширования он позволяет и как работает сайт без сети?",
  answer:
    "Service Worker — скрипт, который браузер регистрирует для сайта и запускает отдельно от страниц. Он получает событие `fetch` на каждый запрос страниц своего сайта и сам решает, откуда взять ответ: из сети, из своего кэша (Cache API) или собрать на месте. Стратегии: «сначала кэш» для статики, «сначала сеть» для свежих данных, stale-while-revalidate — отдать из кэша и обновить в фоне. Так сайт открывается без сети. Работает только на HTTPS, а новая версия воркера вступает в силу, когда закрыты все вкладки со старой — это частая причина «почему не обновилось».",
  theory: {
    p: [
      "HTTP-кэш браузера управляется заголовками сервера. Service Worker добавляет программируемый слой: это скрипт, который браузер устанавливает для сайта, и он стоит между страницами и сетью. Страница регистрирует его один раз, после этого он перехватывает запросы всех страниц в своей области — даже когда сети нет. Своего доступа к странице у него нет, браузер будит его по событиям и усыпляет.",
      "Хранилище — Cache API: пары «запрос → ответ», которые воркер кладёт и достаёт сам. В отличие от HTTP-кэша, срок жизни записей решает код, а не заголовки. При установке (событие `install`) воркер обычно заранее кладёт в кэш оболочку приложения: HTML, стили, скрипты, иконки.",
      "Стратегии для события `fetch`. Сначала кэш, потом сеть — для статики с хэшем в имени. Сначала сеть, при ошибке кэш — для данных, которые должны быть свежими. Stale-while-revalidate — сразу ответ из кэша и тихое обновление из сети для следующего раза. Только сеть — для платежей и всего, что нельзя кэшировать. Библиотека Workbox собирает эти стратегии готовыми.",
      "Жизненный цикл: регистрация → `install` → ожидание → `activate` → управление страницами. Новая версия воркера устанавливается, но ждёт, пока закроются все вкладки со старой — иначе две версии конфликтовали бы. Поэтому пользователи видят старый сайт после выкладки, пока не закроют вкладки; `skipWaiting` ускоряет переход, но требует осторожности. Service Worker работает только на HTTPS (и на `localhost`), иначе его подменили бы по дороге.",
    ],
    flow: {
      actors: ["Страница", "Service Worker", "Cache API", "Сеть"],
      steps: [
        { from: 0, to: 1, label: "GET /app.3f9a.js → событие fetch" },
        { from: 1, to: 2, label: "сначала кэш: есть — отдаём сразу" },
        { from: 0, to: 1, label: "GET /api/feed" },
        { from: 1, to: 3, label: "сначала сеть", note: "нет сети — возьмём прошлый ответ из кэша" },
        { from: 3, to: 1, label: "свежий ответ, копию кладём в кэш" },
        { from: 1, to: 0, label: "ответ странице" },
      ],
    },
    keys: [
      "Service Worker — скрипт между страницами сайта и сетью, перехватывает каждый запрос в событии `fetch`.",
      "Cache API — хранилище «запрос → ответ» под управлением кода. Стратегии: сначала кэш, сначала сеть, stale-while-revalidate.",
      "Только HTTPS. Новая версия ждёт закрытия всех вкладок со старой — отсюда «сайт не обновился».",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь ресурс и подходящую стратегию Service Worker.",
      pairs: [
        ["`app.3f9a.js` с хэшем в имени", "сначала кэш"],
        ["лента новостей", "сначала сеть, без сети — кэш"],
        ["аватар пользователя", "stale-while-revalidate"],
        ["оплата заказа", "только сеть"],
      ],
      why: "Файл с хэшем никогда не меняется, ленте важна свежесть, аватару — скорость с фоновым обновлением, а оплату кэшировать нельзя.",
    },
    {
      type: "order",
      q: "Расставь этапы жизни новой версии Service Worker.",
      items: [
        "страница регистрирует воркер",
        "`install`: воркер кладёт оболочку сайта в кэш",
        "ожидание, пока закроются вкладки со старой версией",
        "`activate`: новая версия удаляет старые кэши",
        "воркер перехватывает запросы страниц",
      ],
      why: "Ожидание — причина, по которой после выкладки часть пользователей ещё долго видит старую версию.",
    },
    {
      type: "quiz",
      q: "Почему Service Worker работает только на HTTPS?",
      opts: [
        "Он перехватывает все запросы сайта: подменённый по дороге воркер получил бы полный контроль над сайтом",
        "Cache API не умеет хранить HTTP-ответы",
        "Так быстрее",
        "HTTP не поддерживает событие fetch",
      ],
      a: 0,
      why: "Воркер живёт долго и видит каждый запрос. Если бы его можно было подменить в открытой сети, атака сохранялась бы и после ухода из неё.",
    },
  ],
};
