import { json, msg } from "../../http";
import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "ck1",
  region: 4,
  title: "Как устроены cookies",
  q: "Как сервер ставит cookie и когда браузер её отправляет? Что задают `Domain`, `Path` и `Max-Age`?",
  answer:
    "Сервер присылает заголовок `Set-Cookie: имя=значение; атрибуты`, браузер сохраняет пару и сам добавляет её в заголовок `Cookie` каждого подходящего запроса. Какие запросы подходят, решают атрибуты: `Domain` и `Path` задают адреса, `Max-Age` или `Expires` — срок жизни. Без срока cookie сессионная и исчезает при закрытии браузера. Так HTTP без состояния узнаёт пользователя между запросами.",
  theory: {
    p: [
      "Cookie — маленькая пара `имя=значение`, которую сервер просит браузер запомнить: `Set-Cookie: sid=a8f3k2; Path=/; Max-Age=86400`. Дальше браузер сам добавляет её в каждый подходящий запрос: `Cookie: sid=a8f3k2`. По `sid` сервер находит сессию и узнаёт пользователя. Одна cookie — до 4 КБ, а в одном ответе может быть несколько `Set-Cookie`.",
      "Куда отправлять. Без `Domain` cookie привязана ровно к хосту, который её поставил: `shop.ru`, но не `api.shop.ru`. С `Domain=shop.ru` — к домену и всем поддоменам. Поставить cookie чужому домену или целой зоне `.ru` сайт не может. `Path=/account` — только для адресов, которые начинаются с `/account`.",
      "Сколько жить. `Max-Age=86400` — сутки, `Expires` — точная дата. Если указаны оба, побеждает `Max-Age`. Без них cookie сессионная: живёт до закрытия браузера, хотя браузеры с восстановлением вкладок могут её сохранить. Chrome ограничивает срок 400 днями. Чтобы удалить cookie, её присылают снова с `Max-Age=0`.",
      "Префиксы в имени защищают от подмены. `__Secure-sid` браузер примет только с флагом `Secure` и только по HTTPS. `__Host-sid` строже: `Secure`, `Path=/` и без `Domain`, то есть cookie привязана ровно к этому хосту, и поддомен не сможет её перезаписать. Флаги `Secure`, `HttpOnly` и `SameSite` разберём в следующих уроках.",
    ],
    requests: [
      {
        name: "login",
        type: "fetch",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 90], ["Загрузка", 1]],
        request: msg("POST /login HTTP/1.1", [["Host", "shop.ru"]], json("{\"email\":\"anna@mail.ru\"}")),
        response: msg("HTTP/1.1 204 No Content", [["Set-Cookie", "sid=a8f3k2; Path=/; Max-Age=86400"]]),
      },
      {
        name: "account",
        type: "document",
        start: 110,
        timing: [["Очередь", 1], ["Ожидание ответа", 50], ["Загрузка", 5]],
        request: msg("GET /account HTTP/1.1", [["Host", "shop.ru"], ["Cookie", "sid=a8f3k2"]]),
        response: msg("HTTP/1.1 200 OK", [], { type: "text/html; charset=utf-8", text: "<h1>Заказы Анны</h1>" }),
      },
      {
        name: "api.shop.ru/me",
        type: "fetch",
        start: 180,
        timing: [["Очередь", 1], ["DNS", 15], ["TCP", 30], ["TLS", 32], ["Ожидание ответа", 40], ["Загрузка", 1]],
        request: msg("GET /me HTTP/1.1", [["Host", "api.shop.ru"]]),
        response: msg("HTTP/1.1 401 Unauthorized", [["WWW-Authenticate", "Bearer"]]),
      },
    ],
    keys: [
      "`Set-Cookie` в ответе ставит cookie, браузер сам присылает её в `Cookie`.",
      "Без `Domain` — только этот хост, с `Domain` — и поддомены. `Path` сужает адреса.",
      "`Max-Age` или `Expires` — срок, без них cookie сессионная. `Max-Age=0` удаляет.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Сервер `shop.ru` прислал `Set-Cookie: sid=1; Path=/` без `Domain`. Отправит ли браузер её на `api.shop.ru`?",
      opts: [
        "Нет: без `Domain` cookie привязана только к `shop.ru`",
        "Да: поддомены получают cookie всегда",
        "Да, но только по HTTPS",
        "Только если `api.shop.ru` сам попросит",
      ],
      a: 0,
      why: "Без `Domain` cookie привязана ровно к хосту, который её поставил, и `api.shop.ru` её не получит. Чтобы поддомены получали cookie, нужно `Domain=shop.ru`.",
    },
    {
      type: "match",
      q: "Сопоставь атрибут и его действие.",
      pairs: [
        ["`Max-Age=0`", "удалить cookie"],
        ["нет `Max-Age` и `Expires`", "сессионная cookie"],
        ["`Domain=shop.ru`", "домен и все его поддомены"],
        ["`Path=/account`", "только адреса внутри `/account`"],
        ["префикс `__Host-`", "только этот хост, `Secure` и `Path=/`"],
      ],
      why: "Атрибуты задают, куда и как долго браузер будет присылать cookie. Префикс в имени — обещание, которое браузер проверяет при установке.",
    },
    {
      type: "quiz",
      q: "Кто добавляет заголовок `Cookie` в запрос?",
      opts: [
        "Браузер, сам, во все подходящие запросы",
        "Сервер, когда отвечает",
        "Код страницы перед каждым запросом",
        "DNS-резолвер",
      ],
      a: 0,
      why: "Отправка cookie автоматическая: подходят домен, путь и срок — cookie уходит. Поэтому она удобна для сессий, но ею могут злоупотребить чужие сайты — об этом урок про `SameSite`.",
    },
  ],
};
