import { msg } from "../../http";
import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "ck2",
  region: 4,
  title: "Флаги HttpOnly и Secure",
  q: "От чего защищают флаги `HttpOnly` и `Secure`?",
  answer:
    "С `Secure` cookie уходит только по HTTPS, и посредник не перехватит её в открытом HTTP. С `HttpOnly` cookie недоступна скриптам страницы через `document.cookie`, поэтому внедрённый злоумышленником скрипт (XSS) не сможет её прочитать и отправить себе. Сессионную cookie всегда ставят с обоими флагами.",
  theory: {
    p: [
      "`Secure`: браузер отправит cookie только по HTTPS. Без флага хватит одного открытого запроса — например, пользователь набрал `http://shop.ru`, — и cookie уйдёт открытым текстом прямо к посреднику. Для разработки есть исключение: на `localhost` браузеры разрешают `Secure` и по HTTP.",
      "XSS (cross-site scripting) — когда злоумышленнику удалось выполнить свой скрипт на твоём сайте. Например, сайт вывел чужой комментарий с тегом `<script>` как есть. Такой скрипт работает с правами страницы. Первое, что он сделает, — прочитает `document.cookie` и отправит сессию на свой сервер. Подробно XSS разберём в регионе «Безопасность».",
      "`HttpOnly` прячет cookie от скриптов: в `document.cookie` её нет, но браузер по-прежнему отправляет её с запросами. Украсть сессию XSS уже не сможет. Но и от XSS `HttpOnly` не лечит: скрипт всё ещё может отправлять запросы от имени пользователя, пока тот на странице. Это ограничение ущерба, а не защита от атаки.",
      "Итог для сессии: `Set-Cookie: __Host-sid=a8f3k2; Path=/; Secure; HttpOnly; SameSite=Lax`. Cookie, которую читает сам интерфейс, — например, выбранную тему, — ставят без `HttpOnly`. Но секретов в такой cookie держать нельзя.",
    ],
    requests: [
      {
        name: "login",
        type: "fetch",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 90], ["Загрузка", 1]],
        request: msg("POST /login HTTP/2", [[":authority", "shop.ru"]], { type: "application/x-www-form-urlencoded", text: "email=anna%40mail.ru" }),
        response: msg("HTTP/2 204", [
          ["set-cookie", "__Host-sid=a8f3k2; Path=/; Secure; HttpOnly; SameSite=Lax"],
          ["set-cookie", "theme=dark; Path=/; Max-Age=31536000; Secure; SameSite=Lax"],
        ]),
      },
    ],
    keys: [
      "`Secure` — cookie только по HTTPS.",
      "`HttpOnly` — скрипты страницы не видят cookie, XSS не украдёт сессию.",
      "`HttpOnly` ограничивает ущерб, но не отменяет XSS: скрипт всё ещё действует от имени пользователя.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Разложи по флагам: что даёт `Secure`, а что `HttpOnly`?",
      groups: ["`Secure`", "`HttpOnly`"],
      items: [
        ["cookie не уйдёт по открытому HTTP", 0],
        ["скрипт на странице не прочитает cookie", 1],
        ["защищает от перехвата в открытом Wi-Fi", 0],
        ["ограничивает ущерб от XSS", 1],
        ["cookie нет в `document.cookie`", 1],
        ["обязателен для префиксов `__Secure-` и `__Host-`", 0],
      ],
      why: "`Secure` — про канал: только HTTPS. `HttpOnly` — про доступ изнутри страницы: только браузер, без скриптов.",
    },
    {
      type: "quiz",
      q: "На сайте есть XSS, а сессионная cookie стоит с `HttpOnly`. Что всё ещё может сделать скрипт злоумышленника?",
      opts: [
        "Отправлять запросы от имени пользователя, пока тот на странице",
        "Прочитать сессию и войти с другого компьютера",
        "Ничего: `HttpOnly` блокирует все скрипты",
        "Отключить HTTPS на сайте",
      ],
      a: 0,
      why: "Браузер прикладывает cookie к запросам и от чужого скрипта. `HttpOnly` не даёт вынести сессию наружу, но XSS всё равно надо устранять.",
    },
    {
      type: "quiz",
      q: "Почему cookie с выбранной темой оформления ставят без `HttpOnly`?",
      opts: [
        "Её должен читать сам интерфейс, а секретов в ней нет",
        "`HttpOnly` запрещён для долгоживущих cookie",
        "С `HttpOnly` cookie не отправляется на сервер",
        "Иначе тема не сохранится после перезагрузки",
      ],
      a: 0,
      why: "`HttpOnly` нужен секретам. Настройки интерфейса не секрет, и страница должна их читать, чтобы сразу показать нужную тему.",
    },
  ],
};
