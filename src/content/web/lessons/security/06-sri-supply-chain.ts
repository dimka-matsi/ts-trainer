import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "sec6",
  region: 8,
  title: "Subresource Integrity и чужой код",
  q: "Как убедиться, что скрипт с CDN не подменили? Чем опасны зависимости из npm?",
  answer:
    "Subresource Integrity: в теге `<script>` или `<link>` указывают `integrity=\"sha384-…\"` — хэш ожидаемого файла. Браузер скачивает файл, считает хэш и не выполнит его, если он не совпал. Так подмена на CDN не пройдёт. Зависимости из npm опаснее: их код попадает прямо в сборку и может выполняться при установке. От этого защищают lock-файл, `npm ci`, проверка новых пакетов и отключение скриптов установки там, где они не нужны.",
  theory: {
    p: [
      "Скрипт с чужого домена выполняется с правами твоей страницы. Если CDN взломают или домен сменит владельца, вместе с библиотекой придёт вредный код. Так было в 2024 году с `polyfill.io`: домен купили, и более ста тысяч сайтов, подключавших скрипт оттуда, начали раздавать вредоносные перенаправления.",
      "Subresource Integrity (SRI): `<script src=\"https://cdn.jsdelivr.net/npm/lib@2.1.0/lib.min.js\" integrity=\"sha384-oqVu…\" crossorigin=\"anonymous\">`. Браузер считает хэш скачанного файла и сравнивает. Не совпало — не выполняет, в консоли ошибка. `crossorigin=\"anonymous\"` нужен, чтобы браузер мог проверить ответ с другого источника (через CORS). SRI работает только для файлов с фиксированной версией: `lib@latest` меняется, и хэш сломается.",
      "Зависимости из npm опаснее: их код попадает прямо в твою сборку, и SRI тут не поможет. Способы атаки: захват аккаунта мейнтейнера (в 2025 году через фишинг заразили популярные пакеты `chalk` и `debug`), опечатки в именах (в 2017 году пакет `crossenv` воровал переменные окружения у тех, кто хотел поставить `cross-env`), скрипты `postinstall`, которые выполняются прямо при `npm install` на твоей машине и в CI.",
      "Что делать. Коммитить lock-файл и ставить через `npm ci` — точно те версии, что проверены. Не обновлять всё подряд вслепую, смотреть, что за пакет добавляют. Запускать `npm audit` и сканеры. Отключать скрипты установки (`--ignore-scripts`) там, где они не нужны. Своя копия файлов вместо подключения с чужого CDN убирает целый класс рисков.",
    ],
    requests: [
      {
        name: "lib.min.js",
        type: "script",
        start: 0,
        timing: [["Очередь", 1], ["DNS", 12], ["TCP", 20], ["TLS", 22], ["Ожидание ответа", 25], ["Загрузка", 6]],
        request: msg("GET /npm/lib@2.1.0/lib.min.js HTTP/2", [[":authority", "cdn.jsdelivr.net"], ["origin", "https://shop.ru"], ["sec-fetch-mode", "cors"]]),
        response: msg("HTTP/2 200", [["access-control-allow-origin", "*"], ["cache-control", "public, max-age=31536000, immutable"], ["content-type", "application/javascript; charset=utf-8"], ["content-length", "24310"]]),
      },
    ],
    keys: [
      "Чужой скрипт работает с правами твоей страницы. Взлом CDN или домена — взлом твоего сайта.",
      "SRI: `integrity` с хэшем и `crossorigin`. Не совпало — не выполнится. Только для фиксированных версий.",
      "npm: lock-файл и `npm ci`, проверка новых пакетов, осторожно со скриптами установки.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "На CDN подменили файл библиотеки, а у тега `<script>` на сайте стоит `integrity` с хэшем старой версии. Что произойдёт?",
      opts: [
        "Браузер скачает файл, увидит несовпадение хэша и не выполнит его",
        "Браузер выполнит новый файл и обновит хэш",
        "CDN вернёт старую версию",
        "Сайт перестанет открываться целиком",
      ],
      a: 0,
      why: "SRI проверяет содержимое, а не адрес. Подменённый файл по тому же адресу не пройдёт проверку.",
    },
    {
      type: "sort",
      q: "Что защищает от атак через зависимости?",
      groups: ["Помогает", "Не помогает"],
      items: [
        ["lock-файл в репозитории и `npm ci`", 0],
        ["`--ignore-scripts` при установке", 0],
        ["проверка пакета перед добавлением", 0],
        ["HTTPS на сайте", 1],
        ["SRI на теге `<script>` для кода из сборки", 1],
      ],
      why: "Код зависимостей попадает прямо в сборку, так что SRI и HTTPS его не проверяют. Помогает контроль над тем, что и как устанавливается.",
    },
    {
      type: "quiz",
      q: "Зачем SRI-тегу `<script>` с другого домена атрибут `crossorigin=\"anonymous\"`?",
      opts: [
        "Чтобы браузер получил ответ через CORS и смог проверить его хэш",
        "Чтобы не отправлять IP-адрес",
        "Чтобы скрипт выполнился быстрее",
        "Чтобы разрешить скрипту читать cookies",
      ],
      a: 0,
      why: "Без режима CORS ответ с чужого источника для проверки закрыт, и браузер откажется выполнять скрипт с `integrity`.",
    },
  ],
};
