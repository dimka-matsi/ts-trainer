import { msg } from "../../http";
import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "sec3",
  region: 8,
  title: "Защита от CSRF",
  q: "Как защищают от CSRF: `SameSite` и CSRF-токен?",
  answer:
    "CSRF опасен, когда сервер узнаёт пользователя по cookie, которую браузер прикладывает сам. Первая линия — `SameSite=Lax` или `Strict`: межсайтовые `POST` уходят без cookie. Вторая — CSRF-токен: сервер кладёт в форму случайное значение, связанное с сессией, и проверяет его при отправке; чужой сайт этот токен узнать не может. Ещё проверяют заголовки `Origin` или `Sec-Fetch-Site` и никогда не меняют данные по `GET`.",
  theory: {
    p: [
      "Корень CSRF: браузер сам прикладывает cookie к запросам, откуда бы они ни пришли. Если сервер узнаёт пользователя только по cookie, запрос с `evil.com` для него неотличим от настоящего. Когда токен передают вручную в `Authorization`, чужой сайт его не знает, и классический CSRF невозможен.",
      "`SameSite=Lax` отсекает межсайтовые `POST`, картинки и iframe. Но есть оговорки. Если сайт меняет данные по `GET`, `Lax` пропустит переход по ссылке. Атаку с захваченного поддомена браузер считает запросом с того же сайта. А старые браузеры `SameSite` не понимают. Поэтому для важных действий добавляют ещё защиту.",
      "CSRF-токен (synchronizer token): сервер создаёт случайное значение, хранит его в сессии и вставляет в каждую форму скрытым полем `csrf_token`. При отправке сравнивает. `evil.com` может отправить форму на твой сайт, но прочитать токен со страницы не может — мешает политика одного источника. Вариант без хранения на сервере — double submit: токен и в cookie, и в поле формы, сервер проверяет, что они совпадают.",
      "Проверка источника: браузеры добавляют заголовки `Origin` и `Sec-Fetch-Site` (`same-origin`, `same-site`, `cross-site`). Сервер может просто отклонять изменяющие запросы с чужих сайтов. Плюс базовая гигиена: `GET` ничего не меняет, а для перевода денег или смены почты просят подтверждение паролем или кодом.",
    ],
    requests: [
      {
        name: "transfer (своя форма)",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 90], ["Загрузка", 1]],
        request: msg("POST /transfer HTTP/2", [
          [":authority", "bank.ru"],
          ["origin", "https://bank.ru"],
          ["sec-fetch-site", "same-origin"],
          ["cookie", "__Host-sid=a8f3k2"],
        ], { type: "application/x-www-form-urlencoded", text: "to=anna&sum=500&csrf_token=Zq81xK" }),
        response: msg("HTTP/2 303", [["location", "/transfer/done"]]),
      },
      {
        name: "transfer (с evil.com)",
        type: "document",
        start: 110,
        timing: [["Очередь", 1], ["Ожидание ответа", 40], ["Загрузка", 1]],
        request: msg("POST /transfer HTTP/2", [
          [":authority", "bank.ru"],
          ["origin", "https://evil.com"],
          ["sec-fetch-site", "cross-site"],
        ], { type: "application/x-www-form-urlencoded", text: "to=hacker&sum=50000" }),
        response: msg("HTTP/2 403", [], { type: "text/plain; charset=utf-8", text: "CSRF-токен не совпал" }),
      },
    ],
    keys: [
      "CSRF возможен, когда пользователя узнают по cookie, которую браузер прикладывает сам.",
      "Защита: `SameSite`, CSRF-токен в форме, проверка `Origin` или `Sec-Fetch-Site`.",
      "`GET` ничего не меняет. Для опасных действий — повторное подтверждение.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Почему чужой сайт не может подставить правильный CSRF-токен в свою форму?",
      opts: [
        "Токен лежит на странице твоего сайта, а прочитать её мешает политика одного источника",
        "Токен зашифрован HTTPS",
        "Браузер удаляет скрытые поля из чужих форм",
        "Токен хранится в `HttpOnly`-cookie",
      ],
      a: 0,
      why: "Отправить форму на твой сайт `evil.com` может, а прочитать ответ с токеном — нет. Без токена сервер отклонит запрос.",
    },
    {
      type: "quiz",
      q: "SPA отправляет токен доступа в заголовке `Authorization`, cookies не использует. Нужна ли защита от классического CSRF?",
      opts: [
        "Нет: чужой сайт не знает токен, а браузер сам его не приложит",
        "Да, как и с cookie",
        "Да, но только для `GET`",
        "Нет, потому что включён CORS",
      ],
      a: 0,
      why: "CSRF работает за счёт автоматической отправки учётных данных. Заголовок `Authorization` браузер сам не добавляет. Но такой токен тогда хранится доступно для скриптов, и растёт цена XSS.",
    },
    {
      type: "match",
      q: "Сопоставь механизм и то, как он мешает CSRF.",
      pairs: [
        ["`SameSite=Lax`", "межсайтовый `POST` уходит без cookie"],
        ["CSRF-токен", "в запросе нужно значение, которое чужой сайт не может узнать"],
        ["`Sec-Fetch-Site: cross-site`", "сервер видит, что запрос пришёл с чужого сайта"],
        ["повторное подтверждение паролем", "без пароля опасное действие не пройдёт"],
      ],
      why: "Защиты складываются: даже если одна не сработает (старый браузер, поддомен), остальные остановят атаку.",
    },
  ],
};
