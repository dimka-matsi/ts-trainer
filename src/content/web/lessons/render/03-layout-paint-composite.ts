import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "rnd3",
  region: 10,
  title: "Layout, paint и composite",
  q: "Что вызывает перерасчёт раскладки и почему анимировать `transform` дешевле, чем `top`?",
  answer:
    "Изменение размеров или положения (`width`, `top`, `margin`, шрифт, добавление элементов) запускает layout заново, а за ним paint и composite — это дорого, особенно для большой страницы. Изменение только вида (`color`, `background`, `box-shadow`) пропускает layout, но требует paint. А `transform` и `opacity` на отдельном слое меняются только на этапе composite, на видеокарте, без раскладки и перерисовки. Поэтому анимации делают через `transform` и `opacity`: так проще уложиться в 16 мс на кадр.",
  theory: {
    p: [
      "Экран обновляется 60 раз в секунду (часто и чаще), значит, на кадр около 16 мс. Если браузер не успевает, кадр пропускается, и анимация дёргается. Сколько работы нужно на кадр, зависит от того, какое свойство изменилось.",
      "Layout — самое дорогое. `width`, `height`, `top`, `left`, `margin`, `padding`, размер шрифта, новый текст, добавление элементов — меняются размеры и положения, и браузеру нужно пересчитать раскладку, иногда всей страницы: сосед сдвинулся, за ним сдвинулись другие. Потом снова paint и composite.",
      "Paint без layout: `color`, `background`, `box-shadow`, `border-radius` не меняют геометрию, раскладку можно не трогать, но пиксели надо нарисовать заново. Composite без того и другого: `transform` и `opacity`, если элемент на отдельном слое. Видеокарта просто сдвигает, масштабирует или делает прозрачной уже нарисованную картинку слоя.",
      "Поэтому движение делают через `transform: translateX(…)`, а не `left`, а появление — через `opacity`. Подсказать браузеру заранее вынести элемент на свой слой можно через `will-change: transform`, но слои едят видеопамять — ставят точечно. Ещё одна ловушка — чередовать чтение размеров элемента и изменение стилей: браузер каждый раз вынужден делать layout немедленно (layout thrashing). Проверяют всё это во вкладке Performance в DevTools.",
    ],
    flow: {
      actors: ["Стиль", "Layout", "Paint", "Composite"],
      steps: [
        { from: 0, to: 1, label: "top: 10px → 20px", note: "геометрия изменилась: все этапы" },
        { from: 1, to: 2, label: "пересчитали положение — перерисовать" },
        { from: 2, to: 3, label: "собрать кадр" },
        { from: 0, to: 2, label: "background: white → gray", note: "геометрия та же: без layout" },
        { from: 2, to: 3, label: "собрать кадр" },
        { from: 0, to: 3, label: "transform: translateX(10px)", note: "только composite на видеокарте" },
      ],
    },
    keys: [
      "Геометрия (`width`, `top`, `margin`, текст) — layout, paint, composite: дорого.",
      "Вид (`color`, `background`, `box-shadow`) — paint и composite.",
      "`transform` и `opacity` на своём слое — только composite. Для анимаций берут их.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "С какого этапа начнёт работу браузер при изменении свойства?",
      groups: ["Layout", "Paint", "Только composite"],
      items: [
        ["`width`", 0],
        ["`margin-top`", 0],
        ["`background-color`", 1],
        ["`box-shadow`", 1],
        ["`transform`", 2],
        ["`opacity`", 2],
      ],
      why: "Если меняются размеры или положение — нужна раскладка. Если только вид — перерисовка. `transform` и `opacity` видеокарта применяет к готовому слою.",
    },
    {
      type: "quiz",
      q: "Модальное окно выезжает анимацией `top` от −300px до 0 и подтормаживает. Как исправить?",
      opts: [
        "Анимировать `transform: translateY()` вместо `top`",
        "Анимировать `margin-top`",
        "Увеличить длительность анимации",
        "Добавить `box-shadow`",
      ],
      a: 0,
      why: "`top` на каждом кадре запускает layout. `transform` меняется на этапе composite и не трогает раскладку остальной страницы.",
    },
    {
      type: "quiz",
      q: "Почему `will-change: transform` не ставят на все элементы подряд?",
      opts: [
        "Каждый слой занимает видеопамять, и слишком много слоёв замедляют страницу",
        "Это свойство не поддерживают браузеры",
        "Оно отключает анимации",
        "Оно блокирует разбор HTML",
      ],
      a: 0,
      why: "Слой — отдельная картинка в памяти видеокарты. Точечно слои ускоряют анимацию, массово — съедают память и замедляют сборку кадра.",
    },
  ],
};
