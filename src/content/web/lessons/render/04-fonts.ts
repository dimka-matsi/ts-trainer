import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rnd4",
  region: 10,
  title: "Шрифты: FOIT, FOUT и font-display",
  q: "Что такое FOIT и FOUT и зачем `font-display`?",
  answer:
    "Пока веб-шрифт грузится, браузер либо прячет текст (FOIT — flash of invisible text), либо показывает его запасным шрифтом, а потом подменяет (FOUT — flash of unstyled text). `font-display` выбирает поведение: `swap` сразу показывает запасной шрифт и меняет, когда загрузится; `optional` даёт шрифту около 100 мс и иначе оставляет запасной до следующего визита; `block` прячет текст до 3 секунд. Шрифт загружают раньше через `preload`, отдают в WOFF2 и подбирают запасной по размерам, чтобы подмена не сдвигала страницу.",
  theory: {
    p: [
      "Веб-шрифт подключают через `@font-face`, и браузер начинает его загрузку, только когда видит текст, которому он нужен. То есть поздно: после HTML и CSS. Что показать, пока шрифта нет?",
      "По умолчанию большинство браузеров прячут текст до 3 секунд (FOIT), потом показывают запасной шрифт, а когда придёт нужный — подменяют. На медленной сети пользователь видит страницу без текста. Другой подход — сразу показать запасной шрифт и потом подменить (FOUT): текст читается сразу, но в момент подмены строки могут перескочить.",
      "`font-display` в `@font-face` выбирает поведение. `block` — прятать до 3 секунд, для иконочных шрифтов. `swap` — сразу запасной, подмена в любой момент: для основного текста. `fallback` — прятать около 100 мс, подменить, если шрифт пришёл в течение 3 секунд, иначе оставить запасной. `optional` — около 100 мс, иначе запасной до конца визита, а в кэше шрифт будет к следующему разу: никаких скачков.",
      "Как ускорить и сгладить. Отдавать шрифт в WOFF2, оставить только нужные символы и начертания (subsetting). Начать загрузку раньше: `<link rel=\"preload\" as=\"font\" type=\"font/woff2\" href=\"…\" crossorigin>` — атрибут `crossorigin` нужен всегда, даже для своего домена, потому что шрифты грузятся в режиме CORS. Подобрать запасной шрифт по метрикам через `size-adjust` и `ascent-override`, чтобы подмена почти не сдвигала текст.",
    ],
    requests: [
      {
        name: "index.html",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 55], ["Загрузка", 5]],
        request: msg("GET / HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [], {
          type: "text/html; charset=utf-8",
          text: "<link rel=\"preload\" as=\"font\" type=\"font/woff2\" href=\"/fonts/onest.woff2\" crossorigin>\n<link rel=\"stylesheet\" href=\"/style.css\">",
        }),
      },
      {
        name: "onest.woff2",
        type: "font",
        start: 62,
        timing: [["Очередь", 1], ["Ожидание ответа", 35], ["Загрузка", 25]],
        request: msg("GET /fonts/onest.woff2 HTTP/2", [[":authority", "shop.ru"], ["origin", "https://shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "font/woff2"], ["content-length", "31200"], ["cache-control", "max-age=31536000, immutable"]]),
      },
      {
        name: "style.css",
        type: "stylesheet",
        start: 63,
        timing: [["Очередь", 1], ["Ожидание ответа", 38], ["Загрузка", 6]],
        request: msg("GET /style.css HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [], {
          type: "text/css",
          text: "@font-face{font-family:Onest;src:url(/fonts/onest.woff2) format(\"woff2\");font-display:swap}",
        }),
      },
    ],
    keys: [
      "FOIT — текст невидим, пока грузится шрифт. FOUT — сначала запасной шрифт, потом подмена.",
      "`font-display`: `swap` для текста, `optional` без скачков, `block` для иконок.",
      "WOFF2, subsetting, `preload` с `crossorigin`, подбор запасного шрифта по метрикам.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь значение `font-display` и поведение.",
      pairs: [
        ["`block`", "прятать текст до 3 секунд"],
        ["`swap`", "сразу запасной шрифт, подмена в любой момент"],
        ["`fallback`", "коротко прятать, подменить, только если шрифт пришёл быстро"],
        ["`optional`", "шрифт не успел за ~100 мс — запасной до конца визита"],
      ],
      why: "Выбор между «текст сразу» и «без скачков». `swap` — сразу, `optional` — без скачков, `fallback` — посередине.",
    },
    {
      type: "quiz",
      q: "Шрифт подключён через `<link rel=\"preload\" as=\"font\" href=\"/f.woff2\">` без `crossorigin`. Что будет?",
      opts: [
        "Шрифт скачается дважды: предзагрузка не совпадёт с запросом из CSS, который идёт в режиме CORS",
        "Всё сработает",
        "Шрифт не загрузится вообще",
        "Браузер покажет ошибку CSP",
      ],
      a: 0,
      why: "Шрифты всегда запрашиваются в режиме CORS. Предзагрузка без `crossorigin` — это другой запрос, и браузер её не переиспользует.",
    },
    {
      type: "quiz",
      q: "Какое значение `font-display` выбрать, если главное — чтобы текст не прыгал после загрузки?",
      opts: ["`optional`", "`swap`", "`block`", "`auto`"],
      a: 0,
      why: "`optional` не подменяет шрифт позже 100 мс, поэтому скачка не будет. В следующий раз шрифт уже будет в кэше и отрисуется сразу.",
    },
  ],
};
