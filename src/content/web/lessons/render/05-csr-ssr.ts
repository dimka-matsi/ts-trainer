import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rnd5",
  region: 7,
  level: "middle",
  title: "CSR, SSR, SSG и гидратация",
  q: "Чем рендеринг на клиенте отличается от серверного и статической генерации? Что такое гидратация?",
  answer:
    "CSR: сервер отдаёт почти пустой HTML и скрипт, а страницу строит браузер — первый экран появляется поздно, поисковикам сложнее, зато дальше приложение быстрое. SSR: сервер на каждый запрос отдаёт готовый HTML — контент виден сразу, но нужен сервер, и ответ дольше. SSG: HTML собирают заранее при сборке и раздают с CDN — быстрее всего, но данные обновляются только при пересборке (ISR пересобирает страницы в фоне). Гидратация — когда скрипт в браузере «оживляет» готовый серверный HTML: навешивает обработчики и связывает его с состоянием.",
  theory: {
    p: [
      "CSR (client-side rendering). Сервер отдаёт `<div id=\"root\"></div>` и ссылку на скрипт. Браузер скачивает скрипт, выполняет его, тот запрашивает данные у API и только потом рисует страницу. До этого пользователь видит пустоту или скелетон. Поисковые роботы, которые не выполняют скрипты, видят пустую страницу. Зато переходы внутри приложения быстрые: сервер отдаёт только данные.",
      "SSR (server-side rendering). На каждый запрос сервер собирает HTML с данными и отдаёт готовую страницу. Контент виден, как только пришёл HTML, и поисковикам всё понятно. Цена — сервер на каждом запросе и время на сборку ответа (TTFB больше). Потоковый SSR отправляет HTML частями: шапку сразу, медленные блоки — по готовности.",
      "SSG (static site generation). HTML собирают один раз при сборке и раздают как файлы, обычно с CDN. Это самый быстрый ответ, но данные меняются только при новой сборке. ISR (incremental static regeneration) — компромисс: страница кэшируется и пересобирается в фоне по таймеру или по событию, как `stale-while-revalidate` для HTML.",
      "Гидратация. HTML от SSR или SSG виден, но неживой: кнопки не работают, пока не загрузится и не выполнится скрипт. Скрипт проходит по готовому DOM, связывает его со своим состоянием и навешивает обработчики — это гидратация. Промежуток «вижу, но не нажимается» ухудшает отзывчивость. Поэтому появились частичная гидратация и острова (islands): оживлять только интерактивные части, а статичный текст оставить HTML.",
    ],
    requests: [
      {
        name: "CSR: index.html",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 25], ["Загрузка", 1]],
        request: msg("GET /product/7 HTTP/2", [[":authority", "spa.shop.ru"]]),
        response: msg("HTTP/2 200", [["cache-control", "no-cache"]], {
          type: "text/html; charset=utf-8",
          text: "<div id=\"root\"></div><script type=\"module\" src=\"/app.js\"></script>",
        }),
      },
      {
        name: "CSR: app.js",
        type: "script",
        start: 30,
        timing: [["Очередь", 1], ["Ожидание ответа", 30], ["Загрузка", 60]],
        request: msg("GET /app.js HTTP/2", [[":authority", "spa.shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "text/javascript"], ["content-length", "250000"]]),
      },
      {
        name: "CSR: api/product/7",
        type: "fetch",
        start: 180,
        timing: [["Очередь", 1], ["Ожидание ответа", 70], ["Загрузка", 2]],
        request: msg("GET /api/product/7 HTTP/2", [[":authority", "spa.shop.ru"]]),
        response: msg("HTTP/2 200", [], { type: "application/json", text: "{\"title\":\"Чайник\",\"price\":1990}" }),
      },
      {
        name: "SSR: product/7",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 95], ["Загрузка", 4]],
        request: msg("GET /product/7 HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [], {
          type: "text/html; charset=utf-8",
          text: "<h1>Чайник</h1><p>1990 ₽</p><script type=\"module\" src=\"/app.js\"></script>",
        }),
      },
    ],
    keys: [
      "CSR — страницу строит браузер: поздний первый экран, быстрые переходы.",
      "SSR — готовый HTML на каждый запрос. SSG — HTML при сборке. ISR — пересборка в фоне.",
      "Гидратация оживляет серверный HTML. Острова оживляют только интерактивные части.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Какой способ рендеринга подходит лучше всего?",
      groups: ["SSG", "SSR", "CSR"],
      items: [
        ["документация, которая меняется раз в неделю", 0],
        ["блог с редкими публикациями", 0],
        ["страница товара с актуальной ценой и остатком", 1],
        ["лента новостей, важная для поисковиков", 1],
        ["внутренняя админка за входом", 2],
        ["онлайн-редактор графики", 2],
      ],
      why: "Если данные меняются редко — HTML можно собрать заранее. Если часто и важны поисковики — собирать на сервере. Если страница за входом и живёт как программа — достаточно клиента.",
    },
    {
      type: "quiz",
      q: "Во вкладке «Сеть» у CSR-страницы товара три запроса подряд: HTML, скрипт, данные. Почему первый экран появляется поздно?",
      opts: [
        "Контент можно нарисовать, только когда скачался и выполнился скрипт и пришли данные",
        "HTML слишком большой",
        "Браузер ждёт гидратацию",
        "Сервер отвечает медленно из-за SSR",
      ],
      a: 0,
      why: "Цепочка из трёх последовательных запросов плюс выполнение скрипта. При SSR контент приходит уже в первом ответе.",
    },
    {
      type: "quiz",
      q: "Что такое гидратация?",
      opts: [
        "Скрипт в браузере связывает готовый серверный HTML с состоянием и навешивает обработчики",
        "Сервер повторно отправляет HTML",
        "Сжатие HTML перед отправкой",
        "Загрузка шрифтов после отрисовки",
      ],
      a: 0,
      why: "До гидратации страница видна, но кнопки не работают. Поэтому большой скрипт гидратации ухудшает отзывчивость даже при быстром SSR.",
    },
  ],
};
