import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "rnd1",
  region: 10,
  title: "Critical rendering path",
  q: "Что происходит от получения HTML до первого кадра на экране?",
  answer:
    "Браузер разбирает байты HTML в токены и строит DOM — дерево элементов. Параллельно из CSS строит CSSOM — дерево стилей. Из них собирается дерево отрисовки: только видимые элементы с вычисленными стилями. Затем layout считает размеры и положения, paint рисует пиксели по слоям, а composite собирает слои в кадр. Этот путь до первого кадра и называют critical rendering path: чем меньше на нём блокирующих ресурсов, тем раньше пользователь что-то увидит.",
  theory: {
    p: [
      "HTML приходит байтами. Парсер превращает их в токены (открывающий тег, текст, закрывающий тег) и строит DOM — дерево узлов. Парсер работает потоково: DOM растёт по мере прихода байтов, браузер не ждёт весь файл. Встретив `<link rel=stylesheet>` или `<img>`, он сразу запрашивает эти ресурсы.",
      "CSS разбирается в CSSOM — дерево правил. В отличие от HTML, CSS нельзя применять частично: правило в конце файла может перекрыть правило в начале. Поэтому браузер ждёт весь CSS, прежде чем рисовать. CSS блокирует отрисовку (render-blocking).",
      "DOM и CSSOM дают дерево отрисовки (render tree): только то, что видно, с вычисленными стилями. `display: none` и `<head>` туда не попадают, а `visibility: hidden` попадает — место он занимает. Дальше layout (раскладка, в Firefox — reflow) считает для каждого блока размер и положение. Paint превращает блоки в команды рисования: фон, текст, рамки, тени — по слоям. Composite собирает слои в итоговый кадр, обычно на видеокарте.",
      "Первый кадр (First Contentful Paint) появляется, когда есть DOM для видимой части и весь блокирующий CSS. Ускорить его — значит сократить путь: меньше и раньше CSS, скрипты без блокировки, критичные стили прямо в HTML, меньше байт до первого экрана. Дальше каждое изменение страницы может запускать этапы заново, и стоимость зависит от того, с какого этапа начать — об этом урок про layout, paint и composite.",
    ],
    flow: {
      actors: ["Сеть", "Парсер HTML", "CSS", "Раскладка\nlayout", "Отрисовка\npaint", "Сборка\ncomposite"],
      steps: [
        { from: 0, to: 1, label: "байты HTML → токены → DOM", note: "по мере прихода байтов" },
        { from: 1, to: 0, label: "нашёл <link rel=stylesheet> — запросить CSS" },
        { from: 0, to: 2, label: "style.css → CSSOM", note: "ждём весь файл: CSS блокирует отрисовку" },
        { from: 2, to: 3, label: "DOM + CSSOM → дерево отрисовки → размеры и положения" },
        { from: 3, to: 4, label: "команды рисования по слоям" },
        { from: 4, to: 5, label: "слои → кадр на экране", note: "First Contentful Paint" },
      ],
    },
    keys: [
      "HTML → DOM, CSS → CSSOM, вместе → дерево отрисовки только из видимого.",
      "Дальше layout (размеры), paint (пиксели), composite (слои в кадр).",
      "CSS блокирует отрисовку: до первого кадра нужен весь CSS для страницы.",
    ],
  },
  tasks: [
    {
      type: "order",
      q: "Расставь этапы от байтов HTML до кадра.",
      items: ["разбор HTML в DOM", "построение CSSOM", "дерево отрисовки", "layout: размеры и положения", "paint: команды рисования", "composite: сборка слоёв в кадр"],
      why: "Чтобы считать размеры, нужны и элементы, и стили. Рисовать можно только то, что разложено. Собирать кадр — из нарисованных слоёв.",
    },
    {
      type: "quiz",
      q: "Какие элементы не попадут в дерево отрисовки?",
      opts: ["С `display: none`", "С `visibility: hidden`", "С `opacity: 0`", "За пределами экрана"],
      a: 0,
      why: "`display: none` убирает элемент из раскладки полностью. `visibility: hidden` и `opacity: 0` скрывают, но место элемент занимает, поэтому в дереве он есть.",
    },
    {
      type: "quiz",
      q: "Почему браузер не рисует страницу, пока не загрузит весь CSS?",
      opts: [
        "Правило в конце файла может перекрыть начало, и страница перерисовалась бы с прыжками",
        "CSS нельзя скачать по частям",
        "Без CSS не строится DOM",
        "Так требует HTTPS",
      ],
      a: 0,
      why: "Рисовать с неполными стилями — показать пользователю мелькание неоформленной страницы. Поэтому CSS блокирует отрисовку, и его стараются сделать маленьким и ранним.",
    },
  ],
};
