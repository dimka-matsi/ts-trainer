import { msg } from "../../../course/http";
import type { WebLesson } from "../../../course/types";

const HTML = "<head>\n  <link rel=\"stylesheet\" href=\"/style.css\">\n  <script src=\"/legacy.js\"></script>\n  <script defer src=\"/app.js\"></script>\n  <script async src=\"/analytics.js\"></script>\n</head>";

export const lesson: WebLesson = {
  id: "rnd2",
  region: 7,
  level: "junior",
  title: "Блокирующие ресурсы: async и defer",
  q: "Почему CSS и скрипты в `<head>` задерживают отрисовку? Чем `async` отличается от `defer`?",
  answer:
    "CSS блокирует отрисовку: без него браузер не рисует. Обычный `<script>` блокирует разбор HTML: парсер останавливается, скачивает и выполняет скрипт, потому что скрипт может изменить документ. `defer` скачивает скрипт параллельно и выполняет после разбора всего HTML, по порядку. `async` скачивает параллельно и выполняет сразу, как скачался, без порядка — подходит для независимых скриптов вроде аналитики. Модули (`type=module`) ведут себя как `defer` по умолчанию.",
  theory: {
    p: [
      "Обычный `<script src>` без атрибутов останавливает парсер. Скрипт может изменить документ, пока он разбирается, поэтому браузер ждёт: скачать, выполнить, потом разбирать дальше. Больше того, скрипт ждёт загрузки CSS выше него, ведь он может спросить стили. Поэтому классический совет — ставить скрипты в конец `<body>`.",
      "Браузер частично спасает положение сканером предзагрузки (preload scanner): пока парсер стоит, отдельный быстрый просмотрщик бежит по HTML вперёд и заранее запрашивает найденные скрипты, стили и картинки. Но выполнение всё равно идёт по порядку.",
      "`defer`: скачать параллельно с разбором, выполнить после того, как весь HTML разобран, в порядке появления в документе, до события DOMContentLoaded. Это правильный выбор для кода приложения. `async`: скачать параллельно, выполнить сразу по готовности, прервав разбор, и в любом порядке. Подходит для независимых скриптов: аналитика, реклама. `<script type=\"module\">` по умолчанию ведёт себя как `defer`.",
      "CSS: подключать рано, в `<head>`, и держать маленьким. Стили, которые не нужны сейчас, можно отделить: для печати — `media=\"print\"`, и такой CSS не блокирует отрисовку экрана. Стили первого экрана иногда вставляют прямо в HTML, чтобы не ждать отдельного запроса. Во вкладке «Сеть» этого урока все файлы качаются параллельно — это работа сканера предзагрузки. Разница между ними не в загрузке, а в том, когда их выполняют.",
    ],
    requests: [
      {
        name: "index.html",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 60], ["Загрузка", 8]],
        request: msg("GET / HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [], { type: "text/html; charset=utf-8", text: HTML }),
      },
      {
        name: "style.css",
        type: "stylesheet",
        start: 70,
        timing: [["Очередь", 1], ["Ожидание ответа", 40], ["Загрузка", 30]],
        request: msg("GET /style.css HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "text/css"], ["content-length", "48000"]]),
      },
      {
        name: "legacy.js",
        type: "script",
        start: 72,
        timing: [["Очередь", 1], ["Ожидание ответа", 38], ["Загрузка", 20]],
        request: msg("GET /legacy.js HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "text/javascript"], ["content-length", "30000"]]),
      },
      {
        name: "app.js",
        type: "script",
        start: 73,
        timing: [["Очередь", 1], ["Ожидание ответа", 40], ["Загрузка", 45]],
        request: msg("GET /app.js HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "text/javascript"], ["content-length", "184000"]]),
      },
      {
        name: "analytics.js",
        type: "script",
        start: 74,
        timing: [["Очередь", 1], ["Ожидание ответа", 42], ["Загрузка", 10]],
        request: msg("GET /analytics.js HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "text/javascript"], ["content-length", "22000"]]),
      },
    ],
    keys: [
      "CSS блокирует отрисовку, обычный `<script>` — разбор HTML.",
      "`defer`: параллельно, выполнить после разбора, по порядку. `async`: параллельно, сразу по готовности, без порядка.",
      "Модули ведут себя как `defer`. Код приложения — `defer`, независимые скрипты — `async`.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь тег и поведение.",
      pairs: [
        ["`<script src>`", "останавливает разбор HTML до выполнения"],
        ["`<script defer src>`", "выполняется после разбора HTML, по порядку"],
        ["`<script async src>`", "выполняется сразу по загрузке, без порядка"],
        ["`<script type=\"module\">`", "по умолчанию ведёт себя как `defer`"],
      ],
      why: "Все, кроме обычного, скачиваются параллельно с разбором. Различаются моментом выполнения и порядком.",
    },
    {
      type: "quiz",
      q: "Скрипт `app.js` зависит от `vendor.js`. Как их подключить, чтобы не блокировать разбор и сохранить порядок?",
      opts: ["Оба с `defer`, `vendor.js` раньше", "Оба с `async`", "`app.js` с `async`, `vendor.js` без атрибутов", "Оба без атрибутов в `<head>`"],
      a: 0,
      why: "`defer` гарантирует порядок выполнения как в документе. С `async` кто первым скачался, тот и выполнится, и `app.js` может запуститься раньше `vendor.js`.",
    },
    {
      type: "quiz",
      q: "Какой CSS не блокирует отрисовку страницы на экране?",
      opts: [
        "`<link rel=\"stylesheet\" href=\"print.css\" media=\"print\">`",
        "`<link rel=\"stylesheet\" href=\"style.css\">` в конце `<head>`",
        "Стили во встроенном `<style>`",
        "CSS, подключённый в `<body>`",
      ],
      a: 0,
      why: "Браузер скачает стили для печати с низким приоритетом и не будет их ждать: к экрану они не относятся.",
    },
  ],
};
