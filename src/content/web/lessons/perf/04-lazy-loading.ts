import { msg } from "../../http";
import type { WebLesson } from "../../types";

const HTML = "<img src=\"/img/hero.avif\" fetchpriority=\"high\" width=\"1200\" height=\"600\" alt=\"Распродажа\">\n…\n<img src=\"/img/review-1.avif\" loading=\"lazy\" width=\"400\" height=\"300\" alt=\"Отзыв\">\n<iframe src=\"https://maps.example/embed\" loading=\"lazy\"></iframe>";

export const lesson: WebLesson = {
  id: "perf4",
  region: 12,
  title: "Ленивая загрузка",
  q: "Как отложить загрузку картинок и iframe ниже первого экрана? Какую картинку лениво грузить нельзя?",
  answer:
    "Атрибут `loading=\"lazy\"` у `<img>` и `<iframe>` откладывает загрузку, пока элемент не приблизится к видимой области: браузер начнёт заранее, за некоторое расстояние до экрана. Так первый экран получает весь канал, а то, до чего пользователь не долистал, вообще не качается. Главную картинку первого экрана лениво грузить нельзя — она определяет, когда пользователь увидит страницу. Ей, наоборот, ставят `fetchpriority=\"high\"`.",
  theory: {
    p: [
      "Страница товара: большой баннер наверху, а ниже — 40 картинок отзывов и встроенная карта. Без подсказок браузер начнёт качать всё сразу, и баннер будет делить канал с картинками, которые пользователь, может, никогда не увидит.",
      "`loading=\"lazy\"` откладывает загрузку `<img>` и `<iframe>` до тех пор, пока элемент не окажется рядом с видимой областью. Браузер сам решает, насколько заранее начинать, с учётом скорости сети: на медленной сети — раньше. Ничего писать не нужно, это просто атрибут. Встроенные карты и видео с чужих сайтов особенно выигрывают: iframe тянет за собой сотни килобайт скриптов.",
      "Ошибка, которую часто делают, — поставить `loading=\"lazy\"` на всё, включая первый экран. Главная картинка тогда начинает грузиться позже: браузер сначала считает раскладку, чтобы понять, видна ли она. Картинку первого экрана грузят сразу и с `fetchpriority=\"high\"`, а лениво — только то, что ниже.",
      "Чтобы ленивые картинки не сдвигали страницу, у них тоже должны быть `width` и `height`. Для кода тот же принцип: сборщик делит приложение на части, и код страницы оплаты грузится, только когда пользователь туда пошёл. Всё, что не нужно для первого экрана, можно отложить.",
    ],
    requests: [
      {
        name: "product/7",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 50], ["Загрузка", 4]],
        request: msg("GET /product/7 HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [], { type: "text/html; charset=utf-8", text: HTML }),
      },
      {
        name: "hero.avif",
        type: "avif",
        start: 60,
        timing: [["Очередь", 1], ["Ожидание ответа", 25], ["Загрузка", 40]],
        request: msg("GET /img/hero.avif HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "image/avif"], ["content-length", "96000"]]),
      },
      {
        name: "review-1.avif",
        type: "avif",
        start: 2400,
        timing: [["Очередь", 1], ["Ожидание ответа", 24], ["Загрузка", 8]],
        request: msg("GET /img/review-1.avif HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "image/avif"], ["content-length", "14000"]]),
      },
    ],
    keys: [
      "`loading=\"lazy\"` у `<img>` и `<iframe>` откладывает загрузку до приближения к экрану.",
      "Первый экран лениво не грузят: главной картинке — `fetchpriority=\"high\"`.",
      "У ленивых картинок тоже нужны `width` и `height`. Код тоже делят на части и грузят по требованию.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Каким элементам страницы товара поставить `loading=\"lazy\"`?",
      groups: ["Лениво", "Сразу"],
      items: [
        ["картинки отзывов внизу страницы", 0],
        ["встроенная карта в подвале", 0],
        ["видео с чужого сайта в середине описания", 0],
        ["главное фото товара на первом экране", 1],
        ["логотип в шапке", 1],
      ],
      why: "Лениво — всё, что ниже первого экрана и может так и не понадобиться. Видимое сразу должно грузиться сразу.",
    },
    {
      type: "quiz",
      q: "На главную картинку первого экрана поставили `loading=\"lazy\"`. Что будет?",
      opts: [
        "Она начнёт грузиться позже: браузер сначала проверит, видна ли она, и страница покажется позже",
        "Ничего не изменится",
        "Она загрузится быстрее",
        "Она не загрузится вообще",
      ],
      a: 0,
      why: "Ленивой загрузке нужна раскладка, чтобы понять положение элемента. Для главной картинки это чистая задержка.",
    },
    {
      type: "quiz",
      q: "Картинка отзыва начала загружаться через 2,4 секунды после страницы, хотя сам запрос занял 30 мс. Почему?",
      opts: [
        "У неё `loading=\"lazy\"`, и браузер начал загрузку, когда пользователь долистал до неё",
        "Сервер медленно отвечал",
        "Картинка очень большая",
        "Браузер ждал DNS",
      ],
      a: 0,
      why: "Ожидание ответа и загрузка короткие, значит, сеть ни при чём. Запрос просто отправили позже, как и задумано.",
    },
  ],
};
