import { msg } from "../../http";
import type { WebLesson } from "../../types";

const HTML = "<img\n  src=\"/img/sofa-800.jpg\"\n  srcset=\"/img/sofa-400.avif 400w, /img/sofa-800.avif 800w, /img/sofa-1600.avif 1600w\"\n  sizes=\"(max-width: 600px) 100vw, 50vw\"\n  width=\"800\" height=\"600\" alt=\"Диван\">";

export const lesson: WebLesson = {
  id: "perf3",
  region: 12,
  title: "Картинки: форматы и размеры",
  q: "Какие форматы картинок выбрать и как отдать разный размер под разные экраны?",
  answer:
    "Фотографии — AVIF или WebP: они на 30–50% легче JPEG при том же качестве. Иконки и логотипы — SVG. PNG — только для графики с прозрачностью, где нужна точность пикселей. Размер под экран задают `srcset` с вариантами ширины и `sizes` с шириной картинки в вёрстке: браузер сам выберет подходящий файл с учётом плотности экрана. `<picture>` нужен, чтобы дать запасной формат или другую картинку для мобильных. И всегда указывают `width` и `height`, чтобы под картинку заранее оставили место.",
  theory: {
    p: [
      "Картинки — обычно самая тяжёлая часть страницы. Форматы: JPEG — фото, старый и универсальный. PNG — без потерь, для скриншотов и графики с прозрачностью, для фото слишком тяжёл. WebP — легче JPEG на 25–35%, поддерживается везде. AVIF — ещё легче, особенно на высоком сжатии, тоже везде в современных браузерах, но кодируется медленнее. SVG — векторная графика: иконки и логотипы в любом размере весят килобайты.",
      "Размер под экран. Картинка шириной 1600 пикселей на телефоне шириной 400 — это четыре лишних размера по каждой стороне. `srcset=\"sofa-400.avif 400w, sofa-800.avif 800w, sofa-1600.avif 1600w\"` перечисляет варианты с их шириной. `sizes=\"(max-width: 600px) 100vw, 50vw\"` говорит, какой ширины картинка будет в вёрстке. Браузер умножает на плотность экрана и берёт ближайший подходящий файл.",
      "`<picture>` — когда нужна не просто другая ширина. Внутри `<source type=\"image/avif\">`, потом `<source type=\"image/webp\">`, и в конце `<img>` с JPEG: браузер возьмёт первый понятный ему формат. Или `<source media=\"(max-width: 600px)\">` с другой картинкой для телефона, например квадратной обрезкой.",
      "`width` и `height` в теге — не размер отображения, а пропорции. Браузер заранее оставит место нужной высоты, и текст не прыгнет вниз, когда картинка загрузится. Резать и конвертировать картинки вручную не нужно: это делает сборка или сервис картинок на CDN, который по параметрам в адресе отдаёт нужный размер и формат.",
    ],
    requests: [
      {
        name: "product/7",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 50], ["Загрузка", 4]],
        request: msg("GET /product/7 HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [], { type: "text/html; charset=utf-8", text: HTML }),
      },
      {
        name: "sofa-800.avif",
        type: "avif",
        start: 60,
        timing: [["Очередь", 1], ["Ожидание ответа", 25], ["Загрузка", 20]],
        request: msg("GET /img/sofa-800.avif HTTP/2", [[":authority", "shop.ru"], ["accept", "image/avif,image/webp,image/*"]]),
        response: msg("HTTP/2 200", [["content-type", "image/avif"], ["content-length", "38400"], ["cache-control", "max-age=31536000, immutable"]]),
      },
    ],
    keys: [
      "Фото — AVIF или WebP, иконки — SVG, PNG — для графики с прозрачностью.",
      "`srcset` с вариантами ширины и `sizes` с шириной в вёрстке: браузер выберет файл сам.",
      "`<picture>` — для запасных форматов и другой обрезки. `width` и `height` — чтобы ничего не прыгало.",
    ],
  },
  tasks: [
    {
      type: "sort",
      q: "Какой формат лучше подходит?",
      groups: ["SVG", "AVIF или WebP", "PNG"],
      items: [
        ["логотип компании", 0],
        ["иконка корзины", 0],
        ["фото товара", 1],
        ["баннер с фотографией на главной", 1],
        ["скриншот интерфейса с мелким текстом", 2],
      ],
      why: "Вектор масштабируется без потерь и весит мало. Фото лучше всего сжимают современные форматы. Скриншоту с текстом нужна точность без артефактов сжатия.",
    },
    {
      type: "quiz",
      q: "Картинка с `srcset` из 400w, 800w и 1600w и `sizes=\"100vw\"` на телефоне шириной 400 пикселей с плотностью экрана 2. Какой файл выберет браузер?",
      opts: ["800w", "400w", "1600w", "Все три"],
      a: 0,
      why: "Картинка займёт 400 CSS-пикселей, а при плотности 2 это 800 физических. Ближайший подходящий вариант — 800w.",
    },
    {
      type: "quiz",
      q: "Зачем тегу `<img>` атрибуты `width` и `height`, если размер задан в CSS?",
      opts: [
        "Браузер знает пропорции заранее и оставляет место, поэтому контент не прыгает при загрузке",
        "Без них картинка не загрузится",
        "Они уменьшают размер файла",
        "Они нужны только для поисковиков",
      ],
      a: 0,
      why: "Из `width` и `height` браузер считает пропорцию и резервирует высоту ещё до загрузки файла. Без них текст ниже сдвинется, когда картинка придёт.",
    },
  ],
};
