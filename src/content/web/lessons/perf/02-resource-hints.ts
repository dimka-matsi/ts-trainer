import { msg } from "../../http";
import type { WebLesson } from "../../types";

const HEAD = "<link rel=\"preconnect\" href=\"https://cdn.shop.ru\" crossorigin>\n<link rel=\"dns-prefetch\" href=\"https://stats.io\">\n<link rel=\"preload\" as=\"image\" href=\"/img/hero.avif\" fetchpriority=\"high\">\n<link rel=\"prefetch\" href=\"/checkout.js\">";

export const lesson: WebLesson = {
  id: "perf2",
  region: 12,
  title: "preload, prefetch, preconnect",
  q: "Чем отличаются `preload`, `prefetch`, `preconnect` и `dns-prefetch`?",
  answer:
    "Это подсказки браузеру начать работу раньше. `dns-prefetch` заранее узнаёт IP чужого домена, `preconnect` сразу открывает соединение — DNS, TCP и TLS, — чтобы первый запрос туда не ждал рукопожатий. `preload` скачивает с высоким приоритетом ресурс, нужный этой странице, но найденный браузером поздно: шрифт, фоновую картинку. `prefetch` с низким приоритетом скачивает то, что понадобится на следующей странице. Ещё сервер может отправить подсказки заранее кодом `103 Early Hints`.",
  theory: {
    p: [
      "Браузер находит ресурсы по мере разбора: шрифт — только из CSS, фоновую картинку — только после расчёта стилей. Подсказки в `<head>` позволяют начать раньше. Их четыре, и отличаются они тем, что именно начинать и для какой страницы.",
      "`dns-prefetch` — только DNS для чужого домена: дёшево, можно для десятка доменов. `preconnect` — DNS, TCP и TLS целиком: экономит 100–300 мс на первом запросе к домену, но держит соединение открытым, поэтому только для двух-трёх самых важных доменов, например CDN со шрифтами. Для шрифтов и других ресурсов в режиме CORS нужен `crossorigin`, иначе браузер откроет другое соединение.",
      "`preload` — «этот ресурс нужен текущей странице, скачай сейчас»: `<link rel=\"preload\" as=\"font\" …>`. Атрибут `as` обязателен, он задаёт приоритет и тип. Если ресурс так и не использовали за несколько секунд, браузер пишет предупреждение в консоль — лишний `preload` отнимает канал у важного. `prefetch` — «понадобится на следующей странице»: качается с самым низким приоритетом, когда сеть свободна, и ложится в кэш.",
      "`fetchpriority=\"high\"` повышает приоритет конкретной картинки или скрипта, например главной картинки первого экрана. `103 Early Hints` — сервер отвечает промежуточным кодом с заголовками `Link: <…>; rel=preload` сразу, ещё до того, как собрал страницу. Браузер начинает качать ресурсы, пока сервер думает над HTML.",
    ],
    requests: [
      {
        name: "index.html (103)",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 12], ["Загрузка", 1]],
        request: msg("GET / HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 103", [["link", "</img/hero.avif>; rel=preload; as=image"], ["link", "<https://cdn.shop.ru>; rel=preconnect"]]),
      },
      {
        name: "index.html",
        type: "document",
        start: 0,
        timing: [["Очередь", 1], ["Ожидание ответа", 180], ["Загрузка", 6]],
        request: msg("GET / HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [], { type: "text/html; charset=utf-8", text: HEAD }),
      },
      {
        name: "hero.avif",
        type: "avif",
        start: 20,
        timing: [["Очередь", 1], ["Ожидание ответа", 30], ["Загрузка", 45]],
        request: msg("GET /img/hero.avif HTTP/2", [[":authority", "shop.ru"]]),
        response: msg("HTTP/2 200", [["content-type", "image/avif"], ["content-length", "84112"]]),
      },
      {
        name: "checkout.js (prefetch)",
        type: "script",
        start: 400,
        timing: [["Очередь", 20], ["Ожидание ответа", 35], ["Загрузка", 25]],
        request: msg("GET /checkout.js HTTP/2", [[":authority", "shop.ru"], ["sec-purpose", "prefetch"]]),
        response: msg("HTTP/2 200", [["content-type", "text/javascript"], ["content-length", "64000"], ["cache-control", "max-age=31536000, immutable"]]),
      },
    ],
    keys: [
      "`dns-prefetch` — только DNS. `preconnect` — DNS, TCP и TLS заранее, для 2–3 важных доменов.",
      "`preload` — ресурс этой страницы, высокий приоритет, `as` обязателен. `prefetch` — для следующей страницы, низкий приоритет.",
      "`fetchpriority` поднимает приоритет, `103 Early Hints` отдаёт подсказки до готовности HTML.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь подсказку и когда её использовать.",
      pairs: [
        ["`dns-prefetch`", "много сторонних доменов, нужен только их IP"],
        ["`preconnect`", "важный чужой домен, запрос к нему будет скоро"],
        ["`preload`", "шрифт этой страницы, который браузер найдёт поздно"],
        ["`prefetch`", "скрипт страницы оплаты, куда пользователь пойдёт дальше"],
      ],
      why: "Подсказки отличаются тем, сколько работы делают заранее и для какой страницы: текущей или следующей.",
    },
    {
      type: "quiz",
      q: "На страницу добавили 15 `preload` на всё подряд, и главная картинка стала грузиться медленнее. Почему?",
      opts: [
        "Все 15 ресурсов получили высокий приоритет и делят канал с главной картинкой",
        "`preload` отключает кэш",
        "Браузер игнорирует больше десяти подсказок",
        "`preload` работает только для шрифтов",
      ],
      a: 0,
      why: "Высокий приоритет у всех — это высокий приоритет ни у кого. `preload` ставят точечно, на то, что действительно нужно раньше.",
    },
    {
      type: "quiz",
      q: "Что даёт ответ `103 Early Hints`?",
      opts: [
        "Браузер начинает качать важные ресурсы, пока сервер ещё собирает HTML",
        "Сервер сообщает, что страница переехала",
        "Браузер показывает страницу из кэша",
        "Сервер просит клиента повторить запрос",
      ],
      a: 0,
      why: "Пока сервер 180 мс собирает страницу, канал простаивает. Ранние подсказки заполняют это время загрузкой картинки и соединением с CDN.",
    },
  ],
};
