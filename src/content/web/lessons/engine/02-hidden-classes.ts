import type { WebLesson } from "../../types";

export const lesson: WebLesson = {
  id: "js2",
  region: 15,
  title: "Скрытые классы",
  q: "Что такое скрытые классы (hidden classes, shapes) и почему важен порядок полей объекта?",
  answer:
    "Скрытый класс — внутреннее описание формы объекта: какие поля и по каким смещениям они лежат в памяти. Объекты с одинаковым набором полей в одинаковом порядке разделяют один скрытый класс, и движок читает поле по готовому смещению, не ища его по имени. Каждое добавленное поле — переход к новому скрытому классу, поэтому `{ x, y }` и `{ y, x }` — разные формы. Удаление поля может перевести объект в медленный режим словаря. Правило: создавай объекты одного вида одинаково — все поля сразу и в одном порядке.",
  theory: {
    p: [
      "В JavaScript у объекта можно в любой момент добавить или удалить поле. Если бы движок хранил каждый объект как словарь «имя → значение», каждое чтение поля было бы поиском по имени. Вместо этого V8 заводит скрытый класс (в V8 он называется Map, в других движках — shape): описание, какие поля есть у объекта и где в его памяти лежит каждое.",
      "Объекты делят скрытый класс, если у них одинаковые поля, добавленные в одинаковом порядке. Сто тысяч точек `{ x, y }` имеют один скрытый класс, и описание хранится один раз, а в каждом объекте — только значения. Чтение `x` — это «взять значение по смещению 0», без поиска имени.",
      "Скрытые классы образуют цепочку переходов. Пустой объект — класс C0. Добавили `x` — переход в C1 «есть `x`». Добавили `y` — переход в C2 «есть `x` и `y`». Если другой объект получил сначала `y`, а потом `x`, он пройдёт по другой ветке и окажется в другом классе, хотя поля те же. Для движка это разные формы, и код, который работает с обеими, оптимизируется хуже.",
      "Что делать. Создавать объекты одного вида в одном месте (конструктор, фабрика, литерал) и сразу со всеми полями, даже если значения пока пустые (`null`). Не добавлять поля потом по условию. Не удалять поля через `delete`: движок может перевести объект в режим словаря, где всё медленнее, — лучше записать `undefined` или `null`. Не менять тип значения поля без нужды: число, потом строка — тоже меняет описание.",
    ],
    flow: {
      actors: ["C0\n{}", "C1\n{ x }", "C2\n{ x, y }", "C3\n{ y }", "C4\n{ y, x }"],
      steps: [
        { from: 0, to: 1, label: "добавили x", note: "объект A" },
        { from: 1, to: 2, label: "добавили y", note: "A в классе C2" },
        { from: 0, to: 1, label: "объект B: добавили x" },
        { from: 1, to: 2, label: "добавили y", note: "B тоже в C2 — класс общий" },
        { from: 0, to: 3, label: "объект D: сначала y" },
        { from: 3, to: 4, label: "потом x", note: "D в другом классе C4, хотя поля те же" },
      ],
    },
    keys: [
      "Скрытый класс описывает поля объекта и их смещения. Одинаковые формы делят один класс.",
      "Каждое новое поле — переход к новому классу, порядок добавления важен.",
      "Создавай объекты сразу со всеми полями в одном порядке, не удаляй поля через `delete`.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Объект A создали как `{ x: 1, y: 2 }`, объект B — пустым, а потом добавили ему `y`, затем `x`. Одинаковые ли у них скрытые классы?",
      opts: [
        "Нет: поля добавлялись в разном порядке, это разные ветки переходов",
        "Да: поля одинаковые",
        "Да: скрытые классы зависят только от количества полей",
        "У объектов нет скрытых классов",
      ],
      a: 0,
      why: "Скрытый класс запоминает историю: какие поля и в каком порядке появились. Разный порядок — разные классы.",
    },
    {
      type: "sort",
      q: "Что помогает объектам сохранять одну форму, а что ломает её?",
      groups: ["Помогает", "Ломает"],
      items: [
        ["создавать все объекты одного вида одной фабрикой", 0],
        ["задавать все поля сразу, пустые — как `null`", 0],
        ["добавлять поле `discount` только некоторым товарам", 1],
        ["удалять поле через `delete`", 1],
        ["класть в одно поле то число, то строку", 1],
      ],
      why: "Движок любит предсказуемость: одинаковые поля, одинаковый порядок, одинаковые типы значений.",
    },
    {
      type: "quiz",
      q: "Что может случиться с объектом после `delete` поля?",
      opts: [
        "Движок переведёт его в медленный режим словаря",
        "Объект будет удалён сборщиком мусора",
        "Все объекты этого класса потеряют поле",
        "Ничего, `delete` всегда бесплатен",
      ],
      a: 0,
      why: "Удаление ломает цепочку переходов. Проще всего движку перестать отслеживать форму и хранить поля как словарь, где доступ медленнее.",
    },
  ],
};
