import type { FollowUp } from "../../course/types";

/** Уточняющие вопросы к урокам регионов «Типы и значения» … «Массивы и коллекции». */
export const BASICS: Record<string, FollowUp[]> = {
  val1: [
    {
      level: "middle",
      q: "Почему `typeof null` равен `\"object\"`? Откуда эта ошибка?",
      a: "В первой реализации значения хранились с тегом типа в младших битах, и у объектов тег был нулевым. `null` был представлен нулевым указателем, поэтому его тег тоже читался как «объект». Исправление предлагали в ES2015, но отказались: оно сломало бы существующие сайты.",
    },
    {
      level: "senior",
      q: "Как V8 хранит числа внутри: что такое Smi и heap number?",
      a: "Небольшие целые числа V8 хранит прямо в указателе — это Smi (small integer), без выделения памяти. Остальные числа, дробные и большие, лежат в куче как HeapNumber, и переменная указывает на них. Поэтому арифметика на небольших целых быстрее, а массив только из Smi хранится компактнее, чем массив с дробями.",
    },
  ],
  val2: [
    {
      level: "middle",
      q: "Как в JavaScript передаются аргументы: по значению или по ссылке?",
      a: "Всегда по значению, но для объектов значение — это ссылка. Такой способ называют call by sharing: функция получает копию ссылки, поэтому может менять свойства объекта, но присваивание параметру не меняет переменную снаружи. Настоящей передачи по ссылке, как `ref` в C#, в JavaScript нет.",
    },
    {
      level: "middle",
      q: "Как сравнить два объекта по содержимому и почему в языке нет встроенного способа?",
      a: "Встроенного глубокого сравнения нет, потому что нет однозначного ответа: учитывать ли прототип, порядок ключей, неперечисляемые свойства, циклы. Для простых данных пишут `deepEqual` или берут `isEqual` из lodash, в тестах — `toEqual`. `JSON.stringify(a) === JSON.stringify(b)` ненадёжен: зависит от порядка ключей и теряет `undefined`.",
    },
  ],
  val3: [
    {
      level: "middle",
      q: "Что выведет `[] + {}` и `{} + []` в консоли браузера и почему результат разный?",
      a: "`[] + {}` — строка `\"[object Object]\"`: пустой массив становится `\"\"`, объект — `\"[object Object]\"`. Если в консоли написать `{} + []` в начале строки, `{}` разбирается как пустой блок кода, остаётся `+[]`, а это число `0`. В выражении, например `console.log({} + [])`, результат тоже будет строкой `\"[object Object]\"`.",
    },
    {
      level: "senior",
      q: "Как спецификация описывает приведение: что такое абстрактные операции ToPrimitive, ToNumber и ToString?",
      a: "Спецификация описывает приведение через абстрактные операции — внутренние алгоритмы. `ToPrimitive` превращает объект в примитив с подсказкой `\"number\"` или `\"string\"`, вызывая `Symbol.toPrimitive`, `valueOf` и `toString`. `ToNumber` и `ToString` превращают примитивы по таблицам: например, `ToNumber(\"\")` — `0`, `ToNumber(undefined)` — `NaN`. Операторы вроде `+` и `==` — это цепочки таких операций, поэтому любой «странный» результат можно вывести по спецификации.",
    },
  ],
  val4: [
    {
      level: "middle",
      q: "Как работает алгоритм `==` для `[] == false` шаг за шагом?",
      a: "Если одна сторона логическая, она приводится к числу: `false` становится `0`. Дальше объект сравнивается с числом, поэтому массив приводится к примитиву: `[]` → `\"\"`. Строка сравнивается с числом через приведение к числу: `\"\"` → `0`. Получается `0 == 0` — `true`, хотя `[]` в `if` истинен.",
    },
    {
      level: "middle",
      q: "Чем отличаются SameValue, SameValueZero и строгое равенство и где используется каждое?",
      a: "Строгое равенство `===` считает `NaN` не равным себе, а `+0` и `-0` равными. SameValue — это `Object.is`: `NaN` равен `NaN`, `+0` не равен `-0`. SameValueZero — как SameValue, но `+0` равен `-0`; его используют `includes`, `Map` и `Set`. React сравнивает состояние и зависимости через `Object.is`.",
    },
  ],
  val5: [
    {
      level: "middle",
      q: "Почему `toFixed` иногда округляет «неправильно», например `(1.005).toFixed(2)` даёт `\"1.00\"`?",
      a: "Число `1.005` нельзя точно записать в двоичном виде, и на самом деле хранится `1.00499999999999989…`. `toFixed` честно округляет это значение вниз. Для денег считают в целых копейках, а для показа используют `Intl.NumberFormat`, которое тоже работает с хранимым значением, так что точность закладывают на этапе вычислений.",
    },
    {
      level: "senior",
      q: "Как устроено 64-битное число IEEE 754 и почему безопасные целые заканчиваются на 2⁵³?",
      a: "В числе 1 бит знака, 11 бит порядка и 52 бита мантиссы, плюс неявная ведущая единица — всего 53 значащих бита. Пока целое помещается в 53 бита, оно представлено точно, а после 2⁵³ шаг между соседними числами становится 2 и больше. Поэтому `Number.MAX_SAFE_INTEGER` равен 2⁵³ − 1, а большие id из базы передают строкой или `BigInt`.",
    },
  ],
  val6: [
    {
      level: "middle",
      q: "Чем `structuredClone` отличается от копирования через spread и что он не умеет?",
      a: "`structuredClone` делает глубокую копию и понимает даты, `Map`, `Set`, типизированные массивы, `ArrayBuffer`, ошибки и циклические ссылки. Он не копирует функции и элементы DOM — бросает `DataCloneError`, а у экземпляров классов теряет прототип, превращая их в обычные объекты. Геттеры вычисляются и сохраняются как значения.",
    },
    {
      level: "senior",
      q: "Как неизменяемые обновления влияют на производительность и что такое структурное разделение?",
      a: "При неизменяемом обновлении копируется только путь от корня до изменённого места, а нетронутые ветки переиспользуются по ссылке — это структурное разделение. Поэтому обновление дешёвое, а сравнение «изменилось ли» — одна проверка ссылки. Библиотеки вроде Immer дают писать мутирующий код, а под капотом строят такие копии через `Proxy`.",
    },
  ],
  val7: [
    {
      level: "middle",
      q: "Как посчитать количество видимых символов в строке с эмодзи и флагами?",
      a: "`length` считает кодовые единицы UTF-16, а `[...str].length` — кодовые точки, но и это не «символ на экране»: флаг или эмодзи семьи — несколько точек. Видимые символы (графемы) считают через `Intl.Segmenter` с `granularity: \"grapheme\"`. Это важно для лимитов длины и обрезки строк: обрезка по `slice` может разорвать эмодзи.",
    },
    {
      level: "middle",
      q: "Что такое тегированные шаблонные строки и где их используют?",
      a: "Функцию можно поставить перед шаблонной строкой: `sql` + обратные кавычки с `${id}` внутри. Функция получает массив статических частей и значения отдельно, поэтому может их экранировать или обработать. Так устроены безопасные SQL-запросы в некоторых библиотеках, `styled-components`, `graphql`-теги и `String.raw`.",
    },
  ],
  val8: [
    {
      level: "middle",
      q: "Какие встроенные символы ты знаешь и что каждый настраивает?",
      a: "`Symbol.iterator` делает объект перебираемым, `Symbol.asyncIterator` — асинхронно перебираемым. `Symbol.toPrimitive` управляет приведением к примитиву, `Symbol.hasInstance` — поведением `instanceof`, `Symbol.toStringTag` — результатом `Object.prototype.toString`. Через них язык даёт встроить свои объекты в стандартные механизмы.",
    },
    {
      level: "middle",
      q: "Можно ли использовать символ как настоящее приватное поле?",
      a: "Нет: символьные ключи скрыты от `Object.keys` и `JSON.stringify`, но видны через `Object.getOwnPropertySymbols` и `Reflect.ownKeys`. Это защита от случайных пересечений имён, а не от чтения. Настоящая приватность — поля `#name` в классах или переменные в замыкании.",
    },
  ],
  scp1: [
    {
      level: "middle",
      q: "Почему `var` на верхнем уровне скрипта становится свойством `window`, а `let` — нет?",
      a: "У глобальной области две части: объектная запись — сам глобальный объект — и декларативная запись. `var` и объявления функций попадают в объектную запись, то есть становятся свойствами `window`. `let`, `const` и `class` — в декларативную: они глобальные, но свойствами объекта не являются. В модулях верхний уровень вообще не глобальный.",
    },
    {
      level: "middle",
      q: "Можно ли переобъявить переменную `let` во вложенном блоке? Что такое затенение?",
      a: "Да: `let x` во вложенном блоке создаёт новую переменную, которая закрывает внешнюю с тем же именем — это затенение. Внутри блока видна только внутренняя, после блока снова внешняя. Повторное объявление в том же блоке — `SyntaxError`. Затенение законно, но часто путает, поэтому линтеры предупреждают о нём.",
    },
  ],
  scp2: [
    {
      level: "middle",
      q: "Поднимаются ли `let`, `const` и `class`? Как это доказать?",
      a: "Поднимаются: их имена регистрируются в начале области, но остаются неинициализированными до строки объявления. Доказательство — затенение: если снаружи есть `let x = 1`, а внутри блока до `let x = 2` прочитать `x`, будет `ReferenceError`, а не `1`. Значит, внутренняя `x` уже существует с начала блока, просто в мёртвой зоне.",
    },
    {
      level: "senior",
      q: "Что такое контекст выполнения и из каких этапов состоит его создание?",
      a: "Контекст выполнения создаётся для глобального кода, каждого вызова функции, модуля и `eval`. Сначала этап создания: окружение, привязки для всех объявлений, `this`. Функции-объявления получают значение сразу, `var` — `undefined`, `let` и `const` остаются неинициализированными. Затем этап выполнения построчно. Контексты складываются в стек вызовов.",
    },
  ],
  scp3: [
    {
      level: "middle",
      q: "Сохраняет ли замыкание все переменные внешней функции или только используемые?",
      a: "По спецификации функция держит ссылку на всё окружение, но движки оптимизируют: V8 выносит в контекст только переменные, которые используются вложенными функциями. Подвох в том, что контекст общий для всех замыканий одной функции: если одно замыкание использует большой массив, он останется жить, пока жива любая из вложенных функций.",
    },
    {
      level: "senior",
      q: "Как замыкания приводят к утечкам памяти в SPA и как их искать?",
      a: "Типичный случай — подписка или таймер, чей колбэк замкнул данные компонента и не был снят при уходе со страницы. Колбэк держит окружение, окружение — данные и иногда элементы DOM. Ищут во вкладке Memory: снимок кучи до и после сценария, сравнение и цепочка удерживающих ссылок до `Closure`. Лечат снятием подписок и таймеров при размонтировании.",
    },
  ],
  scp4: [
    {
      level: "middle",
      q: "Как задачу с циклом и `setTimeout` решали до появления `let`?",
      a: "Создавали новую область на каждую итерацию: IIFE `(function (j) { setTimeout(() => console.log(j)); })(i)`, отдельную функцию-фабрику, `bind` с аргументом или третий аргумент `setTimeout(fn, 0, i)`. Все способы передают текущее значение параметром, а у каждого вызова свой параметр. `let` в `for` делает то же самое автоматически.",
    },
    {
      level: "middle",
      q: "Создаётся ли новая переменная на каждой итерации для `let` в `for...of` и в `while`?",
      a: "В `for...of` и `for...in` — да, у каждой итерации своя привязка, в том числе для `const`. В `while` цикл ничего не создаёт сам: если `let` объявлен до цикла, переменная одна, а если внутри тела — новая на каждой итерации, потому что тело — новый блок. Особое копирование значения из прошлой итерации есть только у классического `for (let ...)`.",
    },
  ],
  scp5: [
    {
      level: "middle",
      q: "Чем паттерн «модуль» на замыканиях отличается от ES-модулей?",
      a: "Паттерн «модуль» — это IIFE, возвращающая объект с публичными методами, а приватное живёт в замыкании. ES-модули дают то же на уровне языка: у файла своя область, экспортируется только нужное, и загрузчик знает зависимости заранее. Кроме того, ES-модули анализируются статически, что даёт tree shaking.",
    },
    {
      level: "senior",
      q: "Приватное поле `#x` или замыкание — что выбрать и чем они различаются?",
      a: "Замыкание прячет данные в функции, но каждый объект получает свои копии методов, и наследование неудобно. `#x` — приватное поле класса: методы общие в прототипе, доступ только из тела класса, проверка `#x in obj` работает как «брендовая» проверка. Сейчас для классов выбирают `#`, а замыкания — для фабрик функций и модулей.",
    },
  ],
  fun1: [
    {
      level: "middle",
      q: "Чем объект `arguments` опасен и почему его заменили rest-параметрами?",
      a: "`arguments` — псевдомассив без методов массива, его нет у стрелочных функций. В нестрогом режиме он связан с параметрами: изменение `arguments[0]` меняет первый параметр, что мешает оптимизации. Утечка `arguments` в другую функцию раньше отключала оптимизации V8. Rest-параметр — честный массив, и код понятнее.",
    },
    {
      level: "middle",
      q: "Что такое именованное функциональное выражение и зачем имя?",
      a: "В выражении `const f = function fact(n) { ... }` имя `fact` видно только внутри функции. Это позволяет надёжно вызвать себя рекурсивно, даже если внешнюю переменную переприсвоят. Кроме того, имя попадает в стек вызовов при ошибках и в профилировщик, что облегчает отладку.",
    },
  ],
  fun2: [
    {
      level: "middle",
      q: "Что будет с `this`, если вызвать связанную через `bind` функцию через `new`?",
      a: "`new` сильнее `bind`: создаётся новый объект, и `this` указывает на него, а привязанный объект игнорируется. Аргументы, заданные в `bind`, при этом сохраняются. Это единственное правило, которое перебивает `bind`.",
    },
    {
      level: "senior",
      q: "Как `this` определяется в геттере, методе прототипа и функции, переданной в `setTimeout`?",
      a: "В геттере и методе прототипа `this` — объект перед точкой, даже если метод найден в прототипе: `child.method()` получает `child`. В `setTimeout` браузер вызывает обычную функцию с `this`, равным глобальному объекту, даже в строгом режиме, а в Node.js — с объектом таймера. Поэтому в колбэки таймеров передают стрелку или функцию с `bind`.",
    },
  ],
  fun3: [
    {
      level: "middle",
      q: "Почему обработчик в классе часто пишут полем-стрелкой, а не методом? Какие у этого минусы?",
      a: "Поле-стрелка создаётся в конструкторе и навсегда берёт `this` экземпляра, поэтому её можно передать как колбэк без `bind`. Минусы: у каждого экземпляра своя копия функции — больше памяти; её нет в прототипе, поэтому её трудно переопределить в наследнике и замокать в тестах через прототип.",
    },
    {
      level: "middle",
      q: "Можно ли использовать стрелку как метод прототипа или конструктор? Почему?",
      a: "Как конструктор — нет: у стрелки нет внутреннего метода `[[Construct]]` и свойства `prototype`, поэтому `new` бросает `TypeError`. Как метод прототипа — технически можно, но `this` будет внешним, а не объектом, так что метод бесполезен. Стрелки задуманы как лёгкие функции для колбэков.",
    },
  ],
  fun4: [
    {
      level: "middle",
      q: "Как `bind` реализован по спецификации и чем связанная функция отличается от обычной?",
      a: "`bind` создаёт экзотический объект — bound function — с внутренними слотами целевой функции, `this` и заранее заданных аргументов. У неё нет `prototype`, её имя — `\"bound \"` плюс имя оригинала, а `length` уменьшен на число заданных аргументов. При вызове через `new` привязанный `this` игнорируется.",
    },
    {
      level: "middle",
      q: "Зачем `call` и `apply` при заимствовании методов и где это встречается?",
      a: "Метод можно вызвать на объекте, у которого его нет: `Array.prototype.slice.call(arguments)` превращал псевдомассив в массив, `Object.prototype.hasOwnProperty.call(obj, key)` проверяет ключ, даже если у `obj` свой `hasOwnProperty` или нет прототипа. Сейчас для этого есть `Array.from` и `Object.hasOwn`, но приём встречается в библиотеках.",
    },
  ],
  fun5: [
    {
      level: "middle",
      q: "Чем каррирование отличается от частичного применения?",
      a: "Каррирование превращает функцию многих аргументов в цепочку функций по одному: `f(a)(b)(c)`. Частичное применение фиксирует часть аргументов и возвращает функцию от остальных, в любом их количестве: `f.bind(null, a)` ждёт `b` и `c` сразу. Каррированную функцию удобно частично применять по шагам, но это разные понятия.",
    },
    {
      level: "senior",
      q: "Почему чистые функции важны для React и кэширования?",
      a: "Чистую функцию можно вызвать повторно, в другом порядке или не вызывать, взяв результат из кэша, — результат не изменится. React опирается на это: он может рендерить компонент дважды в StrictMode, прерывать и повторять рендер, а React Compiler мемоизирует результаты. Побочный эффект во время рендера ломает эти оптимизации.",
    },
  ],
  fun6: [
    {
      level: "middle",
      q: "Что такое хвостовая рекурсия и почему в JavaScript на неё нельзя полагаться?",
      a: "Хвостовой вызов — когда рекурсивный вызов стоит последним действием, и движок мог бы переиспользовать кадр стека. ES2015 требует такую оптимизацию в строгом режиме, но её реализовал только Safari. В V8 и SpiderMonkey её нет, поэтому глубокую рекурсию переписывают на цикл.",
    },
    {
      level: "senior",
      q: "Как обойти большое дерево, не заморозив страницу и не переполнив стек?",
      a: "Стек переполнения избегают явным стеком или очередью вместо рекурсии. Чтобы не заморозить страницу, работу режут на порции: обрабатывают N узлов, затем отдают поток через `scheduler.yield()` или `setTimeout` и продолжают. Очень тяжёлую обработку выносят в Web Worker, передав данные сообщением.",
    },
  ],
  obj1: [
    {
      level: "middle",
      q: "Какой порядок ключей гарантирует спецификация и где он не гарантирован?",
      a: "С ES2020 порядок гарантирован для `Object.keys`, `for...in`, `JSON.stringify` и `Reflect.ownKeys`: целочисленные ключи по возрастанию, потом строки в порядке добавления, потом символы. Раньше `for...in` мог отличаться между движками. Если важен порядок вставки для любых ключей, берут `Map`.",
    },
    {
      level: "middle",
      q: "Чем `Object.hasOwn` лучше `obj.hasOwnProperty`?",
      a: "`obj.hasOwnProperty` ломается, если у объекта нет прототипа (`Object.create(null)`) или он переопределил этот метод, например пришёл из JSON с ключом `hasOwnProperty`. `Object.hasOwn(obj, key)` — статическая функция, ей всё равно, что лежит в объекте. Она появилась в ES2022 как замена `Object.prototype.hasOwnProperty.call`.",
    },
  ],
  obj2: [
    {
      level: "middle",
      q: "Почему менять прототип объекта через `Object.setPrototypeOf` считается плохой практикой?",
      a: "Движки оптимизируют доступ к свойствам по форме объекта и его прототипу. Смена прототипа уже созданного объекта сбрасывает эти оптимизации и может замедлить весь код, который работал с этим объектом. Прототип задают при создании: `Object.create(proto)` или классом.",
    },
    {
      level: "senior",
      q: "Как реализовать наследование без классов и в чём проблема старого приёма `Child.prototype = new Parent()`?",
      a: "Правильно — `Child.prototype = Object.create(Parent.prototype)`, восстановить `constructor` и вызвать `Parent.call(this, ...)` в конструкторе. Старый приём создавал экземпляр родителя, то есть запускал его конструктор с побочными эффектами и клал его свойства в общий прототип всех потомков. Классы с `extends` делают всё правильно автоматически.",
    },
  ],
  obj3: [
    {
      level: "middle",
      q: "Что такое `new.target` и зачем он нужен?",
      a: "`new.target` внутри функции равен конструктору, с которым её вызвали через `new`, и `undefined` при обычном вызове. Им проверяют, что конструктор вызван правильно, или делают абстрактный класс: если `new.target === Base`, бросить ошибку. В наследовании `new.target` — это класс-потомок, что позволяет родителю знать, кого создают.",
    },
    {
      level: "middle",
      q: "Как `instanceof` работает на самом деле и когда он ошибается?",
      a: "`a instanceof F` проходит по цепочке прототипов `a` и ищет `F.prototype`, а поведение можно переопределить через `Symbol.hasInstance`. Он ошибается между окнами и iframe: у каждого свой `Array`, и массив из iframe — не `instanceof Array` основного окна. Ещё он ломается, если после создания объекта заменили `F.prototype`.",
    },
  ],
  obj4: [
    {
      level: "middle",
      q: "Что такое статический блок инициализации класса и чем приватные методы отличаются от приватных полей?",
      a: "`static { ... }` выполняется один раз при создании класса и может инициализировать статические поля сложной логикой, имея доступ к приватным членам. Приватные методы `#method()` общие для всех экземпляров, как обычные методы, но недоступны снаружи. Приватные поля `#x` — свои у каждого экземпляра.",
    },
    {
      level: "senior",
      q: "Почему наследование от встроенных классов вроде `Array` или `Error` раньше работало плохо, а сейчас нормально?",
      a: "В ES5 `Array.call(this)` не создавал настоящий массив: у экземпляра не было особого поведения `length`. Классы ES2015 создают объект в конструкторе родителя, поэтому `class MyArray extends Array` получает настоящий массив, а методы вроде `map` возвращают `MyArray` через `Symbol.species`. У `Error` при транспиляции в ES5 по-прежнему ломается `instanceof`, если не восстановить прототип.",
    },
  ],
  obj5: [
    {
      level: "middle",
      q: "Как сделать свойство только для чтения, но вычисляемым, и чем геттер отличается от свойства-значения?",
      a: "Геттер без сеттера — `get total() { ... }` — вычисляется при каждом чтении, а запись в него в строгом режиме бросает `TypeError`. Свойство-значение хранит данные и может быть `writable: false`. Одно свойство не может быть одновременно значением и аксессором: дескриптор либо с `value` и `writable`, либо с `get` и `set`.",
    },
    {
      level: "senior",
      q: "Как `Object.freeze` влияет на производительность и почему иммутабельность не всегда делают заморозкой?",
      a: "Заморозка — обход объекта и изменение флагов свойств, а глубокая — обход всего дерева, это дорого на больших данных. Сами замороженные объекты читаются так же быстро. Поэтому иммутабельность обычно обеспечивают соглашением и копированием, а заморозку используют как страховку от случайных мутаций — например, Immer по умолчанию замораживает результаты.",
    },
  ],
  obj6: [
    {
      level: "senior",
      q: "Какие инварианты должен соблюдать `Proxy` и что будет при нарушении?",
      a: "Ловушки не могут врать о неизменяемых частях цели. Например, `get` для неконфигурируемого свойства только для чтения должен вернуть настоящее значение, `has` не может скрыть неконфигурируемое свойство, `ownKeys` должен перечислить все неконфигурируемые ключи. При нарушении движок бросает `TypeError` — так язык защищает гарантии `Object.freeze` и дескрипторов.",
    },
    {
      level: "senior",
      q: "Как на `Proxy` устроена реактивность Vue 3?",
      a: "`reactive(obj)` возвращает прокси. Ловушка `get` вызывает `track`: запоминает, что текущий эффект, например рендер компонента, зависит от этого ключа. Ловушка `set` вызывает `trigger`: находит эффекты, зависящие от ключа, и планирует их перезапуск. Вложенные объекты оборачиваются лениво при чтении.",
    },
  ],
  obj7: [
    {
      level: "middle",
      q: "Почему `new Date() + 1` даёт строку, а `new Date() - 1` — число?",
      a: "У `Date` есть свой `Symbol.toPrimitive`, который при подсказке `\"default\"` ведёт себя как при `\"string\"`. Плюс передаёт подсказку `\"default\"`, получает строку и склеивает. Минус передаёт `\"number\"` и получает миллисекунды. У остальных объектов `\"default\"` работает как `\"number\"`.",
    },
    {
      level: "middle",
      q: "Как сделать объект, который в разных контекстах ведёт себя как число и как строка, и где это полезно?",
      a: "Реализовать `[Symbol.toPrimitive](hint)`: для `\"string\"` возвращать строку, для остального — число. Полезно для денежных сумм, единиц измерения, версий: `${price}` даст «100 ₽», а `price * 2` — число. Главное — не злоупотреблять: неявное поведение трудно читать, и часто явный метод `format()` понятнее.",
    },
  ],
  arr1: [
    {
      level: "middle",
      q: "Как `map` и `forEach` обрабатывают «дыры» в разреженном массиве?",
      a: "Методы перебора `forEach`, `map`, `filter` и `reduce` пропускают дыры — индексы, которых нет в массиве. `map` сохраняет дыры в результате. А `for...of`, `Array.from` и новые методы вроде `findLast` и `toSorted` видят дыры как `undefined`. Поэтому `new Array(3).map(...)` ничего не делает, а `Array.from({ length: 3 }, fn)` работает.",
    },
    {
      level: "middle",
      q: "Как прервать перебор массива досрочно и какие методы это умеют?",
      a: "`forEach` не прерывается — только исключением. Досрочно останавливаются `some` (при первом `true`), `every` (при первом `false`), `find` и `findIndex`. Для полного контроля — `for...of` с `break`. Часто нужный метод уже выражает намерение лучше, чем цикл с флагом.",
    },
  ],
  arr2: [
    {
      level: "middle",
      q: "Какую сортировку использует `Array.prototype.sort` и гарантирована ли её устойчивость?",
      a: "С ES2019 сортировка обязана быть устойчивой: равные элементы сохраняют исходный порядок. V8 использует TimSort — гибрид сортировки слиянием и вставками, быструю на частично упорядоченных данных. Функция сравнения должна быть согласованной: возвращать отрицательное, ноль или положительное, иначе результат не определён.",
    },
    {
      level: "middle",
      q: "Почему в React-состоянии нельзя вызывать `state.items.sort()`?",
      a: "`sort` меняет массив на месте и возвращает тот же массив. Состояние испорчено без вызова сеттера, а если передать его в сеттер, ссылка не изменится, и React может пропустить ререндер. Нужна копия: `toSorted` или `[...items].sort(...)`.",
    },
  ],
  arr3: [
    {
      level: "middle",
      q: "Работает ли деструктуризация массива с любым итерируемым объектом и что это значит для производительности?",
      a: "Да: `const [a, b] = iterable` использует протокол итерации, поэтому работает со строками, `Map`, `Set` и генераторами. Для массивов движки это оптимизируют, но на горячих путях с большими объектами деструктуризация через итератор может быть медленнее обращения по индексу. `const [first] = generator()` возьмёт только первое значение и закроет итератор.",
    },
    {
      level: "middle",
      q: "Как удалить свойство из объекта без мутации и почему `delete` хуже?",
      a: "Rest-деструктуризацией: `const { password, ...safe } = user` — получаем новый объект без `password`. `delete` мутирует объект, а в V8 ещё и может перевести объект в медленный режим словаря, если удаляется не последнее добавленное свойство. Для состояния и неизменяемых данных берут rest.",
    },
  ],
  arr4: [
    {
      level: "middle",
      q: "Когда `Map` быстрее объекта, а когда наоборот?",
      a: "`Map` оптимизирован для частых добавлений и удалений произвольных ключей, у него быстрый `size`. Объекты с постоянным набором полей движки превращают в скрытые классы, и доступ к ним быстрее. Итог: `Map` — для словарей данных, объект — для записей с известной формой.",
    },
    {
      level: "senior",
      q: "Что такое `WeakRef` и `FinalizationRegistry` и почему их советуют избегать?",
      a: "`WeakRef` хранит слабую ссылку на объект: `deref()` вернёт его или `undefined`, если сборщик уже его удалил. `FinalizationRegistry` вызывает колбэк после сборки объекта. Их поведение зависит от сборщика мусора, моменты сборки не гарантированы, и код становится непредсказуемым. Применяют их для кэшей больших объектов, но логику на них не строят.",
    },
  ],
  arr5: [
    {
      level: "middle",
      q: "Что делают методы генератора `return` и `throw` и зачем `finally` в генераторе?",
      a: "`it.return(value)` завершает генератор, как будто в точке `yield` выполнился `return`, а `it.throw(err)` бросает ошибку в точке `yield`. При этом выполняются блоки `finally`, поэтому там закрывают ресурсы. `for...of` сам вызывает `return()` при `break`.",
    },
    {
      level: "senior",
      q: "Как генераторы позволяли писать асинхронный код до `async/await`?",
      a: "Генератор может отдать промис через `yield` и приостановиться. Внешняя функция-исполнитель, например библиотека `co`, ждала промис и возвращала результат обратно в генератор через `next(value)`, а ошибку — через `throw`. `async/await` — встроенная версия этого приёма, и redux-saga до сих пор построена на генераторах.",
    },
  ],
  arr6: [
    {
      level: "middle",
      q: "Почему объекты `Intl` советуют создавать один раз и переиспользовать?",
      a: "Создание форматтера загружает и разбирает данные локали — это дорого. В цикле или на каждом рендере это заметно тормозит. Поэтому форматтер создают один раз на модуль или мемоизируют. `toLocaleString` без переиспользования создаёт форматтер при каждом вызове.",
    },
    {
      level: "middle",
      q: "Как показать «3 дня назад» и отформатировать список «Аня, Борис и Вера» без библиотек?",
      a: "`new Intl.RelativeTimeFormat(\"ru\", { numeric: \"auto\" }).format(-3, \"day\")` даст «3 дня назад», а для `-1` — «вчера». `new Intl.ListFormat(\"ru\", { type: \"conjunction\" }).format([\"Аня\", \"Борис\", \"Вера\"])` склеит список с «и». Оба учитывают правила языка, включая формы слов.",
    },
  ],
  arr7: [
    {
      level: "middle",
      q: "Что такое lookahead и lookbehind в регулярных выражениях?",
      a: "Проверки вперёд `(?=...)` и `(?!...)` и назад `(?<=...)` и `(?<!...)` требуют, чтобы рядом был или не было шаблона, но не включают его в совпадение. Например, `/\\d+(?= ₽)/` найдёт число перед « ₽», не захватив знак рубля. Проверки назад появились в ES2018.",
    },
    {
      level: "senior",
      q: "Как защититься от ReDoS в коде, который применяет регулярки к пользовательскому вводу?",
      a: "Избегать вложенных и перекрывающихся квантификаторов вроде `(a+)+` и `(a|a)*`, ограничивать длину входа до проверки. Проверять регулярки линтерами и анализаторами ReDoS. Для сложного разбора писать парсер. В V8 есть экспериментальный движок регулярок без возвратов, но он включается только флагом.",
    },
  ],
};
