import type { FollowUp } from "../../course/types";

/** Уточняющие вопросы к урокам регионов «Асинхронность» … «Live coding». */
export const ADVANCED: Record<string, FollowUp[]> = {
  asy1: [
    {
      level: "middle",
      q: "Где в цикле событий браузера происходит отрисовка и где выполняется `requestAnimationFrame`?",
      a: "После задачи и всех микрозадач браузер может выполнить шаг обновления отрисовки, обычно раз в кадр. В этом шаге сначала вызываются колбэки `requestAnimationFrame`, затем пересчёт стилей, раскладка и рисование. Если задачи идут подряд без паузы, отрисовка откладывается — отсюда «замерзание» интерфейса.",
    },
    {
      level: "senior",
      q: "Чем event loop в Node.js отличается от браузерного?",
      a: "Node.js построен на libuv, и цикл идёт по фазам: таймеры, отложенные колбэки, опрос ввода-вывода, `setImmediate`, закрытие ресурсов. После каждого колбэка выполняются `process.nextTick`, а за ними микрозадачи промисов. Отрисовки нет. Порядок `setTimeout(fn, 0)` и `setImmediate` в основном модуле не гарантирован, а внутри колбэка ввода-вывода `setImmediate` всегда раньше.",
    },
  ],
  asy2: [
    {
      level: "middle",
      q: "Что будет, если вернуть промис из `then`? А объект с методом `then`?",
      a: "Цепочка «примет» его: следующий `then` дождётся результата вернувшегося промиса. То же с любым thenable — объектом с методом `then`: промис вызовет этот метод, передав свои `resolve` и `reject`. Поэтому `await` работает с библиотеками промисов и с самописными thenable.",
    },
    {
      level: "senior",
      q: "Что такое необработанное отклонение промиса и как его ловить глобально?",
      a: "Если отклонённый промис дожил до конца задачи без обработчика, это необработанное отклонение. В браузере приходит событие `unhandledrejection` у `window` — туда подключают отправку ошибок в мониторинг. В Node.js есть `process.on(\"unhandledRejection\")`, а по умолчанию с версии 15 процесс завершается с ошибкой. Если обработчик добавят позже, придёт событие `rejectionhandled`.",
    },
  ],
  asy3: [
    {
      level: "middle",
      q: "Почему `process.nextTick` может «заморозить» Node.js сильнее, чем промисы?",
      a: "Очередь `nextTick` обрабатывается раньше микрозадач промисов и до конца, прежде чем цикл перейдёт к следующей фазе. Если колбэк `nextTick` рекурсивно ставит новый `nextTick`, цикл никогда не дойдёт до ввода-вывода и таймеров. Документация Node.js советует в новом коде предпочитать `queueMicrotask`.",
    },
    {
      level: "senior",
      q: "Как задать приоритет задач в браузере: что такое `scheduler.postTask` и `scheduler.yield`?",
      a: "`scheduler.postTask(fn, { priority })` ставит задачу с приоритетом `user-blocking`, `user-visible` или `background`, и браузер выполнит важное раньше. `await scheduler.yield()` отдаёт поток внутри долгой задачи, а продолжение встаёт в очередь с повышенным приоритетом, в отличие от `setTimeout(0)`, который уходит в конец. Это главный инструмент для улучшения INP.",
    },
  ],
  asy4: [
    {
      level: "middle",
      q: "В чём разница между `return await promise` и `return promise` в `async` функции?",
      a: "Результат почти одинаковый, но есть две разницы. Внутри `try/catch` только `return await` поймает отклонение в этом `catch`, а `return promise` отдаст ошибку наружу. Кроме того, с `await` функция остаётся в стеке асинхронных вызовов, и трассировка ошибки понятнее. Поэтому внутри `try` пишут `return await`.",
    },
    {
      level: "senior",
      q: "Что такое `await` на верхнем уровне модуля и как он влияет на загрузку приложения?",
      a: "В ES-модуле можно писать `await` вне функций: модуль становится асинхронным, и все модули, которые его импортируют, ждут его выполнения. Это удобно для загрузки конфигурации или динамического импорта, но долгий `await` на верхнем уровне блокирует выполнение всего графа зависимостей. Поэтому его не используют для необязательных данных.",
    },
  ],
  asy5: [
    {
      level: "middle",
      q: "Как отменить остальные запросы, если в `Promise.all` один упал?",
      a: "Промисы не отменяются сами: `Promise.all` отклоняется, а остальные запросы продолжают выполняться. Нужен общий `AbortController`: передать его `signal` во все запросы и в `catch` вызвать `controller.abort()`. Библиотеки вроде TanStack Query делают это при размонтировании автоматически.",
    },
    {
      level: "senior",
      q: "Как выполнить сто запросов, но не больше пяти одновременно? Почему `Promise.all` здесь не подходит?",
      a: "`Promise.all` получает уже запущенные промисы — все сто запросов стартуют сразу. Для ограничения нужен пул: передавать функции, а не промисы, запускать пять «работников», каждый берёт следующую задачу после завершения своей. Есть готовые библиотеки вроде `p-limit`, и эта задача часто встречается в live coding.",
    },
  ],
  asy6: [
    {
      level: "middle",
      q: "Почему в старых статьях продолжение после `await` выполнялось на несколько тиков позже?",
      a: "До оптимизации 2018–2019 годов `await promise` оборачивал промис в новый и тратил три микрозадачи. Затем спецификацию изменили: `await` нативного промиса теперь стоит одну микрозадачу. Поэтому ответы из старых статей про порядок `async` и `then` бывают другими, и порядок проверяют запуском.",
    },
    {
      level: "senior",
      q: "Как отладить сложный порядок асинхронных событий в реальном приложении?",
      a: "Вкладка Performance показывает задачи, микрозадачи и отрисовку на временной шкале. В Sources есть асинхронные стеки вызовов — видно, откуда пришёл `then`. Помогают метки времени через `performance.mark` и логирование с номером шага. Главное правило — не полагаться на тонкий порядок микрозадач в продакшен-коде, а явно выстраивать зависимость через `await`.",
    },
  ],
  asy7: [
    {
      level: "senior",
      q: "Как прочитать ответ `fetch` потоком по частям?",
      a: "`response.body` — `ReadableStream`. Его читают через `getReader()` в цикле `read()` или, где поток перебираемый, `for await (const chunk of response.body)` — байты приходят по мере загрузки, а `TextDecoderStream` превращает их в текст. Так показывают прогресс загрузки и выводят ответ ИИ-модели по словам, не дожидаясь конца.",
    },
    {
      level: "senior",
      q: "Что будет с асинхронным генератором, если потребитель перестал читать, и как не допустить утечку ресурсов?",
      a: "Генератор просто приостановится на `yield` и будет ждать. Если потребитель вышел через `break` или `return`, `for await` вызовет `return()`, и выполнятся `finally` — там закрывают соединение. Если же итератор бросили без закрытия, ресурсы останутся открытыми, поэтому такие итераторы всегда перебирают циклом или закрывают вручную.",
    },
  ],
  asy8: [
    {
      level: "middle",
      q: "Что делает `util.promisify.custom` и почему `promisify` не подходит для любой функции?",
      a: "`promisify` рассчитан на соглашение «колбэк последним, ошибка первой». Функции с другим порядком или несколькими результатами он оборачивает неправильно. Автор функции может задать свою промисную версию через свойство `util.promisify.custom`, и `promisify` вернёт её. Для `setTimeout` так сделано: есть `timers/promises`.",
    },
    {
      level: "senior",
      q: "Что такое Zalgo и почему колбэк нельзя вызывать то синхронно, то асинхронно?",
      a: "Если функция иногда вызывает колбэк сразу (например, из кэша), а иногда позже (после запроса), вызывающий код ведёт себя по-разному, и появляются трудноуловимые ошибки порядка. Это называют «выпустить Zalgo». Правило — всегда асинхронно: промисы решают это на уровне языка, их обработчики никогда не вызываются синхронно.",
    },
  ],
  dom1: [
    {
      level: "middle",
      q: "Почему частые изменения DOM медленные и как вставить тысячу элементов быстро?",
      a: "Каждое изменение может заставить браузер пересчитать стили и раскладку, особенно если между изменениями читают размеры. Тысячу элементов собирают вне документа — в `DocumentFragment` или строкой через шаблон — и вставляют одной операцией. Ещё лучше не рисовать тысячу строк, а использовать виртуальный список.",
    },
    {
      level: "middle",
      q: "Чем `innerText` отличается от `textContent`?",
      a: "`textContent` возвращает весь текст узлов, включая скрытые элементы и содержимое `<script>`, и не учитывает стили. `innerText` возвращает текст «как на экране»: без скрытого через CSS, с переносами строк по раскладке. Поэтому `innerText` заставляет браузер посчитать раскладку и заметно медленнее.",
    },
  ],
  dom2: [
    {
      level: "middle",
      q: "Как событие проходит через Shadow DOM и что такое `composedPath()`?",
      a: "События из теневого дерева по умолчанию не выходят за его границу, если у них нет флага `composed`; у пользовательских событий ввода он есть. Выходя наружу, событие перенацеливается: `target` становится самим компонентом. `event.composedPath()` возвращает полный путь, включая элементы внутри теневых деревьев, если они открытые.",
    },
    {
      level: "middle",
      q: "Как создать своё событие, которое всплывает, и передать в нём данные?",
      a: "`new CustomEvent(\"cart:add\", { detail: { id: 42 }, bubbles: true })` и `el.dispatchEvent(event)`. Данные лежат в `event.detail`. Без `bubbles: true` событие не всплывёт, а без `composed: true` не выйдет из Shadow DOM. `dispatchEvent` вызывает обработчики синхронно, в отличие от событий браузера.",
    },
  ],
  dom3: [
    {
      level: "middle",
      q: "Как делегировать события `focus` и `mouseenter`, которые не всплывают?",
      a: "Два способа. Использовать всплывающие аналоги: `focusin` и `focusout` вместо `focus` и `blur`, `mouseover` и `mouseout` вместо `mouseenter` и `mouseleave` — с проверкой `relatedTarget`, чтобы не реагировать на переходы между детьми. Или повесить обработчик на фазу погружения через `capture: true`: погружение проходят все события.",
    },
    {
      level: "senior",
      q: "Как React обрабатывает события и что изменилось в React 17?",
      a: "React вешает по одному обработчику на каждый тип события на корневой контейнер и сам разбирается, какому компоненту передать событие, — это делегирование. До React 17 обработчики висели на `document`, из-за чего `stopPropagation` в React не останавливал обычные обработчики на `document`, а два приложения на странице мешали друг другу. С React 17 обработчики висят на корне приложения.",
    },
  ],
  dom4: [
    {
      level: "middle",
      q: "Как снять все обработчики компонента одной строкой?",
      a: "Создать `AbortController`, передавать `{ signal: controller.signal }` во все `addEventListener`, а при уничтожении компонента вызвать `controller.abort()`. Это снимет все обработчики разом, не нужно хранить ссылки на функции. Тот же сигнал можно передать в `fetch`, чтобы отменить и запросы.",
    },
    {
      level: "middle",
      q: "Что будет, если вызвать `preventDefault()` в пассивном обработчике?",
      a: "Вызов проигнорируется, а браузер выведет предупреждение в консоль. Прокрутка не отменится: браузер уже пообещал себе, что обработчик её не остановит, и начал прокручивать. Если нужно отменить прокрутку, обработчик регистрируют с `passive: false` явно или используют CSS `touch-action` и `overscroll-behavior`.",
    },
  ],
  dom5: [
    {
      level: "middle",
      q: "Как `<link rel=\"modulepreload\">` и `preload` ускоряют загрузку скриптов?",
      a: "Браузер узнаёт о зависимостях модуля только после его загрузки и разбора — получается цепочка запросов. `modulepreload` заранее скачивает и разбирает модуль и его зависимости, `preload` скачивает ресурс с нужным приоритетом. Сборщики сами добавляют `modulepreload` для чанков, нужных сразу.",
    },
    {
      level: "senior",
      q: "Почему `document.write` в скриптах считается вредным?",
      a: "Он вставляет разметку прямо в поток разбора HTML. Для обычного скрипта это блокирует парсер, а если скрипт внешний и вставлен через `document.write`, Chrome на медленных сетях вообще отказывается его загружать. После загрузки страницы `document.write` стирает весь документ. Сторонние виджеты, которые на нём построены, заменяют на асинхронные.",
    },
  ],
  dom6: [
    {
      level: "senior",
      q: "Что такое декларативный Shadow DOM и зачем он нужен?",
      a: "Это теневое дерево, записанное прямо в HTML: `<template shadowrootmode=\"open\">` внутри элемента. Браузер создаёт теневой корень при разборе страницы, без JavaScript. Это позволяет отрисовать Web Components на сервере: пользователь видит готовый компонент до загрузки скриптов, а потом компонент «оживает».",
    },
    {
      level: "senior",
      q: "Как Web Components работают с формами и доступностью?",
      a: "Обычный свой элемент форма не видит. `static formAssociated = true` и `this.attachInternals()` дают `ElementInternals`: элемент становится полем формы, может задать значение через `setFormValue` и валидацию. Через те же `ElementInternals` задают роль и ARIA-свойства по умолчанию, чтобы экранные читалки понимали компонент.",
    },
  ],
  dom7: [
    {
      level: "middle",
      q: "Как отправить форму с файлами и показать прогресс загрузки?",
      a: "`FormData` с файлами отправляют как тело запроса, браузер сам поставит `multipart/form-data` с разделителем. У `fetch` нет события прогресса отправки, поэтому для прогресса используют `XMLHttpRequest` и `xhr.upload.onprogress`. Для больших файлов — загрузка кусками с возможностью продолжить.",
    },
    {
      level: "middle",
      q: "Как сделать доступные сообщения об ошибках в форме?",
      a: "Связать поле с текстом ошибки через `aria-describedby`, отметить неверное поле `aria-invalid=\"true\"`, а сообщения, которые появляются динамически, показывать в элементе с `role=\"alert\"` или `aria-live`. После неудачной отправки переводить фокус на первое поле с ошибкой. Цвет не должен быть единственным признаком ошибки.",
    },
  ],
  dom8: [
    {
      level: "middle",
      q: "Какие свойства и методы вызывают принудительный пересчёт раскладки?",
      a: "Всё, что возвращает размеры и позиции: `offsetWidth` и остальные `offset*`, `client*`, `scroll*`, `getBoundingClientRect`, `getComputedStyle`, `innerText`, а также `scrollIntoView` и `focus`. Если перед ними изменили стили, браузер пересчитает раскладку немедленно. Список ведёт Пол Айриш в заметке «What forces layout».",
    },
    {
      level: "senior",
      q: "Как следить за размером и видимостью элементов, не читая размеры в цикле?",
      a: "`ResizeObserver` сообщает о новом размере после раскладки, не заставляя пересчитывать её. `IntersectionObserver` сообщает о видимости асинхронно. Если размеры всё же нужно читать, чтения группируют в начале кадра, а записи делают в `requestAnimationFrame` — так устроены библиотеки вроде fastdom.",
    },
  ],
  wa1: [
    {
      level: "middle",
      q: "Как отменить `fetch` по тайм-ауту и одновременно по кнопке «Отмена»?",
      a: "Объединить сигналы: `AbortSignal.any([controller.signal, AbortSignal.timeout(5000)])`. Запрос отменится по первому из них. По ошибке можно различить причину: при тайм-ауте имя ошибки `TimeoutError`, при ручной отмене — `AbortError`.",
    },
    {
      level: "senior",
      q: "Какие режимы `fetch` влияют на безопасность и кэш: `mode`, `credentials`, `cache`, `keepalive`?",
      a: "`mode` — `cors`, `no-cors` (непрозрачный ответ, прочитать нельзя) или `same-origin`. `credentials` — отправлять ли cookies: `omit`, `same-origin` или `include`. `cache` управляет HTTP-кэшем для запроса, например `no-store` или `reload`. `keepalive: true` позволяет запросу пережить закрытие страницы — его используют для аналитики вместо `sendBeacon`.",
    },
  ],
  wa2: [
    {
      level: "middle",
      q: "Сколько данных можно хранить в браузере и что будет при нехватке места?",
      a: "`localStorage` — около 5 МБ на источник, запись сверх лимита бросает `QuotaExceededError`. IndexedDB и Cache API делят общую квоту, которая зависит от свободного места на диске — обычно до десятков процентов. Браузер может удалить данные при нехватке места; `navigator.storage.persist()` просит не удалять, а `navigator.storage.estimate()` показывает занятое и доступное.",
    },
    {
      level: "senior",
      q: "Как синхронизировать состояние между вкладками одного сайта?",
      a: "Событие `storage` сообщает другим вкладкам об изменении `localStorage`. `BroadcastChannel` — канал сообщений между вкладками одного источника без хранилища. Для общих вычислений есть `SharedWorker`, а для блокировок между вкладками — Web Locks API. Типичные задачи — выход из аккаунта во всех вкладках и один активный WebSocket на все вкладки.",
    },
  ],
  wa3: [
    {
      level: "middle",
      q: "Почему для анимаций по времени берут метку из колбэка `requestAnimationFrame`, а не счётчик кадров?",
      a: "Частота кадров разная: 60, 120 или 144 Гц, а при нагрузке кадры пропускаются. Если сдвигать элемент на фиксированное расстояние за кадр, анимация будет быстрее на быстрых экранах и рваной при пропусках. Правильно считать положение от прошедшего времени: колбэк получает метку `timestamp`, от неё и вычисляют прогресс.",
    },
    {
      level: "senior",
      q: "Как точнее измерить время выполнения кода, чем `Date.now()`?",
      a: "`performance.now()` возвращает время с высоким разрешением от начала загрузки страницы и не зависит от перевода системных часов. Для отметок видны `performance.mark` и `performance.measure` — они появляются во вкладке Performance. Точность намеренно огрублена браузером для защиты от атак по времени, сильнее всего — без изоляции COOP и COEP.",
    },
  ],
  wa4: [
    {
      level: "middle",
      q: "Как сделать бесконечную ленту на `IntersectionObserver`?",
      a: "В конец списка кладут невидимый элемент-«сторож» и наблюдают за ним с `rootMargin` в несколько сотен пикселей. Когда сторож пересекает область, загружается следующая страница, и новые элементы вставляются перед ним. Защищаются от повторной загрузки флагом, а при длинной ленте добавляют виртуализацию, чтобы DOM не рос бесконечно.",
    },
    {
      level: "senior",
      q: "Что такое `PerformanceObserver` и как с его помощью собирают Web Vitals?",
      a: "`PerformanceObserver` подписывается на записи производительности: `largest-contentful-paint`, `layout-shift`, `event`, `longtask`, `long-animation-frame`, `resource`. Библиотека web-vitals строит на этих записях LCP, CLS и INP с правильными правилами подсчёта. Из них собирают данные реальных пользователей и отправляют в аналитику.",
    },
  ],
  wa5: [
    {
      level: "senior",
      q: "Когда Web Worker не ускоряет, а замедляет?",
      a: "Когда задача короткая, а данных много: копирование через `postMessage` и запуск воркера стоят дороже самой работы. Ещё воркеру недоступен DOM, и результат всё равно нужно отрисовать в основном потоке. Выигрыш есть для долгих вычислений на небольших входных данных или при передаче буферов без копирования.",
    },
    {
      level: "senior",
      q: "Что такое Comlink и как упростить общение с воркером?",
      a: "Comlink превращает обмен сообщениями в вызовы функций: в воркере объект «выставляют» наружу, а в основном потоке получают прокси, у которого можно вызывать методы через `await`. Под капотом всё те же `postMessage` и структурное клонирование. Так код с воркером выглядит как обычный асинхронный вызов.",
    },
  ],
  wa6: [
    {
      level: "middle",
      q: "Как восстанавливать позицию прокрутки при навигации в SPA?",
      a: "Браузер восстанавливает прокрутку сам только для настоящих переходов. В SPA сохраняют позицию для каждой записи истории, например в `history.state` или по ключу записи, перед уходом, а при `popstate` возвращают её после отрисовки. `history.scrollRestoration = \"manual\"` отключает автоматическое восстановление, чтобы оно не мешало. Роутеры вроде React Router делают это компонентом `ScrollRestoration`.",
    },
    {
      level: "senior",
      q: "Что даёт Navigation API по сравнению с History API?",
      a: "Navigation API присылает единое событие `navigate` на все переходы: ссылки, формы, `back`, программные вызовы. В обработчике `event.intercept()` превращает переход в SPA-переход с корректными состояниями загрузки и отменой. Есть список записей истории и их состояние. Это заменяет перехват кликов и `popstate`, но поддержку браузерами нужно проверять.",
    },
  ],
  mod1: [
    {
      level: "middle",
      q: "Что такое циклические зависимости модулей и чем они опасны?",
      a: "Если `a.js` импортирует `b.js`, а `b.js` — `a.js`, один из модулей начнёт выполняться, когда второй ещё не закончил. Обращение к ещё не инициализированному `let` или `class` из другого модуля даст `ReferenceError`. Решение — вынести общее в третий модуль или отложить обращение внутрь функций.",
    },
    {
      level: "senior",
      q: "Как в Node.js совместимы ESM и CommonJS?",
      a: "ESM может импортировать CommonJS: `module.exports` становится экспортом по умолчанию, а именованные экспорты угадываются статическим анализом. CommonJS долго не мог `require` ESM, но с Node.js 22.12 `require` синхронного ES-модуля работает. Модули с `await` на верхнем уровне через `require` не загрузить. Пакеты публикуют оба формата через поле `exports` в `package.json`.",
    },
  ],
  mod2: [
    {
      level: "middle",
      q: "Можно ли включить строгий режим только для части кода и где это ломается?",
      a: "Да, строкой `\"use strict\"` первой в функции. Но в функции с параметрами по умолчанию, деструктуризацией или rest-параметрами эта строка запрещена — `SyntaxError`. Склейка файлов раньше ломала код: строка в начале одного файла делала строгим весь склеенный бандл.",
    },
    {
      level: "senior",
      q: "Как строгий режим помогает движку оптимизировать код?",
      a: "В строгом режиме `arguments` не связан с параметрами, `eval` не создаёт переменных снаружи, `with` запрещён, а `this` не превращается в объект. Движку не нужно учитывать, что переменная может внезапно появиться или измениться через `arguments`, и он может хранить переменные в регистрах и быстрее искать имена.",
    },
  ],
  mod3: [
    {
      level: "middle",
      q: "Что такое границы ошибок в приложении и где ловить ошибки на фронтенде?",
      a: "Ошибки ловят там, где можно что-то сделать: у запроса — показать сообщение и повторить, у компонента — границей ошибок React, глобально — через события `error` и `unhandledrejection` для отправки в мониторинг. Пустой `catch`, который глотает ошибку, — худший вариант: сломанное поведение без следов.",
    },
    {
      level: "senior",
      q: "Как собирать ошибки с продакшена так, чтобы по ним можно было понять причину?",
      a: "Подключают систему мониторинга вроде Sentry: она ловит глобальные ошибки и отклонения, добавляет браузер, адрес, действия пользователя перед ошибкой и версию релиза. Карты исходников загружают в систему, а не публикуют, чтобы стек показывал исходный код. Ошибки группируют и отслеживают рост после выкладки.",
    },
  ],
  mod4: [
    {
      level: "middle",
      q: "Как новые возможности языка попадают в стандарт? Что такое стадии TC39?",
      a: "Предложение проходит стадии комитета TC39: 0 — идея, 1 — предложение, 2 — черновик, 2.7 — одобрено к реализации после тестов, 3 — кандидат, реализуется в браузерах, 4 — готово и входит в ближайший ежегодный стандарт. Использовать без полифилов безопасно после стадии 4 и поддержки в целевых браузерах.",
    },
    {
      level: "senior",
      q: "Как безопасно использовать новые возможности языка в продакшене?",
      a: "Проверить поддержку по MDN и Baseline, задать цель сборки, чтобы синтаксис транспилировался, а недостающие функции закрыть полифилами только для нужных браузеров. Для экспериментальных API — проверка наличия перед использованием и запасной путь. И следить за ошибками в мониторинге по версиям браузеров после выкладки.",
    },
  ],
  mod5: [
    {
      level: "middle",
      q: "Как правильно хранить и передавать даты между фронтендом и сервером?",
      a: "Моменты времени — строкой ISO 8601 в UTC с `Z` на конце или числом миллисекунд. Показ — в поясе пользователя через `Intl.DateTimeFormat`. «Календарные» даты без времени, например день рождения, передают строкой `YYYY-MM-DD` и не превращают в `Date`, иначе в другом поясе получится соседний день.",
    },
    {
      level: "senior",
      q: "Почему перевод часов на летнее время ломает расчёты с датами и как Temporal это решает?",
      a: "В день перевода в сутках 23 или 25 часов, и «прибавить сутки» как 24 часа даст не то время. Встреча «каждый день в 9:00» в другом поясе смещается. `Temporal.ZonedDateTime` хранит пояс, а не смещение, и `add({ days: 1 })` учитывает перевод часов, тогда как `add({ hours: 24 })` честно прибавляет 24 часа.",
    },
  ],
  lc1: [
    {
      level: "middle",
      q: "Как добавить в `debounce` вызов на переднем фронте (`leading`) и метод `flush`?",
      a: "С `leading` функцию вызывают сразу на первом вызове серии, если таймера нет, а последующие вызовы только продлевают тишину. `flush` немедленно выполняет отложенный вызов с сохранёнными аргументами и сбрасывает таймер. Для этого аргументы и `this` хранят в замыкании, а не только в стрелке внутри `setTimeout`.",
    },
    {
      level: "senior",
      q: "Как правильно использовать `debounce` в React-компоненте?",
      a: "Создать отложенную функцию один раз: через `useMemo` или `useRef`, иначе на каждом рендере будет новая функция со своим таймером. Если она читает свежие пропсы, передавать их аргументами или хранить в ref. При размонтировании вызвать `cancel`, иначе вызов сработает после ухода. Часто проще отложить значение через `useDeferredValue` или задержать запрос в библиотеке данных.",
    },
  ],
  lc2: [
    {
      level: "middle",
      q: "Как реализовать `throttle` на `requestAnimationFrame` и когда он лучше таймера?",
      a: "Сохранять последние аргументы и, если кадр ещё не запланирован, вызывать `requestAnimationFrame`, в колбэке которого выполнить функцию и сбросить флаг. Так функция выполняется не чаще раза за кадр и точно перед отрисовкой. Это лучше для визуальных обновлений — позиции, размеров, — а для запросов к серверу нужен throttle по времени.",
    },
    {
      level: "middle",
      q: "Как `throttle` должен обращаться с `this` и возвращаемым значением?",
      a: "`this` и аргументы последнего вызова сохраняют и передают через `apply` — и при немедленном, и при отложенном вызове. Возвращаемое значение отложенного вызова синхронно вернуть нельзя, поэтому обычно возвращают результат последнего выполнения, как lodash, или вовсе ничего. Если результат важен, лучше возвращать промис.",
    },
  ],
  lc3: [
    {
      level: "middle",
      q: "Как `deepClone` должен обработать экземпляры классов, геттеры и символьные ключи?",
      a: "Чтобы сохранить прототип, копию создают через `Object.create(Object.getPrototypeOf(value))`. Символьные и неперечисляемые ключи берут через `Reflect.ownKeys` и копируют дескрипторы через `Object.getOwnPropertyDescriptors`, тогда геттеры останутся геттерами. Но полная копия классов с приватными полями `#` невозможна: их нельзя прочитать снаружи.",
    },
    {
      level: "senior",
      q: "Как скопировать очень глубокий объект без рекурсии?",
      a: "Итеративно: стек пар «исходный объект — объект-копия». Берём пару со стека, для каждого ключа создаём копию значения, кладём в копию-родителя и, если значение — объект, кладём новую пару в стек. `WeakMap` уже скопированных объектов по-прежнему нужен для циклов. Глубина больше не ограничена стеком вызовов.",
    },
  ],
  lc4: [
    {
      level: "middle",
      q: "Как `deepEqual` должен сравнивать `Map`, `Set` и циклические структуры?",
      a: "`Map` — одинаковый размер и для каждого ключа равное значение в другой карте. `Set` из примитивов — каждый элемент есть в другом, а для объектов внутри нужен перебор с глубоким сравнением. Для циклов хранят пары уже сравниваемых объектов в `WeakMap` и считают их равными при повторной встрече.",
    },
    {
      level: "senior",
      q: "Почему для мемоизации и зависимостей эффектов глубокое сравнение обычно — плохая идея?",
      a: "Оно стоит времени, пропорционального размеру данных, и выполняется на каждом рендере, съедая выигрыш. Кроме того, оно прячет настоящую проблему — нестабильные ссылки. Правильнее обеспечить стабильность: неизменяемые обновления, `useMemo` для производных объектов и примитивы в зависимостях.",
    },
  ],
  lc5: [
    {
      level: "middle",
      q: "Как написать `flatten` итеративно?",
      a: "Держать стек пар «элемент, глубина», начав с элементов исходного массива в обратном порядке. Берём пару: если это массив и глубина ещё позволяет, кладём его элементы с глубиной на единицу меньше, иначе добавляем элемент в результат. Так порядок сохраняется, а глубина вложенности не ограничена стеком вызовов.",
    },
    {
      level: "middle",
      q: "Как сделать `flatten` ленивым через генератор?",
      a: "`function* flatten(arr, depth)` перебирает элементы и для массивов делает `yield* flatten(item, depth - 1)`, а для остального — `yield item`. Результат можно перебирать по одному, не создавая промежуточных массивов, и остановиться раньше. Для огромных структур это экономит память.",
    },
  ],
  lc6: [
    {
      level: "middle",
      q: "Как ограничить размер кэша в `memoize`?",
      a: "Хранить кэш в `Map` и при превышении лимита удалять самую старую запись — первый ключ. Для LRU при каждом попадании в кэш запись удаляют и вставляют заново, чтобы она стала самой свежей. Ещё вариант — срок жизни записи: хранить время и считать запись устаревшей.",
    },
    {
      level: "senior",
      q: "Как мемоизировать асинхронную функцию и что делать с ошибками?",
      a: "Кэшировать сам промис, а не результат: тогда одновременные вызовы с теми же аргументами получат один запрос. Если промис отклонился, запись нужно удалить, иначе ошибка будет кэширована навсегда. Для данных с сервера обычно берут библиотеку кэша вроде TanStack Query, где есть срок свежести и повторные запросы.",
    },
  ],
  lc7: [
    {
      level: "senior",
      q: "Как написать свои `Promise.allSettled` и `Promise.any`?",
      a: "`allSettled` — как `all`, но в обоих обработчиках записать по индексу объект `{ status, value }` или `{ status, reason }` и увеличить счётчик, никогда не вызывая `reject`. `any` — наоборот: первый успех вызывает `resolve`, ошибки записываются по индексу, а когда все отклонились, отклонить с `new AggregateError(errors)`. Пустой вход у `any` сразу отклоняется.",
    },
    {
      level: "senior",
      q: "Почему `Promise.all` принимает любой итерируемый объект, а не только массив, и как это учесть в реализации?",
      a: "Спецификация перебирает вход через протокол итерации, поэтому подходит `Set`, генератор или строка. В реализации вход сначала превращают в массив через `Array.from` или перебирают `for...of`, считая индексы вручную. Пустой итератор тоже должен сразу дать `[]`.",
    },
  ],
  lc8: [
    {
      level: "senior",
      q: "Как защитить EventEmitter от утечек памяти?",
      a: "Возвращать функцию отписки и требовать её вызова при уничтожении подписчика. Предупреждать, если на одно событие подписано слишком много обработчиков, — Node.js делает это после десяти. Для подписчиков, которые могут исчезнуть без отписки, слабые ссылки не помогают надёжно, поэтому дисциплина отписки важнее.",
    },
    {
      level: "senior",
      q: "Чем EventEmitter отличается от `EventTarget` и что выбрать в браузере?",
      a: "`EventTarget` — встроенный класс браузера и Node.js: `addEventListener`, `dispatchEvent`, объекты `Event` с фазами, `once` и `signal`. Его можно унаследовать и получить стандартный API без своего кода. EventEmitter из Node.js проще и передаёт аргументы напрямую, без объекта события. В браузере для своих событий обычно достаточно `EventTarget` и `CustomEvent`.",
    },
  ],
  lc9: [
    {
      level: "middle",
      q: "Как сделать `curry` для функций с неизвестным числом аргументов?",
      a: "`fn.length` для них бесполезен, поэтому признак окончания задают явно: например, вызов без аргументов — `sum(1)(2)(3)()` — или явное число аргументов вторым параметром `curry(fn, 3)`. Другой приём — возвращать функцию, у которой переопределён `valueOf`, но он неявный и путает.",
    },
    {
      level: "middle",
      q: "Как сохранить `this` в каррированной функции?",
      a: "Внутреннюю функцию пишут обычной, а не стрелкой, и при финальном вызове используют `fn.apply(this, args)`. Промежуточные функции тоже должны передавать `this` дальше. На практике каррирование применяют к чистым функциям, где `this` не нужен, так что это скорее проверка понимания.",
    },
  ],
  lc10: [
    {
      level: "senior",
      q: "Что такое jitter в повторных попытках и зачем он нужен?",
      a: "Jitter — случайный разброс паузы: вместо ровно `delay * 2 ** n` ждут случайное время от нуля до этого значения. Если тысяча клиентов упала одновременно, без разброса они повторят запрос одновременно и снова положат сервер. Разброс «размазывает» нагрузку по времени.",
    },
    {
      level: "senior",
      q: "Как `retry` должен учитывать отмену и заголовок `Retry-After`?",
      a: "Принимать `AbortSignal`: проверять его перед каждой попыткой и прерывать ожидание паузы при отмене. Для ответов 429 и 503 брать паузу из заголовка `Retry-After`, если он есть, вместо своей формулы. И не повторять ошибки, которые не исправятся сами: 400, 401, 404.",
    },
  ],
  lc11: [
    {
      level: "senior",
      q: "Как пул должен обрабатывать ошибку одной задачи?",
      a: "Два режима. «Быстрый отказ»: первая ошибка отклоняет общий промис, остальные задачи больше не запускаются. «Собрать всё»: ошибки записываются в результат как в `allSettled`, и пул доходит до конца. Выбор зависит от задачи: загрузка файлов пользователя — собрать всё и показать, какие не загрузились.",
    },
    {
      level: "senior",
      q: "Как сделать очередь задач с ограничением параллельности, в которую можно добавлять задачи на ходу?",
      a: "Класс с массивом ожидающих задач и счётчиком активных. Метод `add(task)` возвращает промис: задача кладётся в очередь, и вызывается `next()`. `next` запускает задачи, пока активных меньше лимита, а по завершении каждой уменьшает счётчик и снова вызывает `next`. Так работают библиотеки `p-queue` и `p-limit`.",
    },
  ],
  lc12: [
    {
      level: "senior",
      q: "Как реализовать LRU без `Map` — на хэш-таблице и двусвязном списке?",
      a: "Объект или `Map` хранит ключ → узел списка, а узлы связаны в двусвязный список от свежих к старым с фиктивными головой и хвостом. `get` находит узел и переносит его в голову — O(1), потому что у узла есть ссылки на соседей. `put` добавляет узел в голову, а при переполнении удаляет узел перед хвостом и его ключ из таблицы.",
    },
    {
      level: "senior",
      q: "Чем LRU отличается от LFU и когда какой выбрать?",
      a: "LRU выбрасывает давно не использованное, LFU — то, к чему обращались реже всего. LRU простой и хорошо работает, когда популярность меняется со временем. LFU лучше держит стабильно популярные записи, но медленно забывает бывших лидеров и сложнее в реализации. Есть гибриды, например W-TinyLFU в библиотеке Caffeine.",
    },
  ],
  lc13: [
    {
      level: "middle",
      q: "Как написать парную функцию `set(obj, path, value)`, которая создаёт недостающие уровни?",
      a: "Разобрать путь на ключи, пройти по всем, кроме последнего: если на очередном шаге нет объекта, создать его — массив, если следующий ключ число, иначе объект. В последний ключ записать значение. Для неизменяемой версии на каждом шаге копируют уровень, а не меняют исходный объект.",
    },
    {
      level: "senior",
      q: "Какие ключи в пути нужно запретить в `set` и почему?",
      a: "`__proto__`, `constructor` и `prototype`: путь `__proto__.isAdmin` из пользовательских данных запишет свойство в `Object.prototype` — это prototype pollution. Такие уязвимости находили в функциях `set` популярных библиотек. Ключи проверяют перед записью или хранят данные в объектах без прототипа.",
    },
  ],
  lc14: [
    {
      level: "senior",
      q: "Как добавить в свой промис `finally` и статические `resolve` и `reject`?",
      a: "`finally(fn)` — это `then`, где оба обработчика вызывают `fn()` без аргументов и возвращают исходный результат: значение передают дальше, а ошибку бросают снова. Если `fn` вернул промис, его ждут, а если бросил ошибку — она заменяет исходный результат. `resolve(v)` возвращает `v`, если это уже `MyPromise`, иначе новый выполненный промис; `reject(e)` — новый отклонённый.",
    },
    {
      level: "senior",
      q: "Что такое Promises/A+ и как проверить свою реализацию?",
      a: "Promises/A+ — открытый стандарт поведения `then`, из которого вырос встроенный Promise: порядок вызовов, асинхронность, «принятие» thenable, защита от промиса, который разрешается сам собой. Для него есть набор из сотен тестов promises-aplus-tests, который прогоняют на своей реализации. Требование `resolve(self)` → `TypeError` часто забывают.",
    },
  ],
};
