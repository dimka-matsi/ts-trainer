import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "arr3",
  region: 4,
  title: "Деструктуризация, spread и rest",
  q: "Как работает деструктуризация массивов и объектов? Чем spread отличается от rest?",
  answer:
    "Деструктуризация раскладывает массив или объект по переменным: массив — по позиции, объект — по имени свойства. Можно пропускать элементы, переименовывать `{ name: title }`, задавать значения по умолчанию — они срабатывают только на `undefined` — и собирать остаток. Три точки означают разное по месту: слева от `=` или в параметрах это rest — собрать остаток, справа или в вызове это spread — развернуть массив или объект.",
  theory: {
    p: [
      "Деструктуризация массива идёт по позициям: `const [first, second] = arr`. Пустое место пропускает элемент: `const [, second] = arr`. `const [head, ...tail] = arr` собирает остаток в новый массив. Так же меняют переменные местами без временной: `[a, b] = [b, a]`.",
      "Деструктуризация объекта идёт по именам: `const { name, age } = user`. Переименование — `const { name: title } = user`, вложенность — `const { address: { city } } = user`, остаток — `const { password, ...safe } = user`. Если свойства нет, получится `undefined`, а вложенная деструктуризация от `undefined` бросит `TypeError`.",
      "Значение по умолчанию `const { age = 30 } = user` подставляется, только если значение `undefined` — как у параметров функций. `null` значение по умолчанию не заменяет. В параметрах функции деструктуризацию пишут с пустым объектом по умолчанию: `function f({ a, b } = {})` — тогда вызов `f()` не упадёт.",
      "Spread разворачивает: `[...a, ...b]` склеивает массивы, `{ ...defaults, ...options }` сливает объекты — при совпадении ключей побеждает правый, `f(...args)` передаёт массив списком аргументов. Разворачивать можно любой итерируемый объект: `[...\"abc\"]` — массив букв. Spread копирует поверхностно.",
    ],
    code: `const [a, , b = 5, ...rest] = [1, 2, undefined, 4, 5];
console.log(a, b, rest);                // 1 5 [4, 5]

const user = { name: "Аня", city: "Казань", role: "admin" };
const { name: title, age = 30, ...others } = user;
console.log(title, age, others);

let x = 1;
let y = 2;
[x, y] = [y, x];                        // обмен без временной переменной
console.log(x, y);

const defaults = { theme: "light", size: 14 };
console.log({ ...defaults, ...{ size: 16 } }); // правый побеждает
console.log([..."abc"]);

function connect({ host = "localhost", port = 80 } = {}) {
  return host + ":" + port;
}
console.log(connect(), connect({ port: 8080 }));`,
    keys: [
      "Массив раскладывается по позициям, объект — по именам. Можно переименовать, пропустить, собрать остаток.",
      "Значение по умолчанию срабатывает только на `undefined`. В параметрах пиши `= {}`, чтобы вызов без аргумента не падал.",
      "Три точки: слева — rest (собрать), справа — spread (развернуть). При слиянии объектов побеждает правый ключ.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `const [a, , b = 5, ...rest] = [1, 2, undefined, 4, 5];
console.log(a, b, rest);
const { name: title, age = 30, city = "Москва" } = { name: "Аня", city: null };
console.log(title, age, city);`,
      opts: ["1 5 [4, 5]\nАня 30 null", "1 undefined [4, 5]\nАня 30 Москва", "1 5 [2, 4, 5]\nundefined 30 null", "1 5 [4, 5]\nАня 30 Москва"],
      a: 0,
      why: "Пустое место пропустило `2`, `undefined` заменён на `5`, остаток собран в массив. У объекта значение по умолчанию сработало для отсутствующего `age`, но не для `city: null`.",
    },
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `let x = 1;
let y = 2;
[x, y] = [y, x];
console.log(x, y);
const merged = { ...{ a: 1, b: 2 }, ...{ b: 3 } };
console.log(merged);
console.log([..."abc"]);`,
      opts: ["2 1\n{ a: 1, b: 3 }\n[\"a\", \"b\", \"c\"]", "1 2\n{ a: 1, b: 2 }\n[\"abc\"]", "2 1\n{ a: 1, b: 2 }\n[\"a\", \"b\", \"c\"]", "2 1\n{ b: 3 }\n[\"abc\"]"],
      a: 0,
      why: "Справа создаётся массив `[2, 1]`, и он раскладывается обратно по `x` и `y`. При слиянии объектов правый `b: 3` перезаписал левый. Строка итерируемая, spread разбил её на символы.",
    },
    {
      type: "quiz",
      q: "Что будет при вызове `f()`, если функция объявлена как `function f({ a }) {}`?",
      opts: ["`TypeError`: нельзя деструктурировать `undefined`", "`a` будет `undefined`", "`a` будет `{}`", "`SyntaxError`"],
      a: 0,
      why: "Без аргумента параметр равен `undefined`, а свойства у `undefined` не прочитать. Поэтому пишут `function f({ a } = {})`.",
    },
    {
      type: "run",
      goal: "Напиши `describe(user)`: возвращает строку `\"Имя, возраст лет, город\"`. Если возраста нет, вместо него `?`, если нет адреса или города — `—`.",
      code: `function describe(user) {
  return user.name + ", " + user.age + " лет, " + user.address.city;
}`,
      tests: [
        ["describe({ name: \"Аня\", age: 25, address: { city: \"Сочи\" } })", "\"Аня, 25 лет, Сочи\""],
        ["describe({ name: \"Борис\", address: { city: \"Омск\" } })", "\"Борис, ? лет, Омск\""],
        ["describe({ name: \"Вера\", age: 30 })", "\"Вера, 30 лет, —\""],
        ["describe({ name: \"Гоша\", age: 40, address: {} })", "\"Гоша, 40 лет, —\""],
      ],
      solution: `function describe({ name, age = "?", address: { city = "—" } = {} }) {
  return name + ", " + age + " лет, " + city;
}`,
      hint: "Разложи объект прямо в параметрах: `{ name, age = \"?\", address: { city = \"—\" } = {} }`. `= {}` у `address` нужен, чтобы отсутствующий адрес не ронял деструктуризацию.",
    },
  ],
};
