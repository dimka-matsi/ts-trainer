import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "mod2",
  region: 8,
  title: "Строгий режим",
  q: "Что такое строгий режим и что он меняет?",
  answer:
    "Строгий режим — версия языка без старых опасных решений. Он включается строкой `\"use strict\"` в начале файла или функции, а в ES-модулях и классах действует всегда. Главные изменения: присваивание необъявленной переменной бросает `ReferenceError`, а не создаёт глобальную; `this` при простом вызове функции — `undefined`, а не глобальный объект; молчаливые ошибки становятся исключениями — запись в свойство только для чтения или удаление неудаляемого дают `TypeError`. Ещё запрещены `with`, одинаковые имена параметров и восьмеричные литералы вида `010`.",
  theory: {
    p: [
      "Первые версии JavaScript прощали многое: опечатка в имени переменной создавала глобальную переменную, неудачная запись молча игнорировалась. В ES5 появился строгий режим, который превращает такие ситуации в ошибки. Его включает строка `\"use strict\"` первой инструкцией файла или функции. В ES-модулях и в теле классов он включён всегда, поэтому в современном коде строка почти не нужна.",
      "Необъявленные переменные. Без строгого режима `usre = \"Аня\"` (опечатка) создаёт глобальную переменную. В строгом режиме — `ReferenceError`. Это ловит опечатки сразу.",
      "`this`. При простом вызове функции без строгого режима `this` — глобальный объект, и `this.x = 1` незаметно пишет в глобальную область. В строгом режиме `this` — `undefined`, и такая ошибка падает сразу. Весь код этого курса выполнялся в строгом режиме, поэтому в уроках про `this` простой вызов давал `undefined`.",
      "Молчаливые ошибки становятся исключениями: запись в свойство только для чтения или в замороженный объект, удаление неудаляемого свойства, запись в `NaN` или `undefined` — всё это `TypeError`. Запрещены инструкция `with`, одинаковые имена параметров, восьмеричная запись `010`. `arguments` больше не связан с параметрами, а `eval` не может создавать переменные снаружи себя.",
    ],
    code: `// код курса уже в строгом режиме, как в модулях
try {
  undeclared = 5;                  // опечатка в имени — ошибка, а не глобальная
} catch (e) {
  console.log(e.name);             // ReferenceError
}

function whoAmI() {
  return this;
}
console.log(whoAmI());             // undefined, а не глобальный объект

const frozen = Object.freeze({ x: 1 });
try {
  frozen.x = 2;                    // без строгого режима — молча проигнорировано
} catch (e) {
  console.log(e.name);             // TypeError
}`,
    keys: [
      "Включается `\"use strict\"`, в модулях и классах — всегда.",
      "Необъявленная переменная — `ReferenceError`, `this` при простом вызове — `undefined`.",
      "Молчаливые ошибки становятся `TypeError`. Запрещены `with`, повторные имена параметров, запись `010`.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Код выполняется в строгом режиме. Что он выведет?",
      code: `try {
  undeclared = 5;
} catch (e) {
  console.log(e.name);
}
function f() {
  return this;
}
console.log(f());
try {
  const obj = Object.defineProperty({}, "x", { value: 1 });
  obj.x = 2;
} catch (e) {
  console.log(e.name);
}`,
      opts: ["ReferenceError\nundefined\nTypeError", "undefined\n[object Window]\nTypeError", "ReferenceError\nundefined", "ReferenceError\n{}\nTypeError"],
      a: 0,
      why: "Присвоить необъявленной переменной нельзя. `this` при простом вызове — `undefined`. Свойство из `defineProperty` без `writable` доступно только для чтения, и запись бросает `TypeError`.",
    },
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код в строгом режиме?",
      code: `try {
  delete Object.prototype;
} catch (e) {
  console.log(e.name);
}
try {
  NaN = 1;
} catch (e) {
  console.log(e.name);
}
console.log("готово");`,
      opts: ["TypeError\nTypeError\nготово", "готово", "false\nготово", "SyntaxError"],
      a: 0,
      why: "Без строгого режима обе строки молча ничего бы не сделали. В строгом режиме удаление неудаляемого свойства и запись в свойство только для чтения — `TypeError`.",
    },
    {
      type: "match",
      q: "Сопоставь ситуацию и что будет в строгом режиме.",
      pairs: [
        ["присваивание необъявленной переменной", "`ReferenceError`"],
        ["`this` в функции при простом вызове", "`undefined`"],
        ["запись в замороженный объект", "`TypeError`"],
        ["два параметра с одним именем", "`SyntaxError` ещё до запуска"],
      ],
      why: "Строгий режим превращает ошибки, которые раньше проходили молча, в явные.",
    },
    {
      type: "quiz",
      q: "Где строгий режим включён автоматически, без `\"use strict\"`?",
      opts: ["В ES-модулях и в теле классов", "Во всех файлах с расширением `.js`", "Только в Node.js", "В стрелочных функциях"],
      a: 0,
      why: "Поэтому современный код с `import` и `export` почти всегда строгий, и строку `\"use strict\"` видно в основном в старых скриптах.",
    },
  ],
};
