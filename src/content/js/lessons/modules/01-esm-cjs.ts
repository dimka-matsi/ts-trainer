import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "mod1",
  region: 8,
  title: "ES-модули и CommonJS",
  q: "Чем ES-модули отличаются от CommonJS?",
  answer:
    "ES-модули — стандарт языка: `import` и `export` статические, их видно до выполнения кода, поэтому сборщик может выбросить неиспользуемое. Импорт — живая привязка: если модуль поменял экспортированную переменную, импортирующий увидит новое значение. Модули всегда в строгом режиме, выполняются один раз и поддерживают `await` на верхнем уровне. CommonJS — система Node.js: `require` вызывается во время выполнения, синхронно, и возвращает объект `module.exports`; разобранное из него значение — копия на момент вызова. Динамическая загрузка в ES-модулях — `import()`, он возвращает промис.",
  theory: {
    p: [
      "ES-модули (ESM) — встроенная система модулей: `export const x`, `export default`, `import { x } from \"./a.js\"`. Объявления `import` и `export` стоят только на верхнем уровне и разбираются до выполнения кода. Поэтому сборщик заранее знает граф зависимостей и может удалить неиспользуемые экспорты — это tree shaking.",
      "Импорт в ESM — живая привязка, а не копия: если модуль изменит экспортированную переменную `let count`, все, кто её импортировал, увидят новое значение. Присвоить импортированному имени нельзя — `TypeError`. Модуль выполняется один раз, при первом импорте, дальше все получают тот же экземпляр. Код модуля всегда в строгом режиме, `this` на верхнем уровне — `undefined`, разрешён `await` на верхнем уровне.",
      "CommonJS (CJS) — исторический формат Node.js: `module.exports = ...` и `const x = require(\"./a\")`. `require` — обычная функция, её можно вызвать где угодно, в том числе в `if`, и она синхронно загружает и выполняет файл. Результат — объект экспортов; если разобрать из него примитив, получится копия, которая не обновится. Статически такой код анализировать сложнее, и tree shaking работает хуже.",
      "`import(\"./heavy.js\")` — динамический импорт: работает в любом месте кода и возвращает промис с модулем. Сборщики выносят такие модули в отдельные чанки — так делают загрузку по требованию. В Node.js оба формата живут вместе: `\"type\": \"module\"` в `package.json` или расширения `.mjs` и `.cjs`, а новые версии умеют и `require` ES-модуля.",
    ],
    code: `// counter.js — ES-модуль
export let count = 0;
export function increment() {
  count++;
}
export default function reset() {
  count = 0;
}

// main.js
import reset, { count, increment } from "./counter.js";
increment();
console.log(count);        // 1 — живая привязка
// count = 5;              // TypeError: импорт только для чтения

const { renderChart } = await import("./chart.js"); // загрузка по требованию

// тот же счётчик в CommonJS
// counter.cjs: let count = 0; module.exports = { count, increment() { count++; } };
// const { count, increment } = require("./counter.cjs");
// increment(); console.log(count); // 0 — разобрана копия значения`,
    flow: {
      actors: ["main.js", "Загрузчик модулей", "counter.js"],
      steps: [
        { from: 1, to: 0, label: "разбор: import { count } — граф известен до выполнения" },
        { from: 1, to: 2, label: "counter.js выполняется один раз" },
        { from: 0, to: 2, label: "increment(): count++ в модуле" },
        { from: 2, to: 0, label: "count в main.js уже 1", note: "живая привязка, а не копия" },
      ],
    },
    keys: [
      "ESM: статические `import`/`export`, граф известен заранее, работает tree shaking. Импорт — живая привязка только для чтения.",
      "Модуль выполняется один раз, всегда в строгом режиме, с `await` на верхнем уровне. `import()` — загрузка по требованию, возвращает промис.",
      "CommonJS: `require` во время выполнения, синхронно, возвращает `module.exports`. Разобранные значения — копии.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Что выведет `main.js`?",
      code: `// counter.js
export let count = 0;
export function increment() {
  count++;
}

// main.js
import { count, increment } from "./counter.js";
increment();
increment();
console.log(count);`,
      opts: ["`2`", "`0`", "`undefined`", "`TypeError`"],
      a: 0,
      why: "Импорт в ES-модулях — живая привязка к переменной модуля, а не копия значения. В CommonJS с разбором объекта было бы `0`.",
    },
    {
      type: "sort",
      q: "Что относится к ES-модулям, а что к CommonJS?",
      groups: ["ES-модули", "CommonJS"],
      items: [
        ["`import` и `export` разбираются до выполнения", 0],
        ["живые привязки импортов", 0],
        ["`await` на верхнем уровне", 0],
        ["`require` можно вызвать внутри `if`", 1],
        ["`module.exports`", 1],
        ["синхронная загрузка во время выполнения", 1],
      ],
      why: "Статичность ESM даёт tree shaking и раннее обнаружение ошибок импорта. Гибкость `require` делает анализ сложнее.",
    },
    {
      type: "quiz",
      q: "Что возвращает `import(\"./chart.js\")`?",
      opts: ["Промис, который выполнится объектом модуля", "Сам модуль синхронно", "Функцию по умолчанию из модуля", "`undefined`, модуль просто выполнится"],
      a: 0,
      why: "Динамический импорт асинхронный: модуль нужно загрузить. Сборщики выносят такие модули в отдельные файлы.",
    },
    {
      type: "quiz",
      q: "Модуль `config.js` импортируют десять других модулей. Сколько раз выполнится его код?",
      opts: ["Один раз, все получат тот же экземпляр", "Десять раз", "Столько раз, сколько вызовут его функции", "Ни разу, пока не вызовут `import()`"],
      a: 0,
      why: "Модули кэшируются: первый импорт выполняет код, остальные получают готовые экспорты. Поэтому модуль — естественный синглтон.",
    },
  ],
};
