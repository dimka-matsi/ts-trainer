import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "val8",
  region: 0,
  title: "Symbol",
  q: "Что такое `Symbol` и зачем он нужен?",
  answer:
    "`Symbol` — примитив, каждый экземпляр которого уникален: `Symbol(\"id\") !== Symbol(\"id\")`, описание в скобках нужно только для отладки. Символ используют как ключ свойства, который не столкнётся с чужими ключами и не попадёт в `Object.keys`, `for...in` и `JSON.stringify`. `Symbol.for(\"key\")` берёт символ из общего реестра — одинаковый для всей программы. А встроенные символы позволяют настроить поведение объекта в языке: например, как он превращается в число или строку.",
  theory: {
    p: [
      "`Symbol(\"описание\")` создаёт новое уникальное значение. Два символа с одинаковым описанием не равны: описание — только подпись для отладки, его читают через `sym.description`. Символ — примитив, `typeof` возвращает `\"symbol\"`. `new Symbol()` не работает — только вызов без `new`.",
      "Главное применение — ключи свойств: `obj[id] = 42`, где `id` — символ. Такой ключ не пересечётся со строковыми ключами других библиотек, а перебор его не видит: `Object.keys`, `for...in` и `JSON.stringify` пропускают символьные ключи. Достать их можно через `Object.getOwnPropertySymbols`, так что это не настоящая приватность, а защита от случайных пересечений.",
      "`Symbol.for(\"app.id\")` работает иначе: ищет символ в глобальном реестре по ключу и создаёт его, только если такого нет. Два вызова с одним ключом вернут один и тот же символ — даже из разных модулей и фреймов. `Symbol.keyFor(sym)` возвращает ключ из реестра.",
      "Встроенные (well-known) символы — хуки языка. Одни решают, как объект превращается в число или строку, другие делают объект перебираемым циклом или настраивают `instanceof`. О них — в следующих регионах.",
    ],
    code: `const a = Symbol("id");
const b = Symbol("id");
console.log(a === b, typeof a, a.description); // false symbol id

const user = { name: "Аня", [a]: 42 };
console.log(Object.keys(user));      // ["name"] — символ не виден
console.log(JSON.stringify(user));   // {"name":"Аня"}
console.log(user[a]);                // 42
console.log(Object.getOwnPropertySymbols(user).length); // 1

console.log(Symbol.for("app") === Symbol.for("app")); // true — общий реестр`,
    keys: [
      "Каждый `Symbol()` уникален, описание — только для отладки. `typeof` — `\"symbol\"`.",
      "Символьные ключи не видны в `Object.keys`, `for...in` и `JSON.stringify`, но это не приватность.",
      "`Symbol.for(key)` — общий реестр, одинаковый символ по ключу. Встроенные символы настраивают поведение объектов.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `const id = Symbol("id");
const user = { name: "Аня", [id]: 1 };
console.log(Object.keys(user).length);
console.log(JSON.stringify(user));
console.log(Symbol("x") === Symbol("x"));
console.log(Symbol.for("x") === Symbol.for("x"));`,
      opts: ["1\n{\"name\":\"Аня\"}\nfalse\ntrue", "2\n{\"name\":\"Аня\",\"id\":1}\nfalse\nfalse", "1\n{\"name\":\"Аня\"}\ntrue\ntrue", "2\n{\"name\":\"Аня\"}\nfalse\ntrue"],
      a: 0,
      why: "Символьный ключ пропускают и `Object.keys`, и `JSON.stringify`. Каждый `Symbol()` новый, а `Symbol.for` берёт один символ из реестра.",
    },
    {
      type: "quiz",
      q: "Зачем библиотеке класть служебные данные в объект пользователя под ключом-символом, а не строкой `\"__meta\"`?",
      opts: [
        "Символ не столкнётся с чужими ключами и не попадёт в `Object.keys` и `JSON.stringify`",
        "Символьные свойства нельзя прочитать никаким способом",
        "Символы занимают меньше памяти",
        "Строковые ключи с подчёркиванием запрещены",
      ],
      a: 0,
      why: "Строковый ключ может совпасть с полем пользователя и попадёт в сериализацию. Символ уникален и не мешает перебору, хотя `getOwnPropertySymbols` его всё равно покажет.",
    },
    {
      type: "match",
      q: "Сопоставь запись и что она делает.",
      pairs: [
        ["`Symbol(\"id\")`", "новый уникальный символ"],
        ["`Symbol.for(\"id\")`", "символ из общего реестра по ключу"],
        ["`sym.description`", "подпись символа для отладки"],
        ["`Object.getOwnPropertySymbols(obj)`", "символьные ключи объекта"],
      ],
      why: "Реестр `Symbol.for` нужен, когда один символ должны найти разные части программы, не передавая его друг другу.",
    },
  ],
};
