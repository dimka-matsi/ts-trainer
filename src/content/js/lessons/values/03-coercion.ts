import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "val3",
  region: 0,
  title: "Приведение типов",
  q: "Как работает приведение типов в JavaScript? Почему `1 + \"2\"` — это `\"12\"`, а `\"5\" - 2` — это `3`?",
  answer:
    "Приведение бывает трёх видов: к строке, к числу и к логическому значению. Оператор `+` склеивает строки, если хотя бы один операнд — строка, поэтому `1 + \"2\"` даёт `\"12\"`. Остальные арифметические операторы работают только с числами и приводят к числу: `\"5\" - 2` — это `3`. В логическом контексте ложны только восемь значений: `false`, `0`, `-0`, `0n`, `\"\"`, `null`, `undefined` и `NaN`, всё остальное — истинно, включая `\"0\"`, `[]` и `{}`.",
  theory: {
    p: [
      "Когда операции нужно значение другого типа, JavaScript приводит его сам — это неявное приведение. Явно привести можно функциями `String(x)`, `Number(x)` и `Boolean(x)`. Всего три направления: к строке, к числу и к логическому значению.",
      "К числу приводят арифметика (`-`, `*`, `/`), унарный плюс `+x` и сравнения `<` и `>`. `Number(\"\")` и `Number(\" \")` — `0`, `Number(\"12px\")` — `NaN`, `Number(null)` — `0`, а `Number(undefined)` — `NaN`. Плюс — особый: если один из операндов строка, он склеивает строки. Поэтому `1 + \"2\"` — `\"12\"`, а `1 - \"2\"` — `-1`.",
      "Объект сначала превращается в примитив: для `+` и строк вызывается его `toString`. Массив становится строкой через запятую: `[1, 2] + [3]` — `\"1,23\"`, пустой массив — пустая строка. Обычный объект превращается в `\"[object Object]\"`.",
      "К логическому значению приводят `if`, `!`, `&&` и `||`. Ложных значений восемь: `false`, `0`, `-0`, `0n`, `\"\"`, `null`, `undefined`, `NaN`. Всё остальное истинно, в том числе строка `\"0\"`, строка из пробела, пустой массив и пустой объект. `!!x` — короткий способ получить `true` или `false`.",
    ],
    code: `console.log(1 + "2");      // 12 — плюс со строкой склеивает
console.log("5" - 2);      // 3 — минус приводит к числу
console.log(+"42");        // 42
console.log(Number(""));   // 0
console.log(Number("12px")); // NaN
console.log([1, 2] + [3]); // 1,23
console.log([] + {});      // [object Object]
console.log(!!"0");        // true — непустая строка
console.log(!![]);         // true — любой объект истинен`,
    keys: [
      "Три направления: к строке, к числу, к логическому. `+` со строкой склеивает, остальная арифметика приводит к числу.",
      "Ложны только `false`, `0`, `-0`, `0n`, `\"\"`, `null`, `undefined`, `NaN`. `\"0\"`, `[]` и `{}` истинны.",
      "Объект перед операцией становится примитивом: массив — строкой через запятую, обычный объект — `\"[object Object]\"`.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `console.log(1 + "2");
console.log("5" - 2);
console.log("5" * "2");
console.log(true + 1);
console.log([1, 2] + [3]);`,
      opts: ["12\n3\n10\n2\n1,23", "3\n3\n10\n2\n1,2,3", "12\n52\n10\ntrue1\n1,23", "12\n3\n55\n2\n[1, 2, 3]"],
      a: 0,
      why: "`+` со строкой склеивает: `\"12\"`. `-` и `*` приводят строки к числам. `true` в арифметике — `1`. Массивы для `+` становятся строками `\"1,2\"` и `\"3\"`, и они склеиваются.",
    },
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `console.log(Boolean("0"));
console.log(Boolean(""));
console.log(Boolean([]));
console.log(Boolean(NaN));
console.log(!!" ");`,
      opts: ["true\nfalse\ntrue\nfalse\ntrue", "false\nfalse\nfalse\nfalse\nfalse", "false\nfalse\ntrue\nfalse\ntrue", "true\nfalse\nfalse\nfalse\ntrue"],
      a: 0,
      why: "Ложна только пустая строка, а `\"0\"` и строка из пробела — непустые. Пустой массив — объект, а все объекты истинны. `NaN` — одно из восьми ложных значений.",
    },
    {
      type: "sort",
      q: "Что вернёт `Number(...)` для каждого значения?",
      groups: ["`0`", "`NaN`", "`12`"],
      items: [
        ["`Number(\"\")`", 0],
        ["`Number(null)`", 0],
        ["`Number(false)`", 0],
        ["`Number([])`", 0],
        ["`Number(undefined)`", 1],
        ["`Number(\"12px\")`", 1],
        ["`Number({})`", 1],
        ["`Number(\" 12 \")`", 2],
      ],
      why: "Пустая строка и `null` дают `0`, а `undefined` — `NaN`. Пробелы по краям отбрасываются, но любой лишний символ даёт `NaN`. Пустой массив становится пустой строкой, то есть нулём, а объект — строкой `\"[object Object]\"`, то есть `NaN`.",
    },
    {
      type: "quiz",
      q: "Какое из значений истинно в `if`?",
      opts: ["`\"0\"`", "`0`", "`\"\"`", "`NaN`"],
      a: 0,
      why: "Строка `\"0\"` непустая, поэтому истинна. На этом ловятся, когда проверяют ввод из формы: значение поля всегда строка.",
    },
  ],
};
