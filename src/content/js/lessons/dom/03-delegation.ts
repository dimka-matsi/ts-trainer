import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "dom3",
  region: 6,
  title: "Делегирование событий",
  q: "Что такое делегирование событий и зачем оно нужно?",
  answer:
    "Делегирование — один обработчик на общем родителе вместо обработчика на каждом дочернем элементе. Событие всплывает до родителя, а там по `event.target.closest(\"селектор\")` находим, по какому элементу кликнули. Плюсы: меньше обработчиков и памяти, и сразу работают элементы, добавленные позже. Ограничения: так нельзя ловить события, которые не всплывают, и делегирование ломается, если кто-то ниже вызвал `stopPropagation`.",
  theory: {
    p: [
      "Список из тысячи строк, и на каждой кнопка «удалить». Можно повесить тысячу обработчиков, а потом не забыть вешать их на новые строки и снимать с удалённых. Делегирование делает проще: один обработчик на списке, потому что клик по кнопке всплывёт до него.",
      "В обработчике нужно понять, по чему кликнули. `event.target` — самый глубокий элемент: если в кнопке есть иконка `<svg>` или `<span>`, целью будет именно она. Поэтому ищут вверх: `event.target.closest(\"button[data-action]\")`. Если `closest` вернул `null` — клик был мимо. Для вложенных списков добавляют проверку `list.contains(button)`, чтобы не поймать кнопку из чужого блока.",
      "Плюсы: один обработчик вместо тысячи, меньше памяти, новые элементы работают сразу, не нужно ничего снимать при удалении. Так устроены обработчики в React: он вешает их на корень приложения и сам разбирается, какому компоненту передать событие.",
      "Ограничения. Не всплывающие события (`focus`, `blur`, `mouseenter`) делегировать нельзя — берут всплывающие аналоги `focusin`, `focusout`, `mouseover`. Если внутренний код вызвал `stopPropagation`, событие не дойдёт до родителя. А `data-*` атрибуты удобны, чтобы хранить в разметке, что делать: `data-action=\"delete\"`, `data-id=\"42\"`.",
    ],
    code: `// <ul id="todos">
//   <li data-id="1">Купить хлеб <button data-action="delete"><svg>…</svg></button></li>
//   …
// </ul>
const todos = document.getElementById("todos");

todos.addEventListener("click", (event) => {
  const button = event.target.closest("button[data-action]"); // клик мог быть по svg
  if (!button || !todos.contains(button)) return;             // клик мимо кнопок

  const item = button.closest("li");
  if (button.dataset.action === "delete") item.remove();
});

// новая строка работает сразу — обработчик на родителе
todos.insertAdjacentHTML("beforeend", '<li data-id="2">Позвонить <button data-action="delete">×</button></li>');`,
    flow: {
      actors: ["svg внутри кнопки", "button[data-action]", "li", "ul#todos (обработчик)"],
      steps: [
        { from: 0, to: 1, label: "клик: event.target — svg" },
        { from: 1, to: 2, label: "событие всплывает" },
        { from: 2, to: 3, label: "доходит до ul — срабатывает единственный обработчик" },
        { from: 3, to: 1, label: "target.closest(\"button[data-action]\") находит кнопку", note: "по dataset.action решаем, что делать" },
      ],
    },
    keys: [
      "Один обработчик на родителе ловит всплывшие события детей, включая добавленных позже.",
      "Нужный элемент ищут через `event.target.closest(селектор)`: целью может оказаться вложенная иконка.",
      "Не всплывающие события не делегируются, `stopPropagation` внутри ломает делегирование.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      q: "Почему в делегированном обработчике пишут `event.target.closest(\"button\")`, а не просто `event.target`?",
      opts: [
        "Клик может прийтись по вложенному элементу кнопки — иконке или `span`, и `target` будет им",
        "`event.target` в делегировании всегда равен родителю",
        "`closest` работает быстрее",
        "`event.target` недоступен при всплытии",
      ],
      a: 0,
      why: "`target` — самый глубокий элемент под курсором. `closest` поднимается от него к ближайшей кнопке, включая сам элемент.",
    },
    {
      type: "sort",
      q: "Какие события можно поймать делегированием на родителе?",
      groups: ["всплывает — можно", "не всплывает — нельзя"],
      items: [
        ["`click`", 0],
        ["`input`", 0],
        ["`keydown`", 0],
        ["`focusin`", 0],
        ["`focus`", 1],
        ["`blur`", 1],
        ["`mouseenter`", 1],
        ["`mouseleave`", 1],
      ],
      why: "Для фокуса есть всплывающие `focusin` и `focusout`, для наведения — `mouseover` и `mouseout`.",
    },
    {
      type: "quiz",
      q: "В список с делегированным обработчиком кликов добавили новую строку с кнопкой. Что нужно сделать, чтобы кнопка заработала?",
      opts: ["Ничего: клик всплывёт до обработчика на списке", "Повесить обработчик на новую кнопку", "Перевесить обработчик на список", "Вызвать `dispatchEvent` на списке"],
      a: 0,
      why: "Обработчик живёт на родителе и не зависит от того, какие дети есть сейчас. В этом главный плюс делегирования.",
    },
    {
      type: "order",
      q: "Расставь шаги работы делегированного обработчика на списке.",
      items: [
        "клик по иконке внутри кнопки",
        "событие всплывает до списка",
        "обработчик находит кнопку через `event.target.closest`",
        "по `dataset` кнопки выбирает действие",
      ],
      why: "Сначала всплытие доставляет событие, потом обработчик сам определяет, по чему кликнули.",
    },
  ],
};
