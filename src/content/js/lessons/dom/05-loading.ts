import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "dom5",
  region: 6,
  title: "Загрузка страницы: DOMContentLoaded, async и defer",
  q: "Чем отличаются `DOMContentLoaded` и `load`? Чем `<script async>` отличается от `<script defer>`?",
  answer:
    "`DOMContentLoaded` срабатывает, когда браузер разобрал весь HTML и построил DOM, — картинки и стили могут ещё грузиться. `load` у `window` — когда загружено всё, включая картинки и стили. Обычный `<script>` останавливает разбор HTML, пока не скачается и не выполнится. `defer` скачивается параллельно и выполняется после разбора HTML в порядке следования, до `DOMContentLoaded`. `async` тоже скачивается параллельно, но выполняется сразу, как скачался, в любом порядке. Модули `type=\"module\"` ведут себя как `defer` по умолчанию.",
  theory: {
    p: [
      "Браузер читает HTML сверху вниз и строит DOM. Встретив обычный `<script src>`, он останавливает разбор: скачивает скрипт, выполняет его и только потом продолжает. Скрипт видит только ту часть DOM, что построена до него. Поэтому раньше скрипты ставили в конец `<body>`.",
      "`<script defer>` скачивается параллельно с разбором HTML, а выполняется после того, как весь HTML разобран, строго в порядке следования в документе и до события `DOMContentLoaded`. Это выбор для кода приложения, которому нужен DOM и важен порядок. Скрипты `type=\"module\"` отложены так же по умолчанию.",
      "`<script async>` тоже скачивается параллельно, но выполняется сразу, как только скачан, — может прервать разбор и не соблюдает порядок между скриптами. Подходит для независимых скриптов: аналитики, счётчиков, рекламы. `defer` и `async` работают только для внешних скриптов с `src`.",
      "События загрузки. `document.readyState` проходит `loading` → `interactive` → `complete`. На `interactive` (разобран HTML) срабатывает `DOMContentLoaded` на `document` — можно работать с DOM. На `complete` — `load` на `window`: загружены картинки, стили, iframe. При уходе со страницы — `pagehide` и `visibilitychange`; `beforeunload` используют только чтобы предупредить о несохранённых данных.",
    ],
    code: `// <script src="analytics.js" async></script>  — сразу как скачается, порядок не важен
// <script src="vendor.js" defer></script>     — после разбора HTML, первым
// <script src="app.js" defer></script>        — после разбора HTML, вторым
// <script type="module" src="main.js"></script> — отложен, как defer

console.log(document.readyState); // loading, если скрипт обычный в <head>

document.addEventListener("DOMContentLoaded", () => {
  console.log("DOM готов:", document.readyState); // interactive
});
window.addEventListener("load", () => {
  console.log("всё загружено:", document.readyState); // complete
});`,
    flow: {
      actors: ["Парсер HTML", "Сеть", "Скрипт defer", "Скрипт async"],
      steps: [
        { from: 0, to: 1, label: "нашёл defer и async: качать параллельно, разбор идёт дальше" },
        { from: 1, to: 3, label: "async скачался — выполняется сразу", note: "может выполниться посреди разбора HTML" },
        { from: 0, to: 2, label: "HTML разобран — выполняем defer по порядку" },
        { from: 2, to: 0, label: "defer выполнены → DOMContentLoaded" },
        { from: 1, to: 0, label: "картинки и стили загружены → load" },
      ],
    },
    keys: [
      "`DOMContentLoaded` — DOM построен; `load` — загружено всё, включая картинки.",
      "Обычный скрипт блокирует разбор HTML. `defer` — параллельная загрузка, выполнение после разбора по порядку.",
      "`async` — выполнение сразу после загрузки, порядок не гарантирован. Модули отложены как `defer`.",
    ],
  },
  tasks: [
    {
      type: "match",
      q: "Сопоставь, как подключён скрипт, и как он выполняется.",
      pairs: [
        ["`<script src>`", "останавливает разбор HTML до загрузки и выполнения"],
        ["`<script defer>`", "после разбора HTML, в порядке следования"],
        ["`<script async>`", "сразу после загрузки, порядок не гарантирован"],
        ["`<script type=\"module\">`", "отложен, как `defer`, по умолчанию"],
      ],
      why: "`defer` и модули — для кода приложения, `async` — для независимых скриптов вроде аналитики.",
    },
    {
      type: "order",
      q: "Расставь события загрузки страницы по порядку.",
      items: [
        "`readyState` равен `loading`, идёт разбор HTML",
        "выполняются скрипты с `defer`",
        "`DOMContentLoaded` на `document`",
        "`load` на `window` после картинок и стилей",
      ],
      why: "Скрипты `defer` выполняются до `DOMContentLoaded`, и событие ждёт их. Картинки не задерживают `DOMContentLoaded`, но задерживают `load`.",
    },
    {
      type: "quiz",
      q: "Какой атрибут выбрать для скрипта аналитики, который не зависит от DOM и других скриптов?",
      opts: ["`async`", "`defer`", "Никакой, поставить в `<head>`", "`type=\"module\"`"],
      a: 0,
      why: "Аналитике не важен порядок и готовность DOM, и лучше начать её как можно раньше. Для кода приложения выбирают `defer` или модули.",
    },
    {
      type: "quiz",
      q: "Когда срабатывает `DOMContentLoaded`?",
      opts: [
        "Когда HTML разобран, DOM построен и выполнены скрипты `defer`; картинки могут ещё грузиться",
        "Когда загружены все картинки и стили",
        "Сразу после первого байта HTML",
        "Когда пользователь впервые взаимодействует со страницей",
      ],
      a: 0,
      why: "Ждать `load`, чтобы найти элементы, — лишняя задержка: картинки к DOM отношения не имеют.",
    },
  ],
};
