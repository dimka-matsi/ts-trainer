import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "asy1",
  region: 5,
  level: "middle",
  title: "Однопоточность, стек вызовов и event loop",
  q: "Как работает event loop? Почему `setTimeout(fn, 0)` не выполняется сразу?",
  answer:
    "JavaScript выполняет код в одном потоке: у него один стек вызовов, и пока он не опустеет, ничего другого не происходит. Долгие операции — таймеры, сеть, события — делает окружение: браузер или Node.js. Когда операция готова, её колбэк ставится в очередь задач. Event loop — бесконечный цикл: как только стек пуст, он берёт следующую задачу из очереди и выполняет её. Поэтому `setTimeout(fn, 0)` означает «не раньше, чем через 0 мс и после всего текущего кода», а тяжёлый синхронный цикл замораживает страницу.",
  theory: {
    p: [
      "Стек вызовов — список функций, которые выполняются прямо сейчас. Вызов функции кладёт её на стек, `return` снимает. Поток один, стек один: пока выполняется хоть одна функция, никакой другой код не запустится — ни обработчик клика, ни таймер. Поэтому синхронный цикл на три секунды замораживает страницу: браузер не может ни перерисовать её, ни обработать ввод.",
      "Ожидание делает не JavaScript, а окружение. `setTimeout(fn, 100)` просит браузер засечь время и сразу возвращается. Через 100 мс браузер кладёт `fn` в очередь задач. То же с ответами сети и событиями: окружение ставит колбэк в очередь, когда событие произошло.",
      "Event loop — цикл, который работает всё время жизни страницы: если стек пуст, взять первую задачу из очереди и выполнить её до конца, затем повторить. Задача не прерывается на середине. Поэтому колбэк таймера никогда не вклинится в текущую функцию, а `setTimeout(fn, 0)` выполнится только после всего синхронного кода.",
      "Задержка таймера — минимальная, а не точная. Если стек занят, колбэк подождёт. Вложенные таймеры браузер ограничивает минимумом около 4 мс, а во вкладке в фоне — ещё сильнее. Долгую работу делят на куски или выносят в отдельный поток — об этом урок в регионе «Web API».",
    ],
    code: `console.log("A: синхронный код");
setTimeout(() => console.log("D: таймер 0 мс"), 0);
setTimeout(() => console.log("E: таймер 20 мс"), 20);

function heavy() {
  const start = Date.now();
  while (Date.now() - start < 50) {} // занимаем стек на 50 мс
  console.log("B: тяжёлая функция закончилась");
}
heavy();
console.log("C: конец скрипта");
// оба таймера уже готовы, но ждали, пока освободится стек`,
    flow: {
      actors: ["Стек вызовов", "Окружение (таймеры)", "Очередь задач", "Event loop"],
      steps: [
        { from: 0, to: 1, label: "setTimeout(fn, 0): засечь время", note: "функция сразу возвращается, скрипт продолжается" },
        { from: 1, to: 2, label: "время вышло: fn встаёт в очередь" },
        { from: 3, to: 0, label: "стек ещё занят скриптом — ждём" },
        { from: 3, to: 2, label: "скрипт закончился, стек пуст: берём fn" },
        { from: 2, to: 0, label: "fn выполняется целиком" },
      ],
    },
    keys: [
      "Один поток и один стек вызовов: пока стек не пуст, ничего другого не выполняется.",
      "Таймеры, сеть и события обслуживает окружение и ставит колбэки в очередь задач.",
      "Event loop берёт задачу, только когда стек пуст. `setTimeout(fn, 0)` — после всего текущего кода, задержка минимальная, а не точная.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `console.log("A");
setTimeout(() => console.log("B"), 0);
console.log("C");`,
      opts: ["A\nC\nB", "A\nB\nC", "B\nA\nC", "A\nC"],
      a: 0,
      why: "Колбэк таймера попадает в очередь и ждёт, пока стек освободится. Синхронный `console.log(\"C\")` выполняется раньше.",
    },
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `setTimeout(() => console.log("100"), 100);
setTimeout(() => console.log("0"), 0);
setTimeout(() => console.log("20"), 20);
console.log("sync");`,
      opts: ["sync\n0\n20\n100", "100\n0\n20\nsync", "sync\n100\n0\n20", "0\nsync\n20\n100"],
      a: 0,
      why: "Сначала весь синхронный код. Таймеры встают в очередь, когда истечёт их время, а не в порядке вызова `setTimeout`.",
    },
    {
      type: "order",
      q: "Расставь, что происходит с `setTimeout(fn, 0)`, вызванным посреди скрипта.",
      items: [
        "окружение засекает время, `setTimeout` сразу возвращается",
        "скрипт продолжает выполняться до конца",
        "`fn` стоит в очереди задач",
        "стек пуст — event loop берёт `fn` и выполняет",
      ],
      why: "Время могло истечь раньше, чем закончился скрипт, но задача всё равно ждёт пустого стека.",
    },
    {
      type: "quiz",
      q: "Что случится со страницей, если в обработчике клика выполняется синхронный цикл на 3 секунды?",
      opts: [
        "Страница замрёт: не будет ни перерисовки, ни реакции на ввод, пока цикл не закончится",
        "Браузер выполнит цикл в фоне, страница продолжит работать",
        "Цикл прервётся через 50 мс",
        "Остальные обработчики выполнятся параллельно",
      ],
      a: 0,
      why: "Поток один. Пока стек занят, event loop не может взять ни событие, ни перерисовку. Долгую работу делят на части или уносят в отдельный поток.",
    },
  ],
};
