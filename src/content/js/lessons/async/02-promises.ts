import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "asy2",
  region: 5,
  title: "Промисы",
  q: "Что такое промис? Какие у него состояния и как работают цепочки `then`, `catch` и `finally`?",
  answer:
    "Промис — объект-обещание результата асинхронной операции. У него три состояния: `pending` (ожидание), `fulfilled` (выполнен со значением) и `rejected` (отклонён с ошибкой). Перейти из ожидания он может только один раз. `then` возвращает новый промис: значение из обработчика передаётся дальше, промис из обработчика дожидается, а брошенная ошибка отклоняет цепочку до ближайшего `catch`. `finally` не получает значения и пропускает результат дальше. Функция-исполнитель в `new Promise` выполняется синхронно, а обработчики `then` — всегда асинхронно, после текущего кода.",
  theory: {
    p: [
      "До промисов асинхронный код писали на колбэках, и вложенные колбэки превращались в «пирамиду». Промис — объект, который представляет будущий результат. Его создают через `new Promise((resolve, reject) => { ... })`: функция-исполнитель запускается сразу и синхронно, а когда результат готов, вызывает `resolve(value)` или `reject(error)`.",
      "Состояния: `pending` — ждём, `fulfilled` — готово значение, `rejected` — ошибка. Переход происходит один раз: повторные `resolve` и `reject` игнорируются. Готовые промисы создают короче: `Promise.resolve(value)` и `Promise.reject(error)`.",
      "`p.then(onOk, onError)` подписывается на результат и возвращает новый промис. Что вернёт обработчик — тем и выполнится новый промис; если вернёт промис — цепочка его дождётся; если бросит ошибку — новый промис отклонится. Ошибка идёт по цепочке, пропуская `then`, до первого `catch` — это `then(null, onError)`. После `catch` цепочка снова идёт по успешному пути. `finally(fn)` вызывается в любом случае, не получает значения и передаёт результат дальше без изменений.",
      "Обработчики `then` никогда не вызываются синхронно — даже у уже выполненного промиса они подождут конца текущего кода. Отклонённый промис без `catch` — необработанная ошибка: браузер покажет её в консоли, а Node.js по умолчанию завершит процесс. Точный порядок, в котором выполняются обработчики промисов и таймеры, — в следующем уроке.",
    ],
    code: `console.log("1: синхронно");
const p = new Promise((resolve) => {
  console.log("2: исполнитель тоже синхронно");
  resolve("готово");
  resolve("второй раз — игнорируется");
});
p.then((v) => console.log("4: then получил:", v));
console.log("3: конец скрипта");

Promise.resolve(1)
  .then((x) => x + 1)
  .then((x) => {
    throw new Error("сбой на " + x);
  })
  .then(() => console.log("пропущен: цепочка отклонена"))
  .catch((e) => {
    console.log("catch:", e.message);
    return 10;                       // цепочка снова успешная
  })
  .finally(() => console.log("finally без аргументов"))
  .then((x) => console.log("дальше идёт", x));`,
    keys: [
      "Состояния `pending`, `fulfilled`, `rejected`, переход один раз. Исполнитель `new Promise` выполняется синхронно.",
      "`then` возвращает новый промис: значение идёт дальше, промис дожидается, ошибка летит до `catch`.",
      "`finally` не получает значения и пропускает результат. Обработчики `then` всегда асинхронны.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `console.log("1");
const p = new Promise((resolve) => {
  console.log("2");
  resolve("3");
});
p.then((v) => console.log(v));
console.log("4");`,
      opts: ["1\n2\n4\n3", "1\n4\n2\n3", "1\n2\n3\n4", "2\n1\n4\n3"],
      a: 0,
      why: "Исполнитель промиса выполняется сразу и синхронно, поэтому `2` сразу после `1`. Обработчик `then` вызывается асинхронно — после всего текущего кода.",
    },
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `Promise.resolve(1)
  .then((x) => x + 1)
  .then((x) => {
    throw new Error("сбой " + x);
  })
  .then(() => console.log("пропущен"))
  .catch((e) => {
    console.log(e.message);
    return 10;
  })
  .finally(() => console.log("finally"))
  .then((x) => console.log(x));`,
      opts: ["сбой 2\nfinally\n10", "сбой 2\nпропущен\nfinally\n10", "сбой 1\nfinally\nundefined", "сбой 2\nfinally\nundefined"],
      a: 0,
      why: "Ошибка пропускает `then` до `catch`. `catch` вернул `10`, цепочка снова успешная. `finally` не меняет результат, и `10` доходит до последнего `then`.",
    },
    {
      type: "quiz",
      q: "Что будет, если в исполнителе вызвать сначала `resolve(1)`, а потом `reject(new Error())`?",
      opts: [
        "Промис выполнится со значением `1`, вызов `reject` проигнорируется",
        "Промис станет отклонённым",
        "Будет необработанная ошибка",
        "Промис перейдёт в `pending`",
      ],
      a: 0,
      why: "Промис меняет состояние один раз. Всё, что вызывается после первого `resolve` или `reject`, ничего не делает.",
    },
    {
      type: "run",
      goal: "Напиши `delay(ms, value)`: возвращает промис, который через `ms` миллисекунд выполняется со значением `value`.",
      code: `function delay(ms, value) {
  return value;
}`,
      tests: [
        ["delay(10) instanceof Promise", "true"],
        ["delay(10, \"ok\")", "\"ok\""],
        ["(() => { const t = Date.now(); return delay(50).then(() => Date.now() - t >= 45); })()", "true"],
      ],
      solution: `function delay(ms, value) {
  return new Promise((resolve) => {
    setTimeout(() => resolve(value), ms);
  });
}`,
      hint: "Создай `new Promise` и внутри исполнителя вызови `setTimeout`, который через `ms` вызовет `resolve(value)`.",
    },
  ],
};
