import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "scp5",
  region: 1,
  level: "middle",
  title: "Где пригодятся замыкания: приватность, фабрики, IIFE",
  q: "Где на практике используют замыкания? Что такое IIFE и паттерн «модуль»?",
  answer:
    "Замыкания дают приватное состояние: переменная внутри функции недоступна снаружи, её меняют только возвращённые функции. На них строят фабрики функций, `once`, кэширование результатов и обработчики событий, которые помнят свои данные. IIFE — функция, которую сразу вызывают: `(function () { ... })()`. До появления модулей так создавали отдельную область видимости и прятали детали — паттерн «модуль». Минус замыканий: пока жива функция, живут и все переменные, которые она видит.",
  theory: {
    p: [
      "Приватные данные. Переменная, объявленная внутри функции, не видна снаружи. Если функция возвращает методы, которые работают с этой переменной, получается объект с закрытым состоянием: менять его можно только через методы. Так до появления приватных полей в классах прятали детали реализации.",
      "Фабрики функций. Функция может создавать другие функции, настроенные параметрами: `makeMultiplier(2)` возвращает функцию, которая умножает на два. Так же устроены `once` (выполнить один раз), кэширование результатов и многие задачи live coding — они разобраны в последнем регионе.",
      "IIFE (immediately invoked function expression) — функциональное выражение, которое вызывают сразу: `(function () { ... })()`. Скобки вокруг нужны, чтобы функция стала выражением. До появления модулей каждый файл оборачивали в IIFE, чтобы его переменные не попали в глобальную область. Возвращённый из IIFE объект с методами называли паттерном «модуль». Сейчас эту задачу решают модули, но IIFE встречается в старом коде и в вопросах.",
      "Цена замыканий — память. Окружение живёт, пока жива хоть одна функция, которая его видит. Если обработчик события замкнул большой массив и обработчик не удалили, массив остаётся в памяти. Поэтому обработчики и таймеры снимают, когда они больше не нужны.",
    ],
    code: `function makeMultiplier(k) {
  return function (x) {
    return x * k; // k запомнена в замыкании
  };
}
const double = makeMultiplier(2);
const triple = makeMultiplier(3);
console.log(double(5), triple(5)); // 10 15

const wallet = (function () {
  let balance = 0; // снаружи недоступна
  return {
    add(sum) { balance += sum; },
    get() { return balance; },
  };
})(); // IIFE: функцию вызвали сразу

wallet.add(100);
console.log(wallet.get());    // 100
console.log(wallet.balance);  // undefined`,
    keys: [
      "Замыкание даёт приватное состояние: переменную меняют только функции, созданные рядом с ней.",
      "Фабрики функций, `once` и кэширование строятся на замыканиях.",
      "IIFE `(function () { ... })()` создаёт свою область видимости — так делали модули до `import`. Замыкание держит переменные в памяти, пока жива функция.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `const counter = (function () {
  let value = 0;
  return {
    next() {
      value++;
      return value;
    },
  };
})();
counter.next();
console.log(counter.next());
console.log(counter.value);`,
      opts: ["2\nundefined", "1\nundefined", "2\n2", "1\n0"],
      a: 0,
      why: "IIFE выполнилась один раз и вернула объект, который замкнул `value`. Два вызова `next` дают `2`. Свойства `value` у объекта нет — переменная доступна только внутри.",
    },
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `function makeTotal() {
  let total = 0;
  return {
    add(x) { total += x; },
    read() { return total; },
  };
}
const first = makeTotal();
const second = makeTotal();
first.add(5);
first.add(10);
second.add(1);
console.log(first.read(), second.read());`,
      opts: ["15 1", "16 16", "15 0", "5 1"],
      a: 0,
      why: "Методы из одного вызова видят одну `total`. Второй вызов `makeTotal` создал новое окружение со своей `total`.",
    },
    {
      type: "quiz",
      q: "Почему замыкание может удерживать память?",
      opts: [
        "Пока жива функция, живут и переменные её окружения, даже если они больше не нужны",
        "Замыкание копирует все переменные программы",
        "Замыкания не удаляются, пока страницу не перезагрузят",
        "Каждый вызов замкнутой функции создаёт копию окружения",
      ],
      a: 0,
      why: "Сборщик мусора не удалит окружение, на которое ссылается живая функция. Забытый обработчик или таймер держит всё, что замкнул.",
    },
    {
      type: "run",
      goal: "Напиши `once(fn)`: возвращает функцию, которая вызывает `fn` только в первый раз, а при следующих вызовах возвращает первый результат. Аргументы передаются в `fn`.",
      code: `function once(fn) {
  return fn;
}`,
      tests: [
        ["(() => { let n = 0; const f = once(() => ++n); f(); f(); f(); return n; })()", "1"],
        ["(() => { const f = once((a, b) => a + b); return [f(1, 2), f(5, 5)]; })()", "[3,3]"],
        ["(() => { let n = 0; const f = once(() => { n++; }); f(); f(); return n; })()", "1"],
      ],
      solution: `function once(fn) {
  let called = false;
  let result;
  return function (...args) {
    if (!called) {
      called = true;
      result = fn(...args);
    }
    return result;
  };
}`,
      hint: "Нужны две переменные в замыкании: флаг «уже вызывали» и сохранённый результат. Проверять результат на `undefined` нельзя: функция может честно вернуть `undefined`. Аргументы собери через `...args` и передай в `fn(...args)`.",
    },
  ],
};
