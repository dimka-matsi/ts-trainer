import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "scp1",
  region: 1,
  level: "junior",
  title: "var, let и const",
  q: "Чем отличаются `var`, `let` и `const`?",
  answer:
    "`var` видна во всей функции, где объявлена, её можно объявить повторно, а вне функций она становится свойством глобального объекта. `let` и `const` видны только внутри блока — между фигурными скобками, — и повторно объявить их нельзя. `const` к тому же нельзя переприсвоить, но объект, на который она ссылается, менять можно. В современном коде пишут `const` по умолчанию, `let` — когда значение меняется, а `var` не используют.",
  theory: {
    p: [
      "Область видимости — часть кода, где доступна переменная. У `var` область — вся функция: переменная, объявленная внутри `if` или цикла, видна и после него. У `let` и `const` область — блок, то есть код между `{` и `}`: тело `if`, цикла или просто пара скобок.",
      "`var` можно объявить повторно — вторая строка `var x` молча ничего не сломает, и это прячет ошибки. `let` и `const` при повторном объявлении в том же блоке дают `SyntaxError` ещё до запуска кода. Объявленная вне всех функций `var` в обычном скрипте становится свойством глобального объекта `window`, `let` и `const` — нет.",
      "`const` запрещает переприсваивание: `limit = 20` бросит `TypeError`. Объявить `const` без значения нельзя. Но `const` не делает объект неизменяемым: `const user = {}` и затем `user.name = \"Аня\"` — работает, меняется объект, а не переменная.",
      "Практика: пиши `const` везде, где значение не переприсваивается, — это большинство переменных. `let` — для счётчиков и значений, которые меняются. `var` встречается только в старом коде и в вопросах на собеседовании.",
    ],
    code: `function demo() {
  if (true) {
    var fromVar = "видна во всей функции";
    let fromLet = "видна только в блоке";
  }
  console.log(fromVar);
  console.log(typeof fromLet); // undefined: вне блока её нет
}
demo();

const user = { name: "Аня" };
user.name = "Борис"; // объект менять можно
console.log(user.name);

try {
  const limit = 10;
  limit = 20; // переприсвоить const нельзя
} catch (e) {
  console.log(e.name);
}`,
    keys: [
      "`var` — область функции, `let` и `const` — область блока `{ }`.",
      "`var` можно объявить повторно, `let` и `const` — нет. `const` нельзя переприсвоить, но объект по ссылке менять можно.",
      "Правило: `const` по умолчанию, `let` для меняющихся значений, `var` не используют.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `if (true) {
  var a = 1;
  let b = 2;
}
console.log(a);
console.log(typeof b);`,
      opts: ["1\nundefined", "1\nnumber", "undefined\nundefined", "1\nReferenceError"],
      a: 0,
      why: "`var` не замечает блок `if` и видна после него. `let` живёт только внутри блока. `typeof` для необъявленного имени не бросает ошибку, а возвращает `\"undefined\"`.",
    },
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `function test() {
  for (var i = 0; i < 3; i++) {}
  console.log(i);
  for (let j = 0; j < 3; j++) {}
  console.log(typeof j);
}
test();`,
      opts: ["3\nundefined", "2\nundefined", "3\nnumber", "undefined\nundefined"],
      a: 0,
      why: "`i` объявлена через `var`, поэтому видна после цикла. Цикл остановился, когда `i` стала `3`. `j` через `let` существует только внутри цикла.",
    },
    {
      type: "quiz",
      q: "Что случится при выполнении `const x = 1; x = 2;`?",
      opts: ["`TypeError`: переприсвоить константу нельзя", "`x` станет `2`", "Ничего: присваивание молча не сработает", "`SyntaxError` ещё до запуска"],
      a: 0,
      why: "Ошибка возникает во время работы программы, в момент присваивания, и в строгом режиме, и без него. `SyntaxError` до запуска бывает при повторном объявлении `let` или `const`.",
    },
    {
      type: "match",
      q: "Сопоставь объявление и его свойства.",
      pairs: [
        ["`var`", "видна во всей функции, можно объявить повторно"],
        ["`let`", "видна в блоке, можно переприсвоить"],
        ["`const`", "видна в блоке, переприсвоить нельзя"],
      ],
      why: "Главное различие — область: функция у `var` и блок у `let` и `const`. А `const` отличается от `let` только запретом переприсваивания.",
    },
  ],
};
