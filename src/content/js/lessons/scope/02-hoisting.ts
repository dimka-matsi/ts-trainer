import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "scp2",
  region: 1,
  level: "middle",
  title: "Поднятие и временная мёртвая зона",
  q: "Что такое hoisting (поднятие)? Что такое временная мёртвая зона (TDZ)?",
  answer:
    "Перед выполнением функции или скрипта движок находит все объявления в этой области. Функцию-объявление можно вызвать до её строки — она создана целиком. `var` тоже создаётся заранее, но со значением `undefined`. `let`, `const` и `class` создаются заранее, но до строки объявления находятся во временной мёртвой зоне: обращение к ним даёт `ReferenceError`. Функциональное выражение в `var` до присваивания — это `undefined`, и его вызов даёт `TypeError`.",
  theory: {
    p: [
      "Код выполняется в два этапа. Сначала движок проходит по области видимости и регистрирует все объявления, затем выполняет строки по порядку. Поэтому кажется, что объявления «поднимаются» наверх — отсюда название hoisting, поднятие.",
      "Функция-объявление `function hello() {}` создаётся на первом этапе целиком: её можно вызвать выше по коду. Переменная `var` создаётся со значением `undefined`: чтение до объявления не падает, а даёт `undefined`. Если в `var` лежит функциональное выражение `var greet = function () {}`, до присваивания `greet` — `undefined`, и вызов `greet()` бросает `TypeError: greet is not a function`.",
      "`let` и `const` тоже регистрируются заранее, но не получают значения. От начала блока до строки объявления переменная находится во временной мёртвой зоне (temporal dead zone, TDZ): любое обращение, даже `typeof`, бросает `ReferenceError`. Так язык ловит использование переменной до её объявления.",
      "Мёртвая зона работает и при затенении. Если снаружи есть `let x = 1`, а внутри блока — `let x = 2`, то чтение `x` в начале блока бросит ошибку: внутренняя `x` уже зарегистрирована и закрывает внешнюю, но ещё не объявлена.",
    ],
    code: `console.log(typeof hello); // function — создана заранее целиком
console.log(value);        // undefined — var создана без значения
var value = 5;
function hello() {}

try {
  console.log(count);      // мёртвая зона
  let count = 1;
} catch (e) {
  console.log(e.name);     // ReferenceError
}

try {
  greet();                 // greet пока undefined
  var greet = function () {};
} catch (e) {
  console.log(e.name);     // TypeError
}`,
    keys: [
      "Объявления регистрируются до выполнения кода. Функцию-объявление можно вызвать до её строки.",
      "`var` до объявления — `undefined`. Функциональное выражение в `var` до присваивания вызвать нельзя: `TypeError`.",
      "`let` и `const` до объявления — в мёртвой зоне: `ReferenceError`, даже при затенении внешней переменной.",
    ],
  },
  tasks: [
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `console.log(typeof hello);
console.log(value);
var value = 5;
function hello() {}
console.log(value);`,
      opts: ["function\nundefined\n5", "undefined\nundefined\n5", "function\n5\n5", "undefined\nReferenceError"],
      a: 0,
      why: "Функция-объявление создаётся заранее целиком, поэтому `typeof` видит функцию. `var value` создана заранее без значения — `undefined`, а `5` она получает только на своей строке.",
    },
    {
      type: "quiz",
      output: true,
      q: "Что выведет этот код?",
      code: `let x = 1;
function show() {
  try {
    console.log(x);
    let x = 2;
  } catch (e) {
    console.log(e.name);
  }
}
show();`,
      opts: ["ReferenceError", "1", "2", "undefined"],
      a: 0,
      why: "Внутренняя `let x` зарегистрирована с начала блока и закрывает внешнюю `x`. Но до своей строки она в мёртвой зоне, поэтому чтение бросает `ReferenceError`, а не берёт внешнее значение `1`.",
    },
    {
      type: "sort",
      q: "Что будет, если обратиться к имени до строки, где оно объявлено?",
      groups: ["работает", "даёт `undefined`", "ошибка"],
      items: [
        ["вызвать функцию-объявление `function f() {}`", 0],
        ["прочитать переменную `var`", 1],
        ["прочитать переменную `let`", 2],
        ["прочитать переменную `const`", 2],
        ["вызвать `f` из `var f = function () {}`", 2],
      ],
      why: "Функция-объявление создаётся целиком, `var` — со значением `undefined`. `let` и `const` до объявления в мёртвой зоне. Вызов `undefined` как функции бросает `TypeError`.",
    },
    {
      type: "quiz",
      q: "Зачем нужна временная мёртвая зона?",
      opts: [
        "Чтобы ловить использование переменной до объявления, а не молча получать `undefined`",
        "Чтобы код работал быстрее",
        "Чтобы переменные `let` были видны во всей функции",
        "Чтобы можно было объявить `let` повторно",
      ],
      a: 0,
      why: "С `var` чтение до объявления даёт `undefined`, и ошибка проявится где-то дальше. С `let` и `const` программа падает сразу в том месте, где ошибка.",
    },
  ],
};
