import type { WebLesson } from "../../../course/types";

export const lesson: WebLesson = {
  id: "lc2",
  region: 9,
  title: "throttle",
  q: "Напиши `throttle(fn, ms)`. Чем он отличается от debounce?",
  answer:
    "`throttle` пропускает не больше одного вызова за `ms` миллисекунд: первый вызов выполняется сразу, остальные в пределах интервала отбрасываются, а в хорошей версии последний из них выполняется в конце интервала, чтобы не потерять финальное состояние. Применяют для частых событий, где нужна регулярная реакция: прокрутка, движение мыши, изменение размера. Debounce ждёт паузы и срабатывает один раз в конце, throttle срабатывает равномерно во время серии.",
  theory: {
    p: [
      "При прокрутке событие `scroll` приходит десятки раз в секунду. Если на каждое событие пересчитывать что-то тяжёлое, страница тормозит. Throttle ограничивает частоту: не чаще раза в 100 мс. В отличие от debounce, реакция есть и во время прокрутки, а не только после неё.",
      "Простая версия с меткой времени: запоминаем время последнего запуска и выполняем функцию, только если прошло не меньше `ms`. Её минус — последний вызов серии может пропасть: пользователь остановил прокрутку, а обработчик увидел не финальную позицию. Поэтому добавляют «хвост»: если вызов пришёл внутри интервала, запоминаем его аргументы и ставим таймер на конец интервала.",
      "Для анимаций, привязанных к кадрам, вместо throttle по времени часто берут `requestAnimationFrame`: не больше одного вызова за кадр. А для прокрутки и видимости элементов сначала подумай про `IntersectionObserver` — возможно, обработчик `scroll` вообще не нужен.",
    ],
    code: `function throttleSimple(fn, ms) {
  let last = 0;
  return function (...args) {
    const now = Date.now();
    if (now - last >= ms) {        // прошло достаточно — выполняем
      last = now;
      fn.apply(this, args);
    }                               // иначе вызов теряется
  };
}

const log = [];
const onScroll = throttleSimple((y) => log.push(y), 50);
onScroll(0);    // выполнится сразу
onScroll(100);  // отброшен
onScroll(200);  // отброшен — финальная позиция потеряна
setTimeout(() => console.log(log), 80);`,
    keys: [
      "Throttle — не чаще одного вызова за интервал, первый вызов сразу.",
      "Хорошая версия сохраняет последний вызов серии и выполняет его в конце интервала.",
      "Debounce — один раз после паузы, throttle — регулярно во время серии. Для кадров — `requestAnimationFrame`.",
    ],
  },
  tasks: [
    {
      type: "run",
      goal: "Напиши `throttle(fn, ms)`: первый вызов выполняется сразу, дальше не чаще раза в `ms`. Если внутри интервала были вызовы, последний из них выполняется в конце интервала с его аргументами.",
      code: `function throttle(fn, ms) {
  return fn;
}`,
      tests: [
        ["(async () => { const log = []; const f = throttle((x) => log.push(x), 50); f(1); f(2); f(3); await sleep(90); return log; })()", "[1,3]"],
        ["(async () => { const log = []; const f = throttle((x) => log.push(x), 30); f(1); await sleep(60); f(2); return log; })()", "[1,2]"],
        ["(async () => { const log = []; const f = throttle((x) => log.push(x), 20); f(1); await sleep(50); return log; })()", "[1]"],
      ],
      solution: `function throttle(fn, ms) {
  let last = 0;
  let timer = null;
  let pendingArgs = null;
  let pendingThis = null;
  return function (...args) {
    const left = ms - (Date.now() - last);
    if (left <= 0) {
      last = Date.now();
      fn.apply(this, args);
      return;
    }
    pendingArgs = args;
    pendingThis = this;
    if (!timer) {
      timer = setTimeout(() => {
        timer = null;
        last = Date.now();
        fn.apply(pendingThis, pendingArgs);
      }, left);
    }
  };
}`,
      hint: "Помни время последнего запуска. Если интервал прошёл — выполняй сразу. Если нет — сохрани аргументы и, если таймера ещё нет, поставь его на остаток интервала: он выполнит последний сохранённый вызов.",
    },
    {
      type: "quiz",
      q: "Нужно показывать прогресс чтения статьи, пока пользователь прокручивает страницу. Что выбрать?",
      opts: ["throttle: обновлять полосу регулярно во время прокрутки", "debounce: обновить, когда прокрутка закончится", "`setTimeout(fn, 0)` в обработчике", "Ничего, обработчик на каждое событие — это нормально"],
      a: 0,
      why: "Полоса должна двигаться во время прокрутки — debounce обновил бы её только после остановки. Throttle даёт регулярные обновления без лишней работы.",
    },
  ],
};
